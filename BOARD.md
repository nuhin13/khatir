# BOARD

<!-- timestamp | task-id | role | platform | status | note -->
2026-06-02 | EPIC-00/T-001 | infra | claude | done | -
2026-06-02 | EPIC-00/T-002 | infra | claude | done | -
2026-06-02 | EPIC-00/T-003 | infra | claude | done | -
2026-06-02 | EPIC-00/T-004 | backend | claude | done | -
2026-06-02 | EPIC-00/T-005 | backend | claude | done | -
2026-06-02 | EPIC-00/T-006 | backend | claude | done | -
2026-06-02 | EPIC-00/T-010 | infra | claude | done | -
2026-06-02 | EPIC-00/T-012 | infra | claude | done | -
2026-06-02 | EPIC-00/T-007 | mobile | claude | done | -
2026-06-02 | EPIC-00/T-009 | admin | claude | done | -
2026-06-02 | EPIC-00/T-008 | mobile | claude | done | -
2026-06-02 | EPIC-00/T-011 | infra | claude | done | -
2026-06-02 | EPIC-00/T-013 | infra | claude | done | -
2026-06-02 | EPIC-00/T-014 | infra | claude | done | -
2026-06-02 | EPIC-00/T-015 | cross-cutting | claude | done | -
2026-06-02 | EPIC-00/T-016 | infra | claude | done | -
2026-06-02 | EPIC-01/T-002 | backend | claude | done | -
2026-06-02 | EPIC-01/T-001 | backend | claude | done | -
2026-06-02 | EPIC-01/T-008 | mobile | claude | done | -
2026-06-02 | EPIC-01/T-003 | backend | claude | done | -
2026-06-02 | EPIC-01/T-009 | mobile | claude | done | -
2026-06-02 | EPIC-01/T-004 | backend | claude | done | -
2026-06-02 | EPIC-01/T-010 | mobile | claude | done | -
2026-06-02 | EPIC-01/T-005 | backend | claude | done | -
2026-06-02 | EPIC-01/T-006 | backend | claude | done | -
2026-06-02 | EPIC-01/T-007 | backend | claude | done | -
2026-06-02 | EPIC-01/T-011 | mobile | claude | done | -
2026-06-02 | EPIC-01/T-012 | mobile | claude | done | -
2026-06-03 | EPIC-02/T-001 | backend | claude | done | -
2026-06-03 | EPIC-02/T-002 | backend | claude | done | -
2026-06-03 | EPIC-02/T-003 | mobile | claude | in-progress | no Flutter/Dart toolchain to run analyze+test
2026-06-03 | EPIC-02/T-004 | mobile | claude | in-progress | no Flutter/Dart toolchain to run analyze+test
2026-06-03 | EPIC-02/T-005 | mobile | claude | in-progress | no Flutter/Dart toolchain to run analyze+test
2026-06-03 | EPIC-02/T-006 | mobile | claude | in-progress | no Flutter/Dart toolchain to run analyze+test
2026-06-03 | EPIC-02/T-007 | mobile | claude | in-progress | no Flutter/Dart toolchain to run analyze+test
2026-06-03 | EPIC-02/T-008 | mobile | claude | in-progress | no Flutter/Dart toolchain to run analyze+test
2026-06-03 | EPIC-03/T-006 | backend | claude | done | -
2026-06-03 | EPIC-03/T-001 | backend | claude | done | -
2026-06-03 | EPIC-03/T-008 | mobile | claude | in-progress | no Flutter/Dart toolchain to run analyze+test
2026-06-03 | EPIC-03/T-002 | backend | claude | done | -
2026-06-03 | EPIC-03/T-003 | backend | claude | done | -
2026-06-03 | EPIC-03/T-004 | backend | claude | done | -
2026-06-03 | EPIC-03/T-005 | backend | claude | done | -
2026-06-03 | EPIC-03/T-007 | mobile | claude | in-progress | no Flutter/Dart toolchain to run analyze+test
2026-06-03 | EPIC-03/T-009 | mobile | claude | in-progress | no Flutter/Dart toolchain to run analyze+test
2026-06-03 | EPIC-03/T-012 | mobile | claude | in-progress | no Flutter/Dart toolchain to run analyze+test
2026-06-03 | EPIC-03/T-013 | mobile | claude | in-progress | no Flutter/Dart toolchain to run analyze+test
2026-06-03 | EPIC-03/T-010 | mobile | claude | in-progress | no Flutter/Dart toolchain to run analyze+test
2026-06-03 | EPIC-03/T-011 | mobile | claude | in-progress | no Flutter/Dart toolchain to run analyze+test
2026-06-03 | EPIC-03/T-014 | cross-cutting | claude | in-progress | no Flutter/Dart toolchain to run Dart parity test
2026-06-04 | EPIC-02/T-003 | mobile | claude | done | toolchain installed; flutter analyze+test green (120 pass)
2026-06-04 | EPIC-02/T-004 | mobile | claude | done | verified via flutter analyze+test (120 pass)
2026-06-04 | EPIC-02/T-005 | mobile | claude | done | verified via flutter analyze+test (120 pass)
2026-06-04 | EPIC-02/T-006 | mobile | claude | done | verified via flutter analyze+test (120 pass)
2026-06-04 | EPIC-02/T-007 | mobile | claude | done | verified via flutter analyze+test (120 pass)
2026-06-04 | EPIC-02/T-008 | mobile | claude | done | verified via flutter analyze+test (120 pass)
2026-06-04 | EPIC-03/T-007 | mobile | claude | done | verified via flutter analyze+test (120 pass)
2026-06-04 | EPIC-03/T-008 | mobile | claude | done | flutter_map 8.x tap-debounce fix in test; analyze+test green
2026-06-04 | EPIC-03/T-009 | mobile | claude | done | verified via flutter analyze+test (120 pass)
2026-06-04 | EPIC-03/T-010 | mobile | claude | done | WizardField label RichText→Text fix; analyze+test green
2026-06-04 | EPIC-03/T-011 | mobile | claude | done | provider seed deferred to post-frame; analyze+test green
2026-06-04 | EPIC-03/T-012 | mobile | claude | done | verified via flutter analyze+test (120 pass)
2026-06-04 | EPIC-03/T-013 | mobile | claude | done | controller update()->save() rename (Riverpod clash); analyze+test green
2026-06-04 | EPIC-03/T-014 | cross-cutting | claude | done | unit-label parity test green under flutter test
2026-06-04 | EPIC-04/T-001 | backend | claude | done | tenants app: Tenant+TenantFamilyMember, VerificationStatus, migration; 12 model tests, ruff+mypy clean
2026-06-04 | EPIC-06/T-001 | backend | claude | done | leases app: Lease + RentSchedule + enums; 26 tests; full suite 378 pass
2026-06-04 | EPIC-08/T-001 | backend | claude | done | maintenance app: MaintenanceRequest + Expense + enums; 17 tests; 378 pass
2026-06-04 | EPIC-10/T-001 | backend | claude | done | billing app: PricingTier + Subscription + enums; 17 tests; 378 pass
2026-06-04 | EPIC-11/T-001 | backend | claude | done | admin_portal app: AdminUser (separate from User); 23 tests; 378 pass
2026-06-04 | EPIC-05/T-001 | backend | claude | done | dmpforms app: DMPFormRecord; 11 tests; full suite 471 pass
2026-06-04 | EPIC-13/T-001 | backend | claude | done | featureflags app: FeatureFlag + KillSwitchEvent; 19 tests; 471 pass
2026-06-04 | EPIC-14/T-001 | backend | claude | done | ai_providers app: AIProvider + AIUsageLog; 17 tests; 471 pass
2026-06-04 | EPIC-15/T-001 | backend | claude | done | notifications app: Notification+Delivery+Template; sender FK->AdminUser; 28 tests; 471 pass
2026-06-04 | EPIC-16/T-001 | backend | claude | done | compliance app: ConsentRecord + DataRequest; 18 tests; 471 pass
2026-06-04 | EPIC-04/T-002 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-04/T-007 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-06/T-002 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-06/T-003 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-06/T-006 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-06/T-005 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-05/T-006 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-05/T-002 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-05/T-003 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-05/T-004 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-05/T-005 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-07/T-001 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-07/T-009 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-07/T-003 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-07/T-005 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-07/T-008 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-04/T-003 | backend | claude | done | S3 boto3 storage (SSE, private ACL, presigned URLs) + FS fallback; pytest 634
2026-06-04 | EPIC-04/T-004 | backend | claude | done | TenantExtractionProvider ABC + OCR/ASR impls, normalized DTO, config-driven selection; pytest 642
2026-06-04 | EPIC-04/T-005 | backend | claude | done | POST /tenants/ocr: encrypted image store + provider extract, per-user rate-limit, masked-only fields; pytest 651
2026-06-04 | EPIC-04/T-006 | backend | claude | done | POST /tenants/voice: Bangla audio -> ASR fields, voice_tenant_entry flag gate (default on), audio discarded, per-user rate-limit; pytest 661
2026-06-04 | EPIC-04/T-008 | backend | claude | done | GET /api/v1/usage: tenant_usage selector (for_user count, free_limit from SystemConfig, is_over_free soft signal); seed free_tier_tenant_limit=2; no creation block (EPIC-10 owns enforcement); pytest 669
2026-06-04 | EPIC-06/T-004 | backend | claude | done | GET /leases/{id}/schedule (scoped, chronological) + GET /units/{id}/lease (active lease + tenant summary, none->404); leases selectors + RentSchedule/UnitLease serializers; pytest 678
2026-06-04 | EPIC-05/T-010 | backend | claude | done | DMP template field-verification: canonical field-map doc + FIELD_LAYOUT positioned renderer + golden test (field-set parity, per-field positions, determinism); scanned-master overlay pending founder; pytest 688
2026-06-04 | EPIC-07/T-002 | backend | claude | done | signed link-token service (Django TimestampSigner, dedicated salt, TTL from rent_link_token_ttl_hours); make_token persists, resolve_token raises typed Expired/Invalid (NotFoundError); pytest 688
2026-06-04 | EPIC-07/T-004 | backend | claude | done | rent-link send: status->sent stamp in send_rent_link + POST /rent-requests/{id}/send (send_rent_request service, audited rent.request.send), reuses EPIC-01 NotificationSender (whatsapp->sms, console dev); pytest 697
2026-06-04 | EPIC-07/T-006 | backend | claude | done | POST /r/{token}/proof (txn/screenshot via encrypted storage/note) -> PaymentProof + status proof_submitted, PRG to GET /r/{token}/receipt (pending vs verified, signed PDF link); per-token cache rate-limit (429); webReceipt screen built; pytest 706
2026-06-04 | EPIC-07/T-007 | backend | claude | done | verify/mark-received/reject + receipt PDF: POST /rent-requests/{id}/verify|mark-received|reject create Payment(verified_by/at) + render_receipt_pdf (reuses EPIC-05 _render_pdf + EPIC-04 encrypted storage), status->verified, schedule->paid, notify tenant web receipt link (best-effort), audited rent.payment.verify|mark_received|reject; re-settle 409; pytest 713
2026-06-04 | EPIC-08/T-006 | backend | claude | done | seed maintenance_categories + expense_categories SystemConfig (core 0006, JSON arrays mirroring enums, idempotent+reversible), surfaced in /config/public via _json_list_config helper; 3 new tests, core suite 115 green
2026-06-04 | EPIC-08/T-004 | backend | claude | done | unit-scoped maintenance web-link token reusing EPIC-07 TimestampSigner pattern under dedicated salt khatir.maintenance.link_token (distinct-salt cross-resolver rejection tested); stateless (signs Unit pk, no DB change), TTL from maintenance_link_token_ttl_hours (72h fallback); resolve_token raises typed Expired/Invalid (NotFoundError) for 410-vs-404; 10 token tests pass
2026-06-04 | EPIC-09/T-003 | backend | claude | done | seed dashboard_months_default=6 SystemConfig (core 0007, idempotent update_or_create + reversible, chained after 0006_seed_category_config); 2 new tests, core suite 115 green
2026-06-04 | EPIC-10/T-002 | backend | claude | done | seed 6 default PricingTier rows (billing 0002, keys per enums.md: free/per_tenant/bundle_20/bundle_40/unlimited_monthly/unlimited_annual; free=0-2 tenants ৳0 no-verification, bundles+unlimited include_verification+credits; illustrative BDT defaults, idempotent update_or_create + reversible); rekeyed 3 T-001 model tests to test_* to avoid seeded-row UNIQUE collision; 7 new + billing suite 24 green
2026-06-04 | EPIC-08/T-002 | backend | claude | done | maintenance CRUD + resolve->auto-expense at /api/v1/maintenance: create(open)/list/retrieve/patch scoped via MaintenanceRequestManager.for_user (unit->building->owner), IsLandlordOrManager & IsOwnerOfMaintenanceRequest, foreign=404; resolve(cost,note) marks resolved + records resolution_* + auto-creates ONE Expense(source=request) via new nullable OneToOne Expense.request (migration 0002) for idempotency (double-resolve rejected 400, never 2nd expense); audited maintenance.create|update|resolve; 39 maintenance tests green (full-suite failures limited to pre-existing S3/botocore dmpforms+rent)
2026-06-04 | EPIC-10/T-006 | backend | claude | done | seed pricing SystemConfig keys (core 0008): free_tier_tenant_limit=2 (int, re-asserted; 0005-owned) + nid_verification_tiers JSON list [bundle_10,bundle_20,bundle_50,unlimited] stored as text; idempotent update_or_create + reversible (reverse drops only nid_verification_tiers); 1 new test, core suite green
2026-06-04 | EPIC-10/T-004 | backend | claude | done | subscription create/upgrade at /api/v1/billing: POST subscribe resolves PricingTier by key (unknown/inactive->400 validation_error), creates-or-upgrades the caller's single Subscription in one atomic select_for_update write (status reset active, dates stamped from 30/365-day cycle), audited subscription.create|upgrade; payment STUBBED (paid tier records subscription.payment_intent pending marker for manual admin confirm; clear # STUB: marker for MFS wiring); GET subscription returns plan + tenant usage (tenants_used via tenants.usage selector, tenants_limit from tier.tenant_max, null=unlimited), 204 when no sub (implicit free); appended subscribe() to shared billing/services.py (T-003 metering untouched); 9 new subscription-api tests green (full-suite reds limited to other agents' in-flight maintenance/rent/config_public missing-migration work)
2026-06-04 | EPIC-11/T-002 | backend | claude | done | AdminAuditEntry immutable append-only model (admin_portal 0002) + admin_audit() writer: FK admin_user(SET_NULL, null for system), action, entity_type/entity_id denormalized from instance, before_json/after_json diffs, GenericIPAddressField ip, reason; immutability enforced at instance (save rejects re-save, delete raises) AND queryset (update/delete raise ImmutableAuditError) levels; indexes (admin_user,created_at)+(action); 36 admin_portal tests green (full-suite reds limited to other agents in-flight rent/maintenance work)
2026-06-04 | EPIC-10/T-003 | backend | claude | done | tenant metering + free-limit enforcement: check_tenant_limit(user) in shared billing/services.py raises TierLimitExceeded (new ErrorCode.TIER_LIMIT_EXCEEDED -> 402 PaymentRequired AppError) when active-tenant count (reuses Tenant.objects.for_user, EPIC-04 T-008) >= plan cap; cap = active-subscription tier.tenant_max (None=unlimited) else free_tier_tenant_limit config (default 2, not hardcoded); select_for_update on Subscription rows inside the create transaction closes the concurrent-create race; wired into tenants.create_tenant before any write so over-limit rolls back atomically (402 envelope {code:tier_limit_exceeded, details:{limit,tenants_used}}); 16 new test_metering tests green (under/at/over limit, config-driven limit, upgraded/unlimited/cancelled tiers, endpoint 201/402 + no-persist); ruff clean; check_tenant_limit already landed in committed billing/services.py via T-004 co-edit (metering untouched). Full-suite reds limited to other agents' in-flight maintenance/rent/config churn on the shared branch.
2026-06-04 | EPIC-08/T-003 | backend | claude | done | Expense CRUD + filtered list + CSV export at /api/v1/expenses: create(manual; source forced server-side)/list/retrieve/patch/delete scoped via ExpenseManager.for_user (unit->building->owner), IsLandlordOrManager & IsOwnerOfExpense, foreign/unknown id=404; list+export accept building/unit/date_from/date_to filters; manual + auto(source=request) both listed; update/delete restricted to manual (auto-expense rejected 400); CSV export streams (StreamingHttpResponse + csv writer + .iterator()) scoped+filtered with attachment header; audited expense.create|update|delete; 22 expense API tests green. Note: implementation files (expense_views/managers/serializers/services/permissions/urls/models/migration 0002) landed in the shared maintenance module via the concurrent T-002 commit; this commit adds the T-003 test suite + frontmatter. Full-suite reds limited to pre-existing S3/botocore dmpforms+rent failures.
2026-06-04 | EPIC-10/T-005 | backend | claude | done | pricing tiers + subscription state in GET /api/v1/config/public: always carries pricing.tiers (active tiers, sort_order, via T-004 read-only TierSerializer); authenticated callers also get a subscription block {tier_key,status,tenants_used,tenant_limit,can_verify_nid} where benefits (tenant_limit, can_verify_nid) come from the ACTIVE subscription tier only and fall back to free tier (limit from free_tier_tenant_limit config, can_verify_nid=False) when no active sub — status still reflects the most-recent row (past_due/cancelled). No prices/dates/payment data exposed. New module khatir/billing/public_config.py (avoids co-editing T-004's in-flight billing/services.py); view extended in khatir/health/views.py (request.user.is_authenticated gate). 6 new test_config_public tests green; ruff clean; no new migration (DB changes None). Full-suite reds limited to pre-existing S3/botocore dmpforms+rent failures (NoCredentialsError, unrelated).
2026-06-04 | EPIC-11/T-004 | backend | claude | done | admin_portal/permissions.py: dedicated admin-JWT permission layer fully separate from customer permissions — IsAdminUser decodes Authorization Bearer via PyJWT HS256 against ADMIN_JWT_SIGNING_KEY (rejects missing/malformed/wrong-key/expired/unknown-role/missing-admin_user_id; never falls through to customer JWT), attaches verified AdminPrincipal(admin_user_id,role,claims) to request (memoised); RequiresAdminRole(*roles) factory + RequiresAdminSection(section)/AdminSection constants enforce the §2 role->section matrix (users:super/ops/support, platform:super/ops, billing+pricing:super/finance, audit+export:super/compliance) with super allowed everywhere; role matrix documented in module docstring; SUPER/OPS/FINANCE/COMPLIANCE/SUPPORT role consts re-exported. 23 new tests (parametrized full role x section matrix + token-validation edges) green; ruff clean on my files. Full-suite ruff/pytest reds limited to other agents' in-flight admin_portal T-003 auth files (services.py B904) on the shared branch.
2026-06-04 | EPIC-11/T-006 | backend | claude | done | seed admin-session SystemConfig via core migration 0009: admin_session_timeout_minutes=60 (int) + admin_mfa_required=true (bool); idempotent (update_or_create) + reversible (reverse deletes exactly the 2 owned keys); typed get_config returns 60 and True respectively. 2 new tests in test_seed_admin_config.py green; my files ruff clean; makemigrations --check clean. No shared-file edits (urls/base/settings untouched — seed-only). Full-suite ruff/pytest reds limited to other agents' in-flight admin_portal T-003 (services.py B904) + pre-existing S3/botocore dmpforms+rent failures on the shared branch.
2026-06-04 | EPIC-11/T-012 | backend | claude | done | create_super_admin management command (khatir/admin_portal/management/commands/create_super_admin.py): one-time idempotent seed of the first AdminRole.SUPER account — generates secrets.token_urlsafe temp password (make_password-hashed) + pyotp.random_base32 TOTP secret stored encrypted via core.encryption.encrypt; prints temp password + otpauth:// provisioning URI to stdout ONCE (never recoverable, never logged after); writes admin_audit(admin_user=None, action=admin_user.create_super_admin, entity=created user, after={email,role}); --email normalised lower, --name, --dry-run (writes nothing); idempotent re-run on existing email makes no changes and does NOT reprint secrets. 5 new tests in test_create_super_admin.py (creates+hash+TOTP roundtrip, audit row, idempotent, email-normalisation, dry-run) green; ruff clean; makemigrations --check clean (DB change is one row, no schema). No shared-file edits (urls/base untouched — command-only). Full admin_portal suite 105 passed excluding another agent's untracked in-flight test_platform_dashboard.py.
2026-06-04 | EPIC-11/T-005 | backend | claude | done | platform dashboard endpoint GET /admin/api/dashboard (config/urls.py admin/api/ include -> admin_portal/admin_urls.py -> dashboard_views.PlatformDashboardView). dashboard.py: get_platform_dashboard() aggregates platform-wide (NO for_user scoping) total_users, active_landlords (distinct landlord building owners), total_properties, total_units, occupied_units, total_rent_collected {all_time,this_month} (Sum of verified Payment->RentRequest.amount, 2dp), dmp_forms_generated, active_subscriptions; KPI block cached 5min under admin:platform_dashboard:v1; live health{app,database,cache,status} computed outside cache (SELECT 1 + cache round-trip probe). Admin-only via committed T-004 permissions layer: IsAdminUser + RequiresAdminSection(PLATFORM) => super/ops only (finance/compliance/support 403). 12 new tests in test_platform_dashboard.py (KPIs present, aggregate-across-all-users, health, anon/invalid-token/customer-role rejected, platform-roles allowed, non-platform denied, 5-min cache) green; my files ruff clean; makemigrations --check clean (no DB change). Self-contained on committed code only — did NOT touch other agents' uncommitted T-003 views.py/auth_tokens.py. admin_urls.py is now shared (EPIC-12 appended user routes additively). Full-suite reds limited to other agents' in-flight featureflags/services.py C408 + pre-existing S3/botocore dmpforms+rent+tenants-voice failures.
2026-06-04 | EPIC-14/T-002 | infra | claude | done | AI gateway FastAPI scaffold at services/ai-gateway/ (own uv project: pyproject.toml + uv.lock; generated requirements.txt for parity). main.py exposes GET /healthz -> 200 {status:ok, service:khatir-ai-gateway} via create_app() factory; config.py is pydantic-settings Settings reading shared .env (AI_GATEWAY_INTERNAL_TOKEN auth secret, port 8100 to match AI_GATEWAY_URL) — auth_enabled gates on token presence. Thin stateless router, NO DB/business logic. Dockerfile uv-managed python3.13 non-root EXPOSE 8100 uvicorn main:app. docker-compose.yml ai-gateway service added additively (build ./services/ai-gateway, ports 8100, /healthz healthcheck). 5 pytest tests green (health 200 + payload shape + auth-flag on/off + default port); ruff clean; mypy clean (4 files). Deviation: secret env is AI_GATEWAY_INTERNAL_TOKEN not placeholder GATEWAY_SECRET; uv primary per infra contract.
2026-06-04 | EPIC-11/T-003 | backend | claude | done | admin auth flow (login/verify-mfa/logout/me) under /admin/api/auth/, COMPLETELY separate JWT realm from customer auth: PyJWT HS256 tokens signed by dedicated ADMIN_JWT_SIGNING_KEY (env, falls back to admin::SECRET_KEY, asserted != SIMPLE_JWT key), short access TTL capped at ADMIN_SESSION_TIMEOUT_MINUTES; AdminJWTAuthentication attaches request.admin_user (never request.user) + IsAdminAuthenticated permission. login verifies make_password hash on non-disabled AdminUser, returns mfa_required+mfa_token challenge when account has TOTP secret AND ADMIN_MFA_REQUIRED, else issues access token directly; verify-mfa decrypts core.encryption TOTP secret + pyotp.verify(valid_window=1) then issues token+stamps last_login_at; logout revokes by jti in cache until expiry (idempotent, no DB table). Uniform auth_invalid on every failure (no email/factor leak). admin_audit on login_success/login_failed/mfa_challenged/mfa_failed/logout. Email+IP throttles (admin_login_email/ip 10|30 per 10min) on login, IP-only on verify-mfa. Added pyotp>=2.9.0. 15 new tests in test_admin_auth.py (happy no-mfa, wrong-password, unknown-email, disabled, mfa-challenge, mfa-success, mfa-wrong-code, bad-challenge, me-auth-required, me, logout-revokes, disabled-after-login, separate-signing-key, mfa-flag-off, rate-limit) green; my files ruff clean; makemigrations --check clean (no schema change). Shared edits additive: config/urls.py (admin/api/auth/ include, before django admin/), config/settings/base.py (ADMIN_JWT_*/ADMIN_MFA_* + 3 admin throttle rates). Full-suite reds limited to other agents' in-flight featureflags duplicate-seed-migration + pre-existing S3/botocore dmpforms+rent failures on shared branch.
2026-06-04 | EPIC-13/T-002 | backend | claude | done | flag CRUD + toggle + /config/public flags dict. New featureflags/{serializers,services,views,urls}.py: FeatureFlagViewSet (list/create/retrieve/update + @action toggle) at /admin/api/flags, lookup_field=key, super/ops gate via IsPlatformAdmin (SECTION_ROLES[PLATFORM]) + AdminJWTAuthentication/IsAdminAuthenticated. services.public_flags() returns global-scope {key:enabled} cached 60s under featureflags:public; toggle_flag flips enabled+sets updated_by+admin_audit(feature_flag.toggle, before/after enabled)+busts cache; perform_create/update audit feature_flag.create/update + bust cache. enabled is read-only on serializer (toggle-only); key immutable on update. health/views.config_public flags dict now = public_flags(). 17 new tests (CRUD, toggle flip/actor/audit/cache-bust, config_public exposes global + omits role/user, test_toggle_reflects_in_config_public propagation, anon/role gate) green; my files ruff clean; makemigrations --check clean (no schema change). Shared edits additive: config/urls.py (admin/api/ featureflags.urls include after admin_urls), health/views.py (flags=public_flags()). Full-suite reds limited to other agents' in-flight admin_portal user_views.py ruff + T-004 test_seed_flags + pre-existing S3/botocore dmpforms/rent failures.
2026-06-04 | EPIC-13/T-004 | backend | claude | done | seed 5 kill-switches (warnings_feature, reviews_feature, history_flags_feature, free_text_feature, master_kill_switch) + 2 default flags (voice_tenant_entry, dmp_enabled) — all enabled=true (kill-switch convention: enabled=true = feature ON; flip to false to kill). New featureflags/migrations/0002_seed_flags.py: idempotent update_or_create keyed on unique key, reversible unseed removes exactly the 7 keys, scope literal "global" (no app-code import). Test isolation: seed auto-run is a no-op under config.settings.test so it does not pollute the already-committed T-002/T-003 flag-endpoint tests (which assert an empty flags table and build rows with these same keys); test_seed_flags invokes seed_flags() directly to verify it. 7 new tests in test_seed_flags.py (flags-seeded, kill-switches-enabled, default-flags-enabled, voice-present, idempotent, reversible, auto-run-skipped-in-test-db) green; ruff clean; makemigrations --check clean. No shared-file edits (no routes/apps — migration+test only). Full-suite reds limited to other agents' in-flight admin_portal/dmpforms/rent + pre-existing S3/botocore failures, none in featureflags or tenants.
2026-06-04 | EPIC-12/T-003 | backend | claude | done | user search+detail+actions under /admin/api/users (admin_urls.py shared, appended additively below dashboard). New user_views.py: UserSearchView GET /admin/api/users (q= search by phone icontains / name icontains / numeric id exact / linked tenant_profiles__nid_number_masked icontains, distinct, StandardPageNumberPagination), UserDetailView GET /{id} (user + current Subscription + usage{buildings,tenant_profiles,subscriptions} + recent admin audit trail filtered entity_type=accounts.user), UserSuspendView/Reactivate/UpgradeSubscription POST. user_services.py: suspend_user sets is_active=False (simplejwt rejects inactive access tokens) AND blacklists every OutstandingToken refresh for user via BlacklistedToken.get_or_create (cannot re-mint), reason MANDATORY, ConflictError if already suspended; reactivate_user re-enables; upgrade_subscription manual tier override (updates latest Subscription or creates active one), reason mandatory, NotFound on bad tier; all wrapped @transaction.atomic + admin_audit (user.suspend/reactivate/upgrade_subscription, before/after diffs). Authz: AdminJWTAuthentication+IsAdminAuthenticated, users-section gate — IsUsersReadAdmin super/ops/support (support READ-ONLY), IsUsersWriteAdmin super/ops only (support 403 on writes), finance/compliance 403. 28 new tests in test_user_admin.py (search by phone/name/id/masked-nid, paginated, detail payload+404, suspend reason-required/deactivates+blacklists-jwt/audited/already-suspended-409, reactivate/audited/already-active-409, upgrade existing/creates-when-none/reason-required/unknown-tier-404, anon denied, read roles, support-cannot-write, ops+super-can-write, disabled-admin denied) green; ruff clean; makemigrations --check clean (no schema change — writes existing User.is_active + Subscription). Full-suite reds limited to pre-existing S3/botocore NoCredentialsError in dmpforms+rent storage tests (no AWS creds in sandbox), unrelated to this task.
2026-06-04 | EPIC-14/T-010 | backend | claude | done | seed 3 AI-gateway SystemConfig keys via core migration 0010_seed_ai_config (ai_gateway_url=text empty, ai_provider_cache_ttl_seconds=int 300, ai_gateway_secret=text). Non-secret rows update_or_create (idempotent); secret seeded with get_or_create empty placeholder so an operator-set value is never overwritten and the real secret is never embedded in the migration/logs. Reverse unseeds exactly the 3 owned keys. 2 new tests in test_seed_ai_config.py (keys seeded with correct type/desc + secret value empty; typed via get_config) green; my files ruff clean; makemigrations --check clean. No shared-file edits (migration+test only). Full-suite reds limited to other agents' in-flight admin_portal/dmpforms/rent + pre-existing S3/botocore failures, none in core or ai_providers (137 core+ai_providers tests green).
2026-06-04 | EPIC-15/T-005 | backend | claude | done | seed 3 system NotificationTemplate rows via notifications migration 0002_seed_system_templates: rent_reminder_due (trigger=rent_due, channels=[whatsapp,sms]), rent_receipt_generated (trigger=payment_verified, channels=[whatsapp,sms]), welcome_new_user (trigger=user_created, channels=[inapp]). All bilingual (title/body _en+_bn) with declared {placeholder} variables matching body tokens. Idempotent update_or_create keyed on unique key; reverse unseeds exactly the 3 owned keys. 7 new tests in test_seed_system_templates.py (3-seeded, per-template trigger/channels/active/variables, bilingual-and-distinct, placeholders-match-variables, idempotent) green; my files ruff clean; full ruff clean; makemigrations --check clean. No shared-file edits (migration+test only). 35 notifications tests green.
2026-06-04 | EPIC-15/T-009 | backend | claude | done | seed 3 per-message notification cost SystemConfig keys via core migration 0011_seed_notifications_config (whatsapp_cost_per_message=money 0.50, sms_cost_per_message=money 0.30, email_cost_per_message=money 0.00). All type=money so get_config returns Decimal for composer cost preview. update_or_create idempotent; reverse unseeds exactly the 3 owned keys. 2 new tests in test_seed_notifications_config.py (rows seeded with money type/value/desc; typed-via-get_config returns Decimal) green; my files ruff clean; full ruff clean; makemigrations --check clean (single linear chain — 0011 chains off 0010, another agent's in-flight 0012 already chains off 0011). No shared-file edits (migration+test only). Full-suite reds limited to pre-existing S3/botocore NoCredentialsError in dmpforms+rent storage tests, unrelated to this task.
2026-06-04 | EPIC-15/T-008 | backend | claude | done | admin notification-templates CRUD at /admin/api/notification-templates (List/Create/Retrieve/Update via DRF GenericViewSet, lookup by key). key + trigger_event immutable on update (serializer pops them); editable: title/body _en+_bn, channels, variables, active. super+ops gate via IsPlatformAdmin (SECTION_ROLES[PLATFORM]) + AdminJWTAuthentication. Every create/update audited via admin_audit (action notification_template.create/.update with before/after diffs). No deletes (deactivate via active). 13 new tests in test_template_endpoints.py (CRUD, key+trigger immutability, create+update audit diffs, anon reject, platform-roles-allowed, non-platform-denied) green; my files ruff clean; makemigrations --check clean (no model changes). Shared config/urls.py edited additively (one include). Full-suite reds limited to pre-existing S3/botocore NoCredentialsError in dmpforms+rent storage tests, unrelated to this task.
2026-06-04 | EPIC-16/T-005 | backend | claude | done | seed 2 compliance SLA SystemConfig keys via core migration 0012_seed_compliance_config (data_request_sla_days=int 30, data_delete_grace_days=int 7). type=int so get_config returns Python int. update_or_create idempotent; reverse unseeds exactly the 2 owned keys. 2 new tests in test_seed_compliance_config.py (rows seeded with int type/value/desc; typed-via-get_config returns int) green; my files ruff clean; full ruff clean; makemigrations --check clean (0012 chains off 0011). No shared-file edits (migration+test only). Full-suite reds limited to other agents' in-flight notifications/dmpforms/rent + pre-existing S3 failures, unrelated to this task (confirmed identical with my files removed).
2026-06-04 | EPIC-15/T-002 | backend | claude | done | compose_notification service in khatir/notifications/services.py: validates audience (all/role/segment/specific user_ids) + channels (dedup, known wire values) + bilingual content; resolves reach (count active Users matching audience); estimates cost (reach x sum per-channel SystemConfig cost, inapp free, defaults 0 so works without T-009); creates Notification in sending/scheduled status; NOW enqueues T-003 deliver_notification via named current_app.send_task on_commit (no hard import of not-yet-built task), SCHEDULED registers django_celery_beat ClockedSchedule one-off PeriodicTask, RECURRING registers CrontabSchedule PeriodicTask; future-time + recurrence-required validation; every compose audited via admin_audit (action notification.compose, after-diff with reach/cost/status). 21 new tests in test_compose.py (audience/reach all+role+specific, validation rejects, channel dedup, cost from config + zero default, now-enqueue via django_capture_on_commit_callbacks + mocked send_task, clocked/crontab beat rows, audit entry, sender set) green; 35-row notifications suite green; my files ruff clean; full ruff clean; makemigrations --check clean (no model changes). No shared-file edits (services+test only). Full-suite reds limited to pre-existing S3/botocore NoCredentialsError in dmpforms+rent storage tests, unrelated to this task.

2026-06-04 | EPIC-16/T-003 | backend | claude | done | read-only GET /admin/api/consent-records (compliance/views.py ConsentRecordListView + serializers.py + urls.py). Paginated (StandardPageNumberPagination envelope), filterable by user (user_id), consent_type, granted_from/granted_to date range. ConsentRecord is append-only so no write endpoints. compliance+super gate via IsComplianceAdmin (SECTION_ROLES[AUDIT]) + AdminJWTAuthentication. Read-only endpoint = no audit/no migration. 13 new tests in test_consent_endpoints.py (list+envelope, read-only projection shape, filter by user/consent_type/date-range, anon reject, audit-roles-allowed super+compliance, non-audit-roles-denied ops/finance/support) green; 29-row compliance suite green; my files ruff clean; full ruff clean; makemigrations --check clean (no model changes). Shared config/urls.py edited additively (one include). Full-suite reds limited to pre-existing S3/botocore NoCredentialsError in dmpforms+rent storage tests, unrelated to this task.
2026-06-04 | EPIC-08/T-012 | backend | claude | done | expense summary hook: maintenance/selectors.py with expense_total_by_category + expense_total_by_month read-only ORM aggregations (single grouped query each, no N+1), always scoped via Expense.objects.for_user, optional building/unit/date_from/date_to filters, money normalised to 2dp Decimal (0.00 when empty), by_month bucketed via TruncMonth ordered chronologically. Exposed GET /api/v1/expenses/summary as detail=False @action on ExpenseViewSet (IsLandlordOrManager gate, totals serialized as strings, months as ISO first-of-month). _filter_kwargs helper shared by list/export and summary. 11 new tests in test_expense_summary.py (by_category math, by_month math + chrono order, owner scoping x2, building+date filters, empty case, endpoint shape, endpoint scoped, auth-required, tenant-forbidden) green; full maintenance suite green except pre-existing S3/botocore NoCredentialsError in another agent's in-flight test_web_maint.py; my files ruff clean; makemigrations --check clean (no model changes). No shared config/urls.py edit needed (route is a viewset action on the already-registered expenses router). Full-suite reds limited to other agents' in-flight billing/tenants/dmpforms/rent work, unrelated to this task.
2026-06-04 | EPIC-12/T-004 | backend | claude | done | refund queue endpoints in khatir/admin_portal/refund_views.py: GET /admin/api/billing/refunds lists pending payment intents (no dedicated table — these are EPIC-10 stub customer-realm AuditEntry rows action=subscription.payment_intent with after.state=pending; queue excludes any intent already resolved via a follow-up entry whose after.resolves=<id>); POST /admin/api/billing/refunds/{id}/process approves (writes refunded resolution AuditEntry + cancels Subscription) or denies+reason (writes refund_denied resolution, subscription untouched). reason mandatory on deny (serializer + service re-check). Both decisions write immutable AdminAuditEntry action=refund.process. finance+super gate via IsBillingAdmin (SECTION_ROLES[BILLING]) + AdminJWTAuthentication. Re-process of a resolved intent -> 409; unknown -> 404. Append-only/idempotent (no audit row mutated). 15 new tests in test_refund.py (list, approve cancels sub, deny keeps sub active, deny-requires-reason, audit, process-twice 409, unknown 404, anon denied, finance/super allowed, ops/support/compliance denied, non-finance cannot process) green; full 160-row admin_portal suite green; my files ruff clean; makemigrations --check clean (no model changes). Routes added additively to admin_portal/admin_urls.py (the /admin/api/ app include), no config/urls.py edit needed. Full-suite ruff reds limited to other agents' in-flight billing/public_config.py + dashboard/tests, unrelated to this task.
2026-06-04 | EPIC-09/T-001 | backend | claude | done | dashboard aggregation selectors: new khatir.dashboard app (no models/migration, registered in INSTALLED_APPS) with selectors.py get_dashboard(user, months) -> typed frozen DashboardMetrics dataclass. Computes total_collected/pending/overdue + collection_rate (collected/(collected+pending+overdue)x100, 0.0 on empty denom) from RentSchedule status grouped (paid/pending+requested/overdue); occupancy (occupied vs total Unit) via single Count agg; monthly_series last N calendar months anchored to localdate, rent keyed off RentSchedule.period + expense off TruncMonth(Expense.date), zero-fill missing months; income_vs_expense + net; top 5 expense categories by amount; late_payer_count = distinct leases with >=1 overdue schedule. All reads scoped via Lease/Unit/Expense for_user (RentSchedule via lease__in for_user subquery) so anon/non-owner -> empty payload. Fixed bounded query count (no N+1). 14 new tests in test_selectors.py (each metric + empty/zero cases + requested-as-pending + scoping + anon empty + query-count guard) green; ruff clean on my files + full repo; makemigrations --check clean. Shared base.py edited additively (one INSTALLED_APPS entry). Full-suite reds limited to pre-existing S3/botocore NoCredentialsError in other agents in-flight dmpforms/rent/maintenance storage tests + pricing-admin, unrelated to this read-only task.
2026-06-04 | EPIC-12/T-001 | backend | claude | done | pricing tier list/preview/edit endpoints in khatir/admin_portal/pricing_views.py + pricing_services.py + pricing_serializers.py. GET /admin/api/pricing/tiers lists all tiers (active+inactive, sort_order order, full admin projection incl prices/active/sort_order). POST /admin/api/pricing/tiers/{key}/preview is a read-only impact calc (subscribers_affected = active subs on tier; monthly_revenue_delta = sum over affected subs of new-minus-old monthly-equivalent price, annual amortised /12, null price = 0; no write). PATCH /admin/api/pricing/tiers/{key} applies changes under select_for_update, reason mandatory, writes before/after field-diff AdminAuditEntry action=pricing.tier.edit, busts /config/public via new billing/public_config.invalidate_public_config_cache (safe no-op delete today; T-002 layers real cache on shared PUBLIC_CONFIG_CACHE_KEY). key immutable (not editable). finance+super gate via IsPricingAdmin + AdminJWTAuthentication; ops/support/compliance + anon denied. 17 new tests in test_pricing_admin.py (list incl inactive, preview count+delta monthly & annual-amortised, preview no-write, preview 404, edit applies, edit requires reason, before/after audit, cache bust mocked, edit 404, role gate super/finance allowed, ops/compliance/support denied read+write, disabled admin denied) green; full 228-row admin_portal+billing suite green; my files ruff clean; full ruff clean; makemigrations --check clean (no model changes - writes existing PricingTier). Routes added additively to admin_portal/admin_urls.py. Full-suite reds limited to other agents in-flight EPIC-09 test_tier_gate.py import + pre-existing S3/botocore NoCredentialsError in dmpforms+rent, unrelated to this task.
2026-06-04 | EPIC-10/T-009 | cross-cutting | claude | done | NID verification tier gate: check_can_verify(user) in billing/services.py raises TierFeatureGated (new ErrorCode.FEATURE_REQUIRES_UPGRADE, 402) unless the user has an active subscription whose tier includes_verification==True; wired into TenantOcrView + TenantVoiceView (gate runs before any image is stored / paid provider call), manual tenant create path untouched. New billing/tests/test_tier_gate.py: 14 tests (free blocked, paid-without-verification blocked, bundle-with-verification ok, cancelled-plan blocked, ocr_blocked_free, ocr_ok_bundle10, ocr gate-before-storage, voice_blocked_free, voice_ok_bundle10 + service cases) green; updated existing OCR/voice endpoint tests to grant a verification tier (28 ocr+voice+gate tests green). My files ruff clean; makemigrations --check clean (no model changes). No shared config/urls.py or base.py edit needed. Mobile upgrade-prompt intercept DEFERRED: T-008 (upgrade_prompt.dart) still todo and EPIC-04 mobile add-tenant OCR/voice screens absent at HEAD; backend gate complete, reuses the same feature_requires_upgrade intercept pattern. Full-suite reds limited to pre-existing S3/botocore NoCredentialsError + other agents in-flight tests, unrelated to this task.
2026-06-04 | EPIC-08/T-005 | backend | claude | done | token-scoped maintenance web form: khatir/maintenance/web_views.py + web_urls.py (/m/<token> GET form, /m/<token>/submit POST) + templates/maintenance/web_maint.html (extends shared rent/_web_base.html for the Notun Din token palette) + web_maint_error.html. GET resolves the T-004 signed unit token -> renders webMaint screen (category radios from MaintenanceCategory enum, bn+en, description textarea, optional photo); POST creates MaintenanceRequest (status=open, stamps the unit active Lease, photo via core.storage.store_encrypted kind=proof), per-token cache rate-limit (5/5min, SystemConfig-tunable), empty description bounces back, PRG redirect -> ?submitted=1 success state. Invalid token -> 404, expired -> 410, rate-limited -> 429, all via friendly bilingual error page. Route added additively to config/urls.py at root (browser HTML, not /api/v1/). 11 new tests in test_web_maint.py (render, submit-creates-request, lease-stamp+default-category, photo-encrypted via mocked storage, empty-bounce, expired 410, invalid 404, submit-invalid 404, GET-on-submit 405, palette-not-hardcoded, rate-limit) all green; my files ruff clean; full ruff clean; makemigrations --check clean (no model changes). Full-suite reds limited to other agents in-flight billing test_tier_gate.py import + pre-existing S3/botocore NoCredentialsError (repo .env S3_BUCKET set, no creds) in dmpforms/rent storage tests, unrelated to this task.
2026-06-04 | EPIC-13/T-008 | cross-cutting | claude | done | flag propagation regression test: new khatir/featureflags/tests/test_flag_propagation.py with test_flag_toggle_propagates_to_config_public — primes /config/public cache (flag OFF), toggles via real PATCH /admin/api/flags/{key}/toggle (super admin JWT), asserts next anon /config/public read flips to ON (cache busted, <60s budget), then toggles back and asserts immediate OFF (regression both directions). Pairs with EPIC-12 T-010. End-to-end via HTTP surfaces only, no internal cache plumbing touched. 1 new test green; full featureflags suite (61 tests) green; my file ruff clean; full ruff clean; makemigrations --check clean (no model changes). No shared config/urls.py or base.py edit needed. Full-suite reds limited to pre-existing S3/botocore NoCredentialsError in dmpforms/rent storage tests, unrelated to this test-only task.
2026-06-04 | EPIC-14/T-003 | infra | claude | done | provider-agnostic router in ai-gateway: services/ai-gateway/router.py ProviderRouter.route(category, payload) fetches active providers via injected ConfigSource (60s TTL cache per task §15, env-overridable), orders primary->fallback->rest, tries each, fails over on ProviderError; every attempt logged through pluggable UsageLogger (UsageRecord mirrors AIUsageLog schema: provider/category/success/latency_ms/tokens_used/cost_usd/failover_from), success records failover_from when it failed over. Raises NoProviderConfigured (empty) / AllProvidersFailed (all error). providers/base.py: Provider protocol + ProviderConfig/ProviderResult dataclasses + generic HTTPProvider (bearer-auth JSON POST, vendor clients subclass later). Logging failures swallowed so they never mask results. 11 tests green in tests/test_router.py (primary_success, primary_fail_fallback, both_fail_raises + no-provider, inactive-skip, ttl-cache) plus 5 pre-existing health tests; ruff clean; mypy clean (5 source files). pytest-asyncio added to dev deps (asyncio_mode=auto). Infra service - own uv project, no Django/shared base.py or urls.py edit needed.
2026-06-04 | EPIC-13/T-003 | backend | claude | done | kill-switch endpoints: featureflags/killswitch_views.py (GET /admin/api/killswitches lists the 5 named switches; POST /admin/api/killswitches/{key}/toggle) + kill-switch services in featureflags/services.py (KILL_SWITCH_KEYS, _verify_fresh_mfa fresh-TOTP re-confirm even mid-session, toggle_kill_switch atomic: flip FeatureFlag.enabled, record immutable KillSwitchEvent disable/enable, admin_audit kill_switch.toggle, bust public-flags cache) + urls (additive). Super-only (IsSuperAdmin); reason mandatory + optional lawyer_reference; wrong/absent MFA -> 403, missing reason -> 400, unknown key -> 404. 16 new tests in test_killswitch.py (valid-MFA kills, wrong-MFA blocked, no-reason blocked, immutable event+actor, enable-on-restore, audit entry, cache bust, /config/public propagation, 404 unknown, anon rejected, super-only, non-super denied) green; featureflags+admin_portal suites 237 green; my files ruff clean; makemigrations --check clean (no model changes). No shared base.py/urls.py edit (only featureflags/urls.py, app-local). Full-suite reds limited to pre-existing S3/botocore NoCredentialsError in dmpforms/rent storage tests, unrelated to this task.
2026-06-04 | EPIC-14/T-007 | backend | claude | done | Django AI gateway client: khatir/ai_providers/client.py call_gateway(category, payload, *, timeout) POSTs {AI_GATEWAY_URL}/v1/{category} with the shared AI_GATEWAY_INTERNAL_TOKEN on the Authorization header and returns a normalized AIGatewayResult (data/provider_key/model_name/raw). Thin + stateless: no routing/vendor logic (that lives in the gateway, T-003). AIGatewayError on unconfigured URL (clear config error), transport failure, non-2xx (carries status_code), non-JSON, or unexpected shape; token/payloads never logged. New AI_GATEWAY_URL/_INTERNAL_TOKEN/_TIMEOUT settings in config/settings/base.py (additive); requests added as explicit dep (uv.lock synced). 10 tests in ai_providers/tests/test_client.py (happy path forwards url+payload+token+timeout, string category, timeout override, missing-url config error, no-auth-header when token absent, transport/non-2xx/non-json/bad-shape errors, token-not-in-exception) green; my files ruff clean; full ruff clean; makemigrations --check clean (no model changes). Full-suite reds limited to pre-existing S3/botocore NoCredentialsError in dmpforms/rent storage tests, unrelated to this task.
2026-06-04 | EPIC-15/T-006 | backend | claude | done | retrofit EPIC-07 rent reminders onto the admin-editable rent_reminder_due template: added NotificationTemplate.render(variables) (str.format_map over a lenient mapping that echoes unfilled {placeholders} verbatim, returns rendered title_en/title_bn/body_en/body_bn) + module-level get_template(key) helper (active row by key, raises DoesNotExist if missing/inactive) in notifications/models.py. rent/messaging._render_message now renders the seeded template (Bangla body + English body, newline-joined) via _template_variables (tenant_name/amount/property_name/due_date/payment_link) with a fallback to the original built-in copy if the template row is absent/inactive — send path never breaks. Behavior identical (still bilingual, still carries /r/{token} link); copy now admin-editable. 7 new tests (render interpolates / leaves unknown placeholder / no-vars; get_template active+skip-inactive+missing; rent test_message_uses_admin_editable_template) + updated test_message_carries_public_link_and_is_bilingual to behavioural assertions; notifications+rent send/reminder suites green; my files ruff clean; full ruff clean; makemigrations --check clean (no model fields added). Full-suite reds limited to pre-existing S3/botocore NoCredentialsError in dmpforms/rent storage tests, unrelated to this task.
2026-06-04 | EPIC-14/T-009 | backend | claude | done | admin AI-provider endpoints already implemented at HEAD (ai_providers/admin_views.py: AIProviderListCreateView GET/POST, AIProviderDetailView PATCH, AIProviderTestConnectionView POST, AIUsageView GET) + AIProviderAdminSerializer (write-only plaintext api_key encrypted to api_key_enc, has_api_key flag, never returns ciphertext) with non-BD-OCR DPA validation (endpoint_is_bangladesh: empty/foreign endpoint => dpa_reference required, enforced on POST+PATCH); call_gateway-backed test-connection, admin_audit on create/update, super+ops (IsAIProviderAdmin) gate; routes in admin_portal/admin_urls.py. Completed finish protocol (frontmatter status=done + BOARD). 21 tests in test_providers_admin.py green; full suite 142 green; ruff clean; makemigrations --check clean (no model changes).
2026-06-04 | EPIC-15/T-003 | backend | claude | done | notification delivery Celery tasks (deliver_notification fan-out + deliver_to_recipient) implemented at HEAD in notifications/tasks.py: deliver_notification re-resolves the audience_filter at send time (all/role/segment/specific; fails closed to empty queryset on a malformed filter so a stale Beat row never crashes the worker), fans out one deliver_to_recipient sub-task per recipient x channel, marks the broadcast sent. deliver_to_recipient idempotently upserts the NotificationDelivery row (no duplicate on retry), renders the recipient language variant, calls get_sender per channel (inapp/email synchronous->delivered + delivered_count bump; whatsapp/sms->sent, confirmed later by webhook), records sent/delivered/failed with error capture, bumps Notification sent_count/delivered_count atomically via F(). Completed finish protocol (frontmatter status=done + this BOARD line). 14 tests in test_delivery.py green (fan-out per recipient x channel, marks sent, skips inactive, role/specific audiences, bad-audience fails closed, missing-notification no-op, inapp delivered+counts, sms sent-not-delivered, address=phone, sender-failure->failed, idempotent no-dupe, EN/BN language render); notifications suite 91 green; full suite 1150 passed; ruff clean; makemigrations --check clean (no model changes).
2026-06-04 | EPIC-15/T-007 | backend | claude | done | admin notification broadcast endpoints wired: registered NotificationViewSet on the notifications app router (notifications/urls.py) so /admin/api/notifications resolves -- GET list (newest first), POST compose (delegates to compose_notification: validates audience/channels/schedule, resolves reach, estimates cost, dispatches/schedules delivery, audits notification.compose; DomainValidationError -> 400; sender = acting admin; response carries reach + estimated_cost), GET /{id} (NotificationDetailSerializer embeds per-recipient deliveries), POST /{id}/send-test (preview send to acting admin only via get_sender(INAPP); no audience fan-out, no counter mutation; audited notification.send_test). View+serializer+service logic already present at HEAD; this task added the route registration + endpoint test coverage. Super+ops gate (IsPlatformAdmin). 15 tests in notifications/tests/test_broadcast_endpoints.py green (list, compose creates/sets-sender/audited/400-bad-audience/400-missing-channels, retrieve embeds deliveries, send-test admin-only+counters-untouched+audited, anon rejected, super/ops allowed, finance/compliance/support denied); notifications suite 106 green; my files ruff clean; makemigrations --check clean (no model changes, no migration). Full-suite reds limited to a concurrent agent's in-flight EPIC compliance audit-log endpoint (unrelated to this task; fails identically with its work stashed).
2026-06-04 | EPIC-15/T-006 | backend | claude | done | retrofit EPIC-07 rent-link/reminder copy onto the admin-editable rent_reminder_due NotificationTemplate. rent/messaging._render_message now sources its body from get_template('rent_reminder_due').render(_template_variables(req, link)) (tenant_name=lease.tenant.name, amount=৳{whole-taka}, property_name=lease.unit.building.name, due_date=period, payment_link=link) and joins body_bn + body_en newline-first — delivery behavior unchanged (bilingual body carrying the /r/{token} link via send_with_fallback WhatsApp→SMS). Reuses existing NotificationTemplate.render + module-level get_template from T-005 (no new model/migration). Updated test_message_carries_public_link_and_is_bilingual to assert template-driven copy (Please pay using this link / ভাড়া), interpolated tenant_name, and no leftover {payment_link} token. rent send+reminder + notifications suites green; my files (messaging.py, test_send.py) ruff clean; makemigrations --check clean (no model changes). Lone full-suite red is a concurrent agent's in-flight compliance audit-log-endpoint test (unrelated; its files are uncommitted on the shared branch).
2026-06-04 | EPIC-16/T-002 | backend | claude | done | enhanced admin audit-log endpoint at GET /admin/api/audit-log (compliance/views.AdminAuditEntryListView + route in compliance/urls.py; serializer AdminAuditEntrySerializer already present at HEAD). Paginated read-only JSON envelope over append-only AdminAuditEntry (newest first), full filters admin_user/action/entity_type/entity_id/date_from/date_to, plus ?format=csv streaming export via StreamingHttpResponse + csv.writer over an echo file-like (no full-set buffering, queryset.iterator()). format=csv negotiated through a passthrough _CsvRenderer registered on the view (renderer_classes=[JSONRenderer,_CsvRenderer]) so DRF content negotiation accepts the format value instead of 404ing; branch on request.accepted_renderer.format. Compliance+super (audit section) gate reusing IsComplianceAdmin. 13 tests in compliance/tests/test_audit_log_endpoint.py green (list, read-only shape, filter by admin_user/action/entity_type+id/date-range, csv streams filtered rows + headers, anon rejected, super+compliance allowed, ops/finance/support denied). Full suite 1178 passed; ruff clean; makemigrations --check clean (no model changes).

2026-06-04 | EPIC-16/T-004 | backend | claude | done | PDPA data-request queue endpoints in new compliance/data_request_views.py (kept out of the concurrently-edited views.py to avoid the shared-file race). GET /admin/api/data-requests: paginated read-only DataRequestSerializer (adds derived sla_state on_track/due_soon/overdue), filters status/type/sla (sla buckets from settings.DATA_REQUEST_SLA_DUE_SOON_DAYS, default 7; SLA window DATA_REQUEST_SLA_DAYS default 30 added to settings/base.py). POST /admin/api/data-requests/{id}/process: DataRequestProcessSerializer {action approve|reject, reason} (reason mandatory on reject); approve export -> generate export package stub + status=completed+completed_at; approve delete -> queue erasure stub + status=processing; reject -> status=rejected+completed_at; all stamp handled_by and write admin_audit (data_request.approve_export/approve_delete/reject) with before/after+reason+ip; select_for_update + not-pending=>409 ConflictError (idempotent, no double-audit), missing=>404. Compliance+super (audit section) gate via own IsComplianceAdmin. Routes appended to compliance/urls.py. 19 tests in compliance/tests/test_data_request_endpoints.py green (list, sla_state shape, filter status/type/sla buckets, auth+role gate compliance/super allow & ops/finance/support deny, approve-export completes+audits, approve-delete->processing, reject requires-reason(400)/rejected+audits, 409 already-resolved, 404 missing, process role-gated, no-double-process single audit). Full suite green; ruff clean; mypy clean on my files (pre-existing authentication.py noise only); makemigrations --check clean (no model changes).

2026-06-04 | EPIC-14/T-005 | infra | claude | done | ASR(voice) + chat provider stubs in ai-gateway (services/ai-gateway/providers/asr.py, chat.py) subclassing HTTPProvider: validate payload (audio_b64 / non-empty messages), default language=bn, normalise vendor JSON into voice {transcript,language,fields} and chat {reply,role} envelopes; exported via providers/__init__.py + factory helpers. API key sent as bearer header, never logged/embedded. 7 tests in tests/test_stub_providers.py (httpx.MockTransport mocked vendor, no real API): envelope normalise, sparse defaults, missing-audio/empty-messages reject, router voice+chat dispatch smoke. ruff clean on my files; full gateway suite 24 passed.

2026-06-04 | EPIC-14/T-006 | infra | claude | done | per-call usage logging in ai-gateway: new services/ai-gateway/usage.py HTTPUsageLogger (POSTs each router UsageRecord JSON to Django /admin/api/ai-usage with internal-token bearer auth) + build_usage_logger factory degrading to NoOpUsageLogger when no django_base_url configured; config.py adds django_base_url/ai_usage_path/ai_usage_timeout_seconds + usage_logging_enabled/ai_usage_url props. Internal token only in Authorization header, never in record/log; UsageRecord carries only accounting metadata (no api_key). 6 new tests in tests/test_usage.py via httpx.MockTransport (mocked Django, no real call): posts record+auth, omits header w/o token, raises on 5xx, factory noop-without-url + http-when-configured, router logging-failure never breaks live call. ruff All checks passed; mypy Success on usage.py/config.py; full gateway suite 24 passed.
2026-06-04 | EPIC-09/T-010 | backend | claude | done | dashboard N+1 / query-count guard in khatir/dashboard/tests/test_perf.py. setUpTestData seeds realistic portfolio (5 buildings, 20 units across them, 1 active lease/unit, 50 rent-schedule payments spread over leases/periods/statuses PAID/PENDING/REQUESTED/OVERDUE, 20 expenses across units+categories). test_dashboard_query_count wraps get_dashboard(landlord, months=6) in CaptureQueriesContext and asserts <= QUERY_BUDGET=12 (upper-bound per §15: selectors do ~8 fixed aggregate/grouped passes; any per-row loop over the 50 schedules/20 units adds tens and trips the ceiling). 1 new test green; full suite 1202 passed; ruff clean; makemigrations --check clean (no DB changes).
2026-06-04 | EPIC-12/T-002 | backend | claude | done | post_save/post_delete signal on PricingTier busts the shared /config/public cache key (billing/signals.py, registered via BillingConfig.ready()); reuses EPIC-10 T-005 PUBLIC_CONFIG_CACHE_KEY + invalidate_public_config_cache() so the explicit T-001 edit-service bust and the model-level signal are idempotent (catches admin/migration/other write paths too). 4 new tests in billing/tests/test_cache_invalidation.py: save/create/delete each clear primed key; /config/public reflects new price after bust. ruff clean; makemigrations --check clean (no DB changes); full suite green.
2026-06-04 | EPIC-09/T-002 | backend | claude | done | dashboard read endpoint GET /api/v1/dashboard?months=N (khatir/dashboard/views.py DashboardView APIView, serializers.py DashboardSerializer + MonthPoint/CategoryTotal output serializers, urls.py mounted in config/urls.py under api/v1/). Calls T-001 get_dashboard selectors; months param clamped 1..24, defaults to dashboard_months_default SystemConfig (T-003) via get_config w/ fallback 6; payload cached per-user (key dashboard:<pk>:<months>) 60s TTL (never global, §15); guarded by IsLandlordOrManager; owner-scoped through selectors' for_user. 8 new API tests in tests/test_dashboard_api.py: full response shape (all 13 metric keys), scoped (2nd landlord excluded), months param length, config-default length, invalid-months fallback, per-user caching, requires-auth (401/403), tenant-role forbidden (403). ruff clean; makemigrations --check clean (no DB changes); full suite 1210 passed.
2026-06-04 | EPIC-14/T-004 | infra | claude | done | concrete OCR provider HTTP client in ai-gateway (services/ai-gateway/providers/ocr.py GoogleVisionOcrProvider subclassing HTTPProvider): accepts image bytes (or base64 str), base64-encodes into Google Cloud Vision images:annotate DOCUMENT_TEXT_DETECTION request (endpoint_url defaultable/overridable), api_key from ProviderConfig sent as Vision ?key= query param (never in body/logs), parses fullTextAnnotation/legacy textAnnotations into normalized ExtractedTenant JSON {name,nid_number,dob,address each {value,confidence}} mirroring Django dto; dob normalized to ISO YYYY-MM-DD (DD/MM/YYYY + ISO), nid digits-only; unreadable image -> empty DTO, Vision per-image error/transport failure -> ProviderError (router fails over). build_ocr_provider factory + exports added additively to providers/__init__.py. 11 tests in tests/test_ocr.py via httpx.MockTransport (mocked Vision, no real API): request shape+key-as-query+base64, full extraction+normalization, ISO passthrough, empty image, base64-str input, missing-image/HTTP-5xx/Vision-error -> ProviderError, legacy shape, factory, router dispatch smoke. ruff clean; mypy Success on ocr.py; full gateway suite 35 passed.
2026-06-04 | EPIC-16/T-009 | docs | claude | blocked | MVP quality gate FAILS — MVP not shippable. make status shows EPIC-00→16 NOT all done/verified: 136/197 MVP tasks done (EPIC-04 8/16, EPIC-06 6/10, EPIC-07 9/14, EPIC-08 7/12, EPIC-09 4/10, EPIC-11 7/12, EPIC-12 4/10, EPIC-13 5/8, EPIC-14 9/12, EPIC-15 8/14, EPIC-16 5/9). Own deps EPIC-16 T-006/T-007/T-008 still todo. make screen-coverage target does not exist. EPIC-05 T-010 DMP hard gate IS done (satisfied) but insufficient alone. No _completion_report.md / RELEASE.md written (would be false). Backend ruff + makemigrations --check clean. Re-run only after all EPIC-00→16 reach done/verified and make test && make lint green across all 3 apps. status=in-progress. No tests (docs task).
2026-06-05 | EPIC-04/T-009 | mobile | claude | done | add-tenant method chooser AddTenantScreen (lib/features/tenants/presentation/screens/add_tenant_screen.dart) matching addTenant proto: emoji hero (👋), 3 tappable method cards — OCR (starred/sage-bordered gradient badge), voice, manual — and a sage tip card; all colors/spacing/radii from khatir_tokens. Voice card flag-gated on voice_tenant_entry: added voiceTenantEntry to PublicConfig (reads the flags block of /config/public, default on to match backend default). Routes to /tenants/add/{ocr|voice|manual} by name carrying optional target unit id as ?unit= query param; the three method screens are KShellPlaceholder sub-routes for later EPIC-04 tasks. app_router replaces the old /tenants/add placeholder with the real chooser + sub-routes; landlord/manager FAB already pushes tenantsAdd (no unit, chosen later per §15); unit-detail Add-tenant CTA now passes ?unit=<id>. ARB add_tenant_* keys in bn (template) + en. 5 widget tests in test/add_tenant_test.dart: 3 cards when flag on, voice hidden when off, OCR/manual route carry unit id, voice route from home has no unit. flutter pub get + analyze (No issues on changed files) + test (125 passed) all green.
2026-06-05 | EPIC-04/T-010 | mobile | claude | done | NID OCR capture stage OcrCaptureScreen (lib/features/tenants/presentation/screens/ocr_capture_screen.dart) matching the `ocr` proto capture state: handwritten "Snap your NID" heading, dark NID viewfinder frame (dashed border + butter scan line over KhatirColors.ink), sage privacy reassurance card, filled sage "Take photo" + outlined "From gallery" actions; all colors/spacing/radii from khatir_tokens. States: data (capture), loading (spinner + ocr_processing), error (danger card + retry). Camera/gallery via image_picker ^1.2.2 wrapped behind ImagePickerService interface (PluginImagePicker reads bytes maxWidth 1600/q85, never copies to app storage — §15); widget tests inject a fake. New minimal tenants data slice (T-014 not yet committed, not a dep): ExtractedTenant/ExtractedField freezed models mirroring the OCR wire shape {name,nid_number,dob,address each {value,confidence}, photo_ref}, TenantRepository.ocrExtract(bytes) POSTing multipart image to /api/v1/tenants/ocr (new ApiEndpoints.tenantOcr), tenants_providers. On success pushReplacement to a `review` sub-route carrying typed OcrReviewArgs via go_router extra (review screen is T-011 — placeholder for now); app_router replaces the OCR KShellPlaceholder with the real screen + review sub-route. ARB ocr_* keys (bn template + en). 5 widget tests in test/ocr_capture_test.dart (mocked picker + scripted dio adapter): renders capture actions, capture→upload-once-to-/tenants/ocr→navigates to review with fields+photo_ref, cancel keeps capture (no upload), picker-error shows error+retry, upload-failure shows error+retry. flutter pub get + analyze (no issues in new files; remaining infos/warnings pre-existing in properties/profile/role) + test (130 passed) all green.
2026-06-05 | EPIC-04/T-011 | mobile | claude | done | NID OCR review/edit stage OcrReviewScreen (lib/features/tenants/presentation/screens/ocr_review_screen.dart) matching the `ocr` proto review state (ocrScanned branch): sage "AI extracted, please confirm" banner, four editable TextFormFields prefilled from OcrReviewArgs.extracted (name/nid/dob/address) each with leading icon, an inline family sub-form (add/remove name+relation rows; ahead of reusable T-015 widget), and a sage "Build form" proceed button; all colors/spacing/radii/fonts from khatir_tokens. OCR never trusted blindly: every field editable; low-confidence values (provider confidence ≤ 0.85) flagged with a butter border + "please check" hint (§15; absent confidence = normal field). Validation: name + NID required (autovalidate onUserInteraction). Proceed validates then emits a typed TenantReviewDraft of the EDITED values (name/nid/dob/address + family + photoRef + unitId) via an onProceed seam — the convergence point T-016's saveTenantAndContinue wires (null/no-op default; TODO marks the router site). New value types in ocr_review_args.dart: FamilyMemberDraft, TenantReviewDraft. app_router replaces the OCR review KShellPlaceholder with the real screen (falls back to capture when extra is not OcrReviewArgs). ARB ocr_review_*/tenant_*/ocr_family_*/ocr_confirm keys (bn template + en). 6 widget tests in test/ocr_review_test.dart: prefills fields, flags low-confidence, blocks proceed when required field cleared, proceed emits corrected NID + photo_ref + unit, family add flows into draft, family remove drops row. flutter pub get + analyze (no issues in new files; remaining 18 infos/warnings pre-existing in properties/profile/role) + test (full suite 136 passed) all green.
2026-06-05 | EPIC-04/T-012 | mobile | claude | done | Flutter voice tenant-entry screen VoiceFillScreen (lib/features/tenants/presentation/screens/voice_fill_screen.dart) matching the `voice` proto record state: handwritten "Talk to me!" hero, "Tap the mic" prompt, round rose mic (hold-to-talk: press=record, release=upload) with a pulsing halo, and the butter example-phrasing card; loading (spinner + voice_processing) and error (danger card + retry) states; all colors/spacing/radii/fonts from khatir_tokens. Audio recording abstracted behind a new AudioRecorderService interface (mirrors OCR ImagePickerService) so widget tests inject a fake; default PluginAudioRecorder (record ^6.2.0) owns the mic-permission prompt via hasPermission(), records to a temp file, reads bytes for the upload, then deletes the temp file — the clip is never persisted beyond the upload (§14). TenantRepository.voiceExtract(bytes) POSTs multipart audio to /api/v1/tenants/voice (new ApiEndpoints.tenantVoice); the voice response omits photo_ref (T-006 §7) so ExtractedTenant.photoRef degrades to '' for the voice path. On success pushReplacement to the REUSED OcrReviewScreen (T-011, unchanged per §15) carrying typed OcrReviewArgs via go_router extra. Flag-gated on voice_tenant_entry: the chooser (T-009) hides the entry when off, and the screen defensively re-checks the flag so a deep link cannot bypass the gate (renders an "unavailable" state instead of the mic). app_router replaces the old voice KShellPlaceholder with the real /tenants/add/voice route carrying optional ?unit=. RECORD_AUDIO added to AndroidManifest + NSMicrophoneUsageDescription to ios Info.plist. ARB voice_* keys (en + bn). 6 widget tests in test/voice_fill_test.dart (fake recorder + scripted dio adapter): renders record stage, record→release→upload-once-to-/tenants/voice→reused review prefilled (no photo_ref), denied mic permission shows error (no upload), upload failure shows error+retry, recorder stop failure shows error, flag off → mic not reachable (unavailable state, no upload). flutter pub get + analyze (no issues in changed files; remaining 2 warnings pre-existing in properties_repo_test) + test (full suite 142 passed) all green.
2026-06-05 | EPIC-04/T-013 | mobile | claude | done | Manual tenant-entry form ManualTenantScreen (lib/features/tenants/presentation/screens/manual_tenant_screen.dart) matching the `manualTenant` proto: butter "fill every official field by hand" intro card, four DMP sections (1·Landlord name/NID/mobile, 2·Tenant full-name★/NID★/DOB/occupation/mobile/permanent-address, 3·Current unit building/unit/rent/move-in, 4·Family & staff via the shared add/remove name+relation sub-form), and a sage "Generate PDF" proceed button; all colors/spacing/radii/fonts from khatir_tokens. Field + family widgets mirror the OCR review screen's composition (§15) — same tokens, same family sub-form — emitting a typed ManualTenantDraft (superset of TenantReviewDraft, reuses FamilyMemberDraft) via an onProceed seam (no-op default; T-016's saveTenantAndContinue wires it). Validation: tenant full name + NID required (★), autovalidate onUserInteraction. app_router replaces the manual KShellPlaceholder with the real /tenants/add/manual route carrying optional ?unit=. ARB manual_* + shared tenant_mobile keys (en + bn). 5 widget tests in test/manual_tenant_test.dart: renders the 4 DMP sections + empty form, blocks proceed when required fields empty (name+NID errors), proceed emits entered values + unit when valid, family add flows into draft, family remove drops row. flutter pub get + analyze (no issues in changed files; remaining infos/warnings pre-existing in properties/profile/role) + test (full suite 147 passed) all green.
2026-06-05 | EPIC-04/T-014 | mobile | claude | done | Flutter tenants data layer completed (the OCR/voice slice — ExtractedTenant + ocrExtract/voiceExtract — was already committed by T-010/T-012). Added freezed Tenant (lib/features/tenants/data/models/tenant.dart) mirroring the masked TenantSerializer ({id,name,nid_number_masked,dob,address,photo_ref,verification_status,verified_at,is_app_user,family_members,created_at,updated_at}) with NO plaintext NID field (only nidNumberMasked — masked NID only in list/detail per §15), freezed FamilyMember ({id,name,relation}) with a toCreateJson that drops the server-assigned id, and a VerificationStatus enum (tenant_enums.dart) whose fromWire degrades unknown/null to unverified. TenantRepository gained createTenant({name★, nidNumber (write-only plaintext, sent only when non-empty, server encrypts + never echoes), dob, address, photoRef, family}) POSTing only supplied fields to /api/v1/tenants and parsing the masked 201, and listUnitTenants(unitId) GETting /api/v1/units/{id}/tenants and parsing the BARE JSON array (UnitTenantsView returns no {results,pagination} envelope). New ApiEndpoints.tenants / tenant(id) / unitTenants(id). UnitTenantsController (AsyncNotifier.family keyed by unit id) exposes build=listUnitTenants and create=createTenant+refresh, shared by the manual/OCR/voice add-tenant flows (T-013/T-011/T-012) and the T-016 save action. 11 tests in test/tenants_repo_test.dart (scripted dio adapter): model fromJson (masked NID/family/status/dates + null tolerance), VerificationStatus.fromWire, FamilyMember.toCreateJson, ocrExtract multipart, createTenant field-omission + nested family + masked response + empty-NID omission, listUnitTenants bare-array + 404→ApiException, provider data + create-then-refresh. flutter pub get + build_runner + analyze (no error-severity issues in any file; remaining use_null_aware_elements infos + fake-storage unused-param warnings are the pre-existing baseline pattern shared with properties/profile repos and properties_repo_test) + test (full suite 158 passed) all green.
2026-06-05 | EPIC-04/T-015 | mobile | claude | done | Reusable family-members sub-form FamilyMembersField (lib/features/tenants/presentation/widgets/family_members_field.dart): a single StatefulWidget owning per-row name+relation TextEditingControllers (dynamic add/remove, controllers disposed on row-remove + unmount) that reports the current non-empty rows as List<FamilyMemberDraft> via an onChanged callback on every edit/add/remove; blank-name rows are omitted. keyPrefix param namespaces the per-row widget keys (ocr→ocrFamilyName/ocrFamilyRelation/ocrFamilyAdd, manual→manualFamily*) so each host keeps stable test keys. All colors/spacing/radii/fonts from khatir_tokens. De-duplicated: removed the inline _FamilyDraftRow/_FamilyRow/_AddFamilyButton from both ocr_review_screen.dart and manual_tenant_screen.dart and replaced them with the shared widget (both now hold a useState<List<FamilyMemberDraft>> updated via onChanged); the voice path (T-012) reuses the OCR review screen so it gets the same sub-form transitively — all 3 add-tenant paths share one widget. New canonical ARB keys family_add/family_name/family_relation/family_remove (bn template + en). 5 widget tests in test/family_members_field_test.dart: starts empty (add affordance only), add→row appears+flows to onChanged, blank-name row omitted, remove→row dropped+re-emits empty, keyPrefix namespaces keys. flutter pub get + gen-l10n + analyze (no issues in new/changed files; remaining 21 infos/warnings pre-existing in profile/properties/role/*_repo_test) + test (full suite 163 passed) all green.
2026-06-05 | EPIC-04/T-016 | mobile | claude | done | Convergent tenant save+route action wiring all 3 add-tenant paths (OCR/voice/manual) to the DMP form. New TenantSaveController (lib/features/tenants/presentation/controllers/tenant_save_controller.dart) consumes a normalised TenantSaveDraft (TenantReviewDraft.fromReview / built inline from ManualTenantDraft — single create site, no per-path duplication): drops blank family rows, parses optional dob, routes the create through the unit-scoped UnitTenantsController.createDetailed when a unitId is set (so the unit's list refreshes) else straight through TenantRepository.createTenantDetailed; on success shows the free-tier toast (when present) and pushReplacementNamed → /dmpform/{tenantId}; on ApiException/any error shows a snackbar and returns false (no partial navigation). Repo gained createTenantDetailed returning TenantCreateResult {masked Tenant + optional TenantUsage} (new tenant_create_result.dart; TenantUsage.maybeFromJson reads optional {tenants_used,free_limit,is_over_free} echoed on the create response — T-008 is backend + not a dep, so usage lights up automatically once the server sends it, no toast otherwise); createTenant now delegates to it. OcrReviewScreen + ManualTenantScreen proceed handlers now run the controller (with a saving spinner + disabled button) when onProceed is null (the router path); onProceed kept as a test seam so existing T-011/T-013 tests pass unchanged. Voice path converges via the reused OCR review screen. New DmpPlaceholderScreen at /dmpform/:tenantId (root navigator) stands in until EPIC-05; app_router registers it + drops the T-016 TODOs. ARB tenant_save_error/tenant_free_tier_status(used,limit)/dmp_placeholder_* (bn template + en). 5 widget tests in test/save_tenant_test.dart (scripted dio + GoRouter with the DMP route): manual save POSTs merged fields → DMP (no unit), unit-scoped save refreshes the unit list, free-tier toast when usage reported, error snackbar + no-nav + re-enabled form on 500, OCR/voice review save sends photo_ref → DMP. flutter pub get + gen-l10n + analyze (no issues in new/changed files; remaining infos/warnings pre-existing in properties/profile/role/*_repo_test) + test (full suite 168 passed) all green. EPIC-04 add-tenant flow now works end-to-end.
2026-06-05 | EPIC-05/T-007 | mobile | claude | done | Flutter DMP form preview screen DmpPreviewScreen (lib/features/dmpform/presentation/screens/dmp_preview_screen.dart) matching the `dmp` proto: top bar with a "ready" check chip, a celebratory 🎉 "All done!" hero, a white form card with the 🏛️ DMP·CIMS·tenant-information header (org name + rose badge) and labelled field rows (tenant / masked-NID / landlord / address / present / permanent / DOB / phone, blank fields hidden) plus a household members table, then a sage "Generate PDF" primary button → the PDF screen (T-008) and a soft "Edit" button → back to the tenant flow; loading (spinner) / error (danger icon + retry) / data states; all colors/spacing/radii/fonts from khatir_tokens. New dmpform data layer: DmpPreview/DmpFamilyMember model (lib/features/dmpform/data/models/dmp_preview.dart) mirroring the assembled masked-NID DmpData contract (T-002/T-004 — full NID never on the wire), DmpFormRepository.getPreview GETting /api/v1/tenants/{id}/dmpform (new ApiEndpoints.tenantDmpForm), and DmpPreviewController (AsyncNotifier.family keyed by tenant id, build=getPreview + refresh for the retry). app_router replaces the EPIC-04 DmpPlaceholderScreen with the real preview at /dmpform/:tenantId (route name `dmpForm` UNCHANGED so the T-016 save action keeps routing here) and adds a nested `/pdf` placeholder route (reusing DmpPlaceholderScreen) so the generate flow resolves until T-008 lands the real PDF screen. onGenerate/onEdit injectable seams for tests; default generate pushes /dmpform/{id}/pdf, default edit pops (falls back to /landlord/home). ARB dmp_title/ready/hero_title/hero_sub/org/org_sub/org_badge/field_*/generate/edit/error/retry (bn template + en). Design ledger row 14 (`dmp`) ticked. 4 widget tests in test/dmp_preview_test.dart (fake controller + GoRouter): data state renders assembled fields + masked NID + family + both actions, generate button fires the action, edit button fires the action, error state shows a retry. flutter pub get + gen-l10n + analyze (no issues in new/changed files; remaining 21 infos/warnings pre-existing in profile/properties/role/*_repo_test) + test (full suite 172 passed) all green. Closes the EPIC-04→05 seam.
2026-06-05 | EPIC-05/T-008 | mobile | claude | done | Flutter DMP PDF preview + share screen DmpPdfScreen (lib/features/dmpform/presentation/screens/dmp_pdf_screen.dart) matching the `dmpPdf` proto: top bar with back action + "A4" chip, the rendered A4 PDF on a muted paper-on-grey backdrop, and a sticky footer with a sage "Download" primary + soft "Share" (WhatsApp/system) action; generating (spinner + dmp_generating) / error (danger icon + retry) / data states; all colors/spacing/radii/fonts from khatir_tokens. Pipeline: DmpFormRepository.generatePdf POSTs /api/v1/tenants/{id}/dmpform/pdf (new ApiEndpoints.tenantDmpFormPdf) parsing the committed GeneratePdfResponseSerializer shape {record,signed_url} into DmpPdfResult, then fetchPdfBytes downloads the signed-URL bytes (ResponseType.bytes); DmpPdfController (AsyncNotifier.family keyed by tenant id) chains generate→download into a DmpPdf {bytes,result}, regenerate() for the retry. Free-tier: no entitlement gate — the wedge works for everyone. PDF rendering via printing ^5.14.3 PdfPreview (pdfium) behind an injectable PdfViewBuilder seam so widget tests stub it; share/download side-effects behind a DmpPdfSharer interface (PluginDmpPdfSharer = share_plus SharePlus.instance.share + printing Printing.layoutPdf), share file named dmp-form-{recordId}.pdf; action failures surface as a snackbar (dmp_pdf_action_failed). share_plus pinned ^12.0.2 (latest stable compatible with flutter_secure_storage ^9 → win32 ^5; ^13 forces win32 ^6 collision). app_router replaces the T-007 nested-`/pdf` DmpPlaceholderScreen with the real DmpPdfScreen at /dmpform/:id/pdf (root navigator; placeholder import dropped). ARB dmp_pdf_title/dmp_generating/dmp_pdf_download/dmp_pdf_share/dmp_pdf_whatsapp/dmp_pdf_error/dmp_pdf_action_failed (bn + en). Design ledger row 15 (`dmpPdf`) ticked. 4 widget tests in test/dmp_pdf_test.dart (fake controller + spy sharer + stub renderer): data state renders preview+A4+download/share, share button invokes sharer with the record filename (not download), download button invokes download (not share), error state shows retry. flutter pub get + gen-l10n + analyze (no issues in new/changed files; remaining 21 infos/warnings pre-existing in profile/properties/role/*_repo_test) + test (full suite 176 passed) all green. EPIC-05 wedge now works end-to-end: tenant → DMP preview → PDF → share/download.
2026-06-05 | EPIC-05/T-009 | mobile | claude | done | Canonical typed Flutter DMP data layer (the screen-facing slice — DmpPreview/DmpPdfResult/getPreview/generatePdf — was already committed out of order by T-007/T-008, which depend on this task; this task adds the typed layer alongside it without regressing those green screens). New freezed DmpData (+DmpFamilyMemberData) in lib/features/dmpform/data/models/dmp_data.dart mirroring the assembler `DmpData` returned by GET /api/v1/tenants/{id}/dmpform (T-004) — NID arrives masked server-side, the full value is never on the wire; manual fromJson tolerates missing keys/nested family. New freezed DmpRecord in models/dmp_record.dart mirroring DMPFormRecordSerializer ({id,tenant,template_version,pdf_ref,generated_by,generated_at,created_at}) + the signed_url the detail/generate responses attach — record exposes opaque pdf_ref + metadata only, no field payload/NID; fromGenerateJson folds the POST envelope's top-level signed_url onto the nested record. DmpFormRepository gained the typed accessors getDmpData (GET dmpform → DmpData), generateRecord (POST dmpform/pdf → DmpRecord), and getRecord (GET /dmpforms/{id} → DmpRecord) — all surfacing cross-user 404 as ApiException; new ApiEndpoints.dmpRecord(id). New dmpDataProvider / dmpRecordProvider (FutureProvider.family keyed by tenant/record id). Existing getPreview/generatePdf/fetchPdfBytes + dmpPreviewProvider/dmpPdfProvider left intact so T-007/T-008 screens are unchanged. 12 tests in test/dmp_data_layer_test.dart (scripted dio adapter): DmpData fromJson (fields+family, NID stays masked, empty tolerance), DmpRecord fromJson + fromGenerateJson (signed_url fold, absent record), repo getDmpData/generateRecord/getRecord happy paths + 404→ApiException, provider data states. build_runner codegen + flutter pub get + analyze (no issues in new/changed files; remaining 21 infos/warnings pre-existing in profile/properties/role/*_repo_test) + test (full suite 188 passed) all green.
2026-06-05 | EPIC-06/T-007 | mobile | claude | done | Flutter leases + rent-schedule data layer (the typed slice consumed by the lease form/list/detail screens T-008/T-009/T-010). New freezed Lease (lib/features/leases/data/models/models.dart) mirroring LeaseSerializer ({id,unit_id,tenant_id,landlord_id,start_date,end_date,rent,advance,status,signed_pdf_ref,created_at,updated_at} — FK ids + status read-only, money DecimalField strings parsed to double), freezed RentSchedule mirroring RentScheduleSerializer ({id,lease_id,period,due_day,due_date,amount,status,sent_at,created_at,updated_at} — read-only, period=YYYY-MM), plus LeaseTenantSummary + UnitLease for the UnitLeaseSerializer payload (lease fields top-level + nested tenant summary, masked NID only). New lease_enums.dart: LeaseStatus (draft/active/ended/terminated, fromWire→draft) and RentScheduleStatus (pending/requested/paid/overdue, fromWire→pending) mirroring enums.md + backend leases/enums.py. LeaseRepository (lib/features/leases/data/lease_repository.dart): getLease (GET /leases/{id}), createLease (POST /leases — draft only, landlord server-derived, advance omitted unless supplied), updateLease (PATCH — only non-null fields), activateLease (POST /leases/{id}/activate → server generates schedule), terminateLease (POST /leases/{id}/terminate, status ended/terminated, omitted=server-default terminated), getSchedule (GET /leases/{id}/schedule → bare array), getUnitLease (GET /units/{id}/lease → UnitLease; no-active-lease 404 surfaces as ApiException) — all cross-user/unknown ids resolve to 404→ApiException. New ApiEndpoints.leases/lease(id)/leaseSchedule(id)/leaseActivate(id)/leaseTerminate(id)/unitLease(id). Providers (lib/features/leases/data/providers.dart): leaseRepositoryProvider, leaseScheduleProvider + unitLeaseProvider (FutureProvider.family), and LeaseController (AsyncNotifier.family keyed by lease id) with build=getLease + activate/terminate/editTerms (renamed from update to avoid clashing with inherited AsyncNotifier.update) writing state + invalidating the schedule on activate. 14 tests in test/leases_repo_test.dart (scripted dio adapter): createLease draft body + advance-omission/inclusion + 201 parse, activate, terminate (status sent / omitted), update field-omission, getSchedule bare-array + null sent_at, getUnitLease lease+embedded-tenant + no-active-lease 404, getLease 404→ApiException, model null-tolerance + enum fromWire degradation. build_runner codegen + flutter pub get + analyze (no error/warning issues in new/changed files; remaining use_null_aware_elements infos + fake-storage unused-param warnings are the pre-existing baseline shared with properties/profile/tenants repos and *_repo_test) + test (full suite 202 passed) all green.
2026-06-05 | EPIC-06/T-010 | mobile | claude | done | Lease list/detail screens (the list screen + listLeases/leasesListProvider were laid down in a prior partial run of this task; this run completed the detail screen, routes, and tests). New LeaseDetailScreen (lib/features/leases/presentation/screens/lease_detail_screen.dart) at /lease/:id: a cream scaffold with a status+rent header card, a Terms card (monthly rent, advance when >0, start/end dates), a Rent-schedule summary card (period count + per-status count chips, with its own loading/error/empty states — lease_schedule_empty when no rows), and a danger-outlined Terminate action shown only for an active lease that opens an AlertDialog (lease_terminate_confirm_title/body) and on confirm calls LeaseController.terminate(terminated)+invalidates leasesListProvider, reporting lease_terminated_ok / lease_terminate_error via snackbar; the closed lease's status write hides the action. Reuses the list screen's leaseStatusLabel/termRange helpers (no duplication). All colors/spacing/radii/fonts from khatir_tokens. States: loading (spinner) / error (retry → LeaseController.refresh) / data. app_router registers /leases (LeaseListScreen) + /lease/:id (LeaseDetailScreen) on the root navigator, after the literal /lease/new + /lease/:id/edit form routes so :id never captures "new". BanglaNumerals.format gained an optional grouped flag (default true) so date parts render without a thousands separator (the list screen already called it that way). ARB lease_detail_title/section_schedule/section_terms/schedule_summary/schedule_empty/terminate*/sched_status_* (bn + en) were already present from the prior run; gen-l10n regenerated the AppLocalizations getters this run. 9 widget tests (test/lease_list_test.dart: card-per-lease+status chip, empty, error+retry; test/lease_detail_test.dart: terms+schedule summary render, terminate hidden for non-active, empty-schedule helper, terminate confirm→repo+success+action-hidden, cancel→no repo call, lease-load error→retry) all via fake LeaseRepository. flutter pub get + gen-l10n + analyze (no issues in new files; the 4 remaining use_null_aware_elements infos in lease_repository.dart are the pre-existing baseline pattern shared across profile/properties/tenants repos) + test (full suite 217 passed) all green.
2026-06-05 | EPIC-07/T-011 | mobile | claude | done | Flutter rent-request screen RentRequestScreen (lib/features/rent/presentation/screens/rent_request_screen.dart) at /rent/request matching the `rentReq` proto: a cream scaffold with the 📤 "Ask for rent" emoji-hero + WhatsApp-link reassurance subtitle, an amount field (digits-only, >0 validation → rent_request_err_amount) and a YYYY-MM period field (regex-validated month 01–12 → rent_request_err_period), then a sage "Send WhatsApp link" primary and an outlined "Mark received (cash)" secondary action. Send runs RentRepository.createManual(lease,amount,period) then RentRequestController.send (create + deliver the tenant link) and routes to the verify queue (/landlord/rent); Mark-received runs createManual then RentRequestController.markReceived (settle the off-platform cash payment); both invalidate rentQueueProvider so the queue refreshes on return. States: data (idle form), sending (per-button spinner via busy/busyMarkReceived useState, both buttons disabled), error (ApiException/any → friendly rent_request_error snackbar, form re-enabled). leaseId carried via `?lease=` query param (same convention as the `?unit=` flows) with optional `?amount=`/`?period=` prefills; landlord server-derived from the lease, never collected. GoRouter.maybeOf used for the post-success nav so the screen works headless in widget tests. onProceed test seam kept (null = real flow). All colors/spacing/radii/fonts from khatir_tokens; all 13 rent_* ARB keys (title/heading/subtitle/amount/period/period_hint/send_whatsapp/mark_received/err_amount/err_period/sent/received/error) already present from a prior run, bn + en. app_router registers the route on the root navigator (after the lease form routes). Design ledger row 16 (`rentReq`) ticked. 8 widget tests in test/rent_request_test.dart (fake RentRepository overriding rentRepositoryProvider): renders hero+fields+both actions, prefills amount/period, send → create+send (+correct lease/amount/period) + rent_request_sent, mark-received → create+markReceived + rent_request_received, invalid input blocks the action + shows both field errors (no create call), and failure paths on send/create/mark-received each surface rent_request_error (create-failure makes no send call). build_runner regenerated the T-010 models.freezed.dart (was uncommitted) so freezed copyWith/getters resolve. flutter pub get + analyze (no issues in rent screen / router / test; the 25 remaining infos/warnings are the pre-existing baseline in unit_repository/tenant_repository/properties_repo_test/role_redirect_test) + test (full suite 225 passed, incl. the 8 new) all green.
2026-06-05 | EPIC-07/T-012 | mobile | claude | done | Flutter verify-payment screen VerifyPaymentScreen (lib/features/rent/presentation/screens/verify_payment_screen.dart) at /rent/:id/verify matching the `verifyPay` proto: a cream scaffold with a soft butter claim card (avatar initial + "{name} says they paid" + ৳amount·period subline from the RentRequest), a "Submitted proof" viewer, and a sage "Received" primary + outlined "Not yet received" secondary. The proof viewer shows the screenshot loaded from its signed URL via Image.network (PaymentProof.photoRef treated as the signed URL per §15 — never raw bytes; loadingBuilder spinner, errorBuilder → friendly verify_proof_image_failed placeholder) with the txn id (verify_proof_txn) detail, and degrades to a "no proof yet" placeholder (verifyProofNone) when no proof is supplied. Verify runs RentRequestController.verify (server creates Payment + receipt) → snackbar verify_verified → routes to the receipt (/rent/:id/receipt, T-013); reject opens a required-reason AlertDialog (verify_reason*, empty → verify_reason_required) then RentRequestController.reject(reason) → snackbar verify_rejected → pops back to the queue; both invalidate rentQueueProvider. States: loading (spinner) / load-error (danger icon + verifyRetry → controller.refresh) / data / verifying (per-action spinner via busyAction useState, both buttons disabled) / action-error (ApiException/any → friendly verify_error snackbar, actions re-enabled). Reuses the committed T-010 rentRequestControllerProvider as the single source of truth for both load and the verify/reject transitions. The rent detail endpoint does not surface the tenant name or PaymentProof, so they ride in via a typed router `extra` payload (VerifyPaymentArgs) the queue (T-013) can supply; onAction test seam kept (null = real flow). GoRouter.maybeOf used for post-action nav so the screen works headless in tests. All colors/spacing/radii/fonts from khatir_tokens. app_router registers /rent/:id/verify on the root navigator (after /rent/request). ARB verify_title/claim/amount_period/proof/proof_none/proof_image_failed/proof_txn/proof_amount/confirm/reject/reason/reason_hint/reason_required/reason_cancel/reason_submit/verified/rejected/error/load_error/retry (bn + en). Design ledger row 17 (`verifyPay`) ticked. 10 widget tests in test/verify_payment_test.dart (fake RentRepository overriding rentRepositoryProvider): renders claim+proof+both actions, no-proof placeholder, proof-with-txn details, verify → verify call + verify_verified, reject → reason dialog (empty blocks + shows required error, then reason → reject(reason)+verify_rejected), cancel → no transition, verify-failure + reject-failure → verify_error, load-failure → load-error+retry. flutter pub get + gen-l10n + analyze (no issues in the new screen/router/test; the 25 remaining infos/warnings are the pre-existing baseline in unit/tenant repos + properties/tenants_repo_test + role_redirect_test) + test (full suite 234 passed, incl. the 10 new) all green.
2026-06-05 | EPIC-07/T-013 | mobile | claude | done | Flutter rent receipt screen ReceiptScreen (lib/features/rent/presentation/screens/receipt_screen.dart) at /rent/:id/receipt matching the `receipt` proto — the EPIC-07 validation gate that closes the rent loop (request→pay→verify→receipt): a cream scaffold with the 🧾 "Receipt ready" emoji-hero, a centred receipt card (brand heading, big ৳amount in sage, a dashed tear divider, labelled tenant/unit/period/method/status rows with ✓ Paid, and a muted receipt-no footnote), then a sage "Send to tenant" primary + a soft "PDF · Done" secondary. Share sends the receipt to the tenant via the OS sheet (WhatsApp/system) — the generated PDF when a signed URL is present, else a plain-text summary; Done saves the PDF (when present) then returns to /landlord/home. Reuses the EPIC-05 T-008 PDF share/download seam, mirrored as ReceiptSharer/PluginReceiptSharer + receiptSharerProvider (share_plus + printing); RentRepository.fetchReceiptBytes + receiptBytesProvider download the signed-URL PDF bytes. The amount/period/status come from the committed T-010 rentRequestControllerProvider (single source of truth, shared with verify); the contextual fields the detail endpoint doesn't surface (tenant/unit/method/receipt-no/signed-PDF URL) ride in via a typed router `extra` payload ReceiptArgs (same convention as T-012's proof), degrading missing values to a neutral dash. States: loading (spinner) / load-error (danger icon + receiptRetry → controller.refresh) / data; a failed share/download → friendly receipt_action_failed snackbar (never crashes). onShared/onDone test seams kept (null = real flow); GoRouter.maybeOf used for nav so the screen is headless-testable. All colors/spacing/radii/fonts from khatir_tokens (no proto hex/px). app_router registers /rent/:id/receipt on the root navigator (after /rent/:id/verify). ARB receipt_title/ready/ready_sub/heading/amount/tenant/unit/period/method/status/status_paid/no/share/done/dash/share_text/action_failed/load_error/retry (bn + en). Design ledger row 18 (`receipt`) ticked. 5 widget tests in test/receipt_test.dart (fake RentRepository + spy ReceiptSharer): renders the summary card+amount+tenant+✓Paid+both actions, share-with-signed-URL → sharePdf(rent-receipt-<sanitized-no>.pdf, bytes), share-without-URL → shareText(receipt_share_text), done-with-URL → downloadPdf(rent-receipt-r-1.pdf), load-failure → receipt_load_error+retry. flutter pub get + gen-l10n + analyze (No issues found in the new screen/sharer/repo/providers/router/test; the remaining infos/warnings are the pre-existing baseline in unit/tenant/lease/profile/building repos + properties/tenants_repo_test + role_redirect_test) + test (full suite 239 passed, incl. the 5 new) all green.
2026-06-05 | EPIC-07/T-014 | mobile | claude | done | Late-payers section on the landlord home, filling the EPIC-03 home placeholder. New LatePayersSection (lib/features/rent/presentation/widgets/late_payers_section.dart) watches the committed T-010 rentQueueProvider(null) and derives "late payers" = unpaid requests (status sent / proof_submitted; verified/rejected are settled and excluded), matching the `home` proto rose overdue rows: a rose "{count} rent overdue" heading then per-request rose-tinted rows (clock badge, "Rent overdue" label, ৳amount · YYYY-MM period sub-line — tenant/unit labels are not on RentRequestSerializer) each with a compact rose "Ask" pill routing to /rent/request?lease=…&amount=…&period=… (the T-011 query convention). States: loading (slim sage placeholder), empty/all-paid (sage reassurance row), data (the overdue rows); a queue read error degrades to the all-paid row rather than shouting on home. landlord_home_screen.dart _CollectionCard now renders LatePayersSection in place of the late-payer half of the old TODO placeholder; the collected/expected amount + progress chart stay deferred to EPIC-09 (TODO narrowed, not removed). All colors/spacing/radii/fonts from khatir_tokens; numerals localised via BanglaNumerals (toBangla for the YYYY-MM period). ARB home_late_payers (count plural), home_late_payers_one, home_all_paid, home_quick_request (bn + en). 3 new widget tests in test/landlord_home_test.dart (fake RentRepository overriding rentRepositoryProvider): overdue list shows only the 2 unpaid of 3 with count heading + 2 Ask pills (verified row excluded), Ask routes carrying the lease id, empty queue shows all-paid + no Ask. flutter pub get + gen-l10n + analyze (No issues found in the new widget / home screen / test; the 25 remaining infos/warnings are the pre-existing baseline in unit/tenant repos + properties/tenants_repo_test + role_redirect_test) + test (full suite 242 passed, incl. the 3 new) all green.
2026-06-05 | EPIC-08/T-007 | mobile | claude | done | Flutter maintenance+expense data layer (the typed slice consumed by the expense list/add (T-008/T-009), maintenance queue/on-unit (T-010/T-011) and expense summary hook (T-012) screens). New maintenance_enums.dart (lib/features/maintenance/data/models/): MaintenanceCategory (plumbing/electrical/paint/structural/appliance/utility/other, fromWire→other), MaintenanceStatus (open/resolved, fromWire→open), ExpenseCategory (plumbing/paint/electrical/structural/appliance/utility/other, fromWire→other), ExpenseSource (request/manual, fromWire→manual) mirroring enums.md + backend maintenance/enums.py. New freezed models.dart: MaintenanceRequest mirroring MaintenanceRequestSerializer ({id,unit_id,lease_id,category,description,photo_ref,status,resolved_at,resolution_cost,resolution_note,created_at,updated_at} — FK ids + status + resolution read-only, resolution_cost nullable DecimalField string→double?), Expense mirroring ExpenseSerializer ({id,unit_id,request_id,category,amount,date,source,note,receipt_ref,created_at,updated_at} — FK ids + source read-only, amount DecimalField string→double), plus ExpenseCategoryTotal/ExpenseMonthTotal/ExpenseSummary for the GET /expenses/summary payload ({by_category:[{category,total}], by_month:[{month,total}]}, Decimal-string totals→double, month=first-of-month ISO date, absent buckets→empty lists). MaintenanceRepository (maintenance_repository.dart): listQueue (GET /maintenance, optional status/unit filters, {results} unwrap), getRequest (GET /maintenance/{id}), createRequest (POST /maintenance — unit server-scoped, always open, category/photo_ref/lease_id omitted unless supplied), updateRequest (PATCH — only non-null fields), resolve (POST /maintenance/{id}/resolve cost+note → server auto-creates one expense). ExpenseRepository (expense_repository.dart) + ExpenseFilter (unit/building/date_from/date_to → query params): listExpenses (GET /expenses, {results} unwrap, manual+auto both shown), getExpense, createExpense (POST — source forced manual server-side, date YYYY-MM-DD), updateExpense (PATCH — only non-null, auto-expense immutable server-side), deleteExpense (DELETE→204), summary (GET /expenses/summary→ExpenseSummary), exportCsv (GET /expenses/export→raw CSV text via ResponseType.plain) — all cross-user/unknown ids resolve to 404→ApiException. New ApiEndpoints.maintenance/maintenanceRequest(id)/maintenanceResolve(id) + expenses/expense(id)/expensesExport/expensesSummary. Providers (providers.dart): maintenanceRepositoryProvider + expenseRepositoryProvider; maintenanceQueueProvider (AsyncNotifier.family by status), maintenanceRequestProvider (FutureProvider.family) + MaintenanceRequestController (AsyncNotifier.family by id, build=getRequest, updateRequest [renamed from update to avoid clashing with inherited AsyncNotifier.update] + resolve writing state + invalidating the single-request provider); expenseListProvider (AsyncNotifier.family by ExpenseFilter with delete helper), expenseProvider (FutureProvider.family), expenseSummaryProvider (FutureProvider.family). 16 tests in test/maintenance_data_layer_test.dart (scripted dio adapter): listQueue results-unwrap + status/unit filters, createRequest unit_id+description body + field-omission + 201 parse, updateRequest field-omission, resolve cost/note body + resolved parse, getRequest 404→ApiException; listExpenses filter params + manual/auto parse, createExpense manual body (no source/receipt_ref) + 201, updateExpense field-omission, deleteExpense 204, summary by_category/by_month parse, exportCsv raw text + filter, getExpense 404→ApiException; model null-tolerance (MaintenanceRequest null cost, Expense null date/money, ExpenseSummary empty buckets) + enum fromWire degradation. build_runner codegen (models.freezed.dart committed) + flutter pub get + analyze (no error/warning issues in new/changed files; remaining use_null_aware_elements infos are the pre-existing baseline shared with profile/properties/tenants/leases repos) + test (full suite 258 passed, incl. the 16 new) all green.
2026-06-05 | EPIC-08/T-008 | mobile | claude | done | Flutter maintenance & expenses list screen ExpensesScreen (lib/features/maintenance/presentation/screens/expenses_screen.dart) at /expenses matching the `expenses` proto: a cream scaffold with a butter-gradient "Total expenses" hero (This-month chip + label + big ৳total summed from the rows in view + "{count} expenses" sub-line), a horizontal building filter row, then a "Recent expenses" section. The app-bar carries an Export (CSV) action and an Add action; the list is one white card of rows, each a category emoji + category label + a source chip (sage "Manual" vs butter "Maintenance" per §15, distinguishing manual vs maintenance-sourced expenses) + the date, with a trailing rose ৳amount. Loaded via the committed T-007 expenseListProvider (AsyncNotifier.family keyed by ExpenseFilter, scoped server-side via for_user); the building filter maps to ExpenseFilter.buildingId and is held as a single stable instance in State (ExpenseFilter has no value equality, so re-deriving it each build would spawn a fresh family member and never settle). Building chips come from buildingsProvider, degrading to "All"-only while they load / on read failure so the filter never blocks the list. Export fetches the scoped+filtered CSV via ExpenseRepository.exportCsv and hands it to a new ExpenseCsvSharer/PluginExpenseCsvSharer + expenseCsvSharerProvider seam (share_plus, mirroring the EPIC-07 receipt sharer) as an expenses.csv attachment; a failed fetch/share → friendly expenses_export_failed snackbar. Add pushes /expenses/add (T-009); onAdd test seam kept (null = real nav), context.push used. States: loading (spinner) / error (common_network_error + retry → controller.refresh) / empty (🧾 expenses_empty card) / data. All colors/spacing/radii/fonts from khatir_tokens (butter/sage/rose + spacing/radius scale; no proto hex/px); numerals localised via BanglaNumerals. app_router registers /expenses on the root navigator (before the portfolio routes). ARB expenses_title/this_month/total/total_amount/count/filter_all/section_recent/source_manual/source_request/amount/add/export/export_failed/empty + expenses_category_{plumbing,paint,electrical,structural,appliance,utility,other} (bn + en). Design ledger row 19 (`expenses`) ticked. 6 widget tests in test/expenses_screen_test.dart (fake ExpenseRepository + fake BuildingRepository + spy ExpenseCsvSharer): renders total (3,500+12,000=15,500)+a row per expense+both source chips, empty state, error+retry, building-filter tap re-queries with buildingId=b-1 and narrows the list, export → exportCsv once + shareCsv(expenses.csv) with the CSV text, add action fires its callback. flutter pub get + gen-l10n + analyze (No issues in the new screen/sharer/router/test; the remaining use_null_aware_elements infos are the pre-existing baseline in the T-007 expense/maintenance repos shared across profile/properties/tenants/leases repos) + test (full suite 264 passed, incl. the 6 new) all green.
2026-06-05 | EPIC-08/T-009 | mobile | claude | done | Flutter add-expense screen AddExpenseScreen (lib/features/maintenance/presentation/screens/add_expense_screen.dart) at /expenses/add matching the `addExpense` proto (screens-landlord2.js → reg('addExpense')): a cream scaffold form with the amount input (★ required, digits-only, ৳০ hint), a wrapping category chip row driven by the ExpenseCategory config enum (selected fills sage, default plumbing), the unit picker (the single proto "Unit" field realised as a building selector + unit selector — token-styled dropdowns fed by buildingsProvider / buildingUnitsProvider, since createExpense needs the unit id and a unit is only meaningful within a building), a date field (read-only card → platform date picker, defaults today, YYYY-MM-DD), an optional note field (multiline), an optional receipt picker (ImagePickerService gallery pick → attached-confirmation row with Remove; filename threaded into createExpense(receiptRef:) — T-007 exposes only a receipt_ref string and there is no committed media-upload endpoint yet, so the encrypted-storage upload ref rides the same field once that lands), then the full-width sage "Save expense" CTA. Save validates amount (>0) + unit (inline errors; the unit is not a FormField so it is checked alongside the form on Save), runs the committed T-007 ExpenseRepository.createExpense (source forced manual server-side), invalidates expenseListProvider so the new row shows on return, snackbars expense_saved, and routes back to /expenses; failures → friendly expense_save_failed snackbar (never crashes). onSaved test seam kept (null = real flow, emitting AddExpenseDraft); GoRouter.maybeOf used for nav so the screen is headless-testable. States: data (idle form) / validation (inline amount+unit errors) / saving (CTA spinner, form disabled) / error (snackbar, form re-enabled). All colors/spacing/radii/fonts from khatir_tokens (sage/danger + spacing/radius scale; no proto hex/px). app_router registers /expenses/add on the root navigator (after /expenses). ARB add_expense_title + expense_amount/amount_hint/category/unit/building/unit_hint/building_hint/date/note/receipt/receipt_add/receipt_attached/receipt_remove/save/err_amount/err_unit/saved/save_failed (bn + en). Design ledger row 20 (`addExpense`) ticked. 6 widget tests in test/add_expense_screen_test.dart (fake ExpenseRepository + fake Building/Unit repos + fake ImagePicker, ListView scroll helpers): renders amount + a chip per ExpenseCategory + the save CTA, blocks save without amount/unit (no create call, both inline errors), saves the entered fields (unit u-1, amount 3500, category paint, note) via createExpense, attaching a receipt threads receipt.jpg into the save, onSaved seam intercepts the real create flow, a failed save surfaces expense_save_failed. flutter pub get + gen-l10n + analyze (No issues in the new screen/router/test; the remaining use_null_aware_elements infos + properties/tenants_repo_test unused-param warnings are the pre-existing baseline in the unit/tenant/building/profile/maintenance repos) + test (full suite 270 passed, incl. the 6 new) all green.
2026-06-05 | EPIC-08/T-010 | mobile | claude | done | Flutter maintenance queue MaintenanceQueueScreen (lib/features/maintenance/presentation/screens/maintenance_queue_screen.dart) at /maintenance, reusing the `expenses` proto's "New requests" block (screens-landlord2.js → reg('expenses'); no dedicated design-map key per §15): a cream scaffold listing the caller's OPEN maintenance requests from the committed T-007 maintenanceQueueProvider(open) (scoped server-side via for_user), each a white card with the category emoji, the unit label + a rose category chip, the description, an optional photo thumbnail (Image.network on MaintenanceRequest.photoRef = the backend signed URL, degrading to a neutral placeholder on load/error), and a sage "সমাধান + খরচ · Resolve" action. Resolve opens a modal dialog (cost ★ digits-only >0 → maintenance_err_cost, optional note); confirm runs MaintenanceRequestController.resolve(cost,note) — the T-002 resolve that auto-creates exactly ONE expense server-side — then refreshes the open queue (the resolved request drops off) and invalidates expenseListProvider so the new auto-expense shows on /expenses, snackbaring maintenance_resolved; failures → friendly maintenance_resolve_failed snackbar (never crashes). onResolve test seam kept (null = real flow, emitting ResolveDraft). States: loading (spinner) / error (common_network_error + retry → controller.refresh) / empty (🔧 maintenance_empty card) / data. All colors/spacing/radii/fonts from khatir_tokens (sage/rose + spacing/radius scale; no proto hex/px); numerals localised via BanglaNumerals. app_router registers /maintenance on the root navigator (after /expenses/add). ARB maintenance_title/section_open/open_count/unit/resolve/resolved_badge/cost/cost_hint/resolution_note/resolve_title/resolve_hint/resolve_confirm/resolve_cancel/err_cost/resolved/resolve_failed/empty + maintenance_category_{plumbing,electrical,paint,structural,appliance,utility,other} (bn + en). 6 widget tests in test/maintenance_queue_test.dart (fake MaintenanceRepository overriding maintenanceRepositoryProvider): card-per-open-request + description + resolve action, empty state, error+retry, resolve dialog blocks on invalid cost then fires onResolve with the entered cost(3500)+note, the real resolve flow records cost(4200) on the repo once + surfaces maintenance_resolved, cancel makes no resolve call. flutter pub get + gen-l10n + analyze (no issues in the new screen/router/test; the remaining use_null_aware_elements infos + properties/tenants_repo_test unused-param warnings are the pre-existing baseline in the T-007 maintenance/expense + unit/tenant/building/profile repos) + test (full suite 276 passed, incl. the 6 new) all green.
2026-06-05 | EPIC-08/T-011 | mobile | claude | done | Maintenance + expense summary on the unit detail screen. New UnitMaintExpenseSection (lib/features/maintenance/presentation/widgets/unit_maint_expense_section.dart): two soft cards under the tenant region — a Maintenance card (open-request count sub-line + up to 3 recent rows: category emoji + label + truncated description + a sage/rose status chip) and an Expenses card (total ৳ of the recent expenses + count + up to 3 recent rows: category emoji + label + rose ৳amount) — each with a sage "View all" link routing to /maintenance (T-010, MaintenanceQueueScreen.routeName) and /expenses (T-008, ExpensesScreen.routeName). Data comes from two new unit-scoped reads added to the committed T-007 providers.dart: unitMaintenanceProvider (FutureProvider.family by unitId → listQueue(unitId:)) and unitExpensesProvider (FutureProvider.family by unitId → listExpenses(filter: ExpenseFilter(unitId:))), both scoped server-side via for_user + ?unit=<id>. Each card renders its own loading (inline spinner) / empty (friendly muted line) / error (unit_section_error line) / data state independently so a slow/failed expense fetch never blocks the maintenance card. The proto `unit` screen has no maint/expense block, so this is a new composition from the existing soft-card + chip token vocabulary; integrated into unit_detail_screen.dart _UnitBody below _TenantSection (UnitMaintExpenseSection(unitId: unit.id)). All colors/spacing/radii/fonts from khatir_tokens (sage/rose + spacing/radius scale; no proto hex/px); numerals localised via BanglaNumerals. ARB unit_maintenance/unit_expenses/unit_view_all + unit_maint_open_count/unit_maint_empty/unit_maint_status_open/unit_expenses_total/unit_expenses_count/unit_expenses_empty/unit_section_error (bn template carries the placeholder metadata, en the translations). 7 widget tests in test/unit_maint_section_test.dart (fake Maintenance/Expense repos overriding the T-007 providers, GoRouter with stub /maintenance + /expenses routes): data state shows both headings + open count(1 of 2) + a description + total(5,000) + count(2) + 2 View-all links, reads are unit-scoped (unit filter u1 threaded to both repos), empty state shows both empty lines, loading shows a spinner while the queue hangs, error shows 2 section-error lines, and the two View-all links route to the queue / expenses stubs. Also updated test/unit_detail_test.dart to override maintenanceRepositoryProvider/expenseRepositoryProvider with empty-returning fakes so the now-embedded section settles. flutter pub get + gen-l10n + analyze (No issues in the new widget / providers / unit_detail_screen / tests; the remaining use_null_aware_elements infos + properties/tenants_repo_test unused-param warnings are the pre-existing baseline) + test (full suite 283 passed, incl. the 7 new) all green.
2026-06-05 | EPIC-09/T-004 | mobile | claude | done | Flutter dashboard data layer (the typed slice consumed by the dashboard screen + cards/charts, T-006/T-008/T-009). New freezed dashboard_model.dart (lib/features/dashboard/data/): DashboardData mirroring the committed T-002 DashboardSerializer ({total_collected,total_pending,total_overdue,collection_rate,occupied_units,total_units,occupancy_rate,total_income,total_expense,net,late_payer_count,monthly_series,top_expense_categories}) — money fields are DRF DecimalField strings→double, rates floats, counts ints, both list fields default to empty; MonthPoint ({period,collected,expense} — period kept as the raw YYYY-MM label, money strings→double) and CategoryTotal ({category,amount} — category reuses the shared ExpenseCategory enum/fromWire→other, amount string→double). DashboardRepository (dashboard_repository.dart): fetchDashboard({months}) → GET /dashboard (payload returned directly, no {results} envelope; optional ?months=N omitted when null so the server applies its configured dashboard_months_default; owner-scoped + cached server-side; non-2xx→ApiException). New ApiEndpoints.dashboard. Providers (dashboard_providers.dart): dashboardRepositoryProvider + dashboardProvider (AsyncNotifierProvider.family<DashboardController,DashboardData,int?> keyed by the months window, null=server default) with a refresh() for pull-to-retry, matching the rent/maintenance controller pattern. 9 tests in test/dashboard_data_layer_test.dart (scripted dio adapter + in-memory secure storage): fetchDashboard reads /dashboard + parses every metric/series/category, omits months when null, passes ?months=12, non-2xx→ApiException; dashboardProvider exposes AsyncValue.data + refresh re-fetches into state; model null-tolerance (empty payload→safe defaults, unknown category→other, missing month money→0, string/null numerics). build_runner codegen (dashboard_model.freezed.dart committed) + flutter pub get + analyze (No issues in the new model/repo/providers/endpoints/test; the remaining use_null_aware_elements infos + properties/tenants/role test unused-param/null-aware warnings are the pre-existing baseline) + test (full suite 292 passed, incl. the 9 new) all green.
2026-06-05 | EPIC-09/T-005 | mobile | claude | done | fl_chart shared chart widgets (the generic, themed bar/donut/line widgets consumed by the dashboard screen + any future chart, T-006/T-009). Three thin wrappers over fl_chart 1.2.0 (latest stable, no beta/RC) in lib/core/widgets/charts/, all colors/spacing/radii/fonts pulled from khatir_tokens (sage gradient bars, sage donut arc on a sage-bg track, sage trend line with a sage-bg fill + rose accent dots, muted token label colors; no proto hex/px): KBarChart (List<KBarDatum>{label,value} → spaced sage-gradient bars with token-styled value labels on top + bottom-axis category/month labels, optional maxValue + valueSuffix e.g. '%', touch/grid/border disabled), KDonutChart (List<KDonutSlice>{value,color} → hollow PieChart with a center primary value + optional sublabel, mirroring the proto occupancy ring; plus a .percentage() convenience factory: a single sage arc on a sage-bg track with a locale-formatted NN% center label), KLineChart (List<KLinePoint>{label,value} → curved sage trend line, sage-bg below-fill, rose dots, padded min/max so the line never hugs the edge, bottom-axis labels). Each widget renders independently: isLoading → shared ChartLoadingState (sage-bg box + sage spinner), empty/zero-sum data → shared ChartEmptyState (sage-bg box + caller-supplied localized label, em-dash fallback) — both extracted to lib/core/widgets/charts/chart_states.dart so the three charts reuse one loading/empty widget instead of duplicating. Charts stay generic per §15 (business data → chart data mapping lives in the T-006 screen): callers pass already-localized axis/center labels; numerals on the bar value labels + the donut .percentage factory format via BanglaNumerals from the passed localeCode (88→৮৮%, 78→৭৮% for bn / 78% for en). No DB/API/screens/flags. 13 widget tests in test/chart_widgets_test.dart (sized Scaffold harness): KBarChart renders a BarChart with data / empty state + label / loading state / Bangla numeral value labels; KDonutChart renders a PieChart + center label + sublabel / empty when slices sum to zero / .percentage localizes the center label bn(৭৮%) + en(78%) / loading; KLineChart renders a LineChart with data / empty state + label / loading / flat single-value series without error. flutter pub get (fl_chart ^1.2.0 added) + analyze (No issues in the new charts/test; the 36 remaining use_null_aware_elements infos + properties/tenants_repo_test unused-param warnings are the pre-existing baseline in unrelated repos/tests) + test (full suite 305 passed, incl. the 13 new) all green.
2026-06-05 | EPIC-09/T-006 | mobile | claude | done | Landlord Dashboard / charts screen DashboardScreen (lib/features/dashboard/presentation/screens/dashboard_screen.dart) at /landlord/dashboard, wired into the landlord shell's Charts tab (app_router.dart: the EPIC-09 _placeholderBranch replaced by a real StatefulShellBranch → DashboardScreen, route path/name unchanged), matching the `dashboard` proto (screens-landlord2.js → reg('dashboard')): a cream scaffold ListView with an ink-gradient income hero (এ মাসের মোট আয় + big ৳totalIncome), a 6-month collection-rate KBarChart (each month's collected as a % of the window's peak, max 100%, %-suffixed value labels), an occupancy KDonutChart.percentage (sage ring using the server occupancy_rate so the % matches backend rounding — 11/14→78% — with an occupied/total sublabel) + a sage/rose occupied/vacant legend, an income-vs-expense KLineChart (monthly collected trend) + a sage/rose legend, a top-expense-categories list (one soft-rose row per CategoryTotal: localized category label via the shared categoryLabel + a peak-relative LinearProgressIndicator + rose ৳amount), and a late-payers card (overdue count; when >0 a rose quick-request CTA → /rent/request). All metrics come from one GET /dashboard read via the committed T-004 dashboardProvider (AsyncNotifier.family keyed by the months window, null=server default; owner-scoped+cached server-side). States: loading (charts show their own shimmer over a zeroed body) / error (common_network_error + retry → controller.refresh) / empty (a brand-new landlord with no units/income/expense/series → 📊 dashboard_empty card) / data. All colors/spacing/radii/fonts from khatir_tokens (ink/sage/rose + spacing/radius scale; no proto hex/px); numerals localised via BanglaNumerals; month axis labels derived from the YYYY-MM period (bn/en short month names). onLatePayerRequest test seam kept (null = real context.push('/rent/request')). Deviations: T-004 exposes only latePayerCount (no per-tenant list) and the proto has no late-payers list, so the "late-payers list" is a count summary card + quick-request CTA; the income-vs-expense block is added per §8 though the proto omits it. ARB dashboard_title/income/amount/collection/occupancy/occupancy_units/occupied/vacant/income_expense/income_legend/expense_legend/expenses/late/late_none/late_count/late_request/chart_empty/empty (bn template carries the placeholder metadata, en the translations). Design ledger row `dashboard` built. 5 widget tests in test/dashboard_screen_test.dart (fake DashboardRepository overriding dashboardRepositoryProvider, a tall test surface so the scrolling list lays out): renders the income hero (৳71,000) + all four chart blocks + the 78% donut + late-payers card/CTA + "3 late" count, the quick-request CTA fires its nav callback, empty state for a blank DashboardData (no charts), error + working retry re-fetches the data (≥2 repo calls), zero late payers → no CTA + dashboard_late_none. flutter pub get + gen-l10n + analyze (No issues in the new screen/router/test/ARB; the 36 remaining use_null_aware_elements infos + properties/tenants_repo_test unused-param warnings are the pre-existing baseline in unrelated repos/tests) + test (full suite 310 passed, incl. the 5 new) all green.
2026-06-05 | EPIC-09/T-007 | mobile | claude | done | Filled the EPIC-03 home-screen collection chart placeholder with a real summary card. _CollectionCard (lib/features/properties/presentation/screens/landlord_home_screen.dart) is now a ConsumerWidget watching the committed T-004 dashboardProvider(null) (owner-scoped + cached server-side, same payload the Charts tab reads so the call is shared/deduped): under the existing "Collected this month" heading it shows a tappable sage-bg tile with the current-month collected total (homeCollectedAmount), a "Pending ৳X" sub-line (homePendingAmount), a thin collection-rate LinearProgressIndicator (collection_rate clamped 0..1), and a "View charts" affordance; tapping it switches the landlord shell to the dashboard/Charts tab via context.goNamed(DashboardScreen.routeName) (goNamed on a StatefulShellRoute branch root activates that branch instead of pushing). The EPIC-09 TODO + placeholder copy are removed; while the first read is in flight the tile shows a small spinner, and on read error it degrades to the original home_collected_todo coming-soon copy so the card never collapses. The late-payers section (EPIC-07 T-014) below is unchanged. All colors/spacing/radii/fonts from khatir_tokens (sage/rose/muted + spacing/radius scale; no proto hex/px); numerals localised via BanglaNumerals. New ARB home_collected_pending (placeholder {amount}) + home_view_dashboard (bn + en). 3 new widget tests in test/landlord_home_test.dart (harness extended to override dashboardRepositoryProvider with a fake serving a fixed DashboardData + a stub landlordDashboard route): collection card shows the real collected/pending amounts + view-charts hint and no placeholder copy, tapping it opens the dashboard tab (DASHBOARD_TAB stub), and a failed dashboard read falls back to home_collected_todo with the heading intact. flutter pub get + analyze (No issues in the changed home screen / test / ARB; the 36 remaining use_null_aware_elements infos + properties/tenants_repo_test unused-param warnings are the pre-existing baseline in unrelated repos/tests) + test (full suite 313 passed, incl. the 3 new) all green.
2026-06-05 | EPIC-09/T-008 | mobile | claude | done | Reusable KMetricCard KPI widget (lib/core/widgets/k_metric_card.dart) for the dashboard/home top summary row — a single metric shown as a tinted icon badge + muted label + large value, with an optional trend pill. Built on the existing KCard surface (white card, radius 22, soft shadow, optional onTap forwarded). Deliberately generic per §15 (knows nothing about collection/occupancy): the caller passes a pre-formatted value string ("78%", "৳71,000") so locale/numeral formatting stays its responsibility, plus an `icon`, `label`, optional `trend` (KMetricTrend.up/down/flat → sage ↑ / rose ↓ / muted →, via Icons.arrow_upward/downward/forward) and optional `trendLabel` (e.g. "12%") beside the arrow, and optional `accent`/`accentBackground` for the icon badge (defaults sage on sage-bg). No trend → no pill rendered. All colors/spacing/radii/fonts from khatir_tokens (sage/rose/butter/muted + spacing/radius scale + AppTextStyles headlineMedium/bodySmall; no proto hex/px). No DB/API/screens/flags — component only. 6 widget tests in test/k_metric_card_test.dart (sized Scaffold harness): renders value+label+icon, renders a ৳amount value with a butter accent (generic, not collection-specific), shows an up trend pill with its label, shows a down trend arrow, renders no trend pill when trend is null, forwards taps via onTap. flutter pub get + analyze (No issues in the new widget/test; the 36 remaining use_null_aware_elements infos + properties/tenants_repo_test unused-param warnings are the pre-existing baseline in unrelated repos/tests) + test (full suite 319 passed, incl. the 6 new) all green.
2026-06-05 | EPIC-09/T-009 | mobile | claude | done | Income-vs-expense chart on the landlord Dashboard (EPIC-09 T-009). Replaced T-006's single-income KLineChart stub in _IncomeExpenseCard (lib/features/dashboard/presentation/screens/dashboard_screen.dart) with a grouped two-series KBarChart: for each of the trailing 6 months of monthly_series, a sage income rod (MonthPoint.collected) beside a rose expense rod (MonthPoint.expense), under a sage/rose series legend (new dashboard_income_series / dashboard_expense_series keys). Both rods share one axis maximum (the peak of either series across the window) so income/expense stay visually comparable; an all-zero window renders the KBarChart empty state (dashboard_chart_empty) rather than flat zero-height bars. Extended the shared T-005 KBarChart to stay a thin token-only fl_chart wrapper while gaining a generic grouped mode: KBarDatum.secondValue (optional second rod), barColors/secondBarColors token-gradient overrides (default sage/sageDk + rose/roseDk), and showValueLabels (grouped mode hides the per-bar top value labels to keep the two-rod axis readable, narrower 9px rods with barsSpace); single-series callers (the collection-rate chart) are unchanged (one 14px sage rod, value labels on). Deviations: the proto reg('dashboard') shows single-series sage bars not a literal income-vs-expense block, so per §8 ("grouped bar, income sage vs expense rose") + §15 ("if bars, use KBarChart") implemented as grouped bars; older dashboard_income_legend/expense_legend keys remain present-but-unused. All colors/spacing/radii from khatir_tokens (no proto hex/px); numerals via BanglaNumerals. New ARB dashboard_income_series + dashboard_expense_series (bn + en) + regenerated app_localizations. Tests: chart_widgets_test.dart gains a grouped two-series case (two rods per group, mapped heights 60/20, top value labels hidden) + a single-series-keeps-one-rod case; dashboard_screen_test.dart asserts the income/expense series legend renders. flutter pub get + gen-l10n + analyze (No issues in the changed chart widget / dashboard screen / tests / ARB; the 36 remaining use_null_aware_elements infos + properties/tenants_repo_test unused-param warnings are the pre-existing baseline in unrelated repos/tests) + test (full suite 321 passed, incl. the 2 new) all green.
2026-06-05 | EPIC-10/T-007 | mobile | claude | done | Flutter Plan & billing screen PlanScreen (lib/features/billing/presentation/screens/plan_screen.dart) at /settings/plan, pushed from the More menu's Plan & billing row (more_screen.dart now context.push(PlanScreen.routePath) instead of the coming-soon snackbar; route registered on the root navigator in app_router.dart), matching the `plan` proto (screens-landlord2.js → reg('plan'); design ledger row built): a cream scaffold ListView with a butter 🎁 "You're on X" banner showing the current tier label + a tenant-usage line (1/2 tenants used, or "N tenants · unlimited" when the limit is null), then the active tier catalogue as upgrade cards — each white card (current tier sage-bg + a sage "Now" chip; the unlimited/top tier ringed sage with a "⭐ BEST VALUE" ribbon) showing the localized tier label, tenant band (min–max / min+ / unlimited), the ৳monthly price (falling back to annual) with a /mo or /tenant/mo suffix (free tiers read "Free"), a "+ NID verify" note when includes_verification, and a sage "Choose this plan" subscribe action on every NON-current tier — closing with the mono "Prices admin-configurable · illustrative" footer. New billing data layer (lib/features/billing/data/): plan_models.dart (PlanTier/PlanSubscription/PlanConfig parsing the committed T-005 /config/public envelope — pricing.tiers[] via the read-only TierSerializer shape {key,label,label_bn,tenant_min,tenant_max,monthly_price,annual_price,includes_verification,included_credits} with DRF decimal-strings→double, + the authenticated subscription block {tier_key,status,tenants_used,tenant_limit,can_verify_nid}; lenient int/double coercion, null tenant_limit/tenant_max = unlimited), billing_repository.dart (fetchPlanConfig → GET /config/public projected to the plan slice; subscribe(tierKey) → POST /billing/subscribe {tier_key} per T-004, payment stubbed server-side), billing_providers.dart (billingRepositoryProvider + planConfigProvider AsyncNotifier with refresh() + subscribeControllerProvider AsyncNotifier whose subscribe() posts then re-reads the config so usage + the current-plan highlight refresh). Tier data comes from /config/public not a separate fetch (§15); subscribe is stubbed — on success the screen snackbars plan_billing_confirm_pending ("We'll confirm once payment clears"), on failure plan_billing_error; the cards disable while any subscribe is in flight. New ApiEndpoints.billingSubscribe. States: loading (sage spinner) / error (common_network_error + retry → planConfigProvider.refresh) / empty (no tiers → plan_empty) / data / subscribing. All colors/spacing/radii/fonts from khatir_tokens (butter/sage/rose/muted + spacing/radius scale; no proto hex/px); numerals localised via BanglaNumerals. ARB plan_title/current/current_banner/usage/usage_unlimited/free/upgrade/choose/best_value/per_month/per_tenant_month/band/band_min/band_unlimited/includes_verification/billing_note/billing_confirm_pending/billing_error/empty (bn template carries placeholder metadata, en the translations). 4 widget tests in test/plan_screen_test.dart (fake BillingRepository overriding billingRepositoryProvider, tall surface): renders all tiers with the current (free) tier's Now chip + 1/2 usage + the best-value ribbon + a subscribe button on non-current tiers only, tapping Choose fires repo.subscribe('bundle_10') once + shows the pending confirmation snackbar, error state retries the read, empty state when no tiers. flutter pub get + gen-l10n + analyze (No issues in the new billing data/screen / more_screen / app_router / test / ARB; the 36 remaining use_null_aware_elements infos + properties/tenants_repo_test unused-param warnings are the pre-existing baseline in unrelated repos/tests) + test (full suite 325 passed, incl. the 4 new) all green.
