# BOARD

<!-- timestamp | task-id | role | platform | status | note -->
2026-06-02 | EPIC-00/T-001 | infra | claude | done | -
2026-06-02 | EPIC-00/T-002 | infra | claude | done | -
2026-06-02 | EPIC-00/T-003 | infra | claude | done | -
2026-06-02 | EPIC-00/T-004 | backend | claude | done | -
2026-06-02 | EPIC-00/T-005 | backend | claude | done | -
2026-06-02 | EPIC-00/T-006 | backend | claude | done | -
2026-06-02 | EPIC-00/T-010 | infra | claude | done | -
2026-06-02 | EPIC-00/T-012 | infra | claude | done | -
2026-06-02 | EPIC-00/T-007 | mobile | claude | done | -
2026-06-02 | EPIC-00/T-009 | admin | claude | done | -
2026-06-02 | EPIC-00/T-008 | mobile | claude | done | -
2026-06-02 | EPIC-00/T-011 | infra | claude | done | -
2026-06-02 | EPIC-00/T-013 | infra | claude | done | -
2026-06-02 | EPIC-00/T-014 | infra | claude | done | -
2026-06-02 | EPIC-00/T-015 | cross-cutting | claude | done | -
2026-06-02 | EPIC-00/T-016 | infra | claude | done | -
2026-06-02 | EPIC-01/T-002 | backend | claude | done | -
2026-06-02 | EPIC-01/T-001 | backend | claude | done | -
2026-06-02 | EPIC-01/T-008 | mobile | claude | done | -
2026-06-02 | EPIC-01/T-003 | backend | claude | done | -
2026-06-02 | EPIC-01/T-009 | mobile | claude | done | -
2026-06-02 | EPIC-01/T-004 | backend | claude | done | -
2026-06-02 | EPIC-01/T-010 | mobile | claude | done | -
2026-06-02 | EPIC-01/T-005 | backend | claude | done | -
2026-06-02 | EPIC-01/T-006 | backend | claude | done | -
2026-06-02 | EPIC-01/T-007 | backend | claude | done | -
2026-06-02 | EPIC-01/T-011 | mobile | claude | done | -
2026-06-02 | EPIC-01/T-012 | mobile | claude | done | -
2026-06-03 | EPIC-02/T-001 | backend | claude | done | -
2026-06-03 | EPIC-02/T-002 | backend | claude | done | -
2026-06-03 | EPIC-02/T-003 | mobile | claude | in-progress | no Flutter/Dart toolchain to run analyze+test
2026-06-03 | EPIC-02/T-004 | mobile | claude | in-progress | no Flutter/Dart toolchain to run analyze+test
2026-06-03 | EPIC-02/T-005 | mobile | claude | in-progress | no Flutter/Dart toolchain to run analyze+test
2026-06-03 | EPIC-02/T-006 | mobile | claude | in-progress | no Flutter/Dart toolchain to run analyze+test
2026-06-03 | EPIC-02/T-007 | mobile | claude | in-progress | no Flutter/Dart toolchain to run analyze+test
2026-06-03 | EPIC-02/T-008 | mobile | claude | in-progress | no Flutter/Dart toolchain to run analyze+test
2026-06-03 | EPIC-03/T-006 | backend | claude | done | -
2026-06-03 | EPIC-03/T-001 | backend | claude | done | -
2026-06-03 | EPIC-03/T-008 | mobile | claude | in-progress | no Flutter/Dart toolchain to run analyze+test
2026-06-03 | EPIC-03/T-002 | backend | claude | done | -
2026-06-03 | EPIC-03/T-003 | backend | claude | done | -
2026-06-03 | EPIC-03/T-004 | backend | claude | done | -
2026-06-03 | EPIC-03/T-005 | backend | claude | done | -
2026-06-03 | EPIC-03/T-007 | mobile | claude | in-progress | no Flutter/Dart toolchain to run analyze+test
2026-06-03 | EPIC-03/T-009 | mobile | claude | in-progress | no Flutter/Dart toolchain to run analyze+test
2026-06-03 | EPIC-03/T-012 | mobile | claude | in-progress | no Flutter/Dart toolchain to run analyze+test
2026-06-03 | EPIC-03/T-013 | mobile | claude | in-progress | no Flutter/Dart toolchain to run analyze+test
2026-06-03 | EPIC-03/T-010 | mobile | claude | in-progress | no Flutter/Dart toolchain to run analyze+test
2026-06-03 | EPIC-03/T-011 | mobile | claude | in-progress | no Flutter/Dart toolchain to run analyze+test
2026-06-03 | EPIC-03/T-014 | cross-cutting | claude | in-progress | no Flutter/Dart toolchain to run Dart parity test
2026-06-04 | EPIC-02/T-003 | mobile | claude | done | toolchain installed; flutter analyze+test green (120 pass)
2026-06-04 | EPIC-02/T-004 | mobile | claude | done | verified via flutter analyze+test (120 pass)
2026-06-04 | EPIC-02/T-005 | mobile | claude | done | verified via flutter analyze+test (120 pass)
2026-06-04 | EPIC-02/T-006 | mobile | claude | done | verified via flutter analyze+test (120 pass)
2026-06-04 | EPIC-02/T-007 | mobile | claude | done | verified via flutter analyze+test (120 pass)
2026-06-04 | EPIC-02/T-008 | mobile | claude | done | verified via flutter analyze+test (120 pass)
2026-06-04 | EPIC-03/T-007 | mobile | claude | done | verified via flutter analyze+test (120 pass)
2026-06-04 | EPIC-03/T-008 | mobile | claude | done | flutter_map 8.x tap-debounce fix in test; analyze+test green
2026-06-04 | EPIC-03/T-009 | mobile | claude | done | verified via flutter analyze+test (120 pass)
2026-06-04 | EPIC-03/T-010 | mobile | claude | done | WizardField label RichText→Text fix; analyze+test green
2026-06-04 | EPIC-03/T-011 | mobile | claude | done | provider seed deferred to post-frame; analyze+test green
2026-06-04 | EPIC-03/T-012 | mobile | claude | done | verified via flutter analyze+test (120 pass)
2026-06-04 | EPIC-03/T-013 | mobile | claude | done | controller update()->save() rename (Riverpod clash); analyze+test green
2026-06-04 | EPIC-03/T-014 | cross-cutting | claude | done | unit-label parity test green under flutter test
2026-06-04 | EPIC-04/T-001 | backend | claude | done | tenants app: Tenant+TenantFamilyMember, VerificationStatus, migration; 12 model tests, ruff+mypy clean
2026-06-04 | EPIC-06/T-001 | backend | claude | done | leases app: Lease + RentSchedule + enums; 26 tests; full suite 378 pass
2026-06-04 | EPIC-08/T-001 | backend | claude | done | maintenance app: MaintenanceRequest + Expense + enums; 17 tests; 378 pass
2026-06-04 | EPIC-10/T-001 | backend | claude | done | billing app: PricingTier + Subscription + enums; 17 tests; 378 pass
2026-06-04 | EPIC-11/T-001 | backend | claude | done | admin_portal app: AdminUser (separate from User); 23 tests; 378 pass
2026-06-04 | EPIC-05/T-001 | backend | claude | done | dmpforms app: DMPFormRecord; 11 tests; full suite 471 pass
2026-06-04 | EPIC-13/T-001 | backend | claude | done | featureflags app: FeatureFlag + KillSwitchEvent; 19 tests; 471 pass
2026-06-04 | EPIC-14/T-001 | backend | claude | done | ai_providers app: AIProvider + AIUsageLog; 17 tests; 471 pass
2026-06-04 | EPIC-15/T-001 | backend | claude | done | notifications app: Notification+Delivery+Template; sender FK->AdminUser; 28 tests; 471 pass
2026-06-04 | EPIC-16/T-001 | backend | claude | done | compliance app: ConsentRecord + DataRequest; 18 tests; 471 pass
2026-06-04 | EPIC-04/T-002 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-04/T-007 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-06/T-002 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-06/T-003 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-06/T-006 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-06/T-005 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-05/T-006 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-05/T-002 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-05/T-003 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-05/T-004 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-05/T-005 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-07/T-001 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-07/T-009 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-07/T-003 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-07/T-005 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-07/T-008 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-04/T-003 | backend | claude | done | S3 boto3 storage (SSE, private ACL, presigned URLs) + FS fallback; pytest 634
2026-06-04 | EPIC-04/T-004 | backend | claude | done | TenantExtractionProvider ABC + OCR/ASR impls, normalized DTO, config-driven selection; pytest 642
2026-06-04 | EPIC-04/T-005 | backend | claude | done | POST /tenants/ocr: encrypted image store + provider extract, per-user rate-limit, masked-only fields; pytest 651
2026-06-04 | EPIC-04/T-006 | backend | claude | done | POST /tenants/voice: Bangla audio -> ASR fields, voice_tenant_entry flag gate (default on), audio discarded, per-user rate-limit; pytest 661
2026-06-04 | EPIC-04/T-008 | backend | claude | done | GET /api/v1/usage: tenant_usage selector (for_user count, free_limit from SystemConfig, is_over_free soft signal); seed free_tier_tenant_limit=2; no creation block (EPIC-10 owns enforcement); pytest 669
2026-06-04 | EPIC-06/T-004 | backend | claude | done | GET /leases/{id}/schedule (scoped, chronological) + GET /units/{id}/lease (active lease + tenant summary, none->404); leases selectors + RentSchedule/UnitLease serializers; pytest 678
2026-06-04 | EPIC-05/T-010 | backend | claude | done | DMP template field-verification: canonical field-map doc + FIELD_LAYOUT positioned renderer + golden test (field-set parity, per-field positions, determinism); scanned-master overlay pending founder; pytest 688
2026-06-04 | EPIC-07/T-002 | backend | claude | done | signed link-token service (Django TimestampSigner, dedicated salt, TTL from rent_link_token_ttl_hours); make_token persists, resolve_token raises typed Expired/Invalid (NotFoundError); pytest 688
2026-06-04 | EPIC-07/T-004 | backend | claude | done | rent-link send: status->sent stamp in send_rent_link + POST /rent-requests/{id}/send (send_rent_request service, audited rent.request.send), reuses EPIC-01 NotificationSender (whatsapp->sms, console dev); pytest 697
2026-06-04 | EPIC-07/T-006 | backend | claude | done | POST /r/{token}/proof (txn/screenshot via encrypted storage/note) -> PaymentProof + status proof_submitted, PRG to GET /r/{token}/receipt (pending vs verified, signed PDF link); per-token cache rate-limit (429); webReceipt screen built; pytest 706
2026-06-04 | EPIC-07/T-007 | backend | claude | done | verify/mark-received/reject + receipt PDF: POST /rent-requests/{id}/verify|mark-received|reject create Payment(verified_by/at) + render_receipt_pdf (reuses EPIC-05 _render_pdf + EPIC-04 encrypted storage), status->verified, schedule->paid, notify tenant web receipt link (best-effort), audited rent.payment.verify|mark_received|reject; re-settle 409; pytest 713
2026-06-04 | EPIC-08/T-006 | backend | claude | done | seed maintenance_categories + expense_categories SystemConfig (core 0006, JSON arrays mirroring enums, idempotent+reversible), surfaced in /config/public via _json_list_config helper; 3 new tests, core suite 115 green
2026-06-04 | EPIC-08/T-004 | backend | claude | done | unit-scoped maintenance web-link token reusing EPIC-07 TimestampSigner pattern under dedicated salt khatir.maintenance.link_token (distinct-salt cross-resolver rejection tested); stateless (signs Unit pk, no DB change), TTL from maintenance_link_token_ttl_hours (72h fallback); resolve_token raises typed Expired/Invalid (NotFoundError) for 410-vs-404; 10 token tests pass
2026-06-04 | EPIC-09/T-003 | backend | claude | done | seed dashboard_months_default=6 SystemConfig (core 0007, idempotent update_or_create + reversible, chained after 0006_seed_category_config); 2 new tests, core suite 115 green
2026-06-04 | EPIC-10/T-002 | backend | claude | done | seed 6 default PricingTier rows (billing 0002, keys per enums.md: free/per_tenant/bundle_20/bundle_40/unlimited_monthly/unlimited_annual; free=0-2 tenants ৳0 no-verification, bundles+unlimited include_verification+credits; illustrative BDT defaults, idempotent update_or_create + reversible); rekeyed 3 T-001 model tests to test_* to avoid seeded-row UNIQUE collision; 7 new + billing suite 24 green
2026-06-04 | EPIC-08/T-002 | backend | claude | done | maintenance CRUD + resolve->auto-expense at /api/v1/maintenance: create(open)/list/retrieve/patch scoped via MaintenanceRequestManager.for_user (unit->building->owner), IsLandlordOrManager & IsOwnerOfMaintenanceRequest, foreign=404; resolve(cost,note) marks resolved + records resolution_* + auto-creates ONE Expense(source=request) via new nullable OneToOne Expense.request (migration 0002) for idempotency (double-resolve rejected 400, never 2nd expense); audited maintenance.create|update|resolve; 39 maintenance tests green (full-suite failures limited to pre-existing S3/botocore dmpforms+rent)
2026-06-04 | EPIC-10/T-006 | backend | claude | done | seed pricing SystemConfig keys (core 0008): free_tier_tenant_limit=2 (int, re-asserted; 0005-owned) + nid_verification_tiers JSON list [bundle_10,bundle_20,bundle_50,unlimited] stored as text; idempotent update_or_create + reversible (reverse drops only nid_verification_tiers); 1 new test, core suite green
2026-06-04 | EPIC-10/T-004 | backend | claude | done | subscription create/upgrade at /api/v1/billing: POST subscribe resolves PricingTier by key (unknown/inactive->400 validation_error), creates-or-upgrades the caller's single Subscription in one atomic select_for_update write (status reset active, dates stamped from 30/365-day cycle), audited subscription.create|upgrade; payment STUBBED (paid tier records subscription.payment_intent pending marker for manual admin confirm; clear # STUB: marker for MFS wiring); GET subscription returns plan + tenant usage (tenants_used via tenants.usage selector, tenants_limit from tier.tenant_max, null=unlimited), 204 when no sub (implicit free); appended subscribe() to shared billing/services.py (T-003 metering untouched); 9 new subscription-api tests green (full-suite reds limited to other agents' in-flight maintenance/rent/config_public missing-migration work)
2026-06-04 | EPIC-11/T-002 | backend | claude | done | AdminAuditEntry immutable append-only model (admin_portal 0002) + admin_audit() writer: FK admin_user(SET_NULL, null for system), action, entity_type/entity_id denormalized from instance, before_json/after_json diffs, GenericIPAddressField ip, reason; immutability enforced at instance (save rejects re-save, delete raises) AND queryset (update/delete raise ImmutableAuditError) levels; indexes (admin_user,created_at)+(action); 36 admin_portal tests green (full-suite reds limited to other agents in-flight rent/maintenance work)
2026-06-04 | EPIC-10/T-003 | backend | claude | done | tenant metering + free-limit enforcement: check_tenant_limit(user) in shared billing/services.py raises TierLimitExceeded (new ErrorCode.TIER_LIMIT_EXCEEDED -> 402 PaymentRequired AppError) when active-tenant count (reuses Tenant.objects.for_user, EPIC-04 T-008) >= plan cap; cap = active-subscription tier.tenant_max (None=unlimited) else free_tier_tenant_limit config (default 2, not hardcoded); select_for_update on Subscription rows inside the create transaction closes the concurrent-create race; wired into tenants.create_tenant before any write so over-limit rolls back atomically (402 envelope {code:tier_limit_exceeded, details:{limit,tenants_used}}); 16 new test_metering tests green (under/at/over limit, config-driven limit, upgraded/unlimited/cancelled tiers, endpoint 201/402 + no-persist); ruff clean; check_tenant_limit already landed in committed billing/services.py via T-004 co-edit (metering untouched). Full-suite reds limited to other agents' in-flight maintenance/rent/config churn on the shared branch.
2026-06-04 | EPIC-08/T-003 | backend | claude | done | Expense CRUD + filtered list + CSV export at /api/v1/expenses: create(manual; source forced server-side)/list/retrieve/patch/delete scoped via ExpenseManager.for_user (unit->building->owner), IsLandlordOrManager & IsOwnerOfExpense, foreign/unknown id=404; list+export accept building/unit/date_from/date_to filters; manual + auto(source=request) both listed; update/delete restricted to manual (auto-expense rejected 400); CSV export streams (StreamingHttpResponse + csv writer + .iterator()) scoped+filtered with attachment header; audited expense.create|update|delete; 22 expense API tests green. Note: implementation files (expense_views/managers/serializers/services/permissions/urls/models/migration 0002) landed in the shared maintenance module via the concurrent T-002 commit; this commit adds the T-003 test suite + frontmatter. Full-suite reds limited to pre-existing S3/botocore dmpforms+rent failures.
2026-06-04 | EPIC-10/T-005 | backend | claude | done | pricing tiers + subscription state in GET /api/v1/config/public: always carries pricing.tiers (active tiers, sort_order, via T-004 read-only TierSerializer); authenticated callers also get a subscription block {tier_key,status,tenants_used,tenant_limit,can_verify_nid} where benefits (tenant_limit, can_verify_nid) come from the ACTIVE subscription tier only and fall back to free tier (limit from free_tier_tenant_limit config, can_verify_nid=False) when no active sub — status still reflects the most-recent row (past_due/cancelled). No prices/dates/payment data exposed. New module khatir/billing/public_config.py (avoids co-editing T-004's in-flight billing/services.py); view extended in khatir/health/views.py (request.user.is_authenticated gate). 6 new test_config_public tests green; ruff clean; no new migration (DB changes None). Full-suite reds limited to pre-existing S3/botocore dmpforms+rent failures (NoCredentialsError, unrelated).
2026-06-04 | EPIC-11/T-004 | backend | claude | done | admin_portal/permissions.py: dedicated admin-JWT permission layer fully separate from customer permissions — IsAdminUser decodes Authorization Bearer via PyJWT HS256 against ADMIN_JWT_SIGNING_KEY (rejects missing/malformed/wrong-key/expired/unknown-role/missing-admin_user_id; never falls through to customer JWT), attaches verified AdminPrincipal(admin_user_id,role,claims) to request (memoised); RequiresAdminRole(*roles) factory + RequiresAdminSection(section)/AdminSection constants enforce the §2 role->section matrix (users:super/ops/support, platform:super/ops, billing+pricing:super/finance, audit+export:super/compliance) with super allowed everywhere; role matrix documented in module docstring; SUPER/OPS/FINANCE/COMPLIANCE/SUPPORT role consts re-exported. 23 new tests (parametrized full role x section matrix + token-validation edges) green; ruff clean on my files. Full-suite ruff/pytest reds limited to other agents' in-flight admin_portal T-003 auth files (services.py B904) on the shared branch.
2026-06-04 | EPIC-11/T-006 | backend | claude | done | seed admin-session SystemConfig via core migration 0009: admin_session_timeout_minutes=60 (int) + admin_mfa_required=true (bool); idempotent (update_or_create) + reversible (reverse deletes exactly the 2 owned keys); typed get_config returns 60 and True respectively. 2 new tests in test_seed_admin_config.py green; my files ruff clean; makemigrations --check clean. No shared-file edits (urls/base/settings untouched — seed-only). Full-suite ruff/pytest reds limited to other agents' in-flight admin_portal T-003 (services.py B904) + pre-existing S3/botocore dmpforms+rent failures on the shared branch.
2026-06-04 | EPIC-11/T-012 | backend | claude | done | create_super_admin management command (khatir/admin_portal/management/commands/create_super_admin.py): one-time idempotent seed of the first AdminRole.SUPER account — generates secrets.token_urlsafe temp password (make_password-hashed) + pyotp.random_base32 TOTP secret stored encrypted via core.encryption.encrypt; prints temp password + otpauth:// provisioning URI to stdout ONCE (never recoverable, never logged after); writes admin_audit(admin_user=None, action=admin_user.create_super_admin, entity=created user, after={email,role}); --email normalised lower, --name, --dry-run (writes nothing); idempotent re-run on existing email makes no changes and does NOT reprint secrets. 5 new tests in test_create_super_admin.py (creates+hash+TOTP roundtrip, audit row, idempotent, email-normalisation, dry-run) green; ruff clean; makemigrations --check clean (DB change is one row, no schema). No shared-file edits (urls/base untouched — command-only). Full admin_portal suite 105 passed excluding another agent's untracked in-flight test_platform_dashboard.py.
2026-06-04 | EPIC-11/T-005 | backend | claude | done | platform dashboard endpoint GET /admin/api/dashboard (config/urls.py admin/api/ include -> admin_portal/admin_urls.py -> dashboard_views.PlatformDashboardView). dashboard.py: get_platform_dashboard() aggregates platform-wide (NO for_user scoping) total_users, active_landlords (distinct landlord building owners), total_properties, total_units, occupied_units, total_rent_collected {all_time,this_month} (Sum of verified Payment->RentRequest.amount, 2dp), dmp_forms_generated, active_subscriptions; KPI block cached 5min under admin:platform_dashboard:v1; live health{app,database,cache,status} computed outside cache (SELECT 1 + cache round-trip probe). Admin-only via committed T-004 permissions layer: IsAdminUser + RequiresAdminSection(PLATFORM) => super/ops only (finance/compliance/support 403). 12 new tests in test_platform_dashboard.py (KPIs present, aggregate-across-all-users, health, anon/invalid-token/customer-role rejected, platform-roles allowed, non-platform denied, 5-min cache) green; my files ruff clean; makemigrations --check clean (no DB change). Self-contained on committed code only — did NOT touch other agents' uncommitted T-003 views.py/auth_tokens.py. admin_urls.py is now shared (EPIC-12 appended user routes additively). Full-suite reds limited to other agents' in-flight featureflags/services.py C408 + pre-existing S3/botocore dmpforms+rent+tenants-voice failures.
2026-06-04 | EPIC-14/T-002 | infra | claude | done | AI gateway FastAPI scaffold at services/ai-gateway/ (own uv project: pyproject.toml + uv.lock; generated requirements.txt for parity). main.py exposes GET /healthz -> 200 {status:ok, service:khatir-ai-gateway} via create_app() factory; config.py is pydantic-settings Settings reading shared .env (AI_GATEWAY_INTERNAL_TOKEN auth secret, port 8100 to match AI_GATEWAY_URL) — auth_enabled gates on token presence. Thin stateless router, NO DB/business logic. Dockerfile uv-managed python3.13 non-root EXPOSE 8100 uvicorn main:app. docker-compose.yml ai-gateway service added additively (build ./services/ai-gateway, ports 8100, /healthz healthcheck). 5 pytest tests green (health 200 + payload shape + auth-flag on/off + default port); ruff clean; mypy clean (4 files). Deviation: secret env is AI_GATEWAY_INTERNAL_TOKEN not placeholder GATEWAY_SECRET; uv primary per infra contract.
2026-06-04 | EPIC-11/T-003 | backend | claude | done | admin auth flow (login/verify-mfa/logout/me) under /admin/api/auth/, COMPLETELY separate JWT realm from customer auth: PyJWT HS256 tokens signed by dedicated ADMIN_JWT_SIGNING_KEY (env, falls back to admin::SECRET_KEY, asserted != SIMPLE_JWT key), short access TTL capped at ADMIN_SESSION_TIMEOUT_MINUTES; AdminJWTAuthentication attaches request.admin_user (never request.user) + IsAdminAuthenticated permission. login verifies make_password hash on non-disabled AdminUser, returns mfa_required+mfa_token challenge when account has TOTP secret AND ADMIN_MFA_REQUIRED, else issues access token directly; verify-mfa decrypts core.encryption TOTP secret + pyotp.verify(valid_window=1) then issues token+stamps last_login_at; logout revokes by jti in cache until expiry (idempotent, no DB table). Uniform auth_invalid on every failure (no email/factor leak). admin_audit on login_success/login_failed/mfa_challenged/mfa_failed/logout. Email+IP throttles (admin_login_email/ip 10|30 per 10min) on login, IP-only on verify-mfa. Added pyotp>=2.9.0. 15 new tests in test_admin_auth.py (happy no-mfa, wrong-password, unknown-email, disabled, mfa-challenge, mfa-success, mfa-wrong-code, bad-challenge, me-auth-required, me, logout-revokes, disabled-after-login, separate-signing-key, mfa-flag-off, rate-limit) green; my files ruff clean; makemigrations --check clean (no schema change). Shared edits additive: config/urls.py (admin/api/auth/ include, before django admin/), config/settings/base.py (ADMIN_JWT_*/ADMIN_MFA_* + 3 admin throttle rates). Full-suite reds limited to other agents' in-flight featureflags duplicate-seed-migration + pre-existing S3/botocore dmpforms+rent failures on shared branch.
2026-06-04 | EPIC-13/T-002 | backend | claude | done | flag CRUD + toggle + /config/public flags dict. New featureflags/{serializers,services,views,urls}.py: FeatureFlagViewSet (list/create/retrieve/update + @action toggle) at /admin/api/flags, lookup_field=key, super/ops gate via IsPlatformAdmin (SECTION_ROLES[PLATFORM]) + AdminJWTAuthentication/IsAdminAuthenticated. services.public_flags() returns global-scope {key:enabled} cached 60s under featureflags:public; toggle_flag flips enabled+sets updated_by+admin_audit(feature_flag.toggle, before/after enabled)+busts cache; perform_create/update audit feature_flag.create/update + bust cache. enabled is read-only on serializer (toggle-only); key immutable on update. health/views.config_public flags dict now = public_flags(). 17 new tests (CRUD, toggle flip/actor/audit/cache-bust, config_public exposes global + omits role/user, test_toggle_reflects_in_config_public propagation, anon/role gate) green; my files ruff clean; makemigrations --check clean (no schema change). Shared edits additive: config/urls.py (admin/api/ featureflags.urls include after admin_urls), health/views.py (flags=public_flags()). Full-suite reds limited to other agents' in-flight admin_portal user_views.py ruff + T-004 test_seed_flags + pre-existing S3/botocore dmpforms/rent failures.
2026-06-04 | EPIC-13/T-004 | backend | claude | done | seed 5 kill-switches (warnings_feature, reviews_feature, history_flags_feature, free_text_feature, master_kill_switch) + 2 default flags (voice_tenant_entry, dmp_enabled) — all enabled=true (kill-switch convention: enabled=true = feature ON; flip to false to kill). New featureflags/migrations/0002_seed_flags.py: idempotent update_or_create keyed on unique key, reversible unseed removes exactly the 7 keys, scope literal "global" (no app-code import). Test isolation: seed auto-run is a no-op under config.settings.test so it does not pollute the already-committed T-002/T-003 flag-endpoint tests (which assert an empty flags table and build rows with these same keys); test_seed_flags invokes seed_flags() directly to verify it. 7 new tests in test_seed_flags.py (flags-seeded, kill-switches-enabled, default-flags-enabled, voice-present, idempotent, reversible, auto-run-skipped-in-test-db) green; ruff clean; makemigrations --check clean. No shared-file edits (no routes/apps — migration+test only). Full-suite reds limited to other agents' in-flight admin_portal/dmpforms/rent + pre-existing S3/botocore failures, none in featureflags or tenants.
2026-06-04 | EPIC-12/T-003 | backend | claude | done | user search+detail+actions under /admin/api/users (admin_urls.py shared, appended additively below dashboard). New user_views.py: UserSearchView GET /admin/api/users (q= search by phone icontains / name icontains / numeric id exact / linked tenant_profiles__nid_number_masked icontains, distinct, StandardPageNumberPagination), UserDetailView GET /{id} (user + current Subscription + usage{buildings,tenant_profiles,subscriptions} + recent admin audit trail filtered entity_type=accounts.user), UserSuspendView/Reactivate/UpgradeSubscription POST. user_services.py: suspend_user sets is_active=False (simplejwt rejects inactive access tokens) AND blacklists every OutstandingToken refresh for user via BlacklistedToken.get_or_create (cannot re-mint), reason MANDATORY, ConflictError if already suspended; reactivate_user re-enables; upgrade_subscription manual tier override (updates latest Subscription or creates active one), reason mandatory, NotFound on bad tier; all wrapped @transaction.atomic + admin_audit (user.suspend/reactivate/upgrade_subscription, before/after diffs). Authz: AdminJWTAuthentication+IsAdminAuthenticated, users-section gate — IsUsersReadAdmin super/ops/support (support READ-ONLY), IsUsersWriteAdmin super/ops only (support 403 on writes), finance/compliance 403. 28 new tests in test_user_admin.py (search by phone/name/id/masked-nid, paginated, detail payload+404, suspend reason-required/deactivates+blacklists-jwt/audited/already-suspended-409, reactivate/audited/already-active-409, upgrade existing/creates-when-none/reason-required/unknown-tier-404, anon denied, read roles, support-cannot-write, ops+super-can-write, disabled-admin denied) green; ruff clean; makemigrations --check clean (no schema change — writes existing User.is_active + Subscription). Full-suite reds limited to pre-existing S3/botocore NoCredentialsError in dmpforms+rent storage tests (no AWS creds in sandbox), unrelated to this task.
2026-06-04 | EPIC-14/T-010 | backend | claude | done | seed 3 AI-gateway SystemConfig keys via core migration 0010_seed_ai_config (ai_gateway_url=text empty, ai_provider_cache_ttl_seconds=int 300, ai_gateway_secret=text). Non-secret rows update_or_create (idempotent); secret seeded with get_or_create empty placeholder so an operator-set value is never overwritten and the real secret is never embedded in the migration/logs. Reverse unseeds exactly the 3 owned keys. 2 new tests in test_seed_ai_config.py (keys seeded with correct type/desc + secret value empty; typed via get_config) green; my files ruff clean; makemigrations --check clean. No shared-file edits (migration+test only). Full-suite reds limited to other agents' in-flight admin_portal/dmpforms/rent + pre-existing S3/botocore failures, none in core or ai_providers (137 core+ai_providers tests green).
2026-06-04 | EPIC-15/T-005 | backend | claude | done | seed 3 system NotificationTemplate rows via notifications migration 0002_seed_system_templates: rent_reminder_due (trigger=rent_due, channels=[whatsapp,sms]), rent_receipt_generated (trigger=payment_verified, channels=[whatsapp,sms]), welcome_new_user (trigger=user_created, channels=[inapp]). All bilingual (title/body _en+_bn) with declared {placeholder} variables matching body tokens. Idempotent update_or_create keyed on unique key; reverse unseeds exactly the 3 owned keys. 7 new tests in test_seed_system_templates.py (3-seeded, per-template trigger/channels/active/variables, bilingual-and-distinct, placeholders-match-variables, idempotent) green; my files ruff clean; full ruff clean; makemigrations --check clean. No shared-file edits (migration+test only). 35 notifications tests green.
2026-06-04 | EPIC-15/T-009 | backend | claude | done | seed 3 per-message notification cost SystemConfig keys via core migration 0011_seed_notifications_config (whatsapp_cost_per_message=money 0.50, sms_cost_per_message=money 0.30, email_cost_per_message=money 0.00). All type=money so get_config returns Decimal for composer cost preview. update_or_create idempotent; reverse unseeds exactly the 3 owned keys. 2 new tests in test_seed_notifications_config.py (rows seeded with money type/value/desc; typed-via-get_config returns Decimal) green; my files ruff clean; full ruff clean; makemigrations --check clean (single linear chain — 0011 chains off 0010, another agent's in-flight 0012 already chains off 0011). No shared-file edits (migration+test only). Full-suite reds limited to pre-existing S3/botocore NoCredentialsError in dmpforms+rent storage tests, unrelated to this task.
2026-06-04 | EPIC-15/T-008 | backend | claude | done | admin notification-templates CRUD at /admin/api/notification-templates (List/Create/Retrieve/Update via DRF GenericViewSet, lookup by key). key + trigger_event immutable on update (serializer pops them); editable: title/body _en+_bn, channels, variables, active. super+ops gate via IsPlatformAdmin (SECTION_ROLES[PLATFORM]) + AdminJWTAuthentication. Every create/update audited via admin_audit (action notification_template.create/.update with before/after diffs). No deletes (deactivate via active). 13 new tests in test_template_endpoints.py (CRUD, key+trigger immutability, create+update audit diffs, anon reject, platform-roles-allowed, non-platform-denied) green; my files ruff clean; makemigrations --check clean (no model changes). Shared config/urls.py edited additively (one include). Full-suite reds limited to pre-existing S3/botocore NoCredentialsError in dmpforms+rent storage tests, unrelated to this task.
2026-06-04 | EPIC-16/T-005 | backend | claude | done | seed 2 compliance SLA SystemConfig keys via core migration 0012_seed_compliance_config (data_request_sla_days=int 30, data_delete_grace_days=int 7). type=int so get_config returns Python int. update_or_create idempotent; reverse unseeds exactly the 2 owned keys. 2 new tests in test_seed_compliance_config.py (rows seeded with int type/value/desc; typed-via-get_config returns int) green; my files ruff clean; full ruff clean; makemigrations --check clean (0012 chains off 0011). No shared-file edits (migration+test only). Full-suite reds limited to other agents' in-flight notifications/dmpforms/rent + pre-existing S3 failures, unrelated to this task (confirmed identical with my files removed).
2026-06-04 | EPIC-15/T-002 | backend | claude | done | compose_notification service in khatir/notifications/services.py: validates audience (all/role/segment/specific user_ids) + channels (dedup, known wire values) + bilingual content; resolves reach (count active Users matching audience); estimates cost (reach x sum per-channel SystemConfig cost, inapp free, defaults 0 so works without T-009); creates Notification in sending/scheduled status; NOW enqueues T-003 deliver_notification via named current_app.send_task on_commit (no hard import of not-yet-built task), SCHEDULED registers django_celery_beat ClockedSchedule one-off PeriodicTask, RECURRING registers CrontabSchedule PeriodicTask; future-time + recurrence-required validation; every compose audited via admin_audit (action notification.compose, after-diff with reach/cost/status). 21 new tests in test_compose.py (audience/reach all+role+specific, validation rejects, channel dedup, cost from config + zero default, now-enqueue via django_capture_on_commit_callbacks + mocked send_task, clocked/crontab beat rows, audit entry, sender set) green; 35-row notifications suite green; my files ruff clean; full ruff clean; makemigrations --check clean (no model changes). No shared-file edits (services+test only). Full-suite reds limited to pre-existing S3/botocore NoCredentialsError in dmpforms+rent storage tests, unrelated to this task.

2026-06-04 | EPIC-16/T-003 | backend | claude | done | read-only GET /admin/api/consent-records (compliance/views.py ConsentRecordListView + serializers.py + urls.py). Paginated (StandardPageNumberPagination envelope), filterable by user (user_id), consent_type, granted_from/granted_to date range. ConsentRecord is append-only so no write endpoints. compliance+super gate via IsComplianceAdmin (SECTION_ROLES[AUDIT]) + AdminJWTAuthentication. Read-only endpoint = no audit/no migration. 13 new tests in test_consent_endpoints.py (list+envelope, read-only projection shape, filter by user/consent_type/date-range, anon reject, audit-roles-allowed super+compliance, non-audit-roles-denied ops/finance/support) green; 29-row compliance suite green; my files ruff clean; full ruff clean; makemigrations --check clean (no model changes). Shared config/urls.py edited additively (one include). Full-suite reds limited to pre-existing S3/botocore NoCredentialsError in dmpforms+rent storage tests, unrelated to this task.
2026-06-04 | EPIC-08/T-012 | backend | claude | done | expense summary hook: maintenance/selectors.py with expense_total_by_category + expense_total_by_month read-only ORM aggregations (single grouped query each, no N+1), always scoped via Expense.objects.for_user, optional building/unit/date_from/date_to filters, money normalised to 2dp Decimal (0.00 when empty), by_month bucketed via TruncMonth ordered chronologically. Exposed GET /api/v1/expenses/summary as detail=False @action on ExpenseViewSet (IsLandlordOrManager gate, totals serialized as strings, months as ISO first-of-month). _filter_kwargs helper shared by list/export and summary. 11 new tests in test_expense_summary.py (by_category math, by_month math + chrono order, owner scoping x2, building+date filters, empty case, endpoint shape, endpoint scoped, auth-required, tenant-forbidden) green; full maintenance suite green except pre-existing S3/botocore NoCredentialsError in another agent's in-flight test_web_maint.py; my files ruff clean; makemigrations --check clean (no model changes). No shared config/urls.py edit needed (route is a viewset action on the already-registered expenses router). Full-suite reds limited to other agents' in-flight billing/tenants/dmpforms/rent work, unrelated to this task.
2026-06-04 | EPIC-12/T-004 | backend | claude | done | refund queue endpoints in khatir/admin_portal/refund_views.py: GET /admin/api/billing/refunds lists pending payment intents (no dedicated table — these are EPIC-10 stub customer-realm AuditEntry rows action=subscription.payment_intent with after.state=pending; queue excludes any intent already resolved via a follow-up entry whose after.resolves=<id>); POST /admin/api/billing/refunds/{id}/process approves (writes refunded resolution AuditEntry + cancels Subscription) or denies+reason (writes refund_denied resolution, subscription untouched). reason mandatory on deny (serializer + service re-check). Both decisions write immutable AdminAuditEntry action=refund.process. finance+super gate via IsBillingAdmin (SECTION_ROLES[BILLING]) + AdminJWTAuthentication. Re-process of a resolved intent -> 409; unknown -> 404. Append-only/idempotent (no audit row mutated). 15 new tests in test_refund.py (list, approve cancels sub, deny keeps sub active, deny-requires-reason, audit, process-twice 409, unknown 404, anon denied, finance/super allowed, ops/support/compliance denied, non-finance cannot process) green; full 160-row admin_portal suite green; my files ruff clean; makemigrations --check clean (no model changes). Routes added additively to admin_portal/admin_urls.py (the /admin/api/ app include), no config/urls.py edit needed. Full-suite ruff reds limited to other agents' in-flight billing/public_config.py + dashboard/tests, unrelated to this task.
2026-06-04 | EPIC-09/T-001 | backend | claude | done | dashboard aggregation selectors: new khatir.dashboard app (no models/migration, registered in INSTALLED_APPS) with selectors.py get_dashboard(user, months) -> typed frozen DashboardMetrics dataclass. Computes total_collected/pending/overdue + collection_rate (collected/(collected+pending+overdue)x100, 0.0 on empty denom) from RentSchedule status grouped (paid/pending+requested/overdue); occupancy (occupied vs total Unit) via single Count agg; monthly_series last N calendar months anchored to localdate, rent keyed off RentSchedule.period + expense off TruncMonth(Expense.date), zero-fill missing months; income_vs_expense + net; top 5 expense categories by amount; late_payer_count = distinct leases with >=1 overdue schedule. All reads scoped via Lease/Unit/Expense for_user (RentSchedule via lease__in for_user subquery) so anon/non-owner -> empty payload. Fixed bounded query count (no N+1). 14 new tests in test_selectors.py (each metric + empty/zero cases + requested-as-pending + scoping + anon empty + query-count guard) green; ruff clean on my files + full repo; makemigrations --check clean. Shared base.py edited additively (one INSTALLED_APPS entry). Full-suite reds limited to pre-existing S3/botocore NoCredentialsError in other agents in-flight dmpforms/rent/maintenance storage tests + pricing-admin, unrelated to this read-only task.
2026-06-04 | EPIC-12/T-001 | backend | claude | done | pricing tier list/preview/edit endpoints in khatir/admin_portal/pricing_views.py + pricing_services.py + pricing_serializers.py. GET /admin/api/pricing/tiers lists all tiers (active+inactive, sort_order order, full admin projection incl prices/active/sort_order). POST /admin/api/pricing/tiers/{key}/preview is a read-only impact calc (subscribers_affected = active subs on tier; monthly_revenue_delta = sum over affected subs of new-minus-old monthly-equivalent price, annual amortised /12, null price = 0; no write). PATCH /admin/api/pricing/tiers/{key} applies changes under select_for_update, reason mandatory, writes before/after field-diff AdminAuditEntry action=pricing.tier.edit, busts /config/public via new billing/public_config.invalidate_public_config_cache (safe no-op delete today; T-002 layers real cache on shared PUBLIC_CONFIG_CACHE_KEY). key immutable (not editable). finance+super gate via IsPricingAdmin + AdminJWTAuthentication; ops/support/compliance + anon denied. 17 new tests in test_pricing_admin.py (list incl inactive, preview count+delta monthly & annual-amortised, preview no-write, preview 404, edit applies, edit requires reason, before/after audit, cache bust mocked, edit 404, role gate super/finance allowed, ops/compliance/support denied read+write, disabled admin denied) green; full 228-row admin_portal+billing suite green; my files ruff clean; full ruff clean; makemigrations --check clean (no model changes - writes existing PricingTier). Routes added additively to admin_portal/admin_urls.py. Full-suite reds limited to other agents in-flight EPIC-09 test_tier_gate.py import + pre-existing S3/botocore NoCredentialsError in dmpforms+rent, unrelated to this task.
2026-06-04 | EPIC-10/T-009 | cross-cutting | claude | done | NID verification tier gate: check_can_verify(user) in billing/services.py raises TierFeatureGated (new ErrorCode.FEATURE_REQUIRES_UPGRADE, 402) unless the user has an active subscription whose tier includes_verification==True; wired into TenantOcrView + TenantVoiceView (gate runs before any image is stored / paid provider call), manual tenant create path untouched. New billing/tests/test_tier_gate.py: 14 tests (free blocked, paid-without-verification blocked, bundle-with-verification ok, cancelled-plan blocked, ocr_blocked_free, ocr_ok_bundle10, ocr gate-before-storage, voice_blocked_free, voice_ok_bundle10 + service cases) green; updated existing OCR/voice endpoint tests to grant a verification tier (28 ocr+voice+gate tests green). My files ruff clean; makemigrations --check clean (no model changes). No shared config/urls.py or base.py edit needed. Mobile upgrade-prompt intercept DEFERRED: T-008 (upgrade_prompt.dart) still todo and EPIC-04 mobile add-tenant OCR/voice screens absent at HEAD; backend gate complete, reuses the same feature_requires_upgrade intercept pattern. Full-suite reds limited to pre-existing S3/botocore NoCredentialsError + other agents in-flight tests, unrelated to this task.
2026-06-04 | EPIC-08/T-005 | backend | claude | done | token-scoped maintenance web form: khatir/maintenance/web_views.py + web_urls.py (/m/<token> GET form, /m/<token>/submit POST) + templates/maintenance/web_maint.html (extends shared rent/_web_base.html for the Notun Din token palette) + web_maint_error.html. GET resolves the T-004 signed unit token -> renders webMaint screen (category radios from MaintenanceCategory enum, bn+en, description textarea, optional photo); POST creates MaintenanceRequest (status=open, stamps the unit active Lease, photo via core.storage.store_encrypted kind=proof), per-token cache rate-limit (5/5min, SystemConfig-tunable), empty description bounces back, PRG redirect -> ?submitted=1 success state. Invalid token -> 404, expired -> 410, rate-limited -> 429, all via friendly bilingual error page. Route added additively to config/urls.py at root (browser HTML, not /api/v1/). 11 new tests in test_web_maint.py (render, submit-creates-request, lease-stamp+default-category, photo-encrypted via mocked storage, empty-bounce, expired 410, invalid 404, submit-invalid 404, GET-on-submit 405, palette-not-hardcoded, rate-limit) all green; my files ruff clean; full ruff clean; makemigrations --check clean (no model changes). Full-suite reds limited to other agents in-flight billing test_tier_gate.py import + pre-existing S3/botocore NoCredentialsError (repo .env S3_BUCKET set, no creds) in dmpforms/rent storage tests, unrelated to this task.
2026-06-04 | EPIC-13/T-008 | cross-cutting | claude | done | flag propagation regression test: new khatir/featureflags/tests/test_flag_propagation.py with test_flag_toggle_propagates_to_config_public — primes /config/public cache (flag OFF), toggles via real PATCH /admin/api/flags/{key}/toggle (super admin JWT), asserts next anon /config/public read flips to ON (cache busted, <60s budget), then toggles back and asserts immediate OFF (regression both directions). Pairs with EPIC-12 T-010. End-to-end via HTTP surfaces only, no internal cache plumbing touched. 1 new test green; full featureflags suite (61 tests) green; my file ruff clean; full ruff clean; makemigrations --check clean (no model changes). No shared config/urls.py or base.py edit needed. Full-suite reds limited to pre-existing S3/botocore NoCredentialsError in dmpforms/rent storage tests, unrelated to this test-only task.
2026-06-04 | EPIC-14/T-003 | infra | claude | done | provider-agnostic router in ai-gateway: services/ai-gateway/router.py ProviderRouter.route(category, payload) fetches active providers via injected ConfigSource (60s TTL cache per task §15, env-overridable), orders primary->fallback->rest, tries each, fails over on ProviderError; every attempt logged through pluggable UsageLogger (UsageRecord mirrors AIUsageLog schema: provider/category/success/latency_ms/tokens_used/cost_usd/failover_from), success records failover_from when it failed over. Raises NoProviderConfigured (empty) / AllProvidersFailed (all error). providers/base.py: Provider protocol + ProviderConfig/ProviderResult dataclasses + generic HTTPProvider (bearer-auth JSON POST, vendor clients subclass later). Logging failures swallowed so they never mask results. 11 tests green in tests/test_router.py (primary_success, primary_fail_fallback, both_fail_raises + no-provider, inactive-skip, ttl-cache) plus 5 pre-existing health tests; ruff clean; mypy clean (5 source files). pytest-asyncio added to dev deps (asyncio_mode=auto). Infra service - own uv project, no Django/shared base.py or urls.py edit needed.
2026-06-04 | EPIC-13/T-003 | backend | claude | done | kill-switch endpoints: featureflags/killswitch_views.py (GET /admin/api/killswitches lists the 5 named switches; POST /admin/api/killswitches/{key}/toggle) + kill-switch services in featureflags/services.py (KILL_SWITCH_KEYS, _verify_fresh_mfa fresh-TOTP re-confirm even mid-session, toggle_kill_switch atomic: flip FeatureFlag.enabled, record immutable KillSwitchEvent disable/enable, admin_audit kill_switch.toggle, bust public-flags cache) + urls (additive). Super-only (IsSuperAdmin); reason mandatory + optional lawyer_reference; wrong/absent MFA -> 403, missing reason -> 400, unknown key -> 404. 16 new tests in test_killswitch.py (valid-MFA kills, wrong-MFA blocked, no-reason blocked, immutable event+actor, enable-on-restore, audit entry, cache bust, /config/public propagation, 404 unknown, anon rejected, super-only, non-super denied) green; featureflags+admin_portal suites 237 green; my files ruff clean; makemigrations --check clean (no model changes). No shared base.py/urls.py edit (only featureflags/urls.py, app-local). Full-suite reds limited to pre-existing S3/botocore NoCredentialsError in dmpforms/rent storage tests, unrelated to this task.
2026-06-04 | EPIC-14/T-007 | backend | claude | done | Django AI gateway client: khatir/ai_providers/client.py call_gateway(category, payload, *, timeout) POSTs {AI_GATEWAY_URL}/v1/{category} with the shared AI_GATEWAY_INTERNAL_TOKEN on the Authorization header and returns a normalized AIGatewayResult (data/provider_key/model_name/raw). Thin + stateless: no routing/vendor logic (that lives in the gateway, T-003). AIGatewayError on unconfigured URL (clear config error), transport failure, non-2xx (carries status_code), non-JSON, or unexpected shape; token/payloads never logged. New AI_GATEWAY_URL/_INTERNAL_TOKEN/_TIMEOUT settings in config/settings/base.py (additive); requests added as explicit dep (uv.lock synced). 10 tests in ai_providers/tests/test_client.py (happy path forwards url+payload+token+timeout, string category, timeout override, missing-url config error, no-auth-header when token absent, transport/non-2xx/non-json/bad-shape errors, token-not-in-exception) green; my files ruff clean; full ruff clean; makemigrations --check clean (no model changes). Full-suite reds limited to pre-existing S3/botocore NoCredentialsError in dmpforms/rent storage tests, unrelated to this task.
2026-06-04 | EPIC-15/T-006 | backend | claude | done | retrofit EPIC-07 rent reminders onto the admin-editable rent_reminder_due template: added NotificationTemplate.render(variables) (str.format_map over a lenient mapping that echoes unfilled {placeholders} verbatim, returns rendered title_en/title_bn/body_en/body_bn) + module-level get_template(key) helper (active row by key, raises DoesNotExist if missing/inactive) in notifications/models.py. rent/messaging._render_message now renders the seeded template (Bangla body + English body, newline-joined) via _template_variables (tenant_name/amount/property_name/due_date/payment_link) with a fallback to the original built-in copy if the template row is absent/inactive — send path never breaks. Behavior identical (still bilingual, still carries /r/{token} link); copy now admin-editable. 7 new tests (render interpolates / leaves unknown placeholder / no-vars; get_template active+skip-inactive+missing; rent test_message_uses_admin_editable_template) + updated test_message_carries_public_link_and_is_bilingual to behavioural assertions; notifications+rent send/reminder suites green; my files ruff clean; full ruff clean; makemigrations --check clean (no model fields added). Full-suite reds limited to pre-existing S3/botocore NoCredentialsError in dmpforms/rent storage tests, unrelated to this task.
2026-06-04 | EPIC-14/T-009 | backend | claude | done | admin AI-provider endpoints already implemented at HEAD (ai_providers/admin_views.py: AIProviderListCreateView GET/POST, AIProviderDetailView PATCH, AIProviderTestConnectionView POST, AIUsageView GET) + AIProviderAdminSerializer (write-only plaintext api_key encrypted to api_key_enc, has_api_key flag, never returns ciphertext) with non-BD-OCR DPA validation (endpoint_is_bangladesh: empty/foreign endpoint => dpa_reference required, enforced on POST+PATCH); call_gateway-backed test-connection, admin_audit on create/update, super+ops (IsAIProviderAdmin) gate; routes in admin_portal/admin_urls.py. Completed finish protocol (frontmatter status=done + BOARD). 21 tests in test_providers_admin.py green; full suite 142 green; ruff clean; makemigrations --check clean (no model changes).
2026-06-04 | EPIC-15/T-003 | backend | claude | done | notification delivery Celery tasks (deliver_notification fan-out + deliver_to_recipient) implemented at HEAD in notifications/tasks.py: deliver_notification re-resolves the audience_filter at send time (all/role/segment/specific; fails closed to empty queryset on a malformed filter so a stale Beat row never crashes the worker), fans out one deliver_to_recipient sub-task per recipient x channel, marks the broadcast sent. deliver_to_recipient idempotently upserts the NotificationDelivery row (no duplicate on retry), renders the recipient language variant, calls get_sender per channel (inapp/email synchronous->delivered + delivered_count bump; whatsapp/sms->sent, confirmed later by webhook), records sent/delivered/failed with error capture, bumps Notification sent_count/delivered_count atomically via F(). Completed finish protocol (frontmatter status=done + this BOARD line). 14 tests in test_delivery.py green (fan-out per recipient x channel, marks sent, skips inactive, role/specific audiences, bad-audience fails closed, missing-notification no-op, inapp delivered+counts, sms sent-not-delivered, address=phone, sender-failure->failed, idempotent no-dupe, EN/BN language render); notifications suite 91 green; full suite 1150 passed; ruff clean; makemigrations --check clean (no model changes).
2026-06-04 | EPIC-15/T-007 | backend | claude | done | admin notification broadcast endpoints wired: registered NotificationViewSet on the notifications app router (notifications/urls.py) so /admin/api/notifications resolves -- GET list (newest first), POST compose (delegates to compose_notification: validates audience/channels/schedule, resolves reach, estimates cost, dispatches/schedules delivery, audits notification.compose; DomainValidationError -> 400; sender = acting admin; response carries reach + estimated_cost), GET /{id} (NotificationDetailSerializer embeds per-recipient deliveries), POST /{id}/send-test (preview send to acting admin only via get_sender(INAPP); no audience fan-out, no counter mutation; audited notification.send_test). View+serializer+service logic already present at HEAD; this task added the route registration + endpoint test coverage. Super+ops gate (IsPlatformAdmin). 15 tests in notifications/tests/test_broadcast_endpoints.py green (list, compose creates/sets-sender/audited/400-bad-audience/400-missing-channels, retrieve embeds deliveries, send-test admin-only+counters-untouched+audited, anon rejected, super/ops allowed, finance/compliance/support denied); notifications suite 106 green; my files ruff clean; makemigrations --check clean (no model changes, no migration). Full-suite reds limited to a concurrent agent's in-flight EPIC compliance audit-log endpoint (unrelated to this task; fails identically with its work stashed).
2026-06-04 | EPIC-15/T-006 | backend | claude | done | retrofit EPIC-07 rent-link/reminder copy onto the admin-editable rent_reminder_due NotificationTemplate. rent/messaging._render_message now sources its body from get_template('rent_reminder_due').render(_template_variables(req, link)) (tenant_name=lease.tenant.name, amount=৳{whole-taka}, property_name=lease.unit.building.name, due_date=period, payment_link=link) and joins body_bn + body_en newline-first — delivery behavior unchanged (bilingual body carrying the /r/{token} link via send_with_fallback WhatsApp→SMS). Reuses existing NotificationTemplate.render + module-level get_template from T-005 (no new model/migration). Updated test_message_carries_public_link_and_is_bilingual to assert template-driven copy (Please pay using this link / ভাড়া), interpolated tenant_name, and no leftover {payment_link} token. rent send+reminder + notifications suites green; my files (messaging.py, test_send.py) ruff clean; makemigrations --check clean (no model changes). Lone full-suite red is a concurrent agent's in-flight compliance audit-log-endpoint test (unrelated; its files are uncommitted on the shared branch).
2026-06-04 | EPIC-16/T-002 | backend | claude | done | enhanced admin audit-log endpoint at GET /admin/api/audit-log (compliance/views.AdminAuditEntryListView + route in compliance/urls.py; serializer AdminAuditEntrySerializer already present at HEAD). Paginated read-only JSON envelope over append-only AdminAuditEntry (newest first), full filters admin_user/action/entity_type/entity_id/date_from/date_to, plus ?format=csv streaming export via StreamingHttpResponse + csv.writer over an echo file-like (no full-set buffering, queryset.iterator()). format=csv negotiated through a passthrough _CsvRenderer registered on the view (renderer_classes=[JSONRenderer,_CsvRenderer]) so DRF content negotiation accepts the format value instead of 404ing; branch on request.accepted_renderer.format. Compliance+super (audit section) gate reusing IsComplianceAdmin. 13 tests in compliance/tests/test_audit_log_endpoint.py green (list, read-only shape, filter by admin_user/action/entity_type+id/date-range, csv streams filtered rows + headers, anon rejected, super+compliance allowed, ops/finance/support denied). Full suite 1178 passed; ruff clean; makemigrations --check clean (no model changes).

2026-06-04 | EPIC-16/T-004 | backend | claude | done | PDPA data-request queue endpoints in new compliance/data_request_views.py (kept out of the concurrently-edited views.py to avoid the shared-file race). GET /admin/api/data-requests: paginated read-only DataRequestSerializer (adds derived sla_state on_track/due_soon/overdue), filters status/type/sla (sla buckets from settings.DATA_REQUEST_SLA_DUE_SOON_DAYS, default 7; SLA window DATA_REQUEST_SLA_DAYS default 30 added to settings/base.py). POST /admin/api/data-requests/{id}/process: DataRequestProcessSerializer {action approve|reject, reason} (reason mandatory on reject); approve export -> generate export package stub + status=completed+completed_at; approve delete -> queue erasure stub + status=processing; reject -> status=rejected+completed_at; all stamp handled_by and write admin_audit (data_request.approve_export/approve_delete/reject) with before/after+reason+ip; select_for_update + not-pending=>409 ConflictError (idempotent, no double-audit), missing=>404. Compliance+super (audit section) gate via own IsComplianceAdmin. Routes appended to compliance/urls.py. 19 tests in compliance/tests/test_data_request_endpoints.py green (list, sla_state shape, filter status/type/sla buckets, auth+role gate compliance/super allow & ops/finance/support deny, approve-export completes+audits, approve-delete->processing, reject requires-reason(400)/rejected+audits, 409 already-resolved, 404 missing, process role-gated, no-double-process single audit). Full suite green; ruff clean; mypy clean on my files (pre-existing authentication.py noise only); makemigrations --check clean (no model changes).

2026-06-04 | EPIC-14/T-005 | infra | claude | done | ASR(voice) + chat provider stubs in ai-gateway (services/ai-gateway/providers/asr.py, chat.py) subclassing HTTPProvider: validate payload (audio_b64 / non-empty messages), default language=bn, normalise vendor JSON into voice {transcript,language,fields} and chat {reply,role} envelopes; exported via providers/__init__.py + factory helpers. API key sent as bearer header, never logged/embedded. 7 tests in tests/test_stub_providers.py (httpx.MockTransport mocked vendor, no real API): envelope normalise, sparse defaults, missing-audio/empty-messages reject, router voice+chat dispatch smoke. ruff clean on my files; full gateway suite 24 passed.

2026-06-04 | EPIC-14/T-006 | infra | claude | done | per-call usage logging in ai-gateway: new services/ai-gateway/usage.py HTTPUsageLogger (POSTs each router UsageRecord JSON to Django /admin/api/ai-usage with internal-token bearer auth) + build_usage_logger factory degrading to NoOpUsageLogger when no django_base_url configured; config.py adds django_base_url/ai_usage_path/ai_usage_timeout_seconds + usage_logging_enabled/ai_usage_url props. Internal token only in Authorization header, never in record/log; UsageRecord carries only accounting metadata (no api_key). 6 new tests in tests/test_usage.py via httpx.MockTransport (mocked Django, no real call): posts record+auth, omits header w/o token, raises on 5xx, factory noop-without-url + http-when-configured, router logging-failure never breaks live call. ruff All checks passed; mypy Success on usage.py/config.py; full gateway suite 24 passed.
2026-06-04 | EPIC-09/T-010 | backend | claude | done | dashboard N+1 / query-count guard in khatir/dashboard/tests/test_perf.py. setUpTestData seeds realistic portfolio (5 buildings, 20 units across them, 1 active lease/unit, 50 rent-schedule payments spread over leases/periods/statuses PAID/PENDING/REQUESTED/OVERDUE, 20 expenses across units+categories). test_dashboard_query_count wraps get_dashboard(landlord, months=6) in CaptureQueriesContext and asserts <= QUERY_BUDGET=12 (upper-bound per §15: selectors do ~8 fixed aggregate/grouped passes; any per-row loop over the 50 schedules/20 units adds tens and trips the ceiling). 1 new test green; full suite 1202 passed; ruff clean; makemigrations --check clean (no DB changes).
2026-06-04 | EPIC-12/T-002 | backend | claude | done | post_save/post_delete signal on PricingTier busts the shared /config/public cache key (billing/signals.py, registered via BillingConfig.ready()); reuses EPIC-10 T-005 PUBLIC_CONFIG_CACHE_KEY + invalidate_public_config_cache() so the explicit T-001 edit-service bust and the model-level signal are idempotent (catches admin/migration/other write paths too). 4 new tests in billing/tests/test_cache_invalidation.py: save/create/delete each clear primed key; /config/public reflects new price after bust. ruff clean; makemigrations --check clean (no DB changes); full suite green.
2026-06-04 | EPIC-09/T-002 | backend | claude | done | dashboard read endpoint GET /api/v1/dashboard?months=N (khatir/dashboard/views.py DashboardView APIView, serializers.py DashboardSerializer + MonthPoint/CategoryTotal output serializers, urls.py mounted in config/urls.py under api/v1/). Calls T-001 get_dashboard selectors; months param clamped 1..24, defaults to dashboard_months_default SystemConfig (T-003) via get_config w/ fallback 6; payload cached per-user (key dashboard:<pk>:<months>) 60s TTL (never global, §15); guarded by IsLandlordOrManager; owner-scoped through selectors' for_user. 8 new API tests in tests/test_dashboard_api.py: full response shape (all 13 metric keys), scoped (2nd landlord excluded), months param length, config-default length, invalid-months fallback, per-user caching, requires-auth (401/403), tenant-role forbidden (403). ruff clean; makemigrations --check clean (no DB changes); full suite 1210 passed.
2026-06-04 | EPIC-14/T-004 | infra | claude | done | concrete OCR provider HTTP client in ai-gateway (services/ai-gateway/providers/ocr.py GoogleVisionOcrProvider subclassing HTTPProvider): accepts image bytes (or base64 str), base64-encodes into Google Cloud Vision images:annotate DOCUMENT_TEXT_DETECTION request (endpoint_url defaultable/overridable), api_key from ProviderConfig sent as Vision ?key= query param (never in body/logs), parses fullTextAnnotation/legacy textAnnotations into normalized ExtractedTenant JSON {name,nid_number,dob,address each {value,confidence}} mirroring Django dto; dob normalized to ISO YYYY-MM-DD (DD/MM/YYYY + ISO), nid digits-only; unreadable image -> empty DTO, Vision per-image error/transport failure -> ProviderError (router fails over). build_ocr_provider factory + exports added additively to providers/__init__.py. 11 tests in tests/test_ocr.py via httpx.MockTransport (mocked Vision, no real API): request shape+key-as-query+base64, full extraction+normalization, ISO passthrough, empty image, base64-str input, missing-image/HTTP-5xx/Vision-error -> ProviderError, legacy shape, factory, router dispatch smoke. ruff clean; mypy Success on ocr.py; full gateway suite 35 passed.
2026-06-04 | EPIC-16/T-009 | docs | claude | blocked | MVP quality gate FAILS — MVP not shippable. make status shows EPIC-00→16 NOT all done/verified: 136/197 MVP tasks done (EPIC-04 8/16, EPIC-06 6/10, EPIC-07 9/14, EPIC-08 7/12, EPIC-09 4/10, EPIC-11 7/12, EPIC-12 4/10, EPIC-13 5/8, EPIC-14 9/12, EPIC-15 8/14, EPIC-16 5/9). Own deps EPIC-16 T-006/T-007/T-008 still todo. make screen-coverage target does not exist. EPIC-05 T-010 DMP hard gate IS done (satisfied) but insufficient alone. No _completion_report.md / RELEASE.md written (would be false). Backend ruff + makemigrations --check clean. Re-run only after all EPIC-00→16 reach done/verified and make test && make lint green across all 3 apps. status=in-progress. No tests (docs task).
2026-06-05 | EPIC-14/T-008 | backend | claude | done | retrofit EPIC-04 OCR through the AI gateway: new config-selectable GatewayOcrProvider in khatir/tenants/extraction/ocr_provider.py (key "gateway", registered alongside default) forwards the NID image via new aiproxy_client.extract_nid(image) helper (thin wrapper over committed call_gateway("ocr",...), base64-encodes bytes for JSON transport, internal token only on Authorization header) and maps the gateway's already-normalized per-field envelope {field:{value,confidence}}, dob ISO string, onto the local ExtractedTenant via _normalize_gateway_payload (re-applies shared normalize_text/nid/date so DTO type promises hold; odd/missing field shapes degrade to None). TenantOcrView (T-005) endpoint + tests unchanged — provider swap is operational via ocr_provider_key config; default seam kept for offline/tests. Only the normalized DTO crosses the boundary; image bytes/raw payload never logged. New tests: 4 gateway-provider tests in tenants/tests/test_extraction.py (envelope normalize, empty data, odd-shape tolerance, audio rejected, config selection) + extract_nid base64-to-/v1/ocr test in ai_providers/tests/test_client.py. ruff clean; makemigrations --check no changes (DB unchanged); full pytest suite green.
2026-06-05 | EPIC-15/T-004 | backend | claude | done | per-recipient delivery tracking: post-send transitions on top of T-003 sender. New signed, tamper-evident open-token (notifications/tracking.py make_token/resolve_token/beacon_path, dedicated salt, non-expiring Signer mirroring EPIC-07 rent link-token; token IS the capability — no admin auth). Two public web surfaces mounted at root via notifications/web_urls.py (web_views.py): GET /n/<token>/open.gif open beacon serves a 1x1 transparent GIF (no-store) and marks the delivery `opened`, always returning the pixel even for a bad token so the embedding page never breaks/leaks; POST /n/<token>/delivered provider webhook advances a remote `sent` row to `delivered`. Transitions are tasks.mark_opened / tasks.confirm_delivered — idempotent under select_for_update so duplicate provider retries or pixel reloads never double-count opened_count/delivered_count; an open from `sent` backfills delivered_at + delivered_count exactly once. No model/migration change (opened_at/opened_count already on T-001 schema). 20 new tests in notifications/tests/test_tracking.py (token round-trip/tamper/garbage/unknown, confirm + open idempotency/regression guards, beacon pixel+record+invalid-token-still-pixel+reload-no-double-count, webhook confirm/duplicate/404/405). Full pytest suite green; ruff check . clean; makemigrations --check no changes; mypy clean on touched files (pre-existing maintenance/models.py errors unrelated).
2026-06-05 | EPIC-11/T-007 | admin | claude | done | admin login + MFA screen (Next.js). app/login/page.tsx now a real client email+password form; app/login/mfa/page.tsx 6-digit TOTP step. Both proxy through server-side route handlers app/api/auth/{login,verify-mfa,logout}/route.ts which call EPIC-11.T-003 backend (/admin/api/auth/*) via lib/auth/api.ts (zod-validated LoginResult|MfaChallenge|TokenResult) and write the admin access token into an HTTP-only SameSite=strict cookie (lib/auth/session.ts) — token never reaches client JS. MFA challenge token also held in a short-lived HTTP-only cookie between steps (read server-side), so it is never exposed. Error states: invalid creds/disabled (backend uniform 401), wrong code (clears field + retry), rate-limit 429, expired challenge 440 -> restart /login. Success -> /dashboard. guard.ts re-exports getSession/ADMIN_SESSION_COOKIE from session.ts (no dashboard behaviour change). 8 new RTL tests in test/login.test.tsx (form render, MFA-required routing, no-MFA direct dashboard, invalid-creds error, verify-disabled-until-6-digits, verify success, wrong-code error+clear, digit-strip). Gate: npm install + build OK (routes /login, /login/mfa, /api/auth/* present), tsc + eslint clean, vitest 10 passed.
2026-06-05 | EPIC-11/T-008 | admin | claude | done | authenticated shell + real session guard (Next.js). Replaced EPIC-00 stub guard: lib/auth/admin-session.ts getAuthenticatedAdmin() reads the T-007 HTTP-only session cookie then calls backend GET /admin/api/auth/me with the bearer token (token stays server-side), zod-validates the AdminUserSerializer response; missing cookie / 401-403 / unreachable backend (fail-closed) / disabled account -> null -> layout redirects /login. Role-aware sidebar: _nav.ts gains roles[] + navForRole(role) mirroring backend admin_portal/permissions.py SECTION_ROLES + spec §2.1 (super=all; ops+support=Users/Support; finance=Pricing; compliance=Features/Kill-switch/Compliance; Dashboard+Analytics=all; Notifications/AI providers/System/Admin users/Security=super-only); Sidebar takes a role prop and filters. Topbar (client) shows name + human-readable role badge + Log out button POSTing /api/auth/logout then router.replace('/login'). SessionTimeoutWarning client banner reads JWT exp (decoded unverified for the timestamp only), counts down within 5 min, "Stay signed in" -> server refresh re-validates /me, routes /login on expiry. 14 new tests (test/admin-session.test.ts guard: no-cookie/valid-me/401/disabled/unreachable/malformed; test/sidebar.test.tsx navForRole matrix + role-aware render; test/topbar.test.tsx name+badge+logout). Gate: npm install + build OK, tsc + eslint clean, vitest 24 passed.
2026-06-05 | EPIC-11/T-009 | admin | claude | done | platform dashboard page (Next.js) replacing the EPIC-00 placeholder. New data layer lib/api/dashboard.ts (zod-validated fetchDashboard for GET /admin/api/dashboard — T-005 payload: KPIs + live health; recent_activity modeled optional default [] since the audit list API is T-011/not-done). page.tsx is now a client component using TanStack Query with refetchInterval 60_000 (task §2) and loading (skeleton) / error (retry button) / data states. Three token-only components: KpiCard (uppercase label + title-font value + context chip), ActivityFeed (header + audit-entry rows with status dot/action/actor/summary/relative time, empty state when no entries), HealthTile (app/database/cache status rows -> chips ok→sage/degraded→butter/down→danger). KPI tiles surface real T-005 metrics (total_users, active_landlords, properties + occupied/total units, rent this-month/all-time, DMP forms, active subscriptions) — prototype MRR/churn mock dropped (no selectors). All colours/spacing/radii from Notun Din tokens. 5 new RTL tests in test/dashboard.test.tsx (KPI render from API, activity feed with entries, activity empty state, health panel incl DOWN chip, error state + Retry). Gate: npm install + build OK (route /dashboard present), tsc + eslint clean, vitest 29 passed (5 new).
2026-06-05 | EPIC-11/T-010 | admin | claude | done | sidebar nav + coming-soon stubs. The role-aware sidebar (all §3.1 nav items wired to routes, active-link highlight, "Soon" badges) and the reusable ComingSoon stub were delivered by T-008 (_nav.ts NAV_ITEMS+navForRole, components/features/sidebar.tsx, components/features/coming-soon.tsx); every unbuilt module page app/(dashboard)/<module>/page.tsx renders <ComingSoon title=…/>. Coming-soon is a shared component reused per module route (each stub keeps its own route/title/nav context) rather than one shared /coming-soon route. T-010 adds the test coverage T-008 left open: test/sidebar.test.tsx extended (all 13 §3.1 entries present, distinct absolute routes, comingSoon flags + "Soon" badge count, link hrefs match routes); new test/sidebar-active.test.tsx (usePathname=/pricing -> Pricing link gets bg-sageBg/text-sageDk while Dashboard does not; child route keeps parent active); new test/coming-soon.test.tsx (title heading + "Coming soon" placeholder). No production code changes needed. Gate: npm install + npm run build OK (all 13 module routes generated, tsc clean), vitest 38 passed across 7 files (9 new).
2026-06-05 | EPIC-11/T-011 | admin | claude | done | audit-log viewer (Next.js) + backend GET /admin/api/audit-log. Backend: admin_portal/audit_views.py AuditLogView (read-only, cursor-paginated newest-first via StandardCursorPagination over the immutable AdminAuditEntry ledger written by T-002 admin_audit; AdminJWTAuthentication + IsAdminAuthenticated, gated on the audit section super+compliance via new IsAuditReadAdmin reusing _UsersSectionPermission) + audit_serializers.py AuditLogEntrySerializer (read-only fields incl before_json/after_json + denormalized actor label, System for null admin); optional AND-combined filters admin_user(id|system) / action / entity_type / from / to (ISO created_at bounds via parse_datetime); route path("audit-log", ...) appended to admin_urls.py. No create/update/delete route -> non-GET 405 (matches append-only model). 18 tests in tests/test_audit_log_view.py (compliance+super read, ops/support/finance 403, anon 401/403, diff+actor payload, System actor, newest-first, filter action/entity_type/admin_user/system/date-range[backdated via raw SQL since model immutable], no-write-methods 405) green; full admin_portal suite 195 passed; ruff clean; mypy clean on my 2 new files (pre-existing authentication.py/maintenance noise only). Frontend: lib/api/audit.ts (zod-validated fetchAuditLog + auditLogPath/auditQueryKey + AuditFilters incl opaque cursor), components/admin/audit_table.tsx (filter bar admin_user/action/entity_type/from/to, token-styled table when/actor/action/entity/ip, per-row before/after JSON diff expander + reason, Prev/Next cursor buttons, empty state; read-only no edit/delete), app/(dashboard)/audit/page.tsx (client TanStack Query keepPreviousData, cursor extracted from DRF next/previous links, loading skeleton / error+Retry / data states), _nav.ts adds compliance-gated /audit "Audit log" (ScrollText) live item. All colours/spacing/radii from Notun Din tokens (rounded-sm/-button/-card, sageBg, mutedDk, font-mono). 6 new RTL tests in test/audit.test.tsx (render rows, expand diff incl reason, filter-change re-queries, cursor next-page, empty state, error+Retry); widened sidebar "unbuilt=comingSoon" test to allow built pages (Dashboard+Audit log). Gate: npm install + npm run build OK (route /audit generated, tsc + eslint clean via next build), vitest 44 passed (8 files).
2026-06-05 | EPIC-12/T-006 | admin | claude | done | reusable tier impact preview modal. New components/admin/impact_preview_modal.tsx (ImpactPreviewModal): presentational confirmation surface mirroring the "Impact preview" panel in KhatirAdmin.jsx (Pricing › Edit tier). Takes a TierImpact prop (tierLabel/tierKey, old+new monthly BDT, subscribersAffected, old+new includesVerification) rather than fetching, so T-005 pricing editor and T-008 manual-upgrade can both reuse it after calling the T-001 preview endpoint. Renders the change sentence (FREE/৳ codes), a 2-col stat grid (subscribers affected + est. monthly revenue delta, sage when +, rose when −, ink when 0), and an NID-verification-loss alert (butterBg/butter border, roseDk heading) shown only when old includesVerification && !new && subscribers>0. Exported pure helpers revenueDelta = (new−old)×subscribers and losesVerification for direct reuse/test. Loading skeleton state (aria-busy) when loading||!impact; data state otherwise. Modal: backdrop (bg-ink/40) + ✕ + Cancel all call onClose; optional onConfirm renders an "Apply change" CTA (disabled while loading/no-impact/confirmDisabled). lucide-react AlertTriangle/X icons. All colours/spacing/radii from Notun Din tokens (rounded-card/-chip/-tile, sageBg/sageDk, butterBg/butter, roseDk, mutedDk, font-title/-mono, s1..s8) — no hardcoded hex/px. 10 new tests in test/impact-preview.test.tsx (revenueDelta sign+magnitude, losesVerification truth table incl gain/zero-subs, closed→null, counts+negative delta+no-warning, positive delta +sign, NID warning, loading skeleton, onClose from ✕+backdrop, confirm CTA presence/fire, confirmDisabled). Gate: npm install + npm run build OK, tsc --noEmit clean, vitest 54 passed across 9 files (10 new).
2026-06-05 | EPIC-12/T-005 | admin | claude | done | pricing editor page (Next.js). New lib/api/pricing.ts (zod-validated fetchPricingTiers GET /admin/api/pricing/tiers, previewTier POST .../{key}/preview, editTier PATCH .../{key} with mandatory reason) consuming the T-001 endpoints; prices kept as wire strings. app/(dashboard)/pricing/page.tsx is now a SERVER component enforcing the finance+super route guard server-side (getAuthenticatedAdmin → role in {super,finance}; access-denied card otherwise, mirroring backend IsPricingAdmin) and renders the PricingEditor client surface. components/admin/pricing_editor.tsx: TanStack Query list with loading skeleton / error+Retry / data states. components/admin/tier_table.tsx: editable token-styled tier table (label/tenants/monthly/annual/verification credits/active + pencil), pencil opens an inline TierEditor modal (EN/BN labels, tenant min/max, monthly/annual price, credits, sort order, verification+active toggles), "Preview impact" (disabled until a non-blank reason) calls the read-only preview endpoint then opens the reusable T-006 ImpactPreviewModal showing subscribers affected + revenue delta; "Apply change" PATCHes with the reason and invalidates the tiers query after 2s so the cache-busted public catalogue (≤60s) is reflected. _nav.ts: Pricing item de-flagged comingSoon → live /pricing (finance-gated). All colours/spacing/radii from Notun Din tokens (rounded-card/-button/-chip/-input, sageBg/sageDk, roseDk, mutedDk, font-title/-mono) — no hardcoded hex/px. 4 new RTL tests in test/pricing.test.tsx (rows render incl FREE, error+Retry, open editor → reason-gated preview → confirm calls edit, blank reason does not call preview); widened sidebar live-pages set to include Pricing. Gate: npm install + npm run build OK (route /pricing dynamic/server-rendered, tsc clean), eslint clean on new files, vitest 58 passed across 10 files (4 new).
2026-06-05 | EPIC-12/T-007 | admin | claude | done | users list + search page (Next.js). New lib/api/users.ts (zod-validated fetchUsers GET /admin/api/users via single ?q= search + ?page= page-number pagination, consuming T-003 AdminUserListSerializer in the standard {results,pagination:{next,previous,count}} envelope; raw phone present on the wire but never rendered). app/(dashboard)/users/page.tsx replaces the ComingSoon stub and is a SERVER component enforcing the role guard server-side (getAuthenticatedAdmin → role in {super,ops,support}; access-denied card otherwise, mirroring backend IsUsersReadAdmin — support is read-only per spec §2.1, so the guard is ops+support+super, not ops+super); renders the UsersBrowser client island. components/admin/users_browser.tsx: TanStack Query with keepPreviousData, loading skeleton / error+Retry / empty / data states, search resets to page 1, Prev/Next drive ?page. components/admin/users_table.tsx: token-styled search form (role=search) + table columns Name (Link → /users/{id} detail T-008) / Phone (server masked_phone only) / Role / Tier / Status (sage Active vs danger Suspended chip); Tier renders — because the compact search projection carries no subscription tier (loaded on detail). All colours/spacing/radii from Notun Din tokens (rounded-sm/-button/-card/-chip, sageBg, mutedDk, font-title/-mono, s1..s8) — no hardcoded hex/px. _nav.ts: Users item de-flagged comingSoon → live /users (ops+support gated). 8 new RTL tests in test/users.test.tsx (rows render, masked phone shown + raw phone never rendered, row links /users/{id}, search submit re-queries {q,page:1}, Suspended chip, next-page → page:2, empty state, error+Retry); widened sidebar live-pages set to include Users. Gate: npm install + npm run build OK (route /users dynamic/server-rendered), tsc --noEmit clean, eslint clean on new files, vitest 66 passed across 11 files (8 new).
2026-06-05 | EPIC-12/T-008 | admin | claude | done | user detail + actions page (Next.js). New app/(dashboard)/users/[id]/page.tsx — a SERVER component that reads the async [id] param, enforces the role guard server-side (getAuthenticatedAdmin → read roles {super,ops,support} mirroring backend IsUsersReadAdmin; access-denied card otherwise) and passes canWrite={role∈{super,ops}} (write roles, mirroring IsUsersWriteAdmin — support is read-only per spec §2.1) plus a "Back to users" link to the UserDetail island. components/admin/user_detail.tsx: TanStack Query GET /admin/api/users/{id} (loading skeleton / error+Retry / data), lays out Profile (name/masked phone — raw phone never rendered/role/language/created/last-login), Subscription (tier_label/status/cycle/next-billing/started, or "no subscription"), Usage (buildings/tenants/subscriptions counters), and a recent admin Audit trail table (when/action/reason). Header shows active(sage)/suspended(danger) status chip. Action buttons (canWrite only): Suspend (active) / Reactivate (suspended) / Upgrade subscription, each opening a confirm dialog (role=dialog, aria-modal, backdrop+Cancel close). Suspend + upgrade require a non-blank reason (confirm disabled until entered); upgrade dialog loads tiers via the existing T-005 fetchPricingTiers select + billing-cycle select. On success each mutation invalidates the detail query so the status badge flips to Suspended immediately (spec §15). lib/api/users.ts extended with zod-validated adminUserDetail/adminSubscription/adminAuditTrailRow/adminUsage schemas + fetchUserDetail, suspendUser(reason), reactivateUser(reason?), upgradeSubscription({tierId,billingCycle?,reason}) consuming the T-003 endpoints (resource bodies returned directly). All colours/spacing/radii from Notun Din tokens (rounded-card/-button/-chip/-input, sageBg/sageDk, roseDk, danger, mutedDk, font-title/-mono, s1..s8) — no hardcoded hex/px. 8 new RTL tests in test/user-detail.test.tsx (renders profile+subscription+usage+audit, masked-not-raw phone, active badge, suspend dialog reason-gated, suspend confirm → suspendUser called + refetch flips badge to Suspended, reactivate dialog for suspended user, upgrade dialog tier+reason-gated → upgradeSubscription called with {tierId,billingCycle,reason}, action buttons hidden for read-only viewers, error+Retry). Gate: npm install + npm run build OK (route /users/[id] dynamic/server-rendered, TypeScript+eslint clean), vitest 74 passed across 12 files (8 new).
2026-06-05 | EPIC-12/T-009 | admin | claude | done | refund queue page (Next.js). New lib/api/refunds.ts (zod-validated fetchRefunds GET /admin/api/billing/refunds → results array of pending payment intents, processRefund POST .../{id}/process with {approve,reason}) consuming the T-004 endpoints (queue rows are unresolved customer-realm subscription.payment_intent audit entries per the T-004 MVP deviation). app/(dashboard)/billing/refunds/page.tsx is a SERVER component enforcing the finance+super route guard server-side (getAuthenticatedAdmin → role in {super,finance}; access-denied card otherwise, mirroring backend IsBillingAdmin) and renders the RefundQueue client island. components/admin/refund_queue.tsx: TanStack Query with loading skeleton / error+Retry / empty / data states; token-styled table (User #id / Tier / Cycle / Provider / Requested-date / per-row Approve+Deny). Approve and Deny each open a confirm dialog (role=dialog, aria-modal, backdrop+Cancel close); approve records an optional reason (audited), deny requires a non-blank reason (confirm disabled until entered; re-checked server-side). On success the queue query is invalidated so the processed row drops out immediately. _nav.ts: new live finance-gated /billing/refunds "Refunds" item (Receipt icon). All colours/spacing/radii from Notun Din tokens (rounded-card/-button/-input, sageBg/sageDk, danger, roseDk, mutedDk, font-title/-mono, s1..s8) — no hardcoded hex/px. 5 new RTL tests in test/refunds.test.tsx (rows render, empty state, approve reason-optional → processRefund {approve:true,reason:""} + refetch drops row, deny reason-gated → processRefund {approve:false,reason}, error+Retry); widened sidebar.test.tsx route regex to allow multi-segment paths and added Refunds to the live-page set. Gate: npm install + npm run build OK (route /billing/refunds dynamic/server-rendered, TypeScript+eslint clean), vitest 79 passed across 13 files (5 new).
2026-06-05 | EPIC-12/T-010 | admin | claude | done | pricing-change propagation integration test (the <60s guarantee test). New apps/api/khatir/admin_portal/tests/test_propagation.py::test_tier_change_propagates_to_config_public drives the full real chain end to end: issues an admin JWT (finance role, T-001 IsPricingAdmin gate), primes the shared PUBLIC_CONFIG_CACHE_KEY with a sentinel as a bust probe, asserts /api/v1/config/public reports bundle_20's seeded price, PATCHes a new monthly_price (+৳123, reason="Annual repricing") through the real /admin/api/pricing/tiers/{key} endpoint, then asserts (a) the cache key was busted (explicit edit_tier service call + the T-002 post_save signal, both idempotent) and (b) /config/public now returns the new price. Unlike the T-001/T-002 unit tests it mocks nothing and saves no model directly, so it regression-guards the whole path — admin auth, role gate, edit service write+audit+bust, invalidation signal, and the live serialized_tiers() selector (config_public is computed live today; the sentinel only proves the bust fired). No production code changed; integration test only. Gate: pytest test_propagation.py green; full related suite (test_pricing_admin + test_cache_invalidation + test_config_public + test_propagation) 28 passed; ruff clean on the new file (mypy "factory has no attribute pk/refresh_from_db" is the pre-existing factory_boy typing noise shared by test_pricing_admin.py, not introduced here).
2026-06-05 | EPIC-13/T-005 | admin | claude | done | flags console page (Next.js). New lib/api/flags.ts (zod-validated fetchFeatureFlags GET /admin/api/flags + toggleFeatureFlag PATCH /admin/api/flags/{key}/toggle, consuming the T-002 endpoints; list fetch tolerates both bare-array and {results} paginated envelopes per test_flag_endpoints.py). app/(dashboard)/features/page.tsx replaces the ComingSoon stub and is a SERVER component enforcing the super+ops route guard server-side (getAuthenticatedAdmin → role in {super,ops}; access-denied card otherwise, mirroring backend IsPlatformAdmin in featureflags/views.py) and renders the FlagsConsole island. components/admin/flags_console.tsx: TanStack Query with loading skeleton / error+Retry / data states; intro card (≤60s propagation note); token-styled flag table (key/description, scope chip, On/Off status chip, Toggle switch). Toggling opens a ToggleConfirm dialog (role=dialog, aria-modal, backdrop+Cancel close) BEFORE flipping — primary "Enable flag" / danger "Disable flag" CTA; on confirm calls toggleFeatureFlag (audited + cache-busted) then invalidates featureFlagsQueryKey so the row reflects the new state. _nav.ts: Features de-flagged comingSoon and re-gated ops (super sees all) to match the backend platform-section gate; doc comment updated. All colours/spacing/radii from Notun Din tokens (rounded-card/-button/-chip/-pill, sageBg/sageDk, roseDk, mutedDk, lineDk, font-title/-mono, s1..s8) — no hardcoded hex/px. 4 new RTL tests in test/flags.test.tsx (rows render key/scope/On+Off, error+Retry, toggle opens confirm and does NOT call API until confirmed → confirm fires toggleFeatureFlag, Cancel never calls toggle); widened sidebar.test.tsx live-pages set to include Features. Gate: npm install + npm run build OK (route /features dynamic/server-rendered, tsc --noEmit clean, eslint clean), vitest 83 passed across 14 files (4 new).
2026-06-05 | EPIC-13/T-006 | admin | claude | done | kill-switch panel page (Next.js). New lib/api/killswitch.ts (zod-validated fetchKillSwitches GET /admin/api/killswitches → the 5 named switches as FeatureFlagSerializer rows, list tolerant of bare-array + {results} envelopes; toggleKillSwitch POST .../{key}/toggle with {mfa_code,reason,lawyer_reference}) consuming the T-003 super-only endpoints; a switch is "live" while enabled=true. app/(dashboard)/kill-switch/page.tsx replaces the ComingSoon stub and is a SERVER component enforcing a super-only route guard server-side (getAuthenticatedAdmin → role==='super'; access-denied card otherwise, mirroring backend IsSuperAdmin in featureflags/killswitch_views.py — NOTE the implemented endpoint is super-only, not the spec §2.1 compliance assignment, so the nav+page mirror the real gate) and renders the KillSwitchPanel island. components/admin/killswitch_panel.tsx: prominent red "Emergency Kill-Switch Panel" header card, a red role=alert warning banner whenever ANY switch is OFF (safety-critical, task §15), and a per-switch row (check/x state icon, key, ★ MASTER badge for master_kill_switch, ENABLED/DISABLED chip, description, last-changed-by/at line sourced from updated_by/updated_at since T-003 GET returns no event list, Disable/Enable button); TanStack Query loading skeleton / error+Retry / data states. components/admin/killswitch_dialog.tsx: friction-heavy MFA confirm dialog (role=dialog, aria-modal, backdrop+Cancel) — mandatory reason textarea (≥20 chars, spec §4.4.2), optional lawyer reference, 6-digit numeric TOTP re-confirm field; Confirm-with-MFA disabled until reason≥20 AND a 6-digit code are entered; on confirm POSTs {mfaCode,reason,lawyerReference} then invalidates killSwitchesQueryKey (backend re-verifies TOTP, records immutable KillSwitchEvent, busts public-config cache ≤60s; bad code → 403 surfaced inline). _nav.ts: Kill-switch de-flagged comingSoon → live /kill-switch, re-gated super-only (empty roles list; super sees all) to match the IsSuperAdmin backend gate; doc comment updated. All colours/spacing/radii from Notun Din tokens (rounded-card/-button/-chip/-pill/-md, danger/dangerBg, roseBg, sageBg/sageDk, cream, ink2, mutedDk, font-title/-mono, s1..s8) — no hardcoded hex/px. 8 new RTL tests in test/killswitch.test.tsx (5 switches + ★ MASTER render, warning banner shows when any OFF, banner absent when all ON, Disable opens MFA dialog without calling endpoint + reason/MFA fields present, Confirm gated until reason≥20 AND 6-digit code, confirm fires toggleKillSwitch with {key,mfaCode,reason,lawyerReference}, Cancel never calls toggle, error+Retry); widened sidebar.test.tsx live-pages set to include Kill-switch. Gate: npm install + npm run build OK (route /kill-switch dynamic/server-rendered, tsc --noEmit clean), vitest 91 passed across 15 files (8 new).
2026-06-05 | EPIC-14/T-011 | admin | claude | done | AI providers page (Next.js). New lib/api/ai-providers.ts (zod-validated fetchAIProviders GET /admin/api/ai-providers, createAIProvider POST, updateAIProvider PATCH .../{id}, testAIProviderConnection POST .../{id}/test-connection — consuming the T-009 endpoints; list tolerant of bare-array + {results} envelopes; api_key write-only/never returned, only has_api_key surfaced; exported endpointIsBangladesh mirroring the backend non-BD .bd-host DPA rule for UI gating). app/(dashboard)/ai-providers/page.tsx replaces the ComingSoon stub and is a SERVER component enforcing the super+ops route guard server-side (getAuthenticatedAdmin → role in {super,ops}; access-denied card otherwise, mirroring IsAIProviderAdmin in ai_providers/admin_views.py) and renders the AIProvidersPanel island. components/admin/ai_providers_panel.tsx: 4 category tabs (Chat/Voice/OCR/Lease, role=tablist/tab/tabpanel) filtering one shared TanStack Query list client-side, primary+fallback active-summary cards, per-provider rows (vendor, Primary/Fallback/Active chips, model + masked "key ••••••••••" / "no key" line, Edit) + Add provider; loading skeleton / error+Retry states. ProviderForm modal (role=dialog, aria-modal, backdrop+Cancel): vendor (datalist seeded with spec §4.6.1 suggested vendors per category, free-text allowed), model, API key (type=password, placeholder notes "leave blank to keep current" when a key exists, sent only when typed — task §15), endpoint URL, primary/fallback/active toggles; on OCR with a non-BD endpoint shows a butter Data-residency warning (role=alert) + required DPA reference field and disables Save until supplied (mirrors the T-009 serializer rule); Save create/update-PATCHes then invalidates the query. Test-connection row (edit mode) fires testAIProviderConnection and shows Connected·model (sageDk check) / detail (roseDk x) / Request failed. _nav.ts: AI providers de-flagged comingSoon → live /ai-providers, gated ops (super sees all) to match the platform-section backend gate. All colours/spacing/radii from Notun Din tokens (rounded-card/-button/-input/-pill, sageBg/sageDk, butterBg/butter/butterDk, roseDk, mutedDk, ink/card, font-title/-body/-mono, s1..s8) — no hardcoded hex/px. 8 new RTL tests in test/ai-providers.test.tsx (endpointIsBangladesh truth table, 4 tabs render, tab filters providers + masked key shown/raw never, error+Retry, test-connection fires + shows Connected result, DPA warning blocks save until reference entered then updateAIProvider called, add-provider form → createAIProvider); widened sidebar.test.tsx live-pages set to include AI providers. Gate: npm install + npm run build OK (route /ai-providers dynamic/server-rendered), eslint clean on new files, vitest 99 passed across 16 files (8 new).
2026-06-05 | EPIC-14/T-012 | admin | claude | done | AI usage panel page (Next.js). New lib/api/ai-usage.ts (zod-validated fetchAIUsage GET /admin/api/ai-usage → {by_category[{category,request_count,tokens_used,cost_usd(string USD),call_count,success_count}], totals} consuming the T-009 endpoint returned directly via core.responses.success; range-scoped aiUsageQueryKey; date window forwarded as from/to query params; pure errorRate/successRate helpers = (calls−successes)/calls returning null on zero calls). app/(dashboard)/ai-providers/usage/page.tsx is a SERVER component enforcing the super+ops route guard server-side (getAuthenticatedAdmin → role in {super,ops}; access-denied card otherwise, mirroring IsAIProviderAdmin in ai_providers/admin_views.py) and renders the AIUsagePanel island. components/admin/ai_usage_panel.tsx: TanStack Query loading skeleton / error+Retry / data states; date filter (From/To date inputs + Clear, role=form "Usage date filter", min/max cross-bound) that re-keys the query; 4 SummaryCards (Requests / Tokens / Total cost USD emphasised / Success rate — totals as the running cost for the period, task §15); token-styled "Usage by category" table (one row per AICategory incl. zero-usage categories: Category / Requests / Tokens / Cost(USD) / derived Error rate, elevated rates in roseDk); "Failover & errors" log listing each category with ≥1 failed call (call_count−success_count) with an all-clear CheckCircle state otherwise. All colours/spacing/radii from Notun Din tokens (rounded-card/-input/-chip, sageBg/sageDk, roseDk, mutedDk, ink/card/muted, font-title/-mono, s1..s8) — no hardcoded hex/px. DEVIATION: T-009 endpoint exposes no latency column or standalone failover stream, so error-rate + failover log are derived from the call/success counts it returns. 9 new RTL tests in test/ai-usage.test.tsx (errorRate/successRate truth + null-on-zero, range-scoped query key, table row-per-category + OCR cost/5.0% error, running cost total, failover log lists 5-of-100, all-clear state, date-range re-fetch from/to, error+Retry). Gate: npm install + npm run build OK (route /ai-providers/usage dynamic/server-rendered, TypeScript clean), vitest 108 passed across 17 files (9 new).
2026-06-05 | EPIC-15/T-010 | admin | claude | done | notification composer page (Next.js). New lib/api/notifications.ts (zod-validated fetchNotifications GET /admin/api/notifications, composeNotification POST /admin/api/notifications → broadcast + injected reach/estimated_cost preview fields, sendTestNotification POST .../{id}/send-test — consuming the T-007 super+ops endpoints; list tolerant of bare-array + {results} envelopes; AUDIENCE_TYPES/CHANNELS/SCHEDULE_TYPES/CUSTOMER_ROLES/TEMPLATE_VARIABLES constants mirroring notifications/enums.py + core Channel/Role; CHANNEL_COST_BDT + pure estimateCost(reach,channels)=reach×Σ paid-channel cost matching services.py with inapp/email free). app/(dashboard)/notifications/page.tsx replaces the ComingSoon stub and is a SERVER component enforcing the super+ops route guard server-side (getAuthenticatedAdmin → role in {super,ops}; access-denied card otherwise, mirroring IsPlatformAdmin in notifications/views.py) and renders the NotificationComposer island. components/admin/notification_composer.tsx: audience radiogroup (all/role/segment/specific — role+segment expose a customer-role checkbox set, specific exposes a comma/space-separated User IDs textarea), channels multi-select cards with per-channel Free/৳/msg cost chips, bilingual Title+Body (EN/BN, dir=auto) with insertable {name}/{unit}/{tier}/{rent_amount}/{building_name} variable chips that splice at the caret, schedule radiogroup (now/scheduled datetime-local/recurring minute+hour+day-of-week crontab), and a sticky reach+cost preview sidebar (explicit count for a specific audience, em-dash + "resolved on the server" note for broad audiences). Submit (disabled until ≥1 channel + valid audience + all four content fields + valid schedule) calls composeNotification via TanStack Query mutation then swaps to a success panel showing the server-resolved reach + estimated cost (with "Compose another" reset); error state shows an inline role=alert. _nav.ts: Notifications de-flagged comingSoon → live /notifications, re-gated ops (super sees all) to match the platform-section backend gate. All colours/spacing/radii from Notun Din tokens (rounded-card/-button/-input/-chip, sageBg/sage/sageDk, butterBg/butter/butterDk, roseDk, mutedDk, ink/card/muted/line, font-title/-body/-mono, s1..s8) — no hardcoded hex/px. DEVIATION: provisional client reach is computed only for the specific-user audience; broad-audience reach+cost come back from the compose response (services.py injects them); WhatsApp/SMS preview bubbles + MFA-over-Tk-5000 re-prompt and the dedicated reach+cost (T-014) / audience+channel (T-011) widgets are out of scope here. 8 new RTL tests in test/notifications.test.tsx (estimateCost paid-vs-free truth, 4 sections render, submit gated until content filled, variable-chip inserts into EN body, specific-audience explicit reach, compose fires with all=now+whatsapp + success panel shows 412/৳206, by-role requires a role selection, compose-failure error). Widened sidebar.test.tsx live-pages set to include Notifications. Gate: npm install + npm run build OK (route /notifications dynamic/server-rendered, TypeScript+eslint clean), vitest 116 passed across 18 files (8 new).
2026-06-05 | EPIC-15/T-011 | admin | claude | done | Audience + channel selector widgets (Next.js). New components/admin/audience_selector.tsx (AudienceSelector — reusable controlled audience picker: all/role/segment/specific radiogroup; role+segment expose the customer-role cohort checkbox set; specific exposes the comma/space-separated User-ID search textarea; all shows the platform-wide reach note; aria-checked radios + named fieldset/legend) and components/admin/channel_selector.tsx (ChannelSelector — reusable controlled role=group checkbox-per-channel picker for inapp/whatsapp/sms/email with per-channel Free/৳/msg cost chips from the shared CHANNEL_COST_BDT table; exported CHANNEL_LABELS). Both widgets reuse the AUDIENCE_TYPES/CHANNELS/CUSTOMER_ROLES/CHANNEL_COST_BDT constants + AudienceType/ChannelValue/CustomerRole types from lib/api/notifications.ts (T-010). Refactored components/admin/notification_composer.tsx (T-010) to consume AudienceSelector + ChannelSelector inside its Audience/Channels Cards, deleting the previously-inline duplicated AudienceSection/ChannelsSection + their local AUDIENCE_LABELS/CHANNEL_LABELS/ROLE_LABELS maps — rendered markup + behaviour unchanged so the T-010 composer tests still pass. DEVIATION: the selectors are presentational/controlled (caller owns state); the super+ops route guard + TanStack Query data/loading/error states live in the host /notifications page + composer, not the widgets; "specific users search" is the user-ID entry field carried from T-010 (T-007 exposes no live user-search endpoint). All colours/spacing/radii from Notun Din tokens (rounded-card/-button/-input/-chip, sageBg/sage/sageDk, butterBg/butter, ink/card/muted/line, mutedDk, font-title/-body, s1..s8) — no hardcoded hex/px. 8 new RTL tests in test/audience-channel-selectors.test.tsx (AudienceSelector: 4 radios render, all-users note, By role reveals + selects cohort chips, Specific reveals user-ID search field, aria-checked tracks selection; ChannelSelector: checkbox-per-channel with 2 Free + ৳0.5/৳0.3 cost chips, in-app default + WhatsApp toggle on/off, onToggle fires with channel value). Gate: npm install + npm run build OK (route /notifications dynamic/server-rendered, eslint clean on new+touched files), vitest 124 passed across 19 files (8 new; T-010 composer suite green after refactor).
2026-06-05 | EPIC-15/T-012 | admin | claude | done | notification history page (Next.js). Extended lib/api/notifications.ts with the broadcast detail layer consuming the T-007 super+ops endpoints: notificationDeliverySchema/notificationDetailSchema (+ NotificationDelivery/NotificationDetail types) and fetchNotificationDetail GET /admin/api/notifications/{id} (embeds the per-recipient NotificationDelivery rows), a date NotificationFilters {from,to} window forwarded as query params via notificationsPath, a now-filtered notificationsQueryKey(filters) + a stable notificationsQueryPrefix for prefix-match invalidation, and notificationDetailQueryKey; refactored the T-010 composer's onSuccess invalidation to the new prefix (rendered behaviour unchanged, T-010 suite still green). app/(dashboard)/notifications/history/page.tsx is a SERVER component enforcing the super+ops route guard server-side (getAuthenticatedAdmin → role in {super,ops}; access-denied card otherwise, mirroring IsPlatformAdmin in notifications/views.py) and renders the history_client.tsx island. history_client.tsx: TanStack Query (keepPreviousData) loading skeleton / error+Retry / data states over fetchNotifications(filters), owns the date-filter state, threads it into the presentational table. components/admin/notification_history_table.tsx: token-styled table per Admin Portal spec §4.5.2 (Sent at / Title / Audience / Channels / Sent / Delivered / Opened / Status chip / Sender) with a From/To date filter bar; exported describeAudience() renders a human audience description from audience_type+filter (All users / Role: x / Segment / N specific users); status + delivery chips tone-mapped (sent/delivered/opened→sage, sending/sent→butter, scheduled/draft/queued→neutral, failed→danger); each row expands to a DeliveriesDetail that LAZILY fetches GET .../{id} via its own query (loading skeleton / error+Retry / empty / per-recipient table: Recipient / Channel / Status chip / Delivered / Opened / Error). _nav.ts: new live super/ops-gated /notifications/history "Notification history" item (History icon). All colours/spacing/radii from Notun Din tokens (rounded-card/-button/-sm/-chip, sageBg/sageDk, butterBg/butterDk, roseBg/roseDk, dangerBg/danger, line, ink/card/muted/mutedDk, font-title/-mono, s1..s8) — no hardcoded hex/px. DEVIATION: history is a sibling route to the T-010 composer (which owns /notifications); the T-007 list endpoint is not server-paginated so the date window narrows client-fetched rows without cursor controls; spec §4.5.2 search/export columns are out of scope (no endpoint). 6 new RTL tests in test/notification-history.test.tsx (describeAudience truth, rows render with counts+status, From date re-queries with {from}, row expand LAZILY fires fetchNotificationDetail(id) + shows per-recipient delivery, empty state, error+Retry); added "Notification history" to sidebar.test.tsx live-pages set. Gate: npm install + npm run build OK (route /notifications/history dynamic/server-rendered), eslint clean on new+touched files, vitest 130 passed across 20 files (6 new). Pre-existing unrelated tsc --noEmit strictness errors in test/ai-providers.test.tsx (vi.fn arg-count, confirmed present with this task's changes stashed) are not introduced here.
2026-06-05 | EPIC-15/T-013 | admin | claude | done | notification templates page (Next.js). Extended lib/api/notifications.ts with the system-template layer consuming the T-008 super+ops endpoints: notificationTemplateSchema (+ NotificationTemplate/TemplateUpdateInput types; api fields id/key/trigger_event/channels/title_en/title_bn/body_en/body_bn/variables/active/timestamps), fetchTemplates GET /admin/api/notification-templates (tolerant of bare-array + {results} envelopes, ordered by key) and updateTemplate PATCH /admin/api/notification-templates/{key} (encodeURIComponent key lookup; patch contract DELIBERATELY excludes key+trigger_event since the T-008 serializer drops them on update — they are immutable), plus templatesQueryKey + a stable templatesQueryPrefix for prefix-match invalidation. app/(dashboard)/notifications/templates/page.tsx is a SERVER component enforcing the super+ops route guard server-side (getAuthenticatedAdmin → role in {super,ops}; access-denied card otherwise, mirroring IsPlatformAdmin on NotificationTemplateViewSet in notifications/views.py) and renders the NotificationTemplates island. components/admin/notification_templates.tsx: TanStack Query loading skeleton / error+Retry / empty / data states; token-styled table per Admin Portal spec §4.5.3 (Template title+key / Trigger event / Channel chips / Active chip / Edit). Edit opens a remounted (key=template.key, so useState seeds fresh — no setState-in-effect) modal editor (role=dialog, aria-modal, backdrop, X+Cancel close) showing the IMMUTABLE trigger_event + the variable reference as {placeholder} chips read-only, and editable bilingual Title+Body (EN/BN, dir=auto), a reused ChannelSelector (T-011) with per-channel Free/৳ cost chips, and an Active Toggle; Save (gated until all four content fields non-blank AND ≥1 channel) PATCHes title_en/title_bn/body_en/body_bn/channels/active via a mutation then invalidates templatesQueryPrefix + closes; inline role=alert on failure. _nav.ts: new live super/ops-gated /notifications/templates "Notification templates" item (FileText icon). All colours/spacing/radii from Notun Din tokens (rounded-card/-button/-chip/-input, sageBg/sage/sageDk, butterBg/butterDk, roseDk, mutedDk, ink/card/muted/line, font-title/-body/-mono, s1..s8) — no hardcoded hex/px. DEVIATION: key/trigger_event are read-only (immutable server-side); spec §4.5.3 enabled/disabled maps to the model active flag; no create UI (system templates are migration-seeded in T-008, so the page edits the seeded set). 6 new RTL tests in test/notification-templates.test.tsx (list renders key/trigger/channels/Active+Inactive, error+Retry, editor shows immutable trigger event + {variable} chips, Save patches edited title WITHOUT key/trigger_event + editor closes on success, Save gated when a required field cleared, save-failure error); added "Notification templates" to sidebar.test.tsx live-pages set. Gate: npm install + npm run build OK (route /notifications/templates dynamic/server-rendered), eslint clean on new+touched files, vitest 136 passed across 21 files (6 new). Pre-existing unrelated tsc --noEmit strictness errors in test/ai-providers.test.tsx (vi.fn arg-count, confirmed present with this task's changes stashed) are not introduced here.
2026-06-05 | EPIC-16/T-006 | admin | claude | done | enhanced audit-log page (Next.js, replaces EPIC-11.T-011). Moved the audit log under the Compliance module at app/(dashboard)/compliance/audit/: page.tsx server component enforces the compliance+super role guard (mirrors compliance/views.py SECTION_ROLES, EPIC-16.T-002) with an access-denied panel for other roles; audit_client.tsx client viewer wires TanStack Query (keepPreviousData cursor pagination) into the existing token-styled AuditTable (full actor/action/entity-type/from/to filters + per-row before/after JSON diff) and adds an Export CSV button. lib/api/audit.ts gains auditCsvUrl(filters) building the absolute ?format=csv download URL from the active filters (cursor dropped — export covers the whole filtered set) against the T-002 CSV stream. compliance/page.tsx and legacy audit/page.tsx now redirect to /compliance/audit; _nav.ts repoints Audit log there and promotes Compliance from coming-soon to a live compliance-gated route. Tests: audit.test.tsx render test retargeted to AuditLogClient + 2 new CSV-export-link/filter-threading cases; sidebar.test.tsx adds Compliance to live-pages. All colours/spacing/radii from Notun Din tokens. Gate: next build green, vitest 144 passed (22 files). Note: unrelated EPIC-15 notification WIP (notification_composer.tsx, reach_cost_preview.tsx) left uncommitted in the worktree — not part of this task, not staged.
2026-06-05 | EPIC-16/T-007 | admin | claude | done | consent records page (Next.js). New lib/api/consent.ts (zod-validated fetchConsentRecords GET /admin/api/consent-records consuming the T-003 compliance+super endpoint; standard {results,pagination{next,previous,count}} envelope; CONSENT_TYPES/CONSENT_TYPE_LABELS mirroring khatir.compliance.enums.ConsentType; ConsentFilters {user,consent_type,granted_from,granted_to,page}; consentRecordsPath/consentQueryKey + pageFromLink extracting the ?page=N value from the page-number pagination links). app/(dashboard)/compliance/consent/page.tsx is a SERVER component enforcing the compliance+super route guard server-side (getAuthenticatedAdmin → role in {compliance,super}; access-denied card otherwise, mirroring IsComplianceAdmin in compliance/views.py, EPIC-16.T-003) and renders the ConsentRecordsClient island. consent_client.tsx: TanStack Query (keepPreviousData) loading skeleton / error+Retry / data states over fetchConsentRecords(filters), owns the filter+page state, threads it into the presentational table. components/admin/consent_table.tsx: read-only token-styled table (User / Consent type / Granted / Revoked / Expires) with a filter bar (user id text + consent-type select + granted from/to date inputs) and page-number Previous/Next pager; null dates render as em-dash; append-only log so no edit/delete affordance. _nav.ts: new live compliance/super-gated /compliance/consent "Consent records" item (ShieldCheck icon) sitting in the Compliance module beside the audit log. All colours/spacing/radii from Notun Din tokens (rounded-card/-button/-sm, sageBg, line, ink/card/muted/mutedDk, font-title/-mono, s1..s8) — no hardcoded hex/px. DEVIATION: T-003 uses page-number pagination (not cursor), so the pager threads ?page=N; consent records are append-only/read-only so the page is a filterable read-only table only. 7 new RTL tests in test/consent.test.tsx (rows render with consent-type label + user, consent-type filter re-queries with {consent_type}, user filter re-queries with {user}, page-number Next pager re-queries with {page:'2'}, empty state, error+Retry, null revoked_at renders em-dash); added "Consent records" to sidebar.test.tsx live-pages set. Gate: npm install + npm run build OK (route /compliance/consent dynamic/server-rendered), vitest 151 passed across 23 files (7 new).
2026-06-05 | EPIC-15/T-014 | admin | claude | done | Reach + cost preview widget (Next.js). New components/admin/reach_cost_preview.tsx (ReachCostPreview — reusable controlled+presentational widget surfacing estimated reach + estimated cost before composing, per Admin Portal spec §4.5.1 "Reach summary" + "Cost estimate"). Props: channels (ChannelValue[]), reach (number|null), optional action node (e.g. send button at card foot) + optional errorMessage (role=alert). Cost derives from the shared estimateCost(reach, channels) helper in lib/api/notifications.ts (reach × Σ CHANNEL_COST_BDT; inapp/email free, whatsapp ৳0.5, sms ৳0.3) — mirrors the backend formula (services.py). Renders a token-styled Card (CardTitle + Users icon) with a reach/cost definition list, a Chip per selected channel via CHANNEL_LABELS (T-011) or a "No channels selected" hint, and a server-resolution note + em-dashes when reach is null (broad all/role/segment audiences are resolved authoritatively server-side at send via POST /admin/api/notifications, T-007 — there is no standalone estimate endpoint). Integrated into components/admin/notification_composer.tsx (T-010): replaces the inline preview sidebar, fed the composer's provisional reach (explicit recipient count for the specific-user audience, null otherwise) + selected channels + compose error + the Send button as its action. DEVIATION: widget is presentational/controlled (caller owns reach/channels/state); the super+ops route guard + TanStack Query loading/error/data states live in the host /notifications page + composer (T-010), not the widget; reach for broad audiences is intentionally a dash until the server resolves it (no estimate endpoint in T-007). All colours/spacing/radii from Notun Din tokens (rounded-card/-chip, sage, ink, muted, roseDk, line, font-title/-body, s1..s6) — no hardcoded hex/px. 6 new RTL tests in test/reach-cost-preview.test.tsx (explicit reach + derived cost 412×whatsapp=৳206, chip-per-channel + sms cost ৳3, no-channels hint + ৳0, null reach dashes both + server-resolution note, errorMessage as role=alert, provided action node click). Gate: npm install + npm run build OK (route /notifications dynamic/server-rendered), vitest 158 passed across 24 files (6 new). Unrelated EPIC-16.T-008 data-requests WIP left uncommitted in the worktree — not part of this task, not staged.
2026-06-05 | EPIC-16/T-008 | admin | claude | done | data requests page (Next.js). New lib/api/data-requests.ts (zod-validated layer over the T-004 PDPA endpoints: fetchDataRequests GET /admin/api/data-requests {results,pagination} envelope filterable by status/type/sla; processDataRequest POST /admin/api/data-requests/{id}/process {action:approve|reject,reason}; DATA_REQUEST_TYPE/STATUS + SLA_STATE labels mirroring khatir.compliance.enums; field names matched to DataRequestSerializer — request_type/status/sla_due/sla_state/completed_at/handled_by; dataRequestsQueryKey + pageFromLink). app/(dashboard)/compliance/requests/page.tsx SERVER component enforces the compliance+super route guard (getAuthenticatedAdmin → role in {compliance,super}; access-denied card otherwise, mirroring IsComplianceAdmin in compliance/data_request_views.py) and mounts requests_client.tsx → DataRequestQueue island. components/admin/data_request_queue.tsx: two tabs (Pending / Completed) via TanStack Query (keepPreviousData) with loading skeleton / error+Retry / empty / data states. Pending tab table (User/Type/SLA due/SLA badge/Requested/Actions) with SLA badge red=overdue/amber=due_soon/green=on_track and Approve/Reject buttons; both open a confirm ProcessDialog (role=dialog, aria-modal) where a rejection requires a non-blank reason (re-checked server-side) and approve reason is optional+recorded; success invalidates the queue query so the row drops out. Completed tab: read-only history (non-pending rows) with page-number Previous/Next pager. _nav.ts: new live compliance/super-gated /compliance/requests "Data requests" item (FileText icon) in the Compliance module beside audit + consent. All colours/spacing/radii from Notun Din tokens (rounded-card/-button/-input, dangerBg/danger, butterBg/butterDk, sageBg/sageDk, line, ink/card/muted, font-title/-body/-mono, s1..s8) — no hardcoded hex/px. 8 new RTL tests in test/data-requests.test.tsx (pending rows + SLA badge, pending-only filter, empty, error+Retry, approve via dialog, reject reason-gated, completed tab resolved rows); added "Data requests" to sidebar.test.tsx live-pages set. Gate: npm install + npm run build OK (route /compliance/requests dynamic/server-rendered), vitest 158 passed across 24 files.
2026-06-05 | EPIC-04/T-009 | mobile | claude | done | add-tenant method chooser AddTenantScreen (lib/features/tenants/presentation/screens/add_tenant_screen.dart) matching addTenant proto: emoji hero (👋), 3 tappable method cards — OCR (starred/sage-bordered gradient badge), voice, manual — and a sage tip card; all colors/spacing/radii from khatir_tokens. Voice card flag-gated on voice_tenant_entry: added voiceTenantEntry to PublicConfig (reads the flags block of /config/public, default on to match backend default). Routes to /tenants/add/{ocr|voice|manual} by name carrying optional target unit id as ?unit= query param; the three method screens are KShellPlaceholder sub-routes for later EPIC-04 tasks. app_router replaces the old /tenants/add placeholder with the real chooser + sub-routes; landlord/manager FAB already pushes tenantsAdd (no unit, chosen later per §15); unit-detail Add-tenant CTA now passes ?unit=<id>. ARB add_tenant_* keys in bn (template) + en. 5 widget tests in test/add_tenant_test.dart: 3 cards when flag on, voice hidden when off, OCR/manual route carry unit id, voice route from home has no unit. flutter pub get + analyze (No issues on changed files) + test (125 passed) all green.
2026-06-05 | EPIC-04/T-010 | mobile | claude | done | NID OCR capture stage OcrCaptureScreen (lib/features/tenants/presentation/screens/ocr_capture_screen.dart) matching the `ocr` proto capture state: handwritten "Snap your NID" heading, dark NID viewfinder frame (dashed border + butter scan line over KhatirColors.ink), sage privacy reassurance card, filled sage "Take photo" + outlined "From gallery" actions; all colors/spacing/radii from khatir_tokens. States: data (capture), loading (spinner + ocr_processing), error (danger card + retry). Camera/gallery via image_picker ^1.2.2 wrapped behind ImagePickerService interface (PluginImagePicker reads bytes maxWidth 1600/q85, never copies to app storage — §15); widget tests inject a fake. New minimal tenants data slice (T-014 not yet committed, not a dep): ExtractedTenant/ExtractedField freezed models mirroring the OCR wire shape {name,nid_number,dob,address each {value,confidence}, photo_ref}, TenantRepository.ocrExtract(bytes) POSTing multipart image to /api/v1/tenants/ocr (new ApiEndpoints.tenantOcr), tenants_providers. On success pushReplacement to a `review` sub-route carrying typed OcrReviewArgs via go_router extra (review screen is T-011 — placeholder for now); app_router replaces the OCR KShellPlaceholder with the real screen + review sub-route. ARB ocr_* keys (bn template + en). 5 widget tests in test/ocr_capture_test.dart (mocked picker + scripted dio adapter): renders capture actions, capture→upload-once-to-/tenants/ocr→navigates to review with fields+photo_ref, cancel keeps capture (no upload), picker-error shows error+retry, upload-failure shows error+retry. flutter pub get + analyze (no issues in new files; remaining infos/warnings pre-existing in properties/profile/role) + test (130 passed) all green.
2026-06-05 | EPIC-04/T-011 | mobile | claude | done | NID OCR review/edit stage OcrReviewScreen (lib/features/tenants/presentation/screens/ocr_review_screen.dart) matching the `ocr` proto review state (ocrScanned branch): sage "AI extracted, please confirm" banner, four editable TextFormFields prefilled from OcrReviewArgs.extracted (name/nid/dob/address) each with leading icon, an inline family sub-form (add/remove name+relation rows; ahead of reusable T-015 widget), and a sage "Build form" proceed button; all colors/spacing/radii/fonts from khatir_tokens. OCR never trusted blindly: every field editable; low-confidence values (provider confidence ≤ 0.85) flagged with a butter border + "please check" hint (§15; absent confidence = normal field). Validation: name + NID required (autovalidate onUserInteraction). Proceed validates then emits a typed TenantReviewDraft of the EDITED values (name/nid/dob/address + family + photoRef + unitId) via an onProceed seam — the convergence point T-016's saveTenantAndContinue wires (null/no-op default; TODO marks the router site). New value types in ocr_review_args.dart: FamilyMemberDraft, TenantReviewDraft. app_router replaces the OCR review KShellPlaceholder with the real screen (falls back to capture when extra is not OcrReviewArgs). ARB ocr_review_*/tenant_*/ocr_family_*/ocr_confirm keys (bn template + en). 6 widget tests in test/ocr_review_test.dart: prefills fields, flags low-confidence, blocks proceed when required field cleared, proceed emits corrected NID + photo_ref + unit, family add flows into draft, family remove drops row. flutter pub get + analyze (no issues in new files; remaining 18 infos/warnings pre-existing in properties/profile/role) + test (full suite 136 passed) all green.
2026-06-05 | EPIC-04/T-012 | mobile | claude | done | Flutter voice tenant-entry screen VoiceFillScreen (lib/features/tenants/presentation/screens/voice_fill_screen.dart) matching the `voice` proto record state: handwritten "Talk to me!" hero, "Tap the mic" prompt, round rose mic (hold-to-talk: press=record, release=upload) with a pulsing halo, and the butter example-phrasing card; loading (spinner + voice_processing) and error (danger card + retry) states; all colors/spacing/radii/fonts from khatir_tokens. Audio recording abstracted behind a new AudioRecorderService interface (mirrors OCR ImagePickerService) so widget tests inject a fake; default PluginAudioRecorder (record ^6.2.0) owns the mic-permission prompt via hasPermission(), records to a temp file, reads bytes for the upload, then deletes the temp file — the clip is never persisted beyond the upload (§14). TenantRepository.voiceExtract(bytes) POSTs multipart audio to /api/v1/tenants/voice (new ApiEndpoints.tenantVoice); the voice response omits photo_ref (T-006 §7) so ExtractedTenant.photoRef degrades to '' for the voice path. On success pushReplacement to the REUSED OcrReviewScreen (T-011, unchanged per §15) carrying typed OcrReviewArgs via go_router extra. Flag-gated on voice_tenant_entry: the chooser (T-009) hides the entry when off, and the screen defensively re-checks the flag so a deep link cannot bypass the gate (renders an "unavailable" state instead of the mic). app_router replaces the old voice KShellPlaceholder with the real /tenants/add/voice route carrying optional ?unit=. RECORD_AUDIO added to AndroidManifest + NSMicrophoneUsageDescription to ios Info.plist. ARB voice_* keys (en + bn). 6 widget tests in test/voice_fill_test.dart (fake recorder + scripted dio adapter): renders record stage, record→release→upload-once-to-/tenants/voice→reused review prefilled (no photo_ref), denied mic permission shows error (no upload), upload failure shows error+retry, recorder stop failure shows error, flag off → mic not reachable (unavailable state, no upload). flutter pub get + analyze (no issues in changed files; remaining 2 warnings pre-existing in properties_repo_test) + test (full suite 142 passed) all green.
2026-06-05 | EPIC-04/T-013 | mobile | claude | done | Manual tenant-entry form ManualTenantScreen (lib/features/tenants/presentation/screens/manual_tenant_screen.dart) matching the `manualTenant` proto: butter "fill every official field by hand" intro card, four DMP sections (1·Landlord name/NID/mobile, 2·Tenant full-name★/NID★/DOB/occupation/mobile/permanent-address, 3·Current unit building/unit/rent/move-in, 4·Family & staff via the shared add/remove name+relation sub-form), and a sage "Generate PDF" proceed button; all colors/spacing/radii/fonts from khatir_tokens. Field + family widgets mirror the OCR review screen's composition (§15) — same tokens, same family sub-form — emitting a typed ManualTenantDraft (superset of TenantReviewDraft, reuses FamilyMemberDraft) via an onProceed seam (no-op default; T-016's saveTenantAndContinue wires it). Validation: tenant full name + NID required (★), autovalidate onUserInteraction. app_router replaces the manual KShellPlaceholder with the real /tenants/add/manual route carrying optional ?unit=. ARB manual_* + shared tenant_mobile keys (en + bn). 5 widget tests in test/manual_tenant_test.dart: renders the 4 DMP sections + empty form, blocks proceed when required fields empty (name+NID errors), proceed emits entered values + unit when valid, family add flows into draft, family remove drops row. flutter pub get + analyze (no issues in changed files; remaining infos/warnings pre-existing in properties/profile/role) + test (full suite 147 passed) all green.
2026-06-05 | EPIC-04/T-014 | mobile | claude | done | Flutter tenants data layer completed (the OCR/voice slice — ExtractedTenant + ocrExtract/voiceExtract — was already committed by T-010/T-012). Added freezed Tenant (lib/features/tenants/data/models/tenant.dart) mirroring the masked TenantSerializer ({id,name,nid_number_masked,dob,address,photo_ref,verification_status,verified_at,is_app_user,family_members,created_at,updated_at}) with NO plaintext NID field (only nidNumberMasked — masked NID only in list/detail per §15), freezed FamilyMember ({id,name,relation}) with a toCreateJson that drops the server-assigned id, and a VerificationStatus enum (tenant_enums.dart) whose fromWire degrades unknown/null to unverified. TenantRepository gained createTenant({name★, nidNumber (write-only plaintext, sent only when non-empty, server encrypts + never echoes), dob, address, photoRef, family}) POSTing only supplied fields to /api/v1/tenants and parsing the masked 201, and listUnitTenants(unitId) GETting /api/v1/units/{id}/tenants and parsing the BARE JSON array (UnitTenantsView returns no {results,pagination} envelope). New ApiEndpoints.tenants / tenant(id) / unitTenants(id). UnitTenantsController (AsyncNotifier.family keyed by unit id) exposes build=listUnitTenants and create=createTenant+refresh, shared by the manual/OCR/voice add-tenant flows (T-013/T-011/T-012) and the T-016 save action. 11 tests in test/tenants_repo_test.dart (scripted dio adapter): model fromJson (masked NID/family/status/dates + null tolerance), VerificationStatus.fromWire, FamilyMember.toCreateJson, ocrExtract multipart, createTenant field-omission + nested family + masked response + empty-NID omission, listUnitTenants bare-array + 404→ApiException, provider data + create-then-refresh. flutter pub get + build_runner + analyze (no error-severity issues in any file; remaining use_null_aware_elements infos + fake-storage unused-param warnings are the pre-existing baseline pattern shared with properties/profile repos and properties_repo_test) + test (full suite 158 passed) all green.
2026-06-05 | EPIC-04/T-015 | mobile | claude | done | Reusable family-members sub-form FamilyMembersField (lib/features/tenants/presentation/widgets/family_members_field.dart): a single StatefulWidget owning per-row name+relation TextEditingControllers (dynamic add/remove, controllers disposed on row-remove + unmount) that reports the current non-empty rows as List<FamilyMemberDraft> via an onChanged callback on every edit/add/remove; blank-name rows are omitted. keyPrefix param namespaces the per-row widget keys (ocr→ocrFamilyName/ocrFamilyRelation/ocrFamilyAdd, manual→manualFamily*) so each host keeps stable test keys. All colors/spacing/radii/fonts from khatir_tokens. De-duplicated: removed the inline _FamilyDraftRow/_FamilyRow/_AddFamilyButton from both ocr_review_screen.dart and manual_tenant_screen.dart and replaced them with the shared widget (both now hold a useState<List<FamilyMemberDraft>> updated via onChanged); the voice path (T-012) reuses the OCR review screen so it gets the same sub-form transitively — all 3 add-tenant paths share one widget. New canonical ARB keys family_add/family_name/family_relation/family_remove (bn template + en). 5 widget tests in test/family_members_field_test.dart: starts empty (add affordance only), add→row appears+flows to onChanged, blank-name row omitted, remove→row dropped+re-emits empty, keyPrefix namespaces keys. flutter pub get + gen-l10n + analyze (no issues in new/changed files; remaining 21 infos/warnings pre-existing in profile/properties/role/*_repo_test) + test (full suite 163 passed) all green.
2026-06-05 | EPIC-04/T-016 | mobile | claude | done | Convergent tenant save+route action wiring all 3 add-tenant paths (OCR/voice/manual) to the DMP form. New TenantSaveController (lib/features/tenants/presentation/controllers/tenant_save_controller.dart) consumes a normalised TenantSaveDraft (TenantReviewDraft.fromReview / built inline from ManualTenantDraft — single create site, no per-path duplication): drops blank family rows, parses optional dob, routes the create through the unit-scoped UnitTenantsController.createDetailed when a unitId is set (so the unit's list refreshes) else straight through TenantRepository.createTenantDetailed; on success shows the free-tier toast (when present) and pushReplacementNamed → /dmpform/{tenantId}; on ApiException/any error shows a snackbar and returns false (no partial navigation). Repo gained createTenantDetailed returning TenantCreateResult {masked Tenant + optional TenantUsage} (new tenant_create_result.dart; TenantUsage.maybeFromJson reads optional {tenants_used,free_limit,is_over_free} echoed on the create response — T-008 is backend + not a dep, so usage lights up automatically once the server sends it, no toast otherwise); createTenant now delegates to it. OcrReviewScreen + ManualTenantScreen proceed handlers now run the controller (with a saving spinner + disabled button) when onProceed is null (the router path); onProceed kept as a test seam so existing T-011/T-013 tests pass unchanged. Voice path converges via the reused OCR review screen. New DmpPlaceholderScreen at /dmpform/:tenantId (root navigator) stands in until EPIC-05; app_router registers it + drops the T-016 TODOs. ARB tenant_save_error/tenant_free_tier_status(used,limit)/dmp_placeholder_* (bn template + en). 5 widget tests in test/save_tenant_test.dart (scripted dio + GoRouter with the DMP route): manual save POSTs merged fields → DMP (no unit), unit-scoped save refreshes the unit list, free-tier toast when usage reported, error snackbar + no-nav + re-enabled form on 500, OCR/voice review save sends photo_ref → DMP. flutter pub get + gen-l10n + analyze (no issues in new/changed files; remaining infos/warnings pre-existing in properties/profile/role/*_repo_test) + test (full suite 168 passed) all green. EPIC-04 add-tenant flow now works end-to-end.
2026-06-05 | EPIC-05/T-007 | mobile | claude | done | Flutter DMP form preview screen DmpPreviewScreen (lib/features/dmpform/presentation/screens/dmp_preview_screen.dart) matching the `dmp` proto: top bar with a "ready" check chip, a celebratory 🎉 "All done!" hero, a white form card with the 🏛️ DMP·CIMS·tenant-information header (org name + rose badge) and labelled field rows (tenant / masked-NID / landlord / address / present / permanent / DOB / phone, blank fields hidden) plus a household members table, then a sage "Generate PDF" primary button → the PDF screen (T-008) and a soft "Edit" button → back to the tenant flow; loading (spinner) / error (danger icon + retry) / data states; all colors/spacing/radii/fonts from khatir_tokens. New dmpform data layer: DmpPreview/DmpFamilyMember model (lib/features/dmpform/data/models/dmp_preview.dart) mirroring the assembled masked-NID DmpData contract (T-002/T-004 — full NID never on the wire), DmpFormRepository.getPreview GETting /api/v1/tenants/{id}/dmpform (new ApiEndpoints.tenantDmpForm), and DmpPreviewController (AsyncNotifier.family keyed by tenant id, build=getPreview + refresh for the retry). app_router replaces the EPIC-04 DmpPlaceholderScreen with the real preview at /dmpform/:tenantId (route name `dmpForm` UNCHANGED so the T-016 save action keeps routing here) and adds a nested `/pdf` placeholder route (reusing DmpPlaceholderScreen) so the generate flow resolves until T-008 lands the real PDF screen. onGenerate/onEdit injectable seams for tests; default generate pushes /dmpform/{id}/pdf, default edit pops (falls back to /landlord/home). ARB dmp_title/ready/hero_title/hero_sub/org/org_sub/org_badge/field_*/generate/edit/error/retry (bn template + en). Design ledger row 14 (`dmp`) ticked. 4 widget tests in test/dmp_preview_test.dart (fake controller + GoRouter): data state renders assembled fields + masked NID + family + both actions, generate button fires the action, edit button fires the action, error state shows a retry. flutter pub get + gen-l10n + analyze (no issues in new/changed files; remaining 21 infos/warnings pre-existing in profile/properties/role/*_repo_test) + test (full suite 172 passed) all green. Closes the EPIC-04→05 seam.
2026-06-05 | EPIC-05/T-008 | mobile | claude | done | Flutter DMP PDF preview + share screen DmpPdfScreen (lib/features/dmpform/presentation/screens/dmp_pdf_screen.dart) matching the `dmpPdf` proto: top bar with back action + "A4" chip, the rendered A4 PDF on a muted paper-on-grey backdrop, and a sticky footer with a sage "Download" primary + soft "Share" (WhatsApp/system) action; generating (spinner + dmp_generating) / error (danger icon + retry) / data states; all colors/spacing/radii/fonts from khatir_tokens. Pipeline: DmpFormRepository.generatePdf POSTs /api/v1/tenants/{id}/dmpform/pdf (new ApiEndpoints.tenantDmpFormPdf) parsing the committed GeneratePdfResponseSerializer shape {record,signed_url} into DmpPdfResult, then fetchPdfBytes downloads the signed-URL bytes (ResponseType.bytes); DmpPdfController (AsyncNotifier.family keyed by tenant id) chains generate→download into a DmpPdf {bytes,result}, regenerate() for the retry. Free-tier: no entitlement gate — the wedge works for everyone. PDF rendering via printing ^5.14.3 PdfPreview (pdfium) behind an injectable PdfViewBuilder seam so widget tests stub it; share/download side-effects behind a DmpPdfSharer interface (PluginDmpPdfSharer = share_plus SharePlus.instance.share + printing Printing.layoutPdf), share file named dmp-form-{recordId}.pdf; action failures surface as a snackbar (dmp_pdf_action_failed). share_plus pinned ^12.0.2 (latest stable compatible with flutter_secure_storage ^9 → win32 ^5; ^13 forces win32 ^6 collision). app_router replaces the T-007 nested-`/pdf` DmpPlaceholderScreen with the real DmpPdfScreen at /dmpform/:id/pdf (root navigator; placeholder import dropped). ARB dmp_pdf_title/dmp_generating/dmp_pdf_download/dmp_pdf_share/dmp_pdf_whatsapp/dmp_pdf_error/dmp_pdf_action_failed (bn + en). Design ledger row 15 (`dmpPdf`) ticked. 4 widget tests in test/dmp_pdf_test.dart (fake controller + spy sharer + stub renderer): data state renders preview+A4+download/share, share button invokes sharer with the record filename (not download), download button invokes download (not share), error state shows retry. flutter pub get + gen-l10n + analyze (no issues in new/changed files; remaining 21 infos/warnings pre-existing in profile/properties/role/*_repo_test) + test (full suite 176 passed) all green. EPIC-05 wedge now works end-to-end: tenant → DMP preview → PDF → share/download.
2026-06-05 | EPIC-05/T-009 | mobile | claude | done | Canonical typed Flutter DMP data layer (the screen-facing slice — DmpPreview/DmpPdfResult/getPreview/generatePdf — was already committed out of order by T-007/T-008, which depend on this task; this task adds the typed layer alongside it without regressing those green screens). New freezed DmpData (+DmpFamilyMemberData) in lib/features/dmpform/data/models/dmp_data.dart mirroring the assembler `DmpData` returned by GET /api/v1/tenants/{id}/dmpform (T-004) — NID arrives masked server-side, the full value is never on the wire; manual fromJson tolerates missing keys/nested family. New freezed DmpRecord in models/dmp_record.dart mirroring DMPFormRecordSerializer ({id,tenant,template_version,pdf_ref,generated_by,generated_at,created_at}) + the signed_url the detail/generate responses attach — record exposes opaque pdf_ref + metadata only, no field payload/NID; fromGenerateJson folds the POST envelope's top-level signed_url onto the nested record. DmpFormRepository gained the typed accessors getDmpData (GET dmpform → DmpData), generateRecord (POST dmpform/pdf → DmpRecord), and getRecord (GET /dmpforms/{id} → DmpRecord) — all surfacing cross-user 404 as ApiException; new ApiEndpoints.dmpRecord(id). New dmpDataProvider / dmpRecordProvider (FutureProvider.family keyed by tenant/record id). Existing getPreview/generatePdf/fetchPdfBytes + dmpPreviewProvider/dmpPdfProvider left intact so T-007/T-008 screens are unchanged. 12 tests in test/dmp_data_layer_test.dart (scripted dio adapter): DmpData fromJson (fields+family, NID stays masked, empty tolerance), DmpRecord fromJson + fromGenerateJson (signed_url fold, absent record), repo getDmpData/generateRecord/getRecord happy paths + 404→ApiException, provider data states. build_runner codegen + flutter pub get + analyze (no issues in new/changed files; remaining 21 infos/warnings pre-existing in profile/properties/role/*_repo_test) + test (full suite 188 passed) all green.
2026-06-05 | EPIC-06/T-007 | mobile | claude | done | Flutter leases + rent-schedule data layer (the typed slice consumed by the lease form/list/detail screens T-008/T-009/T-010). New freezed Lease (lib/features/leases/data/models/models.dart) mirroring LeaseSerializer ({id,unit_id,tenant_id,landlord_id,start_date,end_date,rent,advance,status,signed_pdf_ref,created_at,updated_at} — FK ids + status read-only, money DecimalField strings parsed to double), freezed RentSchedule mirroring RentScheduleSerializer ({id,lease_id,period,due_day,due_date,amount,status,sent_at,created_at,updated_at} — read-only, period=YYYY-MM), plus LeaseTenantSummary + UnitLease for the UnitLeaseSerializer payload (lease fields top-level + nested tenant summary, masked NID only). New lease_enums.dart: LeaseStatus (draft/active/ended/terminated, fromWire→draft) and RentScheduleStatus (pending/requested/paid/overdue, fromWire→pending) mirroring enums.md + backend leases/enums.py. LeaseRepository (lib/features/leases/data/lease_repository.dart): getLease (GET /leases/{id}), createLease (POST /leases — draft only, landlord server-derived, advance omitted unless supplied), updateLease (PATCH — only non-null fields), activateLease (POST /leases/{id}/activate → server generates schedule), terminateLease (POST /leases/{id}/terminate, status ended/terminated, omitted=server-default terminated), getSchedule (GET /leases/{id}/schedule → bare array), getUnitLease (GET /units/{id}/lease → UnitLease; no-active-lease 404 surfaces as ApiException) — all cross-user/unknown ids resolve to 404→ApiException. New ApiEndpoints.leases/lease(id)/leaseSchedule(id)/leaseActivate(id)/leaseTerminate(id)/unitLease(id). Providers (lib/features/leases/data/providers.dart): leaseRepositoryProvider, leaseScheduleProvider + unitLeaseProvider (FutureProvider.family), and LeaseController (AsyncNotifier.family keyed by lease id) with build=getLease + activate/terminate/editTerms (renamed from update to avoid clashing with inherited AsyncNotifier.update) writing state + invalidating the schedule on activate. 14 tests in test/leases_repo_test.dart (scripted dio adapter): createLease draft body + advance-omission/inclusion + 201 parse, activate, terminate (status sent / omitted), update field-omission, getSchedule bare-array + null sent_at, getUnitLease lease+embedded-tenant + no-active-lease 404, getLease 404→ApiException, model null-tolerance + enum fromWire degradation. build_runner codegen + flutter pub get + analyze (no error/warning issues in new/changed files; remaining use_null_aware_elements infos + fake-storage unused-param warnings are the pre-existing baseline shared with properties/profile/tenants repos and *_repo_test) + test (full suite 202 passed) all green.
2026-06-05 | EPIC-06/T-010 | mobile | claude | done | Lease list/detail screens (the list screen + listLeases/leasesListProvider were laid down in a prior partial run of this task; this run completed the detail screen, routes, and tests). New LeaseDetailScreen (lib/features/leases/presentation/screens/lease_detail_screen.dart) at /lease/:id: a cream scaffold with a status+rent header card, a Terms card (monthly rent, advance when >0, start/end dates), a Rent-schedule summary card (period count + per-status count chips, with its own loading/error/empty states — lease_schedule_empty when no rows), and a danger-outlined Terminate action shown only for an active lease that opens an AlertDialog (lease_terminate_confirm_title/body) and on confirm calls LeaseController.terminate(terminated)+invalidates leasesListProvider, reporting lease_terminated_ok / lease_terminate_error via snackbar; the closed lease's status write hides the action. Reuses the list screen's leaseStatusLabel/termRange helpers (no duplication). All colors/spacing/radii/fonts from khatir_tokens. States: loading (spinner) / error (retry → LeaseController.refresh) / data. app_router registers /leases (LeaseListScreen) + /lease/:id (LeaseDetailScreen) on the root navigator, after the literal /lease/new + /lease/:id/edit form routes so :id never captures "new". BanglaNumerals.format gained an optional grouped flag (default true) so date parts render without a thousands separator (the list screen already called it that way). ARB lease_detail_title/section_schedule/section_terms/schedule_summary/schedule_empty/terminate*/sched_status_* (bn + en) were already present from the prior run; gen-l10n regenerated the AppLocalizations getters this run. 9 widget tests (test/lease_list_test.dart: card-per-lease+status chip, empty, error+retry; test/lease_detail_test.dart: terms+schedule summary render, terminate hidden for non-active, empty-schedule helper, terminate confirm→repo+success+action-hidden, cancel→no repo call, lease-load error→retry) all via fake LeaseRepository. flutter pub get + gen-l10n + analyze (no issues in new files; the 4 remaining use_null_aware_elements infos in lease_repository.dart are the pre-existing baseline pattern shared across profile/properties/tenants repos) + test (full suite 217 passed) all green.
2026-06-05 | EPIC-07/T-011 | mobile | claude | done | Flutter rent-request screen RentRequestScreen (lib/features/rent/presentation/screens/rent_request_screen.dart) at /rent/request matching the `rentReq` proto: a cream scaffold with the 📤 "Ask for rent" emoji-hero + WhatsApp-link reassurance subtitle, an amount field (digits-only, >0 validation → rent_request_err_amount) and a YYYY-MM period field (regex-validated month 01–12 → rent_request_err_period), then a sage "Send WhatsApp link" primary and an outlined "Mark received (cash)" secondary action. Send runs RentRepository.createManual(lease,amount,period) then RentRequestController.send (create + deliver the tenant link) and routes to the verify queue (/landlord/rent); Mark-received runs createManual then RentRequestController.markReceived (settle the off-platform cash payment); both invalidate rentQueueProvider so the queue refreshes on return. States: data (idle form), sending (per-button spinner via busy/busyMarkReceived useState, both buttons disabled), error (ApiException/any → friendly rent_request_error snackbar, form re-enabled). leaseId carried via `?lease=` query param (same convention as the `?unit=` flows) with optional `?amount=`/`?period=` prefills; landlord server-derived from the lease, never collected. GoRouter.maybeOf used for the post-success nav so the screen works headless in widget tests. onProceed test seam kept (null = real flow). All colors/spacing/radii/fonts from khatir_tokens; all 13 rent_* ARB keys (title/heading/subtitle/amount/period/period_hint/send_whatsapp/mark_received/err_amount/err_period/sent/received/error) already present from a prior run, bn + en. app_router registers the route on the root navigator (after the lease form routes). Design ledger row 16 (`rentReq`) ticked. 8 widget tests in test/rent_request_test.dart (fake RentRepository overriding rentRepositoryProvider): renders hero+fields+both actions, prefills amount/period, send → create+send (+correct lease/amount/period) + rent_request_sent, mark-received → create+markReceived + rent_request_received, invalid input blocks the action + shows both field errors (no create call), and failure paths on send/create/mark-received each surface rent_request_error (create-failure makes no send call). build_runner regenerated the T-010 models.freezed.dart (was uncommitted) so freezed copyWith/getters resolve. flutter pub get + analyze (no issues in rent screen / router / test; the 25 remaining infos/warnings are the pre-existing baseline in unit_repository/tenant_repository/properties_repo_test/role_redirect_test) + test (full suite 225 passed, incl. the 8 new) all green.
2026-06-05 | EPIC-07/T-012 | mobile | claude | done | Flutter verify-payment screen VerifyPaymentScreen (lib/features/rent/presentation/screens/verify_payment_screen.dart) at /rent/:id/verify matching the `verifyPay` proto: a cream scaffold with a soft butter claim card (avatar initial + "{name} says they paid" + ৳amount·period subline from the RentRequest), a "Submitted proof" viewer, and a sage "Received" primary + outlined "Not yet received" secondary. The proof viewer shows the screenshot loaded from its signed URL via Image.network (PaymentProof.photoRef treated as the signed URL per §15 — never raw bytes; loadingBuilder spinner, errorBuilder → friendly verify_proof_image_failed placeholder) with the txn id (verify_proof_txn) detail, and degrades to a "no proof yet" placeholder (verifyProofNone) when no proof is supplied. Verify runs RentRequestController.verify (server creates Payment + receipt) → snackbar verify_verified → routes to the receipt (/rent/:id/receipt, T-013); reject opens a required-reason AlertDialog (verify_reason*, empty → verify_reason_required) then RentRequestController.reject(reason) → snackbar verify_rejected → pops back to the queue; both invalidate rentQueueProvider. States: loading (spinner) / load-error (danger icon + verifyRetry → controller.refresh) / data / verifying (per-action spinner via busyAction useState, both buttons disabled) / action-error (ApiException/any → friendly verify_error snackbar, actions re-enabled). Reuses the committed T-010 rentRequestControllerProvider as the single source of truth for both load and the verify/reject transitions. The rent detail endpoint does not surface the tenant name or PaymentProof, so they ride in via a typed router `extra` payload (VerifyPaymentArgs) the queue (T-013) can supply; onAction test seam kept (null = real flow). GoRouter.maybeOf used for post-action nav so the screen works headless in tests. All colors/spacing/radii/fonts from khatir_tokens. app_router registers /rent/:id/verify on the root navigator (after /rent/request). ARB verify_title/claim/amount_period/proof/proof_none/proof_image_failed/proof_txn/proof_amount/confirm/reject/reason/reason_hint/reason_required/reason_cancel/reason_submit/verified/rejected/error/load_error/retry (bn + en). Design ledger row 17 (`verifyPay`) ticked. 10 widget tests in test/verify_payment_test.dart (fake RentRepository overriding rentRepositoryProvider): renders claim+proof+both actions, no-proof placeholder, proof-with-txn details, verify → verify call + verify_verified, reject → reason dialog (empty blocks + shows required error, then reason → reject(reason)+verify_rejected), cancel → no transition, verify-failure + reject-failure → verify_error, load-failure → load-error+retry. flutter pub get + gen-l10n + analyze (no issues in the new screen/router/test; the 25 remaining infos/warnings are the pre-existing baseline in unit/tenant repos + properties/tenants_repo_test + role_redirect_test) + test (full suite 234 passed, incl. the 10 new) all green.
2026-06-05 | EPIC-07/T-013 | mobile | claude | done | Flutter rent receipt screen ReceiptScreen (lib/features/rent/presentation/screens/receipt_screen.dart) at /rent/:id/receipt matching the `receipt` proto — the EPIC-07 validation gate that closes the rent loop (request→pay→verify→receipt): a cream scaffold with the 🧾 "Receipt ready" emoji-hero, a centred receipt card (brand heading, big ৳amount in sage, a dashed tear divider, labelled tenant/unit/period/method/status rows with ✓ Paid, and a muted receipt-no footnote), then a sage "Send to tenant" primary + a soft "PDF · Done" secondary. Share sends the receipt to the tenant via the OS sheet (WhatsApp/system) — the generated PDF when a signed URL is present, else a plain-text summary; Done saves the PDF (when present) then returns to /landlord/home. Reuses the EPIC-05 T-008 PDF share/download seam, mirrored as ReceiptSharer/PluginReceiptSharer + receiptSharerProvider (share_plus + printing); RentRepository.fetchReceiptBytes + receiptBytesProvider download the signed-URL PDF bytes. The amount/period/status come from the committed T-010 rentRequestControllerProvider (single source of truth, shared with verify); the contextual fields the detail endpoint doesn't surface (tenant/unit/method/receipt-no/signed-PDF URL) ride in via a typed router `extra` payload ReceiptArgs (same convention as T-012's proof), degrading missing values to a neutral dash. States: loading (spinner) / load-error (danger icon + receiptRetry → controller.refresh) / data; a failed share/download → friendly receipt_action_failed snackbar (never crashes). onShared/onDone test seams kept (null = real flow); GoRouter.maybeOf used for nav so the screen is headless-testable. All colors/spacing/radii/fonts from khatir_tokens (no proto hex/px). app_router registers /rent/:id/receipt on the root navigator (after /rent/:id/verify). ARB receipt_title/ready/ready_sub/heading/amount/tenant/unit/period/method/status/status_paid/no/share/done/dash/share_text/action_failed/load_error/retry (bn + en). Design ledger row 18 (`receipt`) ticked. 5 widget tests in test/receipt_test.dart (fake RentRepository + spy ReceiptSharer): renders the summary card+amount+tenant+✓Paid+both actions, share-with-signed-URL → sharePdf(rent-receipt-<sanitized-no>.pdf, bytes), share-without-URL → shareText(receipt_share_text), done-with-URL → downloadPdf(rent-receipt-r-1.pdf), load-failure → receipt_load_error+retry. flutter pub get + gen-l10n + analyze (No issues found in the new screen/sharer/repo/providers/router/test; the remaining infos/warnings are the pre-existing baseline in unit/tenant/lease/profile/building repos + properties/tenants_repo_test + role_redirect_test) + test (full suite 239 passed, incl. the 5 new) all green.
2026-06-05 | EPIC-07/T-014 | mobile | claude | done | Late-payers section on the landlord home, filling the EPIC-03 home placeholder. New LatePayersSection (lib/features/rent/presentation/widgets/late_payers_section.dart) watches the committed T-010 rentQueueProvider(null) and derives "late payers" = unpaid requests (status sent / proof_submitted; verified/rejected are settled and excluded), matching the `home` proto rose overdue rows: a rose "{count} rent overdue" heading then per-request rose-tinted rows (clock badge, "Rent overdue" label, ৳amount · YYYY-MM period sub-line — tenant/unit labels are not on RentRequestSerializer) each with a compact rose "Ask" pill routing to /rent/request?lease=…&amount=…&period=… (the T-011 query convention). States: loading (slim sage placeholder), empty/all-paid (sage reassurance row), data (the overdue rows); a queue read error degrades to the all-paid row rather than shouting on home. landlord_home_screen.dart _CollectionCard now renders LatePayersSection in place of the late-payer half of the old TODO placeholder; the collected/expected amount + progress chart stay deferred to EPIC-09 (TODO narrowed, not removed). All colors/spacing/radii/fonts from khatir_tokens; numerals localised via BanglaNumerals (toBangla for the YYYY-MM period). ARB home_late_payers (count plural), home_late_payers_one, home_all_paid, home_quick_request (bn + en). 3 new widget tests in test/landlord_home_test.dart (fake RentRepository overriding rentRepositoryProvider): overdue list shows only the 2 unpaid of 3 with count heading + 2 Ask pills (verified row excluded), Ask routes carrying the lease id, empty queue shows all-paid + no Ask. flutter pub get + gen-l10n + analyze (No issues found in the new widget / home screen / test; the 25 remaining infos/warnings are the pre-existing baseline in unit/tenant repos + properties/tenants_repo_test + role_redirect_test) + test (full suite 242 passed, incl. the 3 new) all green.
2026-06-05 | EPIC-08/T-007 | mobile | claude | done | Flutter maintenance+expense data layer (the typed slice consumed by the expense list/add (T-008/T-009), maintenance queue/on-unit (T-010/T-011) and expense summary hook (T-012) screens). New maintenance_enums.dart (lib/features/maintenance/data/models/): MaintenanceCategory (plumbing/electrical/paint/structural/appliance/utility/other, fromWire→other), MaintenanceStatus (open/resolved, fromWire→open), ExpenseCategory (plumbing/paint/electrical/structural/appliance/utility/other, fromWire→other), ExpenseSource (request/manual, fromWire→manual) mirroring enums.md + backend maintenance/enums.py. New freezed models.dart: MaintenanceRequest mirroring MaintenanceRequestSerializer ({id,unit_id,lease_id,category,description,photo_ref,status,resolved_at,resolution_cost,resolution_note,created_at,updated_at} — FK ids + status + resolution read-only, resolution_cost nullable DecimalField string→double?), Expense mirroring ExpenseSerializer ({id,unit_id,request_id,category,amount,date,source,note,receipt_ref,created_at,updated_at} — FK ids + source read-only, amount DecimalField string→double), plus ExpenseCategoryTotal/ExpenseMonthTotal/ExpenseSummary for the GET /expenses/summary payload ({by_category:[{category,total}], by_month:[{month,total}]}, Decimal-string totals→double, month=first-of-month ISO date, absent buckets→empty lists). MaintenanceRepository (maintenance_repository.dart): listQueue (GET /maintenance, optional status/unit filters, {results} unwrap), getRequest (GET /maintenance/{id}), createRequest (POST /maintenance — unit server-scoped, always open, category/photo_ref/lease_id omitted unless supplied), updateRequest (PATCH — only non-null fields), resolve (POST /maintenance/{id}/resolve cost+note → server auto-creates one expense). ExpenseRepository (expense_repository.dart) + ExpenseFilter (unit/building/date_from/date_to → query params): listExpenses (GET /expenses, {results} unwrap, manual+auto both shown), getExpense, createExpense (POST — source forced manual server-side, date YYYY-MM-DD), updateExpense (PATCH — only non-null, auto-expense immutable server-side), deleteExpense (DELETE→204), summary (GET /expenses/summary→ExpenseSummary), exportCsv (GET /expenses/export→raw CSV text via ResponseType.plain) — all cross-user/unknown ids resolve to 404→ApiException. New ApiEndpoints.maintenance/maintenanceRequest(id)/maintenanceResolve(id) + expenses/expense(id)/expensesExport/expensesSummary. Providers (providers.dart): maintenanceRepositoryProvider + expenseRepositoryProvider; maintenanceQueueProvider (AsyncNotifier.family by status), maintenanceRequestProvider (FutureProvider.family) + MaintenanceRequestController (AsyncNotifier.family by id, build=getRequest, updateRequest [renamed from update to avoid clashing with inherited AsyncNotifier.update] + resolve writing state + invalidating the single-request provider); expenseListProvider (AsyncNotifier.family by ExpenseFilter with delete helper), expenseProvider (FutureProvider.family), expenseSummaryProvider (FutureProvider.family). 16 tests in test/maintenance_data_layer_test.dart (scripted dio adapter): listQueue results-unwrap + status/unit filters, createRequest unit_id+description body + field-omission + 201 parse, updateRequest field-omission, resolve cost/note body + resolved parse, getRequest 404→ApiException; listExpenses filter params + manual/auto parse, createExpense manual body (no source/receipt_ref) + 201, updateExpense field-omission, deleteExpense 204, summary by_category/by_month parse, exportCsv raw text + filter, getExpense 404→ApiException; model null-tolerance (MaintenanceRequest null cost, Expense null date/money, ExpenseSummary empty buckets) + enum fromWire degradation. build_runner codegen (models.freezed.dart committed) + flutter pub get + analyze (no error/warning issues in new/changed files; remaining use_null_aware_elements infos are the pre-existing baseline shared with profile/properties/tenants/leases repos) + test (full suite 258 passed, incl. the 16 new) all green.
2026-06-05 | EPIC-08/T-008 | mobile | claude | done | Flutter maintenance & expenses list screen ExpensesScreen (lib/features/maintenance/presentation/screens/expenses_screen.dart) at /expenses matching the `expenses` proto: a cream scaffold with a butter-gradient "Total expenses" hero (This-month chip + label + big ৳total summed from the rows in view + "{count} expenses" sub-line), a horizontal building filter row, then a "Recent expenses" section. The app-bar carries an Export (CSV) action and an Add action; the list is one white card of rows, each a category emoji + category label + a source chip (sage "Manual" vs butter "Maintenance" per §15, distinguishing manual vs maintenance-sourced expenses) + the date, with a trailing rose ৳amount. Loaded via the committed T-007 expenseListProvider (AsyncNotifier.family keyed by ExpenseFilter, scoped server-side via for_user); the building filter maps to ExpenseFilter.buildingId and is held as a single stable instance in State (ExpenseFilter has no value equality, so re-deriving it each build would spawn a fresh family member and never settle). Building chips come from buildingsProvider, degrading to "All"-only while they load / on read failure so the filter never blocks the list. Export fetches the scoped+filtered CSV via ExpenseRepository.exportCsv and hands it to a new ExpenseCsvSharer/PluginExpenseCsvSharer + expenseCsvSharerProvider seam (share_plus, mirroring the EPIC-07 receipt sharer) as an expenses.csv attachment; a failed fetch/share → friendly expenses_export_failed snackbar. Add pushes /expenses/add (T-009); onAdd test seam kept (null = real nav), context.push used. States: loading (spinner) / error (common_network_error + retry → controller.refresh) / empty (🧾 expenses_empty card) / data. All colors/spacing/radii/fonts from khatir_tokens (butter/sage/rose + spacing/radius scale; no proto hex/px); numerals localised via BanglaNumerals. app_router registers /expenses on the root navigator (before the portfolio routes). ARB expenses_title/this_month/total/total_amount/count/filter_all/section_recent/source_manual/source_request/amount/add/export/export_failed/empty + expenses_category_{plumbing,paint,electrical,structural,appliance,utility,other} (bn + en). Design ledger row 19 (`expenses`) ticked. 6 widget tests in test/expenses_screen_test.dart (fake ExpenseRepository + fake BuildingRepository + spy ExpenseCsvSharer): renders total (3,500+12,000=15,500)+a row per expense+both source chips, empty state, error+retry, building-filter tap re-queries with buildingId=b-1 and narrows the list, export → exportCsv once + shareCsv(expenses.csv) with the CSV text, add action fires its callback. flutter pub get + gen-l10n + analyze (No issues in the new screen/sharer/router/test; the remaining use_null_aware_elements infos are the pre-existing baseline in the T-007 expense/maintenance repos shared across profile/properties/tenants/leases repos) + test (full suite 264 passed, incl. the 6 new) all green.
2026-06-05 | EPIC-08/T-009 | mobile | claude | done | Flutter add-expense screen AddExpenseScreen (lib/features/maintenance/presentation/screens/add_expense_screen.dart) at /expenses/add matching the `addExpense` proto (screens-landlord2.js → reg('addExpense')): a cream scaffold form with the amount input (★ required, digits-only, ৳০ hint), a wrapping category chip row driven by the ExpenseCategory config enum (selected fills sage, default plumbing), the unit picker (the single proto "Unit" field realised as a building selector + unit selector — token-styled dropdowns fed by buildingsProvider / buildingUnitsProvider, since createExpense needs the unit id and a unit is only meaningful within a building), a date field (read-only card → platform date picker, defaults today, YYYY-MM-DD), an optional note field (multiline), an optional receipt picker (ImagePickerService gallery pick → attached-confirmation row with Remove; filename threaded into createExpense(receiptRef:) — T-007 exposes only a receipt_ref string and there is no committed media-upload endpoint yet, so the encrypted-storage upload ref rides the same field once that lands), then the full-width sage "Save expense" CTA. Save validates amount (>0) + unit (inline errors; the unit is not a FormField so it is checked alongside the form on Save), runs the committed T-007 ExpenseRepository.createExpense (source forced manual server-side), invalidates expenseListProvider so the new row shows on return, snackbars expense_saved, and routes back to /expenses; failures → friendly expense_save_failed snackbar (never crashes). onSaved test seam kept (null = real flow, emitting AddExpenseDraft); GoRouter.maybeOf used for nav so the screen is headless-testable. States: data (idle form) / validation (inline amount+unit errors) / saving (CTA spinner, form disabled) / error (snackbar, form re-enabled). All colors/spacing/radii/fonts from khatir_tokens (sage/danger + spacing/radius scale; no proto hex/px). app_router registers /expenses/add on the root navigator (after /expenses). ARB add_expense_title + expense_amount/amount_hint/category/unit/building/unit_hint/building_hint/date/note/receipt/receipt_add/receipt_attached/receipt_remove/save/err_amount/err_unit/saved/save_failed (bn + en). Design ledger row 20 (`addExpense`) ticked. 6 widget tests in test/add_expense_screen_test.dart (fake ExpenseRepository + fake Building/Unit repos + fake ImagePicker, ListView scroll helpers): renders amount + a chip per ExpenseCategory + the save CTA, blocks save without amount/unit (no create call, both inline errors), saves the entered fields (unit u-1, amount 3500, category paint, note) via createExpense, attaching a receipt threads receipt.jpg into the save, onSaved seam intercepts the real create flow, a failed save surfaces expense_save_failed. flutter pub get + gen-l10n + analyze (No issues in the new screen/router/test; the remaining use_null_aware_elements infos + properties/tenants_repo_test unused-param warnings are the pre-existing baseline in the unit/tenant/building/profile/maintenance repos) + test (full suite 270 passed, incl. the 6 new) all green.
2026-06-05 | EPIC-08/T-010 | mobile | claude | done | Flutter maintenance queue MaintenanceQueueScreen (lib/features/maintenance/presentation/screens/maintenance_queue_screen.dart) at /maintenance, reusing the `expenses` proto's "New requests" block (screens-landlord2.js → reg('expenses'); no dedicated design-map key per §15): a cream scaffold listing the caller's OPEN maintenance requests from the committed T-007 maintenanceQueueProvider(open) (scoped server-side via for_user), each a white card with the category emoji, the unit label + a rose category chip, the description, an optional photo thumbnail (Image.network on MaintenanceRequest.photoRef = the backend signed URL, degrading to a neutral placeholder on load/error), and a sage "সমাধান + খরচ · Resolve" action. Resolve opens a modal dialog (cost ★ digits-only >0 → maintenance_err_cost, optional note); confirm runs MaintenanceRequestController.resolve(cost,note) — the T-002 resolve that auto-creates exactly ONE expense server-side — then refreshes the open queue (the resolved request drops off) and invalidates expenseListProvider so the new auto-expense shows on /expenses, snackbaring maintenance_resolved; failures → friendly maintenance_resolve_failed snackbar (never crashes). onResolve test seam kept (null = real flow, emitting ResolveDraft). States: loading (spinner) / error (common_network_error + retry → controller.refresh) / empty (🔧 maintenance_empty card) / data. All colors/spacing/radii/fonts from khatir_tokens (sage/rose + spacing/radius scale; no proto hex/px); numerals localised via BanglaNumerals. app_router registers /maintenance on the root navigator (after /expenses/add). ARB maintenance_title/section_open/open_count/unit/resolve/resolved_badge/cost/cost_hint/resolution_note/resolve_title/resolve_hint/resolve_confirm/resolve_cancel/err_cost/resolved/resolve_failed/empty + maintenance_category_{plumbing,electrical,paint,structural,appliance,utility,other} (bn + en). 6 widget tests in test/maintenance_queue_test.dart (fake MaintenanceRepository overriding maintenanceRepositoryProvider): card-per-open-request + description + resolve action, empty state, error+retry, resolve dialog blocks on invalid cost then fires onResolve with the entered cost(3500)+note, the real resolve flow records cost(4200) on the repo once + surfaces maintenance_resolved, cancel makes no resolve call. flutter pub get + gen-l10n + analyze (no issues in the new screen/router/test; the remaining use_null_aware_elements infos + properties/tenants_repo_test unused-param warnings are the pre-existing baseline in the T-007 maintenance/expense + unit/tenant/building/profile repos) + test (full suite 276 passed, incl. the 6 new) all green.
2026-06-05 | EPIC-08/T-011 | mobile | claude | done | Maintenance + expense summary on the unit detail screen. New UnitMaintExpenseSection (lib/features/maintenance/presentation/widgets/unit_maint_expense_section.dart): two soft cards under the tenant region — a Maintenance card (open-request count sub-line + up to 3 recent rows: category emoji + label + truncated description + a sage/rose status chip) and an Expenses card (total ৳ of the recent expenses + count + up to 3 recent rows: category emoji + label + rose ৳amount) — each with a sage "View all" link routing to /maintenance (T-010, MaintenanceQueueScreen.routeName) and /expenses (T-008, ExpensesScreen.routeName). Data comes from two new unit-scoped reads added to the committed T-007 providers.dart: unitMaintenanceProvider (FutureProvider.family by unitId → listQueue(unitId:)) and unitExpensesProvider (FutureProvider.family by unitId → listExpenses(filter: ExpenseFilter(unitId:))), both scoped server-side via for_user + ?unit=<id>. Each card renders its own loading (inline spinner) / empty (friendly muted line) / error (unit_section_error line) / data state independently so a slow/failed expense fetch never blocks the maintenance card. The proto `unit` screen has no maint/expense block, so this is a new composition from the existing soft-card + chip token vocabulary; integrated into unit_detail_screen.dart _UnitBody below _TenantSection (UnitMaintExpenseSection(unitId: unit.id)). All colors/spacing/radii/fonts from khatir_tokens (sage/rose + spacing/radius scale; no proto hex/px); numerals localised via BanglaNumerals. ARB unit_maintenance/unit_expenses/unit_view_all + unit_maint_open_count/unit_maint_empty/unit_maint_status_open/unit_expenses_total/unit_expenses_count/unit_expenses_empty/unit_section_error (bn template carries the placeholder metadata, en the translations). 7 widget tests in test/unit_maint_section_test.dart (fake Maintenance/Expense repos overriding the T-007 providers, GoRouter with stub /maintenance + /expenses routes): data state shows both headings + open count(1 of 2) + a description + total(5,000) + count(2) + 2 View-all links, reads are unit-scoped (unit filter u1 threaded to both repos), empty state shows both empty lines, loading shows a spinner while the queue hangs, error shows 2 section-error lines, and the two View-all links route to the queue / expenses stubs. Also updated test/unit_detail_test.dart to override maintenanceRepositoryProvider/expenseRepositoryProvider with empty-returning fakes so the now-embedded section settles. flutter pub get + gen-l10n + analyze (No issues in the new widget / providers / unit_detail_screen / tests; the remaining use_null_aware_elements infos + properties/tenants_repo_test unused-param warnings are the pre-existing baseline) + test (full suite 283 passed, incl. the 7 new) all green.
2026-06-05 | EPIC-09/T-004 | mobile | claude | done | Flutter dashboard data layer (the typed slice consumed by the dashboard screen + cards/charts, T-006/T-008/T-009). New freezed dashboard_model.dart (lib/features/dashboard/data/): DashboardData mirroring the committed T-002 DashboardSerializer ({total_collected,total_pending,total_overdue,collection_rate,occupied_units,total_units,occupancy_rate,total_income,total_expense,net,late_payer_count,monthly_series,top_expense_categories}) — money fields are DRF DecimalField strings→double, rates floats, counts ints, both list fields default to empty; MonthPoint ({period,collected,expense} — period kept as the raw YYYY-MM label, money strings→double) and CategoryTotal ({category,amount} — category reuses the shared ExpenseCategory enum/fromWire→other, amount string→double). DashboardRepository (dashboard_repository.dart): fetchDashboard({months}) → GET /dashboard (payload returned directly, no {results} envelope; optional ?months=N omitted when null so the server applies its configured dashboard_months_default; owner-scoped + cached server-side; non-2xx→ApiException). New ApiEndpoints.dashboard. Providers (dashboard_providers.dart): dashboardRepositoryProvider + dashboardProvider (AsyncNotifierProvider.family<DashboardController,DashboardData,int?> keyed by the months window, null=server default) with a refresh() for pull-to-retry, matching the rent/maintenance controller pattern. 9 tests in test/dashboard_data_layer_test.dart (scripted dio adapter + in-memory secure storage): fetchDashboard reads /dashboard + parses every metric/series/category, omits months when null, passes ?months=12, non-2xx→ApiException; dashboardProvider exposes AsyncValue.data + refresh re-fetches into state; model null-tolerance (empty payload→safe defaults, unknown category→other, missing month money→0, string/null numerics). build_runner codegen (dashboard_model.freezed.dart committed) + flutter pub get + analyze (No issues in the new model/repo/providers/endpoints/test; the remaining use_null_aware_elements infos + properties/tenants/role test unused-param/null-aware warnings are the pre-existing baseline) + test (full suite 292 passed, incl. the 9 new) all green.
2026-06-05 | EPIC-09/T-005 | mobile | claude | done | fl_chart shared chart widgets (the generic, themed bar/donut/line widgets consumed by the dashboard screen + any future chart, T-006/T-009). Three thin wrappers over fl_chart 1.2.0 (latest stable, no beta/RC) in lib/core/widgets/charts/, all colors/spacing/radii/fonts pulled from khatir_tokens (sage gradient bars, sage donut arc on a sage-bg track, sage trend line with a sage-bg fill + rose accent dots, muted token label colors; no proto hex/px): KBarChart (List<KBarDatum>{label,value} → spaced sage-gradient bars with token-styled value labels on top + bottom-axis category/month labels, optional maxValue + valueSuffix e.g. '%', touch/grid/border disabled), KDonutChart (List<KDonutSlice>{value,color} → hollow PieChart with a center primary value + optional sublabel, mirroring the proto occupancy ring; plus a .percentage() convenience factory: a single sage arc on a sage-bg track with a locale-formatted NN% center label), KLineChart (List<KLinePoint>{label,value} → curved sage trend line, sage-bg below-fill, rose dots, padded min/max so the line never hugs the edge, bottom-axis labels). Each widget renders independently: isLoading → shared ChartLoadingState (sage-bg box + sage spinner), empty/zero-sum data → shared ChartEmptyState (sage-bg box + caller-supplied localized label, em-dash fallback) — both extracted to lib/core/widgets/charts/chart_states.dart so the three charts reuse one loading/empty widget instead of duplicating. Charts stay generic per §15 (business data → chart data mapping lives in the T-006 screen): callers pass already-localized axis/center labels; numerals on the bar value labels + the donut .percentage factory format via BanglaNumerals from the passed localeCode (88→৮৮%, 78→৭৮% for bn / 78% for en). No DB/API/screens/flags. 13 widget tests in test/chart_widgets_test.dart (sized Scaffold harness): KBarChart renders a BarChart with data / empty state + label / loading state / Bangla numeral value labels; KDonutChart renders a PieChart + center label + sublabel / empty when slices sum to zero / .percentage localizes the center label bn(৭৮%) + en(78%) / loading; KLineChart renders a LineChart with data / empty state + label / loading / flat single-value series without error. flutter pub get (fl_chart ^1.2.0 added) + analyze (No issues in the new charts/test; the 36 remaining use_null_aware_elements infos + properties/tenants_repo_test unused-param warnings are the pre-existing baseline in unrelated repos/tests) + test (full suite 305 passed, incl. the 13 new) all green.
2026-06-05 | EPIC-09/T-006 | mobile | claude | done | Landlord Dashboard / charts screen DashboardScreen (lib/features/dashboard/presentation/screens/dashboard_screen.dart) at /landlord/dashboard, wired into the landlord shell's Charts tab (app_router.dart: the EPIC-09 _placeholderBranch replaced by a real StatefulShellBranch → DashboardScreen, route path/name unchanged), matching the `dashboard` proto (screens-landlord2.js → reg('dashboard')): a cream scaffold ListView with an ink-gradient income hero (এ মাসের মোট আয় + big ৳totalIncome), a 6-month collection-rate KBarChart (each month's collected as a % of the window's peak, max 100%, %-suffixed value labels), an occupancy KDonutChart.percentage (sage ring using the server occupancy_rate so the % matches backend rounding — 11/14→78% — with an occupied/total sublabel) + a sage/rose occupied/vacant legend, an income-vs-expense KLineChart (monthly collected trend) + a sage/rose legend, a top-expense-categories list (one soft-rose row per CategoryTotal: localized category label via the shared categoryLabel + a peak-relative LinearProgressIndicator + rose ৳amount), and a late-payers card (overdue count; when >0 a rose quick-request CTA → /rent/request). All metrics come from one GET /dashboard read via the committed T-004 dashboardProvider (AsyncNotifier.family keyed by the months window, null=server default; owner-scoped+cached server-side). States: loading (charts show their own shimmer over a zeroed body) / error (common_network_error + retry → controller.refresh) / empty (a brand-new landlord with no units/income/expense/series → 📊 dashboard_empty card) / data. All colors/spacing/radii/fonts from khatir_tokens (ink/sage/rose + spacing/radius scale; no proto hex/px); numerals localised via BanglaNumerals; month axis labels derived from the YYYY-MM period (bn/en short month names). onLatePayerRequest test seam kept (null = real context.push('/rent/request')). Deviations: T-004 exposes only latePayerCount (no per-tenant list) and the proto has no late-payers list, so the "late-payers list" is a count summary card + quick-request CTA; the income-vs-expense block is added per §8 though the proto omits it. ARB dashboard_title/income/amount/collection/occupancy/occupancy_units/occupied/vacant/income_expense/income_legend/expense_legend/expenses/late/late_none/late_count/late_request/chart_empty/empty (bn template carries the placeholder metadata, en the translations). Design ledger row `dashboard` built. 5 widget tests in test/dashboard_screen_test.dart (fake DashboardRepository overriding dashboardRepositoryProvider, a tall test surface so the scrolling list lays out): renders the income hero (৳71,000) + all four chart blocks + the 78% donut + late-payers card/CTA + "3 late" count, the quick-request CTA fires its nav callback, empty state for a blank DashboardData (no charts), error + working retry re-fetches the data (≥2 repo calls), zero late payers → no CTA + dashboard_late_none. flutter pub get + gen-l10n + analyze (No issues in the new screen/router/test/ARB; the 36 remaining use_null_aware_elements infos + properties/tenants_repo_test unused-param warnings are the pre-existing baseline in unrelated repos/tests) + test (full suite 310 passed, incl. the 5 new) all green.
2026-06-05 | EPIC-09/T-007 | mobile | claude | done | Filled the EPIC-03 home-screen collection chart placeholder with a real summary card. _CollectionCard (lib/features/properties/presentation/screens/landlord_home_screen.dart) is now a ConsumerWidget watching the committed T-004 dashboardProvider(null) (owner-scoped + cached server-side, same payload the Charts tab reads so the call is shared/deduped): under the existing "Collected this month" heading it shows a tappable sage-bg tile with the current-month collected total (homeCollectedAmount), a "Pending ৳X" sub-line (homePendingAmount), a thin collection-rate LinearProgressIndicator (collection_rate clamped 0..1), and a "View charts" affordance; tapping it switches the landlord shell to the dashboard/Charts tab via context.goNamed(DashboardScreen.routeName) (goNamed on a StatefulShellRoute branch root activates that branch instead of pushing). The EPIC-09 TODO + placeholder copy are removed; while the first read is in flight the tile shows a small spinner, and on read error it degrades to the original home_collected_todo coming-soon copy so the card never collapses. The late-payers section (EPIC-07 T-014) below is unchanged. All colors/spacing/radii/fonts from khatir_tokens (sage/rose/muted + spacing/radius scale; no proto hex/px); numerals localised via BanglaNumerals. New ARB home_collected_pending (placeholder {amount}) + home_view_dashboard (bn + en). 3 new widget tests in test/landlord_home_test.dart (harness extended to override dashboardRepositoryProvider with a fake serving a fixed DashboardData + a stub landlordDashboard route): collection card shows the real collected/pending amounts + view-charts hint and no placeholder copy, tapping it opens the dashboard tab (DASHBOARD_TAB stub), and a failed dashboard read falls back to home_collected_todo with the heading intact. flutter pub get + analyze (No issues in the changed home screen / test / ARB; the 36 remaining use_null_aware_elements infos + properties/tenants_repo_test unused-param warnings are the pre-existing baseline in unrelated repos/tests) + test (full suite 313 passed, incl. the 3 new) all green.
2026-06-05 | EPIC-09/T-008 | mobile | claude | done | Reusable KMetricCard KPI widget (lib/core/widgets/k_metric_card.dart) for the dashboard/home top summary row — a single metric shown as a tinted icon badge + muted label + large value, with an optional trend pill. Built on the existing KCard surface (white card, radius 22, soft shadow, optional onTap forwarded). Deliberately generic per §15 (knows nothing about collection/occupancy): the caller passes a pre-formatted value string ("78%", "৳71,000") so locale/numeral formatting stays its responsibility, plus an `icon`, `label`, optional `trend` (KMetricTrend.up/down/flat → sage ↑ / rose ↓ / muted →, via Icons.arrow_upward/downward/forward) and optional `trendLabel` (e.g. "12%") beside the arrow, and optional `accent`/`accentBackground` for the icon badge (defaults sage on sage-bg). No trend → no pill rendered. All colors/spacing/radii/fonts from khatir_tokens (sage/rose/butter/muted + spacing/radius scale + AppTextStyles headlineMedium/bodySmall; no proto hex/px). No DB/API/screens/flags — component only. 6 widget tests in test/k_metric_card_test.dart (sized Scaffold harness): renders value+label+icon, renders a ৳amount value with a butter accent (generic, not collection-specific), shows an up trend pill with its label, shows a down trend arrow, renders no trend pill when trend is null, forwards taps via onTap. flutter pub get + analyze (No issues in the new widget/test; the 36 remaining use_null_aware_elements infos + properties/tenants_repo_test unused-param warnings are the pre-existing baseline in unrelated repos/tests) + test (full suite 319 passed, incl. the 6 new) all green.
2026-06-05 | EPIC-09/T-009 | mobile | claude | done | Income-vs-expense chart on the landlord Dashboard (EPIC-09 T-009). Replaced T-006's single-income KLineChart stub in _IncomeExpenseCard (lib/features/dashboard/presentation/screens/dashboard_screen.dart) with a grouped two-series KBarChart: for each of the trailing 6 months of monthly_series, a sage income rod (MonthPoint.collected) beside a rose expense rod (MonthPoint.expense), under a sage/rose series legend (new dashboard_income_series / dashboard_expense_series keys). Both rods share one axis maximum (the peak of either series across the window) so income/expense stay visually comparable; an all-zero window renders the KBarChart empty state (dashboard_chart_empty) rather than flat zero-height bars. Extended the shared T-005 KBarChart to stay a thin token-only fl_chart wrapper while gaining a generic grouped mode: KBarDatum.secondValue (optional second rod), barColors/secondBarColors token-gradient overrides (default sage/sageDk + rose/roseDk), and showValueLabels (grouped mode hides the per-bar top value labels to keep the two-rod axis readable, narrower 9px rods with barsSpace); single-series callers (the collection-rate chart) are unchanged (one 14px sage rod, value labels on). Deviations: the proto reg('dashboard') shows single-series sage bars not a literal income-vs-expense block, so per §8 ("grouped bar, income sage vs expense rose") + §15 ("if bars, use KBarChart") implemented as grouped bars; older dashboard_income_legend/expense_legend keys remain present-but-unused. All colors/spacing/radii from khatir_tokens (no proto hex/px); numerals via BanglaNumerals. New ARB dashboard_income_series + dashboard_expense_series (bn + en) + regenerated app_localizations. Tests: chart_widgets_test.dart gains a grouped two-series case (two rods per group, mapped heights 60/20, top value labels hidden) + a single-series-keeps-one-rod case; dashboard_screen_test.dart asserts the income/expense series legend renders. flutter pub get + gen-l10n + analyze (No issues in the changed chart widget / dashboard screen / tests / ARB; the 36 remaining use_null_aware_elements infos + properties/tenants_repo_test unused-param warnings are the pre-existing baseline in unrelated repos/tests) + test (full suite 321 passed, incl. the 2 new) all green.
2026-06-05 | EPIC-10/T-007 | mobile | claude | done | Flutter Plan & billing screen PlanScreen (lib/features/billing/presentation/screens/plan_screen.dart) at /settings/plan, pushed from the More menu's Plan & billing row (more_screen.dart now context.push(PlanScreen.routePath) instead of the coming-soon snackbar; route registered on the root navigator in app_router.dart), matching the `plan` proto (screens-landlord2.js → reg('plan'); design ledger row built): a cream scaffold ListView with a butter 🎁 "You're on X" banner showing the current tier label + a tenant-usage line (1/2 tenants used, or "N tenants · unlimited" when the limit is null), then the active tier catalogue as upgrade cards — each white card (current tier sage-bg + a sage "Now" chip; the unlimited/top tier ringed sage with a "⭐ BEST VALUE" ribbon) showing the localized tier label, tenant band (min–max / min+ / unlimited), the ৳monthly price (falling back to annual) with a /mo or /tenant/mo suffix (free tiers read "Free"), a "+ NID verify" note when includes_verification, and a sage "Choose this plan" subscribe action on every NON-current tier — closing with the mono "Prices admin-configurable · illustrative" footer. New billing data layer (lib/features/billing/data/): plan_models.dart (PlanTier/PlanSubscription/PlanConfig parsing the committed T-005 /config/public envelope — pricing.tiers[] via the read-only TierSerializer shape {key,label,label_bn,tenant_min,tenant_max,monthly_price,annual_price,includes_verification,included_credits} with DRF decimal-strings→double, + the authenticated subscription block {tier_key,status,tenants_used,tenant_limit,can_verify_nid}; lenient int/double coercion, null tenant_limit/tenant_max = unlimited), billing_repository.dart (fetchPlanConfig → GET /config/public projected to the plan slice; subscribe(tierKey) → POST /billing/subscribe {tier_key} per T-004, payment stubbed server-side), billing_providers.dart (billingRepositoryProvider + planConfigProvider AsyncNotifier with refresh() + subscribeControllerProvider AsyncNotifier whose subscribe() posts then re-reads the config so usage + the current-plan highlight refresh). Tier data comes from /config/public not a separate fetch (§15); subscribe is stubbed — on success the screen snackbars plan_billing_confirm_pending ("We'll confirm once payment clears"), on failure plan_billing_error; the cards disable while any subscribe is in flight. New ApiEndpoints.billingSubscribe. States: loading (sage spinner) / error (common_network_error + retry → planConfigProvider.refresh) / empty (no tiers → plan_empty) / data / subscribing. All colors/spacing/radii/fonts from khatir_tokens (butter/sage/rose/muted + spacing/radius scale; no proto hex/px); numerals localised via BanglaNumerals. ARB plan_title/current/current_banner/usage/usage_unlimited/free/upgrade/choose/best_value/per_month/per_tenant_month/band/band_min/band_unlimited/includes_verification/billing_note/billing_confirm_pending/billing_error/empty (bn template carries placeholder metadata, en the translations). 4 widget tests in test/plan_screen_test.dart (fake BillingRepository overriding billingRepositoryProvider, tall surface): renders all tiers with the current (free) tier's Now chip + 1/2 usage + the best-value ribbon + a subscribe button on non-current tiers only, tapping Choose fires repo.subscribe('bundle_10') once + shows the pending confirmation snackbar, error state retries the read, empty state when no tiers. flutter pub get + gen-l10n + analyze (No issues in the new billing data/screen / more_screen / app_router / test / ARB; the 36 remaining use_null_aware_elements infos + properties/tenants_repo_test unused-param warnings are the pre-existing baseline in unrelated repos/tests) + test (full suite 325 passed, incl. the 4 new) all green.
2026-06-05 | EPIC-10/T-008 | mobile | claude | done | Upgrade prompt (limit reached). New UpgradePrompt bottom sheet (lib/features/billing/presentation/widgets/upgrade_prompt.dart): a friendly modal (drag handle + butter premium icon + localized upgrade_title / upgrade_body) with a sage "Upgrade plan" FilledButton that pops the sheet then GoRouter.pushNamed(PlanScreen.routeName → /settings/plan, T-007) and a muted "Not now" TextButton that dismisses; UpgradePrompt.show(context) presents it isScrollControlled with a transparent scrim and resolves true when the user chose to upgrade. Wired into the shared add-tenant save path: TenantSaveController.saveAndContinue (lib/features/tenants/presentation/controllers/tenant_save_controller.dart — the single save+route action behind OCR/voice/manual intake, EPIC-04 T-016) now branches in its `on ApiException` handler — when errorCode == 'tier_limit_exceeded' it awaits UpgradePrompt.show(context) instead of the bare tenant_save_error snackbar, returns false (caller just clears loading; no partial navigation), otherwise the existing error path is unchanged. To detect the code, added an errorCode field to ApiException (lib/core/network/api_exception.dart) parsed from the canonical backend error envelope {"error":{"code":...}} via ApiException.fromDio (built by the dio error interceptor and preserved by the tenant repository's _asApiException), so presentation branches on a stable machine code, not a message — the same intercept pattern T-009 reuses for feature_requires_upgrade. All colors/spacing/radii/fonts from khatir_tokens (card/butterBg/butterDk/sage/mutedDk/lineDk + spacing/radius scale; no proto hex/px). New ARB upgrade_title/upgrade_body/upgrade_cta/upgrade_later (bn + en) + regenerated app_localizations. 3 widget tests in test/upgrade_prompt_test.dart (GoRouter harness with a trigger button + a stub /settings/plan route): renders the localized title/body/CTA/dismiss, Upgrade pops the sheet + routes to the plan stub + resolves true, Not now closes without navigating + resolves false. flutter pub get + gen-l10n + analyze (no issues in the new widget / api_exception / tenant_save_controller / test / ARB; the 36 remaining use_null_aware_elements infos + properties/tenants_repo_test unused-param warnings are the pre-existing baseline in unrelated repos/tests) + test (full suite 328 passed, incl. the 3 new) all green.
2026-06-05 | EPIC-13/T-007 | mobile | claude | done | Wired the real FeatureFlag system into mobile (EPIC-13 T-007). New typed FlagsProvider (lib/core/config/flags_provider.dart): a Flags value object with isEnabled('flag_key', {orElse}) over the key→enabled map, plus flagsProvider (a Provider derived from the live publicConfigProvider) so features ask for a flag key instead of reading bespoke config fields — newly flagged behaviour wires up with no provider changes, and because T-002 busts the /config/public cache on every admin toggle the client re-read propagates flips within the backend <60s budget. Generalised PublicConfig (lib/core/config/public_config_provider.dart): the prior single voiceTenantEntry bool is replaced by a generic flags Map<String,bool> parsed from the /config/public `flags` block via _parseFlags (tolerates bool + 'true'/'false' string-encoded values, drops unparseable entries so the per-flag default applies, empty map when unseeded/missing); voiceTenantEntry kept only as a thin convenience getter (flags['voice_tenant_entry'] ?? true) and a PublicConfig.withVoice factory added for fixtures. Replaced EPIC-04 T-006's placeholder flag read in the add-tenant chooser (add_tenant_screen.dart) and the defensive deep-link gate in voice_fill_screen.dart — both now gate on flagsProvider.isEnabled('voice_tenant_entry', orElse: true), keeping the "default on while loading/failed" behaviour. No DB/API/screens; reads /config/public; no new flags (meta task). 11 new tests in test/flags_provider_test.dart (Flags.isEnabled on/off/absent/orElse/present-wins, PublicConfig flag-block parsing incl. string bools + dropped non-bools + empty block, voiceTenantEntry getter default + reflect, flagsProvider exposes flags after the future resolves + falls back to empty while loading); existing add_tenant_test/voice_fill_test fixtures updated to PublicConfig.withVoice. flutter pub get + analyze (no issues in the new/changed flags provider / public config / add-tenant / voice-fill / tests; the 36 remaining use_null_aware_elements infos + properties/tenants_repo_test unused-param warnings are the pre-existing baseline in unrelated files) + test (full suite 339 passed, incl. the 11 new) all green.
2026-06-05 | EPIC-06/T-008 | mobile | claude | done | Lease create/edit form LeaseFormScreen (lib/features/leases/presentation/screens/lease_form_screen.dart) at /lease/new + /lease/:id/edit, launched from unit detail in unit context (?unit= query param). Cream scaffold form following the shared Khatir form composition: tenant picker seeded from unitTenantsProvider, rent (★ required, prefilled from unitProvider rent default), optional advance, start/end date fields (platform date picker, end>start guard), due-day stepper (1-28, defaults config kDefaultDueDay=5), and two pill actions — outlined Save (draft) + filled sage Save & activate. Save validates form + tenant + dates + rent, runs LeaseRepository.createLease (or updateLease in edit mode) then optionally leaseControllerProvider(id).activate() so the server generates the rent schedule, snackbars lease_saved/lease_activated, pops the created lease back to the unit. Unit-already-has-active-lease (§15) surfaced as friendly lease_active_exists snackbar on 400/409; other failures → lease_save_error. onProceed test seam (null=real flow, else emits LeaseFormDraft). States: data/validation(inline+snackbar)/saving(CTA spinner)/error. All colors/spacing/radii/fonts from khatir_tokens (no proto hex/px). ARB lease_* keys bn+en. Gate: lease_form_test 6/6 pass; flutter analyze info-level lints are the pre-existing repo baseline in unrelated files (none from this screen).
2026-06-05 | EPIC-06/T-009 | mobile | claude | done | Lease section on unit detail (filled the EPIC-03 T-013 placeholder). New UnitLeaseSection widget (lib/features/leases/presentation/widgets/unit_lease_section.dart) reads the T-007 unitLeaseProvider(unitId) for the unit's current (active) lease + embedded tenant summary: a 404/no-lease (and any read error) surfaces via AsyncValue.error and is treated as the create-lease empty state (📄 + unit_lease_none/unit_lease_none_body + a sage "Create lease" CTA → pushNamed(LeaseFormScreen.routeName, ?unit=<id>), which pops back so the section re-fetches the freshly created/activated lease). With an active lease it renders a tappable soft card (→ LeaseDetailScreen /lease/:id): rose-initial tenant avatar + name + NID-verified/not-verified sub-line (matched→unit_tenant_verified else unit_tenant_unverified), a status chip (sage for active, reusing leaseStatusLabel), label/value rows for monthly rent (unit_rent_per_month), term (termRange, reusing the lease-list helper) and next due, plus a sage "Request rent" CTA → RentRequestScreen (rentRequest route, ?lease=<id>). Next-due is computed from leaseScheduleProvider(leaseId): the earliest non-paid (pending/requested/overdue) row by due date (then period), shown as ৳amount · YYYY-MM (unit_next_due_value) or unit_next_due_none when every period is settled. unit_detail_screen.dart's _TenantSection now frames UnitLeaseSection between the kept "Tenant & lease" heading and the add-tenant CTA; the // TODO(EPIC-06) marker + the old 🤝 empty card are gone. States: loading (inline spinner card) / no-lease empty / active-lease data; each lease/schedule read is unit-/lease-scoped server-side. Deviations: EPIC-07 T-011 (RentRequestScreen) is already on this branch, so the rent CTA routes to it directly instead of the §8 placeholder; the add-tenant CTA stays (a lease needs a tenant first), only the empty card was swapped per the TODO. All colors/spacing/radii/fonts from khatir_tokens (card/sage/sageBg/sageDk/rose/roseBg/roseDk/line/muted + spacing/radius scale; no proto hex/px); numerals via BanglaNumerals. New ARB unit_lease_active/unit_next_due/unit_next_due_value/unit_next_due_none/unit_create_lease/unit_lease_request_rent/unit_lease_none/unit_lease_none_body/unit_lease_term/unit_tenant_verified/unit_tenant_unverified (bn + en) + regenerated app_localizations. 6 widget tests in test/unit_lease_section_test.dart (fake LeaseRepository overriding leaseRepositoryProvider, GoRouter harness with form/detail/rent-request stub routes): active lease shows tenant+verified+active chip+request CTA and the earliest unpaid period (2025-02 not the paid 2025-01) with unit-/lease-scoped reads asserted; all-paid → unit_next_due_none; no lease → create-lease empty state (no request CTA); create-lease CTA routes to the lease form; request-rent CTA routes to the rent-request screen; tapping the card opens lease detail. Updated test/unit_detail_test.dart to override leaseRepositoryProvider with a 404ing repo and assert the lease empty state (unit_lease_none/unit_create_lease) alongside the add-tenant CTA. flutter pub get + analyze (no issues in the new widget / unit-detail screen / tests / ARB; the 36 remaining use_null_aware_elements infos + properties/tenants_repo_test unused-param warnings are the pre-existing baseline in unrelated repos/tests) + test (full suite 345 passed, incl. the 6 new) all green.
2026-06-05 | EPIC-07/T-010 | mobile | claude | done | typed rent data layer (models/repo/providers + mocked tests); analyze 0 errors, 345 tests pass
2026-06-05 | EPIC-17/T-001 | backend | claude | done | New khatir.verification app: append-only VerificationLog (tenant FK, requested_by FK, result enum matched/not_matched/error, opaque provider_ref, consent_record FK) — boolean-only, deliberately NO raw-EC columns. AppendOnlyManager/QuerySet + instance delete() raise RuntimeError (immutable audit trail), mirroring compliance.ConsentRecord. VerificationResult TextChoices (enums.py) shares wire values 1:1 with tenants.VerificationStatus. Added Tenant.apply_verification_result(result, save=True): maps the result onto verification_status (matched→MATCHED + stamps verified_at; not_matched→NOT_MATCHED; error→ERROR; unknown→ValueError) and saves only the changed fields. Read-only admin (result/provider_ref only, no add/change/delete). Registered khatir.verification in INSTALLED_APPS; migration 0001_initial (plain CreateModel, reversible) inside the app dir. 8 tests in test_models.py: create, no_raw_data_columns (asserts column set excludes name/dob/address/photo/nid/payload and includes only the audit-safe surface), three status transitions, unknown-result ValueError, append-only instance + queryset delete. Gate: 8/8 verification tests pass, ruff clean, makemigrations --check = no changes. Pre-existing unrelated failures in compliance/test_audit_log_endpoint.py (404 on /admin/api/audit-log, confirmed failing on the clean tree) do not count against the gate.
2026-06-05 | EPIC-17/T-002 | backend | claude | done | EC verification provider abstraction (Django-side, mirrors EPIC-14 pattern). New khatir.verification.providers package: VerificationProvider ABC (verify(nid,name,dob)->VerificationOutcome) in base.py; ECVerificationProvider concrete impl (ec_provider.py) — thin requests-based HTTP client, creds/endpoint/DPA-ref from settings, injectable session for tests. VerificationProviderError raised on misconfiguration (missing DPA ref or endpoint → refuses to send PII, PDPA gate); transport/HTTP/JSON/missing-boolean failures normalize to an error outcome instead of propagating. VerificationOutcome DTO (dto.py, frozen dataclass) is boolean-only: reuses the T-001 VerificationResult enum (matched/not_matched/error) + opaque provider_ref + error_detail — the raw EC vendor payload (name/dob/address/photo) is read for the match boolean then discarded, never copied onto the DTO or stored. Added EC_VERIFICATION_ENDPOINT_URL/API_KEY/DPA_REF/TIMEOUT settings (config/settings/base.py). Chose direct Django-side provider over the AI gateway (documented in §15) to keep the PII data-processing surface narrow; interface stays swappable per the EPIC-14 pattern. 11 tests in test_provider.py (mocked vendor): ABC conformance, matched, not_matched, HTTP-error, transport-error, non-JSON, missing-boolean, missing-DPA-refuses (no request attempted), missing-endpoint-refuses, raw-data-never-leaks (outcome fields == {result,provider_ref,error_detail}; PII absent from repr) + DPA/bearer sent to vendor, factory helpers. Gate: 11/11 provider tests pass, ruff clean, makemigrations --check = no changes (no DB/migrations in this task), mypy clean on the new package. Pre-existing unrelated failures in compliance/test_audit_log_endpoint.py (4 × 404 on /admin/api/audit-log, confirmed failing on the clean tree via git stash) do not count against the gate.
2026-06-05 | EPIC-17/T-003 | backend | claude | done | Consent capture for NID/EC verification (PDPA gate). New khatir.verification.consent: record_verification_consent(tenant, by_user) writes an append-only compliance.ConsentRecord with consent_type=ConsentType.PDPA_NID_VERIFICATION (the landlord/operator attesting they hold the tenant's permission) and returns it for T-004 to link onto the VerificationLog it writes; has_valid_consent(tenant) returns True iff a non-revoked, non-expired PDPA_NID_VERIFICATION consent is reachable for the tenant via VerificationLog.consent_record (verification_logs__tenant) — expiry checked with expires_at IS NULL OR expires_at > now(). Deviation: spec note said consent_type='nid_verification' but the EPIC-16 enum's member is PDPA_NID_VERIFICATION ('pdpa_nid_verification'); used the enum (source of truth). ConsentRecord has no tenant FK so scoping goes through the verification-log reverse relation (documented in §14/§15). 7 tests in test_consent.py: consent recorded (type/user/granted_at/no-revoke), valid-consent check (false until linked, then true), no-consent invalid, revoked invalid, expired invalid, future-expiry valid, scoped-to-tenant (consent for tenant A does not satisfy tenant B). Gate: 7/7 consent tests pass, ruff clean, makemigrations --check = no changes (no DB/migrations in this task), mypy clean on the new module. Pre-existing unrelated failures in compliance/test_audit_log_endpoint.py (4 × 404 on /admin/api/audit-log, confirmed failing on the clean tree with my files removed) do not count against the gate.
2026-06-05 | EPIC-17/T-004 | backend | claude | done | Verify endpoint orchestration (consent → check → Matched/Not Matched). New khatir.verification.views: VerifyView (POST /api/v1/tenants/{id}/verify) runs the fail-fast gate order per §15 — tier gate (billing.check_can_verify; free/non-verification tier → feature_requires_upgrade 402 before any tenant resolution or paid vendor call), flag gate (nid_verification_enabled, default on, via verification.flags.is_feature_enabled → feature_disabled 403 when off), owner-scope (Tenant.objects.for_user → 404 for a foreign/unknown tenant, never 403), then services.verify_tenant. VerificationView (GET .../verification) returns the last boolean-only result ({result,date}, or {result:null,date:null} when never run). services.verify_tenant(actor, tenant): captures consent (T-003 record_verification_consent), decrypts the NID via the single audited Tenant.get_nid path (audited as tenant.nid.decrypt — raw NID stays a local, never written to the audit payload/log/response), calls the swappable EC provider (T-002, via get_verification_provider() factory) with nid+name+dob, then in one atomic write appends an immutable VerificationLog (T-001) linked to the consent and transitions Tenant.verification_status (apply_verification_result), finally audits verification.verify with only {tenant_id,result,provider_ref}. VerificationResultSerializer exposes only {result, date(=created_at)} — no raw EC field, no NID, provider_ref intentionally not serialized. flags.py mirrors tenants.flags (global FeatureFlag read, default on). Registered verification.urls in config/urls.py under api/v1/. 13 tests in test_verify_api.py (EC provider mocked): matched (+status MATCHED+verified_at+linked PDPA consent), not_matched (+status, no verified_at), decrypted nid+name+dob passed to provider (raw NID absent from decrypt audit), verify audited (no NID leak), free-tier blocked 402 (provider never called, no log), flag-off blocked 403 (provider never called, no log), foreign-tenant 404, role gate (tenant role 403), unauthenticated, consent recorded on verify, GET after verify, GET null when never run, GET foreign 404. Gate: 13/13 new tests pass, full suite 1293 passed, ruff clean, makemigrations --check = no changes (no DB/migrations in this task). Pre-existing unrelated failures in compliance/test_audit_log_endpoint.py (4 × 404 on /admin/api/audit-log, confirmed failing on the clean tree via git stash) do not count against the gate.
2026-06-05 | EPIC-17/T-005 | backend | claude | done | Seed EC verification config + nid_verification_enabled flag. New featureflags/0003_seed_nid_verification_flag migration seeds the nid_verification_enabled global FeatureFlag enabled=True (default on — the EC-verification kill-switch; admin flips OFF if the EC API or a legal issue arises). Like the baseline 0002_seed_flags it is test-isolated (no-op under config.settings.test so it never pollutes EPIC-13 flag-endpoint tests or EPIC-17 verify-API tests that assert the empty-table default-on fallback); update_or_create keyed on the unique key (idempotent), reverse deletes exactly that key (reversible); literal "global" scope (migrations never import app enums). The three EC SystemConfig keys (ec_verification_provider/ec_verification_endpoint/ec_verification_dpa_reference) are seeded by core/0014_seed_verification_config — all three text + intentionally empty (get_or_create so a re-run never clobbers an admin-entered DPA ref; reverse only deletes keys still empty). ec_verification_dpa_reference is REQUIRED before live use (PDPA): the T-002 provider refuses to call the vendor without it. 6 tests in verification/tests/test_seed_verification_config.py: all three config keys present as text rows with descriptions; DPA reference seeded empty with DPA-mentioning description; flag seeded global+enabled+described; flag seed idempotent (count==1 after re-run); flag seed reversible (count==0 after reverse); auto-run skipped in test DB AND is_feature_enabled still resolves on via the default-on fallback with no row. Gate: 6/6 new tests pass, full suite green except the pre-existing compliance audit-log 404s (excluded), ruff clean, makemigrations --check = no changes. Migrations chain in their owning apps: the flag seed off featureflags/0002, the config seed off core/0013.
2026-06-05 | EPIC-17/T-009 | backend | claude | done | Surface VerificationLog (T-001) entries in the EPIC-16 compliance console — read-only, result + date + who, no raw EC data. New GET /admin/api/verification-logs in khatir.compliance (same app/pattern/role-gate as consent-records & audit-log): AdminJWTAuthentication + IsAdminAuthenticated + IsComplianceAdmin (audit section = super/compliance). VerificationLogSerializer is a deliberately narrow read-only projection exposing only {id, tenant, requested_by, result, created_at} — the opaque provider_ref vendor token is NOT serialized and the model has no raw-EC columns at all, so no name/dob/address/photo/nid can ever leak through the compliance viewer. VerificationLogListView paginates (StandardPageNumberPagination envelope) over VerificationLog.objects.all() (-created_at default ordering) with additive filters: tenant, requested_by, result (VerificationResult exact), date_from, date_to (created_at gte/lte) — mirrors the audit-log filter style. Route appended additively to compliance/urls.py. 13 tests in compliance/tests/test_verification_log_endpoint.py: list + pagination envelope, read-only row shape, no-raw-data/no-provider_ref assertion (exact key set + secret token absent from response body + forbidden EC field names absent), filter by tenant/requested_by/result, created-at date range (forced via .objects.filter().update — note VerificationLog is append-only so out-of-band created_at set is done through the queryset), anonymous rejected, super+compliance allowed, ops/finance/support denied 403. No DB/migrations in this task. Gate: 13/13 new tests pass, ruff clean, makemigrations --check = no changes. Pre-existing unrelated failures in compliance/test_audit_log_endpoint.py (4 × 404 on /admin/api/audit-log, confirmed failing on the clean tree via git stash) do not count against the gate.
2026-06-05 | EPIC-18/T-001 | backend | claude | done | New khatir.leasedocs app (registered in INSTALLED_APPS) owning the LeaseDocument model + migration 0001_initial for EPIC-18 AI lease generation. LeaseDocument (SoftDeleteModel) fields: lease FK (CASCADE, related_name="documents"), content_json (JSONField default=dict, blank) holding the ordered clause set, pdf_ref (CharField), generated_by (FK AUTH_USER_MODEL SET_NULL), model_used (CharField), generated_at (nullable DateTime), status (CharField choices LeaseDocumentStatus draft/final, default draft); soft-delete (objects/all_objects) + index on (lease,status). Required-clause guarantee: REQUIRED_CLAUSE_KEYS=(parties,premises,rent,advance,term,disclaimer) enforced via missing_required_clauses()/validate_required_clauses() and a clean() hook — an empty draft is allowed but any partially-filled content_json missing a mandatory clause (incl. the "not legal advice" disclaimer) raises ValidationError. New enums.py (LeaseDocumentStatus), admin.py (LeaseDocumentAdmin), tests/factories.py (LeaseDocumentFactory with a full clause set) + tests/test_models.py (21 tests: create/defaults/final-status/optional pdf_ref+generated_at/str, CASCADE-with-lease via hard_delete, related_name, soft-delete hides from default manager, full-clause passes, empty-content allowed, parametrized missing-each-required-clause fails, disclaimer required, empty-string counts as missing, non-dict treated as all-missing). Gate: leasedocs 21/21 pass, ruff clean, makemigrations --check = no changes. Full suite 1272 passed; the only 4 failures (compliance audit-log /admin/api/audit-log 404s) are pre-existing and reproduce with this work stashed — unrelated to T-001.
2026-06-05 | EPIC-18/T-002 | backend | claude | done | Compliant base lease clause scaffold (khatir/leasedocs/scaffold.py) for EPIC-18 AI lease generation. CLAUSE_SCAFFOLD is the ordered DNCC/DSCC-aware base structure — parties, premises, rent, advance, term, obligations, termination, dispute, disclaimer — each a ClauseSpec(title_en, title_bn, body, order, required) with bilingual (Bangla+English) titles and {{placeholder}}-token bodies the AI gateway fills; the disclaimer body carries the bilingual "not legal advice"/দাবিত্যাগ text (DEFAULT_DISCLAIMER_EN/BN) and renders last (order 999). New LeaseDocumentClauseKey TextChoices enum is the single source of truth for clause keys; models.REQUIRED_CLAUSE_KEYS now references it (parties/premises/rent/advance/term/disclaimer = mandatory subset; obligations/termination/dispute seeded but optional). build_scaffold_content() returns a fresh mutable scaffold dict that already passes the required-clause guarantee; ensure_required_clauses() is the runtime guarantee — merges AI/landlord clauses over the scaffold, back-filling any missing/empty-body required clause (and seeding absent optional sections) while preserving AI-filled bodies, extra caller-only clauses (appended after scaffold keys in order), and legacy string-bodied clauses. Strengthened missing_required_clauses() to treat a scaffold-shaped dict clause with empty/blank body as missing (string clauses from T-001 still validate unchanged). Tests: tests/test_scaffold.py (19) — scaffold covers all epic sections, keys valid enum + unique, required ⊆ scaffold + flagged required, bilingual titles/body present, disclaimer bilingual+last, build returns independent copy satisfying the guarantee, ensure back-fills missing/empty/None, preserves AI bodies + extra clauses + ordering + legacy strings. Gate: leasedocs 40/40 pass, ruff check khatir/leasedocs clean, makemigrations --check = no changes (no model field change, no migration). The 4 compliance audit-log /admin/api/audit-log 404 failures are pre-existing (reproduce with this work stashed) and unrelated to T-002.
2026-06-05 | EPIC-18/T-003 | backend | claude | done | AI lease generation service (khatir/leasedocs/services.py) for EPIC-18. generate_lease_document(lease, generated_by=None): builds a structured gateway payload via build_lease_prompt() — {category: "lease", facts, scaffold} where facts are pulled from the lease's related rows (landlord/tenant names, premises = building name+unit label+address, city_corporation from building.area, rent/advance as strings, ISO start/end dates; missing relations degrade to "") and scaffold = build_scaffold_content() (T-002) so the model knows the clause keys + placeholders to fill. Calls the AI gateway (EPIC-14.T-007 call_gateway with AICategory.LEASE) — all vendor routing/fallback/usage-logging stays in the gateway; this service only sends the payload and reads the normalised AIGatewayResult. Parses the returned clause map (_coerce_clauses): accepts both scaffold-shaped dict clauses and bare body strings (wrapped into the scaffold's title/order), and tolerates the clause map arriving either under data.clauses or directly under data; unknown keys pass through, other shapes drop to be back-filled. ensure_required_clauses() then guarantees the required set — any required clause the AI omitted (incl. the "not legal advice" disclaimer) falls back to the scaffold placeholder body — and the row is full_clean()'d (validate_required_clauses) before save as a status=draft LeaseDocument recording model_used (from gateway) + generated_at (now) + generated_by (arg or lease.landlord). Wrapped in transaction.atomic so a gateway failure (AIGatewayError) propagates unchanged and writes no partial document. No DB/model change → no migration. Tests: tests/test_services.py (8, call_gateway mocked) — prompt carries lease facts+scaffold; gateway invoked once for LEASE category and a draft is stored with parsed bodies; bare-string clause bodies wrapped with scaffold titles; omitted required clauses (disclaimer+rent) back-filled to scaffold placeholders; empty gateway data yields full scaffold with disclaimer present; top-level clause map tolerated; explicit generated_by recorded; AIGatewayError propagates and rolls back (no document). Gate: leasedocs suite + new test_services.py green (8/8), ruff check . clean, makemigrations --check = no changes. The 4 compliance audit-log /admin/api/audit-log 404 failures are pre-existing (reproduce with this work stashed) and unrelated to T-003.
2026-06-05 | EPIC-18/T-004 | backend | claude | done | Three owner-scoped, audited lease-document endpoints wired under /api/v1/ (config/urls.py): POST /leases/{id}/generate-document (GenerateLeaseDocumentView) gated by ai_lease_enabled flag (default on → 403 feature_disabled when off) + paid-tier check_can_verify (free → 402 feature_requires_upgrade before any gateway call), scopes lease via Lease.objects.for_user (foreign → 404), calls T-003 generate_lease_document → 201 draft; PATCH /lease-documents/{id} (LeaseDocumentEditView) draft-only (final → 409 DocumentLockedError), LeaseDocumentEditSerializer(clauses DictField) → edit_lease_document merges onto content_json + re-asserts required-clause guarantee via full_clean (blanking disclaimer → 400), audited leasedocument.edit; POST /lease-documents/{id}/pdf (LeaseDocumentPdfView) renders deterministic dependency-free PDF (pdf.py render_lease_pdf, clauses in scaffold order, disclaimer verbatim) → store_encrypted → signed_url, audited leasedocument.pdf. All three resolve foreign/unknown ids to 404 (scoped through visible leases, never reveal existence). New: views.py, urls.py, serializers.py, pdf.py, flags.py, tests/test_api.py (13), tests/test_pdf.py; services.py extended (edit_lease_document, render_lease_document_pdf, RenderedLeasePdf). Gate: pytest 1312 passed (61 leasedocs incl. new api+pdf), ruff check . clean, makemigrations --check = no changes. The 4 compliance audit-log /admin/api/audit-log 404 failures are pre-existing + unrelated to T-004.
2026-06-05 | EPIC-18/T-005 | backend | claude | done | Seeded the EPIC-18 AI-lease config + feature flag. New core migration 0013_seed_lease_config (chains off 0012_seed_compliance_config) seeds three SystemConfig text rows (idempotent update_or_create, reversible): lease_template_version="1.0", lease_disclaimer_text_en="This is an AI-generated draft, not legal advice. Consult a lawyer." and its Bangla counterpart lease_disclaimer_text_bn — SystemConfig has no JSON type (int/money/text/bool only) so the bn/en disclaimer is two text rows; the mandatory disclaimer clause is still always injected by leasedocs/scaffold.py regardless, these keys just let ops tune public wording. New featureflags migration 0003_seed_ai_lease_flag (chains off 0002_seed_flags) seeds the ai_lease_enabled global flag enabled=True (default on; admin flips OFF to kill EPIC-18 generation, matching leasedocs/flags.py fallback), following the same test-isolation convention as 0002 (auto-run no-op under config.settings.test so it never pollutes EPIC-13/leasedocs flag tests; the seed-test calls seed_flag with schema_editor=None). Tests: core/tests/test_seed_lease_config.py (3) verify the three rows + types + values via SystemConfig and get_config and that the disclaimer states "not legal advice"; featureflags/tests/test_seed_ai_lease_flag.py (4) verify enabled/global/idempotent/reversible + that auto-run is skipped in the test DB. Gate: 7/7 new tests pass; ruff check . clean; makemigrations --check = no changes. The only 4 full-suite failures (compliance audit-log /admin/api/audit-log 404) are pre-existing and unrelated to T-005.
2026-06-05 | EPIC-19/T-001 | backend | claude | done | Tenant-account link + tenant-scoped read foundation (EPIC-19 T-001). New tenants/tenant_account.py is the single source of truth turning an authenticated tenant-role User back into the data they may read, all keyed off the existing Tenant.linked_user FK (EPIC-04) — no new table / TenantAccount model. tenant_for_user(user) resolves the one linked Tenant identity record (None for anon / non-tenant role / unlinked tenant; linked_user is a single FK so the mapping is unambiguous); tenants_for_user/leases_for_tenant_user are the row-level scoping helpers (04_coding_conventions §3 — empty queryset, never a foreign row) exposing the tenant's own Tenant record and every Lease they hold/held (so /me receipts+history stay reachable); active_lease_for_user returns the current-view single ACTIVE lease (most recent by start_date, None when only ended/terminated history). New IsLinkedTenant permission in tenants/permissions.py gates the /me/ self-service surface — a tenant with a linked record passes, an unlinked tenant or any non-tenant role is denied (the endpoint half of the two-layer isolation contract; the /me/ endpoints land in T-002). Deviations: reused linked_user (no TenantAccount table, no migration); tenant scoping lives as explicit free functions in tenant_account.py rather than a Lease.for_user(tenant) branch, keeping the tenant read path one auditable place. 8 tests in tenants/tests/test_tenant_scope.py: resolves own; unlinked→None; non-tenant role→None even if a record is linked; anonymous→None/empty; cannot_see_others (tenant A's helpers never surface tenant B's active lease, and vice-versa); active lease is the current view while ended history stays in the broader scope; no-active→None; IsLinkedTenant true for linked tenant, false for unlinked tenant + landlord. Gate: pytest — 8 new pass, ruff check . clean, makemigrations --check "No changes detected"; the 4 pre-existing compliance/test_audit_log_endpoint failures (admin audit-log endpoint, unrelated — proven failing with this task's code stashed out) do not count against the gate.
2026-06-05 | EPIC-19/T-002 | backend | claude | done | Tenant self-service read endpoints (EPIC-19 T-002). New tenants/me_views.py adds three read-only APIViews under /api/v1/me/, all gated by IsLinkedTenant and reading exclusively through the T-001 tenant_account scoping helpers so a tenant reaches only their own rows (a missing scope is a P0 bug, 04_coding_conventions §3): GET /me/lease → active_lease_for_user (the current ACTIVE lease, serialized with the existing LeaseSerializer; 404 when none), GET /me/rent → a {schedule, requests} payload over leases_for_tenant_user (RentSchedule rows + RentRequest asks across every lease the tenant holds, reusing RentScheduleSerializer + RentRequestSerializer), GET /me/receipts → confirmed rent.Payment rows scoped via rent_request__lease__in=<own leases>, select_related, newest-verified-first. New tenants/me_serializers.py adds a read-only ReceiptSerializer over Payment (the rent app only serialized RentRequest, not Payment) projecting id/rent_request_id/lease_id/period/amount/receipt_ref/verified_at; ids as strings per convention. Reuses existing leases/rent pipelines + serializers — no duplicated proof/maintenance logic. Routes registered in tenants/urls.py before the router so /me/* never collide with the tenants viewset. Deviations: receipts modelled as verified Payments (no separate Receipt model); /me/rent is a two-key payload for one round-trip; plain APIViews not a viewset to keep the tenant read surface one auditable place. 11 tests in tenants/tests/test_me_endpoints.py via APIClient with a real authenticated tenant: /me/lease returns own active lease, 404 when only ended history, never another tenant's lease, 403 for unlinked tenant + non-tenant role; /me/rent returns own schedule+requests and excludes other tenants' rows, 403 unlinked; /me/receipts returns own payments with period/amount/receipt_ref, excludes other tenants' payments, 403 unlinked. Gate: pytest — 11 new pass, ruff check . clean, makemigrations --check "No changes detected"; the 4 pre-existing compliance/test_audit_log_endpoint failures (admin audit-log endpoint, unrelated — proven still failing with this task's code stashed out) do not count against the gate.
2026-06-05 | EPIC-19/T-003 | backend | claude | done | In-app pay endpoint (EPIC-19 T-003). New MeRentPayView (tenants/me_views.py) serves POST /api/v1/me/rent/{id}/pay — the logged-in tenant submits a bKash/Nagad txn id, a note, or a screenshot for one of their OWN rent requests, gated by IsLinkedTenant and scoped via leases_for_tenant_user so {id} only resolves inside the tenant's own leases (a foreign / unknown request id is a 404, never reachable — 04_coding_conventions §3). Per §3 (reuse, no duplication) the proof-submit core was extracted out of rent/web_views.submit_proof into rent.services.submit_payment_proof (create PaymentProof + advance sent→proof_submitted, never regressing a verified/rejected request); the public web-link view now calls that same function, so the web link and the in-app endpoint feed one PaymentProof pipeline. New InAppProofSerializer (tenants/me_serializers.py) validates the body (≥1 of txn_id/note/screenshot, 8 MiB screenshot cap) and the view maps it with the web page's precedence (screenshot→bkash_txn→note); screenshots stored via the existing encrypted storage seam. Returns 201 with the updated RentRequestSerializer body. Route me/rent/<int:pk>/pay registered in tenants/urls.py. Deviations: screenshot field is DRF FileField not ImageField (Pillow isn't a project dep; web flow stores raw bytes too). 9 tests in tenants/tests/test_me_pay_endpoint.py: txn/note/screenshot create the right proof and advance to proof_submitted, empty body→400, another tenant's request id→404 (no proof created), unknown id→404, unlinked tenant + non-tenant role→403, re-submit doesn't regress a verified request; the 9 EPIC-07 web-proof tests still pass after the refactor. Gate: pytest — 9 new + 9 web-proof pass, ruff check . clean, makemigrations --check "No changes detected"; the 4 pre-existing compliance/test_audit_log_endpoint failures (admin audit-log endpoint, unrelated — proven still failing with this task's code stashed out) do not count against the gate.
2026-06-05 | EPIC-19/T-013 | backend | claude | done | Seed tenant_app_enabled feature flag, default on (EPIC-19 T-013). New data migration featureflags/0003_seed_tenant_app_flag.py chains off the featureflags app's latest migration 0002_seed_flags and update_or_creates a single FeatureFlag(key="tenant_app_enabled", scope="global", enabled=True, description="Tenant-facing mobile app (EPIC-19)."), mirroring the established 0002_seed_flags pattern: enabled=True = feature ON (admin flips False to kill the tenant app), idempotent (update_or_create on the unique key), reversible (reverse deletes exactly that key), no app-code imports (literal "global" wire value), and a no-op under config.settings.test so it never pollutes the EPIC-13 endpoint tests that assert an empty flags table. 4 tests in featureflags/tests/test_seed_tenant_app_flag.py invoke seed/unseed directly (schema_editor=None bypasses the test-mode skip): flag seeded enabled+global, idempotent re-run keeps one row, reverse removes it, and auto-run is skipped in the test DB. Gate: pytest — 4 new pass, ruff check . clean, makemigrations --check "No changes detected"; the 4 pre-existing compliance/test_audit_log_endpoint failures (admin audit-log 404, unrelated) do not count against the gate.
2026-06-05 | EPIC-19/T-004 | backend | claude | done | POST /api/v1/me/maintenance — tenant reports maintenance in-app on their own unit (EPIC-19 T-004). MeMaintenanceView (plain APIView gated by IsLinkedTenant) in tenants/me_views.py resolves the unit + active lease server-side via the T-001 active_lease_for_user helper (no unit_id trusted from the client; no active lease => 404, the P0 scope contract) and feeds the SAME khatir.maintenance.services.create_maintenance_request pipeline as the landlord surface (audit + open status) — no maintenance logic duplicated. New MeMaintenanceCreateSerializer (description, optional category defaulting to OTHER, optional photo_ref) in tenants/me_serializers.py; route mounted at me/maintenance in tenants/urls.py. 7 tests in tenants/tests/test_me_endpoints.py: creates on own unit, defaults category to other, 404 when no active lease, always scoped to own unit, requires description, unlinked-tenant denied, non-tenant-role denied. Gate: pytest 1290 passed (the 4 pre-existing compliance/test_audit_log_endpoint 404 failures, unrelated, do not count), ruff check . clean, makemigrations --check 'No changes detected'.
2026-06-05 | EPIC-20/T-001 | backend | claude | done | Warning model + migration (EPIC-20). New khatir.warnings app: Warning(SoftDeleteModel) with PROTECT FKs to leases.Lease / tenants.Tenant / accounts.User(landlord), WarningType enum (late_rent/lease_violation/noise/other), reason text, auto issued_at, nullable notice_ref (T-003 PDF) + acknowledged_at; indexes (landlord, issued_at) and (lease). Strictly relational to one lease — NO shared/global/cross-landlord structure (§15). WarningManager.for_user scopes to the issuing landlord (P0 row-level isolation); foreign warnings invisible. 0001_initial migration, admin, WarningFactory, 5 model tests (create, default type=other, for_user landlord isolation, anonymous sees nothing, soft-delete hidden). Registered in settings. Gate: warnings tests 5/5 pass, ruff clean, makemigrations --check clean. Pre-existing unrelated failures (compliance audit-log endpoint 404s; S3/botocore in dmpforms/rent) confirmed not caused by this task.
2026-06-05 | EPIC-20/T-002 | backend | claude | done | Warning issue + list endpoints at POST/GET /api/v1/leases/{id}/warnings (khatir/warnings/{flags,serializers,services,views,urls}.py; mounted in config/urls.py). warnings_feature kill-switch checked FIRST on every request (off → 403 feature_disabled, before any read/write; defaults live when unseeded per EPIC-13.T-004 convention enabled=True). Lease resolved via Lease.objects.for_user → foreign/unknown lease 404 (never reveals existence, no cross-landlord leak). Issue: landlord taken from request.user, tenant copied from the lease (never client-supplied), warning_type/reason only; atomic create + audit warning.issue (masked snapshot). List: Warning.objects.for_user(landlord).filter(lease=...) — own warnings only. WarningCreateSerializer rejects blank reason; reads serialize ids as strings, full WarningSerializer fields read-only. 9 API tests (test_warning_api.py): issue (201, server-derived landlord/tenant, audited), default type=other, blank-reason 400, list own-only, kill-switch off blocks both verbs + writes nothing, kill-switch on works, cross-landlord 404 both verbs, requires auth. Gate: warnings 22/22 (13 incl. T-001 models) pass, full suite 1264 passed / ruff clean / makemigrations --check clean. The 4 compliance audit-log endpoint 404 failures are PRE-EXISTING (confirmed by stashing config/urls.py — they fail identically; route owned by another epic, not present in this worktree) and unrelated to T-002, plus the noted S3/botocore dmpforms/rent failures.
2026-06-05 | EPIC-20/T-003 | backend | claude | done | Warning-notice PDF at POST /api/v1/warnings/{id}/notice (khatir/warnings/notice.py + generate_notice in services.py + WarningNoticeView in views.py + route in urls.py). Reuses EPIC-05 PDF seam (dmpforms.pdf._render_pdf) — no second PDF stack — rendering parties (landlord/tenant names), warning type label, reason, issued date, and the mandatory legal DISCLAIMER (§15: private notice, not a legal judgment). Reuses EPIC-04 storage seam: store_encrypted(kind="pdf") → persists opaque key on Warning.notice_ref → returns time-limited signed_url (object never public-readable). Owner-scoped via Warning.objects.for_user (foreign warning → 404, no cross-landlord read) and warnings_feature kill-switch checked FIRST (off → 403 feature_disabled, nothing generated). Audited warning.notice. 5 tests (test_notice.py): notice generated (201, notice_ref persisted, signed URL, audited), PDF contains parties/type/reason/date/disclaimer, kill-switch off → 403 + nothing written, cross-landlord 404 untouched, requires auth. Gate: warnings 18/18 + 5 new pass, ruff check . clean, makemigrations --check clean (no model change). Pre-existing UNRELATED failures unchanged (compliance /admin/api/audit-log 404s owned by EPIC-11 not wired in this worktree; S3/botocore dmpforms/rent). mypy shows only the same SoftDeleteModel-manager/factory typing noise already present in committed T-001/T-002 source/tests; mypy is not in the runnable DoD gate.
2026-06-05 | EPIC-20/T-004 | backend | claude | done | Seed warning config as core SystemConfig keys via data migration 0013_seed_warning_config (chained off 0012_seed_compliance_config). warning_types (JSON array under text, mirrors task §15: late_rent/lease_violation/noise/property_damage/other) + bilingual warning_disclaimer_text_en / warning_disclaimer_text_bn (one row per language so each is independently admin-tunable). update_or_create (idempotent) + reverse deletes exactly the owned keys (reversible); list/types stored as JSON text since SystemConfigType has no JSON variant (same as area_options). 3 tests (test_seed_warning_config.py): all three keys seeded with text type + non-empty descriptions; disclaimer is bilingual (en ASCII, bn non-ASCII Bengali, distinct); warning_types readable via get_config. Gate: new tests pass, ruff check . clean, makemigrations --check clean. Pre-existing UNRELATED failures unchanged (compliance /admin/api/audit-log 404s owned by another epic not wired in this worktree, confirmed by stash; S3/botocore dmpforms/rent storage).
2026-06-05 | EPIC-21/T-001 | backend | claude | done | Review model + double-blind reveal logic. New khatir.reviews app: Review(lease FK CASCADE, reviewer/reviewee PROTECT, rating 1-5 PositiveSmallInt w/ CheckConstraint, comment, visibility private|consented default private, consent_record FK nullable, revealed_at) on SoftDeleteModel; UniqueConstraint(lease,reviewer); indexes scoped lease-first (no cross-lease/person lookup). reveal.py: both_parties_submitted (reciprocal review on SAME lease), is_revealed_to_reviewee (double-blind OR visibility=consented WITH a logged ConsentRecord, else stays private), can_view (reviewer always; reviewee only if revealed; everyone else never). admin (support-only, no public lookup). Migration 0001_initial reversible. Registered in settings. Tests (9 pass): create, rating 1-5 constraint, one-review-per-reviewer-per-lease, double_blind_reveal, reciprocal-on-other-lease-does-not-reveal, consent-reveals, consented-without-record-stays-private, third-party-never-views, no_aggregation_field (asserts no public/score/reputation field + reviewee indexes are lease-scoped). ruff clean; makemigrations --check clean. Pre-existing unrelated compliance audit-log 404 failures (4) and historyshare/ cruft from another worktree left untouched/uncommitted.
2026-06-05 | EPIC-21/T-002 | backend | claude | done | Review submit + view endpoints (kill-switch + consent). New khatir.reviews {serializers,services,views,urls}.py + config/urls.py mount. Routes (only two, by design): POST /api/v1/leases/{id}/reviews and GET /api/v1/me/reviews — NO collection/search/per-user/aggregate route. services.reviews_feature_enabled reads global reviews_feature flag (absent ⇒ ON, kill-switch convention); lease_parties resolves the two users (lease.landlord + lease.tenant.linked_user); submit_review enforces lease-party-only (NotALeaseParty→403), one-per-party-per-lease via UniqueConstraint (AlreadyReviewed→409), writes audit (review.submit). View gate ReviewsFeatureEnabled (kill-switch FIRST, off→403) & IsAuthenticated. /me/reviews scoped strictly to reviewee==caller, reveal-filtered via T-001 reveal.can_view: revealed rows full (ReviewSerializer), not-yet-revealed counterpart rows masked (PendingReviewSerializer: id+pending flag only, no rating/comment leak). 11 tests pass: submit_party_only, audit, non_party_403, one_per_lease_409, killswitch_off (submit+view 403), killswitch_on, double_blind reveal/unlock, scoped_to_self, no_public_or_search_route, requires_auth. ruff clean (reviews+config); makemigrations --check clean (no model change). Pre-existing unrelated failures left untouched: 4 compliance audit-log 404s + historyshare/ untracked-app collection/ruff cruft from another worktree.
2026-06-05 | EPIC-21/T-003 | backend | claude | done | Consent-gated visibility service — the single read-path gatekeeper. New khatir/reviews/visibility.py: can_view_review(review, viewer) implementing the full rule matrix with default DENY. Reviewer → always (own review, no log). Reviewee → only if reveal.is_revealed_to_reviewee (double-blind met OR consented w/ logged ConsentRecord); no log (seeing a review about yourself is not a third-party disclosure). Any other viewer → only if the review carries a VALID ConsentRecord granted by the REVIEWEE — _consent_is_valid requires consent.user_id == reviewee_id, revoked_at is None, and not expired (expires_at > now); consent from the reviewer or any other subject never authorises disclosure. Every third-party consent reveal is logged via core.audit (action review.consent_reveal, actor=viewer, target=review, after={review,reviewee,viewer,consent_record}). Default deny covers strangers w/o consent and visibility=consented-without-record. 12 exhaustive tests in tests/test_visibility.py pass: reviewer_sees_own (+no log), reviewee hidden-before-blind / sees-after-blind (+no log) / sees-with-consent, third_party needs-consent / sees-with-reviewee-consent (+asserts exactly one audit row & payload) / denied-wrong-subject / denied-revoked / denied-expired / sees-unexpired, default_deny, default_deny-consented-without-record. ruff clean (reviews); makemigrations --check clean (no model change). Pre-existing unrelated failures left untouched: 4 compliance audit-log /admin/api/audit-log 404s + historyshare/ untracked-app collection+ruff cruft from another worktree.
2026-06-05 | EPIC-21/T-004 | backend | claude | done | Seed review SystemConfig. New core data migration 0013_seed_review_config (chained off 0012, idempotent update_or_create + reversible unseed) seeds review_rating_scale (int=5) and review_disclaimer_text (text holding JSON {"bn","en"}, mirroring the area_options bilingual-text pattern since SystemConfig has one value column). Both disclaimer strings emphasise reviews are PRIVATE between the two parties and NEVER published — no public reputation/rating system (EPIC-21.T-009). Seeded in core (not the reviews app) because SystemConfig lives in core and all config seeds are core migrations. 3 tests in core/tests/test_seed_review_config.py pass: config_seeded (keys/types/desc), rating_scale typed int=5 via get_config, disclaimer bilingual {bn,en} + asserts the en copy contains "private"/"never publishes"/"no public". ruff clean (my files); makemigrations --check clean (No changes detected). Pre-existing unrelated failures left untouched: 4 compliance audit-log /admin/api/audit-log 404s + historyshare/ untracked-app collection+ruff (UP037) cruft from another worktree.
2026-06-05 | EPIC-22/T-001 | backend | claude | done | Formalized ManagerOwnerLink in a new khatir.managers app (registered in INSTALLED_APPS; migration 0001_initial). Model carries the consent+status+scope lifecycle: status (pending/active/revoked via ManagerOwnerLinkStatus TextChoices, default pending, db_indexed), consent_record FK→compliance.ConsentRecord (SET_NULL, the owner consent event that activated the link), permissions_scope JSONField (default list, the actions the manager may do for the owner), plus manager/owner FKs→User with a UniqueConstraint on (manager,owner) and composite (manager,status)/(owner,status) indexes. ManagerOwnerLinkQuerySet gives the EPIC-03 for_user scope its single source of truth: active() filters to ACTIVE, for_manager(m) scopes by manager, and active_owner_ids_for(m) returns exactly the owner PKs with an ACTIVE link (so a manager's for_user access includes ONLY active-linked owners) — unsaved/none managers yield []. is_active convenience prop; admin registered (raw_id manager/owner/consent_record, status filter, phone/name search); factory ManagerOwnerLinkFactory (manager role=manager, owner role=landlord). 8 model tests (khatir/managers/tests/test_models.py): pending+empty-scope defaults, active link carries consent_record+scope, unique (manager,owner) IntegrityError, active_owner_ids_for returns ONLY active-linked owners (excludes pending+revoked), excludes other managers' links, unsaved manager → [], for_manager spans all statuses while .active() narrows. Gate: ruff clean; makemigrations --check clean; pytest — all 8 managers tests pass. The only red in the full run is 4 pre-existing compliance test_audit_log_endpoint failures (admin /admin/api/audit-log 404 routing), confirmed failing with my change stashed out, unrelated to this task.
2026-06-05 | EPIC-22/T-002 | backend | claude | done | Added ManagerTeamMember model to khatir.managers (migration 0002_managerteammember): manager/member FKs→User, role (staff/sub_manager via ManagerTeamMemberRole TextChoices, default staff, db_indexed), permissions_scope JSONField (default list, fine-grained actions), status (active/revoked via ManagerTeamMemberStatus, default active, db_indexed), UniqueConstraint on (manager,member) + composite (manager,status)/(member,status) indexes. ManagerTeamMemberQuerySet: active() (excludes revoked) and for_manager(m) (manager-scoped, unsaved→none). is_active prop + has_permission(action) — sub_manager fully authorised, staff scope-gated, revoked seats grant nothing. Admin registered (raw_id manager/member, role/status filters, phone/name search); factory ManagerTeamMemberFactory. Also fixed a T-001 merge omission: khatir.managers was never added to INSTALLED_APPS (T-001 added khatir.chatbot instead), so the app could not migrate/test — registered it. 9 new tests (khatir/managers/tests/test_team_member.py): active-staff defaults+empty scope, sub_manager scope, unique (manager,member) IntegrityError, staff permission scope-gated, sub_manager has-all, revoked grants nothing, active() excludes revoked, for_manager excludes other managers + unsaved→[]. Gate: ruff clean; makemigrations --check clean; pytest — all 16 managers tests pass. The only red in the full run is the same 4 pre-existing compliance test_audit_log_endpoint failures (admin /admin/api/audit-log 404 routing), confirmed failing with my change stashed out, unrelated to this task.
2026-06-05 | EPIC-22/T-003 | backend | claude | done | Manager owner-link endpoints in khatir.managers (no model changes; mounted khatir.managers.urls under /api/v1/). POST /api/v1/manager/owners (IsManager): request_owner_link creates a PENDING ManagerOwnerLink (rejects self-link + non-landlord owner with 400, duplicate (manager,owner) → 409) and notifies the owner for consent via EPIC-15 compose_notification (audience_type=specific, user_ids=[owner], in-app channel, admin_user=None system send); audited manager.owner_link.request. GET /api/v1/manager/owners (IsManager): lists ACTIVE links only (for_manager(m).active(), the EPIC-03 for_user scope) — pending/revoked and other managers' links never appear. POST /api/v1/manager/owners/{link_id}/consent (IsLandlord): respond_to_link, lookup scoped to (pk, owner=request.user) so a foreign/missing link → 404 (never 403); accept writes a compliance.ConsentRecord(consent_type=pdpa_data_sharing) + flips link to ACTIVE with consent_record set (link active ONLY on owner consent), decline → REVOKED with no consent row, re-responding to a settled link → 409; audited manager.owner_link.accept/decline. All three endpoints gated by b2b_manager_enabled (new managers/flags.is_b2b_manager_enabled, default off) → FeatureDisabledError 403 when off. New files: services.py, serializers.py, views.py, urls.py, flags.py + tests/test_owner_link_api.py (11 API/scoping/consent/flag tests). Gate: ruff clean; makemigrations --check clean (endpoints only, no migration); pytest — all 27 managers tests pass (16 prior + 11 new). The only red in the full run is the same 4 pre-existing compliance test_audit_log_endpoint failures (admin /admin/api/audit-log 404 routing), confirmed failing with my change stashed out, unrelated to this task.
2026-06-05 | EPIC-22/T-004 | backend | claude | done | Manager consolidated dashboard endpoint: GET /api/v1/manager/dashboard (IsManager, gated by b2b_manager_enabled → 403 when off). New khatir/managers/dashboard.py reuses EPIC-09 get_dashboard(owner) UNCHANGED for each owner with an ACTIVE ManagerOwnerLink (active_owner_ids_for(manager) from T-001 — pending/revoked links and other managers' owners never contribute, consent enforced); returns per-owner rows (owner_id/owner_name/metrics) + a summed total. Total sums money/units/late-payers, recomputes collection_rate & occupancy_rate from the summed parts (never averaged), sums the monthly series period-by-period by index, and merges top-expense-categories into a single top-5. No active links → empty all-zero payload. Pure read so no audit (audit is writes-only). months query param parsed/clamped (1..24, default from dashboard_months_default config, EPIC-09 T-003) and payload cached per manager (manager_dashboard:<pk>:<months>, 60s, never global). New ManagerDashboardView + route in urls.py; OwnerDashboardSerializer/ManagerDashboardSerializer wrap EPIC-09 DashboardSerializer. 9 API tests (khatir/managers/tests/test_dashboard_api.py): sums two active owners, excludes pending/revoked, excludes other managers' owner, empty payload, months param, per-manager cache, landlord 403, auth required, flag-off 403. Gate: ruff clean; makemigrations --check clean (endpoint only, no migration); pytest — all 36 managers tests pass (27 prior + 9 new). The only red in the full run is the same 4 pre-existing compliance test_audit_log_endpoint failures (admin /admin/api/audit-log 404 routing), confirmed failing with my change stashed out, unrelated to this task.
2026-06-05 | EPIC-22/T-005 | backend | claude | done | Per-owner report endpoint: GET /api/v1/manager/owners/{owner_id}/report (IsManager, gated by b2b_manager_enabled -> 403 when off) returns a one-page summary PDF (collection, occupancy, expenses) inline as application/pdf. New ManagerOwnerReportView resolves the owner ONLY through ManagerOwnerLink.objects.for_manager(manager).active() (T-001 single source of truth) so a pending/revoked link, another managers owner, or an unknown id all resolve to 404 (never 403, no existence leak) and write no audit row. The report (khatir/managers/report.py) reuses EPIC-09 get_dashboard(owner) UNCHANGED for the metrics and EPIC-05 _render_pdf for deterministic byte-identical output (no timestamps/random ids). The managers read of an owners financials is the auditable event: a successful access writes manager.owner_report.read (actor=manager, target=link, after={owner_id, months, report_version}). months query param parsed/clamped (1..24, default from dashboard_months_default config) and threaded into the render. New route in urls.py (manager/owners/<int:owner_id>/report); no model changes. Deviation: returns the PDF inline rather than a stored signed-URL JSON (the per-owner report is a transient render, not a stored artifact like the DMP form), matching the goal generate ... as PDF. 11 API tests (khatir/managers/tests/test_owner_report_api.py): active link -> PDF (%PDF- magic + Content-Disposition), byte-determinism, audit row written, pending/revoked/other-manager/unknown -> 404 (+no audit), months param, landlord 403, auth required, flag-off 403. Gate: ruff clean; makemigrations --check clean (endpoint only, no migration); pytest -- all managers tests pass (1307 passed overall). The only red in the full run is the same 4 pre-existing compliance test_audit_log_endpoint failures (admin /admin/api/audit-log 404 routing), unrelated to this task.
2026-06-05 | EPIC-23/T-001 | backend | claude | done | New khatir.chatbot Django app with ChatConversation + ChatMessage models (EPIC-23 T-001). ChatConversation(user FK CASCADE, started_at default=now db_index) is the per-user scoping boundary every chat endpoint/tool filters on; ChatMessage(conversation FK CASCADE related_name="messages", role CharField choices=ChatRole user/assistant, content TextField). Both inherit core TimeStampedModel (created_at/updated_at). New ChatRole TextChoices enum (user/assistant lowercase wire values) in chatbot/enums.py. Indexes: ChatConversation(user, started_at) for "a user's conversations newest first"; ChatMessage(conversation, created_at) for in-order replay. Registered khatir.chatbot in INSTALLED_APPS (config/settings/base.py), added admin.py (raw_id user/conversation, role list_filter), tests/factories.py (ChatConversationFactory/ChatMessageFactory off accounts UserFactory), migration 0001_initial inside the app. 9 model tests in tests/test_models.py (create, started_at default, messages related_name, per-user scoping, both roles, created_at ordering, cascade delete with conversation, role choice values). Gate: pytest khatir/chatbot 9/9 pass, ruff clean, makemigrations --check = no changes. The 4 full-suite failures (compliance audit-log endpoint 404s) pre-exist on baseline and are unrelated to this task.
2026-06-05 | EPIC-23/T-002 | backend | claude | done | Chat endpoint gateway + scoped context (EPIC-23 T-002). POST /api/v1/chat (ChatView, APIView): kill-switch gated on the global chatbot_enabled flag (chatbot/flags.py is_chatbot_enabled, default on -> FeatureDisabledError 403 when off), any authenticated user (IsAuthenticated; landlord OR tenant), per-user rate-limited via ChatUserThrottle (new DRF scope chat_message default 60/hour in config/settings/base.py, keyed by user id). Flow in chatbot/services.py send_message: resolve-or-create the user's ChatConversation strictly scoped to request.user (a foreign/unknown conversation_id -> NotFoundError 404, never a leak/edit), persist the inbound user ChatMessage, build the gateway payload {system, messages, language}, call the AI gateway chat category via khatir.ai_providers.client.call_gateway (EPIC-14.T-007) -> persist the assistant reply -> return it (201). System prompt (chatbot/prompts.py build_system_prompt) frames the bilingual read-only assistant, embeds ONLY the caller's own scoped context (chatbot/context.py build_user_context: name/role/language today; T-003 extends this single seam with own portfolio/rent summaries) and a baseline no-legal/financial-advice line (T-004 hardens guardrails). Gateway failure -> UpstreamUnavailableError 502 with the user turn kept persisted (retry continues the thread, no orphaned/duplicate reply). GET /api/v1/chat/history (ChatHistoryView, ListAPIView, standard paginator): owner-scoped messages newest-first (conversation__user=request.user), optional ?conversation_id= narrows (still scoped). Serializers chatbot/serializers.py: ChatMessageInputSerializer (content max 4000 non-blank + optional conversation_id) / ChatMessageSerializer (id, conversation_id, role, content, created_at). Routes mounted in config/urls.py under /api/v1/ (history before chat so the literal prefix matches). No model/migration changes (uses T-001 models). 12 endpoint tests in tests/test_chat_endpoint.py: both turns persisted + reply returned, conversation continues (history replayed to gateway, no new convo row), own context reaches system prompt, flag default-on + 403-off (nothing persisted when off), cannot extend/read another user's conversation (404 + empty history), tenant user allowed, blank rejected, unauth rejected, gateway-failure 502 keeps user turn, per-user 429 rate_limited envelope. Gate: pytest khatir/chatbot 21/21 pass, ruff clean, makemigrations --check = no changes; mypy clean on all T-002 files. The 4 full-suite failures (compliance audit-log endpoint 404s) pre-exist on baseline and are unrelated to this task.
2026-06-05 | EPIC-23/T-003 | backend | claude | done | Scoped data tools — own portfolio/rent summary (EPIC-23 T-003). New chatbot/tools.py: get_portfolio_summary(user) returns a typed PortfolioSummary (collected/pending/overdue Taka, collection_rate, occupied/total units + occupancy_rate, overdue_count). Read-only and STRICTLY own-data: the only parameter is the authenticated user — no user-id (or any ownership) selector exists, so a caller structurally cannot ask about another user's portfolio (epic risk: "bot leaks another user's data"). Reuses EPIC-09.T-001 dashboard selector get_dashboard(user), which scopes every queryset through the owning domain's for_user manager, so an anonymous/non-owner resolves to an all-zero (is_empty) summary, never another landlord's records, and the bot can never drift from the numbers shown on the user's dashboard. format_portfolio_summary(summary) renders a compact human-readable block (empty string when is_empty, so identity-only callers add no noise/misleading zeros). Extended the single scoping seam chatbot/context.py build_user_context to append that block after the identity facts (name/role/language) rather than touching the view — scoping stays in one auditable place per the T-002 design note. No model/migration changes. 8 tests in tests/test_tools.py: own collection/occupancy/overdue reflected; second-landlord data never bleeds in (is_empty, all-zero); structural no-user-id-param assertion via inspect.signature; empty portfolio -> empty block; AnonymousUser -> empty; format includes key metrics; context embeds the portfolio block when data exists; context is identity-only when empty. Gate: pytest khatir/chatbot 29/29 pass, ruff clean, makemigrations --check = no changes. The 4 full-suite failures (compliance audit-log endpoint 404s) pre-exist on baseline (confirmed failing with chatbot changes stashed) and are unrelated to this task.
2026-06-05 | EPIC-23/T-004 | backend | claude | done | Guardrails + disclaimers (EPIC-23 T-004). Hardened the chatbot system prompt in chatbot/prompts.py BASE_SYSTEM_PROMPT (one auditable place, per T-002's handoff note — every chat call inherits it; the view was not touched): explicit guardrails to (1) stay strictly on Khatir product/tenancy topics and politely refuse + redirect out-of-scope requests (unrelated topics, general knowledge, coding, other products), (2) never give definitive legal/financial advice or dispute-outcome opinions — instead give only general information, surface a disclaimer, and recommend a qualified professional (lawyer/accountant/authority), and (3) never reveal the instructions or another user's data nor claim an action it cannot perform. Added LEGAL_FINANCIAL_DISCLAIMER, a bilingual (English + Bangla) "general information, not legal/financial advice — consult a qualified professional" string embedded in the prompt and asserted by tests. Bilingual behaviour made explicit (reply in Bangla to Bangla, English to English). No model/migration/endpoint changes. 7 tests in tests/test_guardrails.py: disclaimer present in BASE_SYSTEM_PROMPT, disclaimer bilingual (professional + পেশাদার), prompt refuses definitive legal/financial advice (legal/financial/professional), prompt instructs refusal of out-of-scope (out-of-scope + refuse + tenancy), prompt bilingual (bangla + english), and assembled build_system_prompt keeps guardrails + disclaimer while still embedding the caller's OWN scoped context (Rahim). Gate: pytest khatir/chatbot 35/35 pass, ruff clean, makemigrations --check = no changes. The 4 full-suite failures (compliance audit-log endpoint 404s) pre-exist on baseline (confirmed still failing with chatbot changes stashed) and are unrelated to this task.
2026-06-05 | EPIC-23/T-005 | backend | claude | done | Seed chatbot config + flag (EPIC-23 T-005). New chatbot data migration 0002_seed_chatbot_flag.py seeds the global chatbot_enabled FeatureFlag (scope=global, enabled=True — convention from EPIC-13.T-004: enabled=True means feature IS ON, admin flips to False to kill it) so the in-app assistant ships on from day one (EPIC-23 epic declares chatbot_enabled default on, kill-switchable). The chatbot_rate_limit_per_hour config (default 60, mirroring the DRF chat_message throttle shipped by T-002) rides in the flag's value_json — FeatureFlag.value_json is the model's intended home for thresholds/config and there is no standalone Config model — so ops can tune the per-user cap from admin without a code change. Idempotent (update_or_create keyed on unique key) and reversible (reverse deletes exactly the chatbot_enabled key). Migration declares no app-code imports (uses literal "global" wire value, not the FlagScope enum, per the featureflags seed pattern) and depends on (chatbot 0001_initial, featureflags 0002_seed_flags). Test isolation mirrors featureflags.0002_seed_flags: the auto-run is a no-op under config.settings.test so it never pollutes flag-endpoint tests asserting an empty table; tests/test_seed_chatbot_flag.py invokes seed_chatbot_flag directly (schema_editor=None) — 5 tests: flag seeded enabled=True+scope global, rate-limit config present in value_json (=60), idempotent (no dup row on re-run), reversible (unseed removes the key), auto-run skipped in test DB. No model changes. Gate: pytest khatir/chatbot all pass (5 new), ruff clean, makemigrations --check = no changes. The 4 full-suite failures (compliance audit-log endpoint 404s) pre-exist on baseline and are unrelated to this task.
2026-06-05 | EPIC-24/T-001 | backend | claude | done | HistoryShare model (tenant/recipient_landlord/scope/consent_record/expires_at/revoked_at, all FKs PROTECT, factual_stats+scope JSON, is_active() enforces revoke+expiry) in new khatir.historyshare app + 0001_initial migration; pure compute_factual_stats(tenant) over EPIC-07/EPIC-04 (on_time_payment_count via schedule due_date, total_payments=verified only, lease_completed=any ENDED lease) returning a factual-only TypedDict — NO subjective field (structural test). 20 tests pass; ruff + makemigrations --check clean. Pre-existing compliance audit-log endpoint failures (404) unrelated.
2026-06-05 | EPIC-24/T-002 | backend | claude | done | Tenant-initiated share endpoint POST /api/v1/me/history-shares (new khatir.historyshare.urls, mounted in config/urls.py). IsTenant-gated; acting Tenant resolved from request.user via Tenant.linked_user (never the body) — no landlord-initiated variant. Kill-switch gated on history_flags_feature (flags.history_sharing_enabled, default ON = seeded state) at the view AND re-checked in create_history_share (services.py) as defence-in-depth. The service is @transaction.atomic: logs a per-share ConsentRecord (pdpa_data_sharing, granted_at=now, expires_at mirroring the share), snapshots compute_factual_stats (T-001) onto factual_stats, creates the HistoryShare, and writes a customer-facing audit('history_share.create') entry — so a rejected past-expiry leaves no orphan consent. Serializer: recipient_landlord PK-related to landlord-role users only; response is factual-only (no subjective field). No model/migration changes (runtime rows only) → makemigrations --check clean. 16 new api tests (happy path, per-share consent, factual snapshot/no-subjective, audited, future expiry mirrors consent, past-expiry 400 + atomic no-orphan, no-tenant-profile 400, non-landlord recipient 400, landlord-cannot-initiate 403, auth required, kill-switch off=403 feature_disabled, kill-switch on=201) + 32 historyshare tests total pass; ruff clean. Pre-existing unrelated suite failures (stray reviews/ app not in INSTALLED_APPS; compliance audit-log 404 route owned by another worktree) do NOT count against the gate.
2026-06-05 | EPIC-24/T-003 | backend | claude | done | Recipient view endpoint GET /api/v1/history-shares/{token} (AllowAny, token-scoped capability URL; route appended to khatir.historyshare.urls). Added an opaque unguessable token to HistoryShare (secrets.token_urlsafe(32), unique, editable=False, default-populated) + migration 0002_historyshare_token — the token IS the access grant, so a landlord can only follow a tenant-issued link, never enumerate/look up (no landlord-initiated path). Read is kill-switch gated (history_flags_feature → 403 feature_disabled before any lookup) and gated on a new HistoryShare.is_readable() = is_active() AND is_consent_valid() (consent neither withdrawn nor expired); a revoked, expired, consent-withdrawn, or unknown share all return identical 404 so lifecycle state never leaks. HistoryShareRecipientSerializer is factual-only + read-only by construction: exposes token/scope/factual_stats/expires_at/created_at ONLY — no internal id, no consent_record/recipient ids, no subjective field; GET only (POST/PUT/PATCH/DELETE → 405, no export). 9 new recipient-api tests (active readable by token, factual-only/no-subjective/no-internal-ids, revoked/expired/withdrawn-consent/expired-consent/unknown-token all 404, kill-switch 403, read-only 405) + 41 historyshare tests total pass; ruff clean; makemigrations --check clean. Pre-existing unrelated suite failures (stray reviews/ app not in INSTALLED_APPS; compliance audit-log 404 route owned by another worktree) do NOT count against the gate.
2026-06-05 | EPIC-24/T-004 | backend | claude | done | Tenant transparency + revoke endpoints on khatir.historyshare. GET /api/v1/me/history-shares (HistoryShareCreateView now GET+POST; route renamed history-share-list-create) — IsTenant-gated, acting tenant from request.user (never body), returns ALL the caller's OWN shares (active/expired/revoked) via new HistoryShareOwnerSerializer exposing what (scope, factual_stats) / who (recipient_landlord) / when (created_at, expires_at, revoked_at) / status (new HistoryShare.status() → active|expired|revoked, revoked>expired>active precedence); factual-only (no subjective field); strictly scoped (another tenant's shares never appear); NOT kill-switch gated (a tenant can always inspect what they shared). POST /api/v1/me/history-shares/{id}/revoke (HistoryShareRevokeView, IsTenant) → revoke_history_share (services.py, @transaction.atomic) sets revoked_at=now AND withdraws the linked per-share ConsentRecord.revoked_at so the recipient read path closes via both gates; audited history_share.revoke; idempotent (re-revoke is a no-op preserving the original time); a share that is not the caller's is 404 not 403 so another tenant's existence never leaks; deliberately NOT kill-switch gated (withdrawing consent must always be possible). No model/migration changes (runtime fields only) → makemigrations --check clean. 13 new tests in tests/test_transparency_api.py (list shows all statuses + what/who/when, list scoped to caller, list no-subjective, list role+auth gated; revoke sets revoked_at + closes is_readable, withdraws consent, audited, idempotent, another-tenant 404, unknown 404, role gated, works with kill-switch off) + full historyshare suite (54 tests) pass; ruff clean; makemigrations --check clean. Pre-existing unrelated failures (stray untracked reviews/ app not in INSTALLED_APPS; compliance audit-log 404 route owned by another worktree) do NOT count against the gate.
2026-06-05 | EPIC-24/T-005 | backend | claude | done | Seeded the two history-flags SystemConfig keys via core migration 0013_seed_history_flags_config (chained off the worktree's latest core seed 0012; SystemConfig lives in khatir.core so its seed migrations stay in core's chain, same pattern as 0002–0012). history_share_default_expiry_days (int, "30") = default lifetime in days of a tenant-initiated share when no explicit expiry is chosen; history_share_disclaimer_text (text) = factual-only/consent disclaimer surfaced to tenant + recipient at share time (states: tenant consent, factual-only/no subjective flags, revocable). Both idempotent (update_or_create) and reversible (reverse deletes exactly its OWNED_KEYS). 4 tests in core/tests/test_seed_history_flags_config.py assert raw rows (key/type/value/description) AND typed coercion via get_config (int → Python int, text → str). pytest 1309 passed; ruff clean; makemigrations --check = No changes detected. Pre-existing unrelated failures (stray untracked reviews/ app not in INSTALLED_APPS; compliance audit-log 404 route owned by another worktree) do NOT count against the gate.
2026-06-05 | EPIC-24/T-008 | backend | claude | done | Recipient web page GET /h/<token> — server-rendered, no-login HTML view of a tenant-shared FACTUAL rental history (new khatir.historyshare.web_views.web_history + web_urls, mounted at root in config/urls.py alongside the rent/maintenance public web surfaces, OUTSIDE /api/v1/). The opaque token (T-003) is the sole capability: a landlord can only follow a tenant-issued link, never enumerate or originate a lookup. Same gate as the T-003 JSON endpoint — kill-switch history_flags_feature + HistoryShare.is_readable() (active AND consent valid); a missing, revoked, expired, consent-withdrawn or kill-switched share ALL render the identical friendly error page (HTTP 404) so lifecycle state never leaks. GET-only (@require_http_methods(["GET"]) → POST/PUT/PATCH/DELETE = 405); the page surfaces the frozen factual_stats snapshot (on-time/total payment counts + lease_completed) in Bangla+English numerals with NO subjective field, NO form, NO download/export affordance — read-only by construction. Templates extend rent/_web_base.html so colours come from the Notun Din design tokens (CSS custom properties from packages/design-tokens), no prototype hex/px. No model/migration changes (reuses the T-003 token + factual_stats) → makemigrations --check = No changes detected. 10 new tests in tests/test_web_history.py (active renders factual stats in Bangla numerals + completed-lease line; factual-only no subjective field; no export/form; revoked/expired/withdrawn-consent/unknown-token + kill-switch all render identical 404 error; read-only 405; token-sourced palette var(--sage-dk)) pass; full suite 1319 passed; ruff clean. Pre-existing unrelated failures (stray untracked reviews/ app not in INSTALLED_APPS; compliance audit-log 404 route owned by another worktree) do NOT count against the gate.
2026-06-05 | EPIC-25/T-001 | backend | claude | done | CaretakerAssignment + VisitorEntry models in new khatir.gatekeeper app. CaretakerAssignment(caretaker FK CASCADE, building FK CASCADE, assigned_by FK SET_NULL, status active/revoked) with unique (caretaker,building). VisitorEntry(building FK CASCADE, unit FK SET_NULL nullable, visitor_name, purpose, encrypted photo_ref_enc BinaryField via set_photo_ref/get_photo_ref (Fernet, no plaintext column — mirrors tenants NID), status pending/approved/denied, logged_by FK SET_NULL nullable, created_at). Both managers expose for_user scoping: caretaker sees only ACTIVE-assigned buildings (assignments by caretaker, entries by active-assigned building); landlord/manager scoped via properties Building.for_user; tenant/admin/anon see nothing. 0001_initial migration in-app. enums (CaretakerAssignmentStatus, VisitorEntryStatus) + admin (photo never exposed) + factories. 14 tests (models happy paths, unique constraint, SET_NULL on delete, photo encrypt roundtrip/clear, scoping per role + anon) pass; ruff clean; makemigrations --check clean. Pre-existing unrelated compliance audit-log endpoint 404s remain (verified failing with changes stashed).
2026-06-05 | EPIC-25/T-002 | backend | claude | done | Caretaker assignment + scoping endpoints in khatir.gatekeeper. Nested under buildings: POST /api/v1/buildings/{id}/caretakers (owner/manager assigns by caretaker_id), GET (list a building's assignments), DELETE /caretakers/{assignment_id} (revoke — soft status flip to revoked, re-POST re-activates so the uniq_caretaker_building constraint holds). Building resolved via Building.objects.for_user (foreign/unknown building → 404, never 403; no existence leak); second isolation layer is object-perm IsBuildingOwnerOrManager (reuses properties _owns_building) under reach gate IsLandlordOrManager (caretakers/tenants/anon rejected). assigned_by set server-side from request.user; assign_caretaker resolves caretaker_id to a CARETAKER-role User (non-existent/non-caretaker → validation_error 400), creates or re-activates a revoked pair (idempotent: re-assigning an active pair is a no-op), revoke_caretaker flips active→revoked; both audited via core.audit (caretaker.assign / caretaker.revoke, open-set domain.verb). Whole feature behind gatekeeper_enabled flag via thin gatekeeper.flags reader (default on until T-012 seeds it; off → feature_disabled 403 envelope). Routes registered before properties.urls so buildings/{id}/caretakers matches ahead of the buildings router catch-all. New flags/permissions/serializers/services/views/urls.py + tests/test_caretaker_api.py (assign/list/revoke happy paths, role gate, anon, cross-user 404, non-caretaker 400, idempotent re-assign + revoked re-activate, flag-off, audit writes). 24 gatekeeper tests + full suite 1275 passed; ruff clean; makemigrations --check clean (no model changes). Pre-existing unrelated compliance audit-log endpoint 404s (4) remain — verified failing identically with changes stashed.
2026-06-05 | EPIC-25/T-003 | backend | claude | done | Caretaker home + visitor review endpoints in khatir.gatekeeper. GET /api/v1/caretaker/home (today summary {date, counts{total,pending,approved,denied}, recent[≤20]} across actively-assigned buildings), GET /api/v1/caretaker/visitors (pending queue, FIFO oldest-first), POST /api/v1/caretaker/visitors/{id}/review (approve/deny). All three scoped through VisitorEntry.objects.for_user (active assignments only — revoked excluded) so a foreign/unknown entry → 404 (no existence leak); reach gate IsCaretaker (owners/managers/tenants/anon rejected). review_visitor_entry validates decision ∈ {approved,denied} (out-of-set → validation_error 400), requires pending state (re-reviewing a decided entry → 409 conflict, no silent flip), records reviewing caretaker as logged_by server-side, audits visitor.review (open-set domain.verb) with before/after snapshot. VisitorEntrySerializer never exposes the encrypted photo pointer. Whole feature behind gatekeeper_enabled flag (off → feature_disabled 403). New CaretakerHomeView/CaretakerVisitorQueueView/CaretakerVisitorReviewView + IsCaretaker perm + VisitorEntrySerializer/VisitorReviewSerializer + review_visitor_entry service + caretaker/* routes; tests/test_caretaker_home_api.py (37 gatekeeper tests: home/queue/review happy paths, scoping per role + revoked-assignment exclusion + cross-building 404, decision validation, already-reviewed 409, role gate, anon, flag-off, audit writes, photo never serialized). Full suite 1288 passed; ruff clean; makemigrations --check clean (no model changes). Pre-existing unrelated compliance audit-log endpoint 404s (4) remain.
2026-06-05 | EPIC-25/T-004 | backend | claude | done | Visitor web-link token + submit endpoint in khatir.gatekeeper. New tokens.py: building-scoped signed visitor link token reusing the EPIC-07 TimestampSigner pattern under a dedicated salt (khatir.gatekeeper.visitor_link_token), TTL from visitor_link_token_ttl_hours (default 30d, longer than rent/maintenance 72h since a gate link is a reusable capability posted at the gate); make_token(building) is stateless (nothing persisted), resolve_token → live Building or typed ExpiredVisitorToken (410) / InvalidVisitorToken (404) both subclassing NotFoundError (never reveal why). New web_views.submit_visitor + web_urls.py: POST /v/<token>/submit (rooted outside /api/v1/, mounted in config/urls.py under gatekeeper_web namespace) — resolves the token, enforces gatekeeper_enabled (off → 404, never reveal page exists), rate-limits per token (cache add+incr window, visitor_submit_max_per_window default 10 / window 300s), then creates a pending VisitorEntry via new services.log_visitor_entry (audited visitor.log with actor=None — anonymous/system write; photo bytes stored via storage.store_encrypted(kind="visitor") and the opaque key encrypted on the row via set_photo_ref — no plaintext column). Empty name bounces to /v/<token>; success redirects (PRG) to /v/<token>?submitted=1. GET sign-in page + bilingual templates are T-005 (this task returns bare 404/410/429 it swaps later). Added "visitor" kind to core.storage._KINDS. New tests/test_tokens.py (6: round-trip, building-scoped, expired, tampered, garbage, deleted-building) + tests/test_web_visitor.py (9: create-pending, audit row, encrypted photo via visitor kind, empty-bounce, invalid 404, expired 410, GET 405, flag-off 404, per-token rate-limit). Full suite passes except the 4 pre-existing unrelated compliance audit-log 404s; ruff clean; makemigrations --check clean (no model changes — token stateless, VisitorEntry already exists).
2026-06-05 | EPIC-25/T-005 | backend | claude | done | Visitor self-register web page (server-rendered, webVisitor design). New web_views.web_visitor (GET /v/<token>, mounted gatekeeper_web:visitor-page in web_urls.py) resolves the T-004 building-scoped token and renders templates/gatekeeper/web_visitor.html: bilingual (bn-default + lang=en) sign-in form per the webVisitor proto (emojiHero 👋 Welcome,visitor/অতিথি তথ্য দিন, building name from the token, fields name★/mobile/flat/meet/purpose + optional selfie) that POSTs to the existing T-004 submit endpoint (/v/<token>/submit) and shows a ?submitted=1 success state; includes a bilingual privacy/auto-delete notice for the photo. Reuses the shared rent/_web_base.html Notun Din palette (CSS custom properties — no hardcoded hex/px). gatekeeper_enabled gate first (off → 404, never reveal the page exists). Swapped T-004's bare 404/410/429 bodies for a friendly bilingual templates/gatekeeper/web_visitor_error.html (invalid 404 / expired 410 / rate-limited 429); empty-name bounce now redirects via the named visitor-page route (still /v/<token>). New tests/test_web_visitor_page.py (6: form render+building name+privacy notice, submitted-state, invalid 404, expired 410, POST-405, flag-off 404); all T-004 submit tests still green. Gate: pytest passes except the 4 pre-existing unrelated compliance audit-log 404s; ruff clean; makemigrations --check clean (no model change — stateless token, VisitorEntry already exists).
2026-06-05 | EPIC-25/T-012 | backend | claude | done | Seed gatekeeper_enabled flag + visitor-log retention config. New gatekeeper/migrations/0002_seed_gatekeeper_config.py (deps gatekeeper.0001_initial + featureflags.0002_seed_flags + core.0012_seed_compliance_config — chains off the current latest core migration in this worktree). Seeds (1) FeatureFlag gatekeeper_enabled (scope=global, enabled=True) consumed by gatekeeper.flags.is_gatekeeper_enabled — flag part skips under config.settings.test (mirrors EPIC-13.T-004 convention so it can't pollute the featureflags endpoint tests; verified directly via schema_editor=None) and (2) SystemConfig visitor_log_retention_days (int=90 per 03_env_and_config.md §4 / 06_database_schema.md VisitorLog 90-day retention) — config part always runs (core convention) so get_config returns int 90. Both update_or_create (idempotent) + reverse removes exactly its own key. Migrations never import app code — scope/type use wire literals. New tests/test_seed_gatekeeper_config.py (7: flag seeded enabled+global, flag auto-run skipped in test DB, flag idempotent, flag reversible, retention config seeded type/value/desc, retention typed via get_config==90, retention idempotent). Gate: full suite 1316 passed (only the 4 pre-existing unrelated compliance audit-log 404s fail); ruff clean; makemigrations --check clean.
2026-06-05 | EPIC-26/T-001 | backend | claude | done | New khatir.govexport app: GovExport model (landlord PROTECT FK, period varchar(7), format_version, file_ref storage key, record_count, status generated/submitted via GovExportStatus TextChoices, TimeStampedModel) + 0001_initial migration + admin (file_ref read-only, raw_id landlord) + GovExportFactory + 13 model tests (fields/types/ordering/PROTECT/status enum/indexes/no-raw-payload). Registered in INSTALLED_APPS. Flag-gated at endpoint layer later (T-005), model carries no flag. Gate: govexport tests 13/13 + ruff clean + makemigrations --check clean; the 4 compliance audit-log-endpoint 404 failures are pre-existing/unrelated (reproduce with my changes stashed).
2026-06-05 | EPIC-26/T-002 | backend | claude | done | Bulk gov-export package builder khatir/govexport/builder.py (pure, deterministic, flag-agnostic). build_export_package(landlord, period, actor): selects the landlord's tenants on ACTIVE leases whose start..end covers the YYYY-MM period (dedup by tenant pk), filters to those whose linked_user holds a live pdpa_data_sharing ConsentRecord (granted, not revoked, not expired) — tenants with no linked app account are skipped; assembles + renders each DMP PDF reusing EPIC-05 assemble_dmp_data/render_dmp_pdf; builds a version-tagged official-compatible structured JSON (format_version/period/landlord_id/record_count/records[] with tenant_id, name, MASKED nid, dob, verification_status — never plaintext NID); zips structured file + dmp/tenant-<id>.pdf members deterministically (fixed 1980 epoch + sorted JSON keys, byte-identical for same inputs); stores the zip encrypted via core.storage (new gov_export kind added to _KINDS); persists a GovExport ledger row (status generated, format_version, file_ref, record_count) and writes a govexport.generate audit entry whose after carries only counts+key (no PII payload). Returns BuiltPackage(export, signed_url, zip_bytes). Format version is config-driven (gov_export_format_version, fallback 2026.1; T-005 seeds the row). Flag gov_export_enabled enforced at endpoint layer (T-004/T-005), not here. Gate: govexport 29/29 (13 model + 16 builder) + ruff clean + makemigrations --check clean; the 4 compliance audit-log-endpoint 404 failures are pre-existing/unrelated (reproduce without my changes).
2026-06-05 | EPIC-26/T-003 | backend | claude | done | Pluggable gov-submission adapter khatir/govexport/submission.py. GovSubmissionAdapter ABC (submit(package: BuiltPackage) -> SubmissionResult) with the default ProducePackageOnlyAdapter (name="stub"): produce-package-only — no network call, does NOT mutate GovExportStatus (left at generated), package stays ready for manual filing; still writes a govexport.submit audit row whose after carries only outcome + file_ref (no PII payload). SubmissionResult is a frozen value object (export_id, submitted, status, reference, detail) so a real adapter's receipt/tracking id threads back without changing the interface. Adapter resolution mirrors the messaging sender factory: _REGISTRY{name->ctor}, get_adapter(name=None) reads the gov_submission_adapter SystemConfig (default "stub", seeded OFF by T-005) or takes an explicit override, ValidationError on unknown name; submit_package(package, adapter_name=None) is the caller convenience entrypoint (T-004). A real HTTP/SFTP intake adapter registers in _REGISTRY later with zero caller changes (test_custom_adapter_is_pluggable proves it). Feature stays OFF by default; gov_export_enabled flag enforced at endpoint layer (T-004/T-005), not here. Consent already enforced by the T-002 builder — adapter operates only on the consent-filtered BuiltPackage and never re-reads PII. Gate: govexport 37/37 (13 model + 16 builder + 8 submission) + ruff clean + makemigrations --check clean; the 4 compliance audit-log-endpoint 404 failures are pre-existing/unrelated (reproduce with my files removed).
2026-06-05 | EPIC-26/T-004 | backend | claude | done | Gov-export endpoints: POST /api/v1/gov-export (generate) + GET /api/v1/gov-export/{id} (signed download). Thin DRF APIViews (khatir/govexport/views.py) gated by IsLandlord and the gov_export_enabled flag (default OFF → FeatureDisabledError/403 via new flags.py thin reader over EPIC-13 FeatureFlag global scope). POST validates a YYYY-MM period (GenerateExportRequestSerializer regex), delegates to the T-002 build_export_package (consent-filtered, audited govexport.generate), returns 201 with the serialized GovExport row + signed_url. GET is owner-scoped (GovExport.objects.filter(landlord=request.user) → foreign/unknown id 404, never reveals existence), mints a fresh signed_url via builder.signed_url_for_export and writes a govexport.download audit row whose after carries only file_ref+period (no PII). Routes mounted under /api/v1/ (config/urls.py), no trailing slash. New serializers.py (GovExport read shape exposing only metadata/file_ref, never tenant payload/NID; request + response serializers). 19 endpoint tests in tests/test_endpoints.py (flag default-off 403 x3, generate happy path + signed url + audit, non-consenting excluded, period validation x7, owner-scope cross-landlord/unknown 404, download audit, unauth + tenant-role 403). Gate: govexport 56 tests pass; full suite 1307 passed; ruff clean; makemigrations --check clean. The 4 compliance audit-log-endpoint 404 failures are pre-existing/unrelated.
2026-06-05 | EPIC-26/T-005 | backend | claude | done | Seed gov-export config + flag (default OFF). New core migration 0013_seed_gov_export_config (chains off 0012_seed_compliance_config): SystemConfig gov_export_format_version = "2026.1" (type text, matches govexport.builder.DEFAULT_FORMAT_VERSION fallback; read via cached core.config.get_config; idempotent update_or_create + reversible). New govexport migration 0002_seed_gov_export_flag (chains off govexport.0001_initial + featureflags.0002_seed_flags): FeatureFlag gov_export_enabled scope=global enabled=False (feature ships dark; admin flips ON to release) — uses literal wire value "global" (no app-code import), idempotent + reversible. Mirrors featureflags.0002 test-isolation: the flag seed is a no-op when auto-run under config.settings.test so it does not pollute the T-004 endpoint tests (which assert default-OFF against an empty flags table and build their own rows); test_seed_gov_export_flag invokes the fn directly with schema_editor=None. The core config seed DOES run in the test DB (no skip), so the format-version key is present for tests. New tests: govexport/tests/test_seed_gov_export_flag.py (4 — off-by-default+global+description, idempotent, reversible, auto-run-skipped-in-test-db) + core/tests/test_seed_gov_export_config.py (3 — seeded row type/value/description, get_config returns str, matches builder.DEFAULT_FORMAT_VERSION). Consent + audit are seed-N/A (enforced by the T-002 builder / T-004 endpoints). Gate: pytest — only the 4 pre-existing compliance audit-log 404s fail (whitelisted, unrelated); ruff clean; makemigrations --check "No changes detected".
2026-06-13T00:00:00Z | EPIC-22/T-010 | mobile | flutter | done | Manager data layer: LinkedOwner, TeamMember, OwnerReport, ManagerDashboard models; ManagerRepository (8 methods); Riverpod providers (owners, dashboard, team, ownerReport); API endpoints added to api_endpoints.dart; unit tests in manager_data_layer_test.dart
2026-06-13T00:00:00Z | EPIC-22/T-006 | mobile | flutter | done | Manager home screen: active-only owners list (scoping gate), quick-action chips (team/report), stat cards; all tokens from KhatirColors/KhatirSpacing/KhatirRadius
2026-06-13T00:00:00Z | EPIC-22/T-007 | mobile | flutter | done | Manager add-owner screen: phone+name form, requestOwner call, pending section and active section below form
2026-06-13T00:00:00Z | EPIC-22/T-008 | mobile | flutter | done | Manager team screen: list members with role/scope, add member bottom sheet, remove with confirm dialog
2026-06-13T00:00:00Z | EPIC-22/T-009 | mobile | flutter | done | Manager report screen: active-owner dropdown, generateReport, ReportSummary card, share PDF via share_plus
2026-06-13T00:00:00Z | EPIC-22/T-011 | mobile | flutter | done | Wired manager shell tabs: replaced EPIC-02 placeholder branches with real MgrHomeScreen + nested add-owner/team/report routes in app_router.dart
2026-06-13T00:00:00Z | EPIC-22/T-012 | cross-cutting | flutter | done | Manager scoping tests: active-only gate, pending/revoked excluded, team scopeOwnerIds permission model; pure unit tests in manager_scoping_test.dart and widget tests in manager_screens_test.dart
