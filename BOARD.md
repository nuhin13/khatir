# BOARD

<!-- timestamp | task-id | role | platform | status | note -->
2026-06-02 | EPIC-00/T-001 | infra | claude | done | -
2026-06-02 | EPIC-00/T-002 | infra | claude | done | -
2026-06-02 | EPIC-00/T-003 | infra | claude | done | -
2026-06-02 | EPIC-00/T-004 | backend | claude | done | -
2026-06-02 | EPIC-00/T-005 | backend | claude | done | -
2026-06-02 | EPIC-00/T-006 | backend | claude | done | -
2026-06-02 | EPIC-00/T-010 | infra | claude | done | -
2026-06-02 | EPIC-00/T-012 | infra | claude | done | -
2026-06-02 | EPIC-00/T-007 | mobile | claude | done | -
2026-06-02 | EPIC-00/T-009 | admin | claude | done | -
2026-06-02 | EPIC-00/T-008 | mobile | claude | done | -
2026-06-02 | EPIC-00/T-011 | infra | claude | done | -
2026-06-02 | EPIC-00/T-013 | infra | claude | done | -
2026-06-02 | EPIC-00/T-014 | infra | claude | done | -
2026-06-02 | EPIC-00/T-015 | cross-cutting | claude | done | -
2026-06-02 | EPIC-00/T-016 | infra | claude | done | -
2026-06-02 | EPIC-01/T-002 | backend | claude | done | -
2026-06-02 | EPIC-01/T-001 | backend | claude | done | -
2026-06-02 | EPIC-01/T-008 | mobile | claude | done | -
2026-06-02 | EPIC-01/T-003 | backend | claude | done | -
2026-06-02 | EPIC-01/T-009 | mobile | claude | done | -
2026-06-02 | EPIC-01/T-004 | backend | claude | done | -
2026-06-02 | EPIC-01/T-010 | mobile | claude | done | -
2026-06-02 | EPIC-01/T-005 | backend | claude | done | -
2026-06-02 | EPIC-01/T-006 | backend | claude | done | -
2026-06-02 | EPIC-01/T-007 | backend | claude | done | -
2026-06-02 | EPIC-01/T-011 | mobile | claude | done | -
2026-06-02 | EPIC-01/T-012 | mobile | claude | done | -
2026-06-03 | EPIC-02/T-001 | backend | claude | done | -
2026-06-03 | EPIC-02/T-002 | backend | claude | done | -
2026-06-03 | EPIC-02/T-003 | mobile | claude | in-progress | no Flutter/Dart toolchain to run analyze+test
2026-06-03 | EPIC-02/T-004 | mobile | claude | in-progress | no Flutter/Dart toolchain to run analyze+test
2026-06-03 | EPIC-02/T-005 | mobile | claude | in-progress | no Flutter/Dart toolchain to run analyze+test
2026-06-03 | EPIC-02/T-006 | mobile | claude | in-progress | no Flutter/Dart toolchain to run analyze+test
2026-06-03 | EPIC-02/T-007 | mobile | claude | in-progress | no Flutter/Dart toolchain to run analyze+test
2026-06-03 | EPIC-02/T-008 | mobile | claude | in-progress | no Flutter/Dart toolchain to run analyze+test
2026-06-03 | EPIC-03/T-006 | backend | claude | done | -
2026-06-03 | EPIC-03/T-001 | backend | claude | done | -
2026-06-03 | EPIC-03/T-008 | mobile | claude | in-progress | no Flutter/Dart toolchain to run analyze+test
2026-06-03 | EPIC-03/T-002 | backend | claude | done | -
2026-06-03 | EPIC-03/T-003 | backend | claude | done | -
2026-06-03 | EPIC-03/T-004 | backend | claude | done | -
2026-06-03 | EPIC-03/T-005 | backend | claude | done | -
2026-06-03 | EPIC-03/T-007 | mobile | claude | in-progress | no Flutter/Dart toolchain to run analyze+test
2026-06-03 | EPIC-03/T-009 | mobile | claude | in-progress | no Flutter/Dart toolchain to run analyze+test
2026-06-03 | EPIC-03/T-012 | mobile | claude | in-progress | no Flutter/Dart toolchain to run analyze+test
2026-06-03 | EPIC-03/T-013 | mobile | claude | in-progress | no Flutter/Dart toolchain to run analyze+test
2026-06-03 | EPIC-03/T-010 | mobile | claude | in-progress | no Flutter/Dart toolchain to run analyze+test
2026-06-03 | EPIC-03/T-011 | mobile | claude | in-progress | no Flutter/Dart toolchain to run analyze+test
2026-06-03 | EPIC-03/T-014 | cross-cutting | claude | in-progress | no Flutter/Dart toolchain to run Dart parity test
2026-06-04 | EPIC-02/T-003 | mobile | claude | done | toolchain installed; flutter analyze+test green (120 pass)
2026-06-04 | EPIC-02/T-004 | mobile | claude | done | verified via flutter analyze+test (120 pass)
2026-06-04 | EPIC-02/T-005 | mobile | claude | done | verified via flutter analyze+test (120 pass)
2026-06-04 | EPIC-02/T-006 | mobile | claude | done | verified via flutter analyze+test (120 pass)
2026-06-04 | EPIC-02/T-007 | mobile | claude | done | verified via flutter analyze+test (120 pass)
2026-06-04 | EPIC-02/T-008 | mobile | claude | done | verified via flutter analyze+test (120 pass)
2026-06-04 | EPIC-03/T-007 | mobile | claude | done | verified via flutter analyze+test (120 pass)
2026-06-04 | EPIC-03/T-008 | mobile | claude | done | flutter_map 8.x tap-debounce fix in test; analyze+test green
2026-06-04 | EPIC-03/T-009 | mobile | claude | done | verified via flutter analyze+test (120 pass)
2026-06-04 | EPIC-03/T-010 | mobile | claude | done | WizardField label RichText→Text fix; analyze+test green
2026-06-04 | EPIC-03/T-011 | mobile | claude | done | provider seed deferred to post-frame; analyze+test green
2026-06-04 | EPIC-03/T-012 | mobile | claude | done | verified via flutter analyze+test (120 pass)
2026-06-04 | EPIC-03/T-013 | mobile | claude | done | controller update()->save() rename (Riverpod clash); analyze+test green
2026-06-04 | EPIC-03/T-014 | cross-cutting | claude | done | unit-label parity test green under flutter test
2026-06-04 | EPIC-04/T-001 | backend | claude | done | tenants app: Tenant+TenantFamilyMember, VerificationStatus, migration; 12 model tests, ruff+mypy clean
2026-06-04 | EPIC-06/T-001 | backend | claude | done | leases app: Lease + RentSchedule + enums; 26 tests; full suite 378 pass
2026-06-04 | EPIC-08/T-001 | backend | claude | done | maintenance app: MaintenanceRequest + Expense + enums; 17 tests; 378 pass
2026-06-04 | EPIC-10/T-001 | backend | claude | done | billing app: PricingTier + Subscription + enums; 17 tests; 378 pass
2026-06-04 | EPIC-11/T-001 | backend | claude | done | admin_portal app: AdminUser (separate from User); 23 tests; 378 pass
2026-06-04 | EPIC-05/T-001 | backend | claude | done | dmpforms app: DMPFormRecord; 11 tests; full suite 471 pass
2026-06-04 | EPIC-13/T-001 | backend | claude | done | featureflags app: FeatureFlag + KillSwitchEvent; 19 tests; 471 pass
2026-06-04 | EPIC-14/T-001 | backend | claude | done | ai_providers app: AIProvider + AIUsageLog; 17 tests; 471 pass
2026-06-04 | EPIC-15/T-001 | backend | claude | done | notifications app: Notification+Delivery+Template; sender FK->AdminUser; 28 tests; 471 pass
2026-06-04 | EPIC-16/T-001 | backend | claude | done | compliance app: ConsentRecord + DataRequest; 18 tests; 471 pass
2026-06-04 | EPIC-04/T-002 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-04/T-007 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-06/T-002 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-06/T-003 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-06/T-006 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-06/T-005 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-05/T-006 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-05/T-002 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-05/T-003 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-05/T-004 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-05/T-005 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-07/T-001 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-07/T-009 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-07/T-003 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-07/T-005 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-07/T-008 | backend | claude | done | integrated on bench/claude 4fb56f2; full gate green (pytest 625)
2026-06-04 | EPIC-04/T-003 | backend | claude | done | S3 boto3 storage (SSE, private ACL, presigned URLs) + FS fallback; pytest 634
2026-06-04 | EPIC-04/T-004 | backend | claude | done | TenantExtractionProvider ABC + OCR/ASR impls, normalized DTO, config-driven selection; pytest 642
2026-06-04 | EPIC-04/T-005 | backend | claude | done | POST /tenants/ocr: encrypted image store + provider extract, per-user rate-limit, masked-only fields; pytest 651
2026-06-04 | EPIC-04/T-006 | backend | claude | done | POST /tenants/voice: Bangla audio -> ASR fields, voice_tenant_entry flag gate (default on), audio discarded, per-user rate-limit; pytest 661
2026-06-04 | EPIC-04/T-008 | backend | claude | done | GET /api/v1/usage: tenant_usage selector (for_user count, free_limit from SystemConfig, is_over_free soft signal); seed free_tier_tenant_limit=2; no creation block (EPIC-10 owns enforcement); pytest 669
2026-06-04 | EPIC-06/T-004 | backend | claude | done | GET /leases/{id}/schedule (scoped, chronological) + GET /units/{id}/lease (active lease + tenant summary, none->404); leases selectors + RentSchedule/UnitLease serializers; pytest 678
2026-06-04 | EPIC-05/T-010 | backend | claude | done | DMP template field-verification: canonical field-map doc + FIELD_LAYOUT positioned renderer + golden test (field-set parity, per-field positions, determinism); scanned-master overlay pending founder; pytest 688
2026-06-04 | EPIC-07/T-002 | backend | claude | done | signed link-token service (Django TimestampSigner, dedicated salt, TTL from rent_link_token_ttl_hours); make_token persists, resolve_token raises typed Expired/Invalid (NotFoundError); pytest 688
2026-06-04 | EPIC-07/T-004 | backend | claude | done | rent-link send: status->sent stamp in send_rent_link + POST /rent-requests/{id}/send (send_rent_request service, audited rent.request.send), reuses EPIC-01 NotificationSender (whatsapp->sms, console dev); pytest 697
2026-06-04 | EPIC-07/T-006 | backend | claude | done | POST /r/{token}/proof (txn/screenshot via encrypted storage/note) -> PaymentProof + status proof_submitted, PRG to GET /r/{token}/receipt (pending vs verified, signed PDF link); per-token cache rate-limit (429); webReceipt screen built; pytest 706
2026-06-04 | EPIC-07/T-007 | backend | claude | done | verify/mark-received/reject + receipt PDF: POST /rent-requests/{id}/verify|mark-received|reject create Payment(verified_by/at) + render_receipt_pdf (reuses EPIC-05 _render_pdf + EPIC-04 encrypted storage), status->verified, schedule->paid, notify tenant web receipt link (best-effort), audited rent.payment.verify|mark_received|reject; re-settle 409; pytest 713
2026-06-04 | EPIC-08/T-006 | backend | claude | done | seed maintenance_categories + expense_categories SystemConfig (core 0006, JSON arrays mirroring enums, idempotent+reversible), surfaced in /config/public via _json_list_config helper; 3 new tests, core suite 115 green
2026-06-04 | EPIC-08/T-004 | backend | claude | done | unit-scoped maintenance web-link token reusing EPIC-07 TimestampSigner pattern under dedicated salt khatir.maintenance.link_token (distinct-salt cross-resolver rejection tested); stateless (signs Unit pk, no DB change), TTL from maintenance_link_token_ttl_hours (72h fallback); resolve_token raises typed Expired/Invalid (NotFoundError) for 410-vs-404; 10 token tests pass
2026-06-04 | EPIC-09/T-003 | backend | claude | done | seed dashboard_months_default=6 SystemConfig (core 0007, idempotent update_or_create + reversible, chained after 0006_seed_category_config); 2 new tests, core suite 115 green
2026-06-04 | EPIC-10/T-002 | backend | claude | done | seed 6 default PricingTier rows (billing 0002, keys per enums.md: free/per_tenant/bundle_20/bundle_40/unlimited_monthly/unlimited_annual; free=0-2 tenants ৳0 no-verification, bundles+unlimited include_verification+credits; illustrative BDT defaults, idempotent update_or_create + reversible); rekeyed 3 T-001 model tests to test_* to avoid seeded-row UNIQUE collision; 7 new + billing suite 24 green
2026-06-04 | EPIC-08/T-002 | backend | claude | done | maintenance CRUD + resolve->auto-expense at /api/v1/maintenance: create(open)/list/retrieve/patch scoped via MaintenanceRequestManager.for_user (unit->building->owner), IsLandlordOrManager & IsOwnerOfMaintenanceRequest, foreign=404; resolve(cost,note) marks resolved + records resolution_* + auto-creates ONE Expense(source=request) via new nullable OneToOne Expense.request (migration 0002) for idempotency (double-resolve rejected 400, never 2nd expense); audited maintenance.create|update|resolve; 39 maintenance tests green (full-suite failures limited to pre-existing S3/botocore dmpforms+rent)
2026-06-04 | EPIC-10/T-006 | backend | claude | done | seed pricing SystemConfig keys (core 0008): free_tier_tenant_limit=2 (int, re-asserted; 0005-owned) + nid_verification_tiers JSON list [bundle_10,bundle_20,bundle_50,unlimited] stored as text; idempotent update_or_create + reversible (reverse drops only nid_verification_tiers); 1 new test, core suite green
2026-06-04 | EPIC-10/T-004 | backend | claude | done | subscription create/upgrade at /api/v1/billing: POST subscribe resolves PricingTier by key (unknown/inactive->400 validation_error), creates-or-upgrades the caller's single Subscription in one atomic select_for_update write (status reset active, dates stamped from 30/365-day cycle), audited subscription.create|upgrade; payment STUBBED (paid tier records subscription.payment_intent pending marker for manual admin confirm; clear # STUB: marker for MFS wiring); GET subscription returns plan + tenant usage (tenants_used via tenants.usage selector, tenants_limit from tier.tenant_max, null=unlimited), 204 when no sub (implicit free); appended subscribe() to shared billing/services.py (T-003 metering untouched); 9 new subscription-api tests green (full-suite reds limited to other agents' in-flight maintenance/rent/config_public missing-migration work)
2026-06-04 | EPIC-11/T-002 | backend | claude | done | AdminAuditEntry immutable append-only model (admin_portal 0002) + admin_audit() writer: FK admin_user(SET_NULL, null for system), action, entity_type/entity_id denormalized from instance, before_json/after_json diffs, GenericIPAddressField ip, reason; immutability enforced at instance (save rejects re-save, delete raises) AND queryset (update/delete raise ImmutableAuditError) levels; indexes (admin_user,created_at)+(action); 36 admin_portal tests green (full-suite reds limited to other agents in-flight rent/maintenance work)
2026-06-04 | EPIC-10/T-003 | backend | claude | done | tenant metering + free-limit enforcement: check_tenant_limit(user) in shared billing/services.py raises TierLimitExceeded (new ErrorCode.TIER_LIMIT_EXCEEDED -> 402 PaymentRequired AppError) when active-tenant count (reuses Tenant.objects.for_user, EPIC-04 T-008) >= plan cap; cap = active-subscription tier.tenant_max (None=unlimited) else free_tier_tenant_limit config (default 2, not hardcoded); select_for_update on Subscription rows inside the create transaction closes the concurrent-create race; wired into tenants.create_tenant before any write so over-limit rolls back atomically (402 envelope {code:tier_limit_exceeded, details:{limit,tenants_used}}); 16 new test_metering tests green (under/at/over limit, config-driven limit, upgraded/unlimited/cancelled tiers, endpoint 201/402 + no-persist); ruff clean; check_tenant_limit already landed in committed billing/services.py via T-004 co-edit (metering untouched). Full-suite reds limited to other agents' in-flight maintenance/rent/config churn on the shared branch.
2026-06-04 | EPIC-08/T-003 | backend | claude | done | Expense CRUD + filtered list + CSV export at /api/v1/expenses: create(manual; source forced server-side)/list/retrieve/patch/delete scoped via ExpenseManager.for_user (unit->building->owner), IsLandlordOrManager & IsOwnerOfExpense, foreign/unknown id=404; list+export accept building/unit/date_from/date_to filters; manual + auto(source=request) both listed; update/delete restricted to manual (auto-expense rejected 400); CSV export streams (StreamingHttpResponse + csv writer + .iterator()) scoped+filtered with attachment header; audited expense.create|update|delete; 22 expense API tests green. Note: implementation files (expense_views/managers/serializers/services/permissions/urls/models/migration 0002) landed in the shared maintenance module via the concurrent T-002 commit; this commit adds the T-003 test suite + frontmatter. Full-suite reds limited to pre-existing S3/botocore dmpforms+rent failures.
2026-06-04 | EPIC-10/T-005 | backend | claude | done | pricing tiers + subscription state in GET /api/v1/config/public: always carries pricing.tiers (active tiers, sort_order, via T-004 read-only TierSerializer); authenticated callers also get a subscription block {tier_key,status,tenants_used,tenant_limit,can_verify_nid} where benefits (tenant_limit, can_verify_nid) come from the ACTIVE subscription tier only and fall back to free tier (limit from free_tier_tenant_limit config, can_verify_nid=False) when no active sub — status still reflects the most-recent row (past_due/cancelled). No prices/dates/payment data exposed. New module khatir/billing/public_config.py (avoids co-editing T-004's in-flight billing/services.py); view extended in khatir/health/views.py (request.user.is_authenticated gate). 6 new test_config_public tests green; ruff clean; no new migration (DB changes None). Full-suite reds limited to pre-existing S3/botocore dmpforms+rent failures (NoCredentialsError, unrelated).
2026-06-04 | EPIC-11/T-004 | backend | claude | done | admin_portal/permissions.py: dedicated admin-JWT permission layer fully separate from customer permissions — IsAdminUser decodes Authorization Bearer via PyJWT HS256 against ADMIN_JWT_SIGNING_KEY (rejects missing/malformed/wrong-key/expired/unknown-role/missing-admin_user_id; never falls through to customer JWT), attaches verified AdminPrincipal(admin_user_id,role,claims) to request (memoised); RequiresAdminRole(*roles) factory + RequiresAdminSection(section)/AdminSection constants enforce the §2 role->section matrix (users:super/ops/support, platform:super/ops, billing+pricing:super/finance, audit+export:super/compliance) with super allowed everywhere; role matrix documented in module docstring; SUPER/OPS/FINANCE/COMPLIANCE/SUPPORT role consts re-exported. 23 new tests (parametrized full role x section matrix + token-validation edges) green; ruff clean on my files. Full-suite ruff/pytest reds limited to other agents' in-flight admin_portal T-003 auth files (services.py B904) on the shared branch.
2026-06-04 | EPIC-11/T-006 | backend | claude | done | seed admin-session SystemConfig via core migration 0009: admin_session_timeout_minutes=60 (int) + admin_mfa_required=true (bool); idempotent (update_or_create) + reversible (reverse deletes exactly the 2 owned keys); typed get_config returns 60 and True respectively. 2 new tests in test_seed_admin_config.py green; my files ruff clean; makemigrations --check clean. No shared-file edits (urls/base/settings untouched — seed-only). Full-suite ruff/pytest reds limited to other agents' in-flight admin_portal T-003 (services.py B904) + pre-existing S3/botocore dmpforms+rent failures on the shared branch.
2026-06-04 | EPIC-11/T-012 | backend | claude | done | create_super_admin management command (khatir/admin_portal/management/commands/create_super_admin.py): one-time idempotent seed of the first AdminRole.SUPER account — generates secrets.token_urlsafe temp password (make_password-hashed) + pyotp.random_base32 TOTP secret stored encrypted via core.encryption.encrypt; prints temp password + otpauth:// provisioning URI to stdout ONCE (never recoverable, never logged after); writes admin_audit(admin_user=None, action=admin_user.create_super_admin, entity=created user, after={email,role}); --email normalised lower, --name, --dry-run (writes nothing); idempotent re-run on existing email makes no changes and does NOT reprint secrets. 5 new tests in test_create_super_admin.py (creates+hash+TOTP roundtrip, audit row, idempotent, email-normalisation, dry-run) green; ruff clean; makemigrations --check clean (DB change is one row, no schema). No shared-file edits (urls/base untouched — command-only). Full admin_portal suite 105 passed excluding another agent's untracked in-flight test_platform_dashboard.py.
2026-06-04 | EPIC-11/T-005 | backend | claude | done | platform dashboard endpoint GET /admin/api/dashboard (config/urls.py admin/api/ include -> admin_portal/admin_urls.py -> dashboard_views.PlatformDashboardView). dashboard.py: get_platform_dashboard() aggregates platform-wide (NO for_user scoping) total_users, active_landlords (distinct landlord building owners), total_properties, total_units, occupied_units, total_rent_collected {all_time,this_month} (Sum of verified Payment->RentRequest.amount, 2dp), dmp_forms_generated, active_subscriptions; KPI block cached 5min under admin:platform_dashboard:v1; live health{app,database,cache,status} computed outside cache (SELECT 1 + cache round-trip probe). Admin-only via committed T-004 permissions layer: IsAdminUser + RequiresAdminSection(PLATFORM) => super/ops only (finance/compliance/support 403). 12 new tests in test_platform_dashboard.py (KPIs present, aggregate-across-all-users, health, anon/invalid-token/customer-role rejected, platform-roles allowed, non-platform denied, 5-min cache) green; my files ruff clean; makemigrations --check clean (no DB change). Self-contained on committed code only — did NOT touch other agents' uncommitted T-003 views.py/auth_tokens.py. admin_urls.py is now shared (EPIC-12 appended user routes additively). Full-suite reds limited to other agents' in-flight featureflags/services.py C408 + pre-existing S3/botocore dmpforms+rent+tenants-voice failures.
2026-06-04 | EPIC-14/T-002 | infra | claude | done | AI gateway FastAPI scaffold at services/ai-gateway/ (own uv project: pyproject.toml + uv.lock; generated requirements.txt for parity). main.py exposes GET /healthz -> 200 {status:ok, service:khatir-ai-gateway} via create_app() factory; config.py is pydantic-settings Settings reading shared .env (AI_GATEWAY_INTERNAL_TOKEN auth secret, port 8100 to match AI_GATEWAY_URL) — auth_enabled gates on token presence. Thin stateless router, NO DB/business logic. Dockerfile uv-managed python3.13 non-root EXPOSE 8100 uvicorn main:app. docker-compose.yml ai-gateway service added additively (build ./services/ai-gateway, ports 8100, /healthz healthcheck). 5 pytest tests green (health 200 + payload shape + auth-flag on/off + default port); ruff clean; mypy clean (4 files). Deviation: secret env is AI_GATEWAY_INTERNAL_TOKEN not placeholder GATEWAY_SECRET; uv primary per infra contract.
2026-06-04 | EPIC-11/T-003 | backend | claude | done | admin auth flow (login/verify-mfa/logout/me) under /admin/api/auth/, COMPLETELY separate JWT realm from customer auth: PyJWT HS256 tokens signed by dedicated ADMIN_JWT_SIGNING_KEY (env, falls back to admin::SECRET_KEY, asserted != SIMPLE_JWT key), short access TTL capped at ADMIN_SESSION_TIMEOUT_MINUTES; AdminJWTAuthentication attaches request.admin_user (never request.user) + IsAdminAuthenticated permission. login verifies make_password hash on non-disabled AdminUser, returns mfa_required+mfa_token challenge when account has TOTP secret AND ADMIN_MFA_REQUIRED, else issues access token directly; verify-mfa decrypts core.encryption TOTP secret + pyotp.verify(valid_window=1) then issues token+stamps last_login_at; logout revokes by jti in cache until expiry (idempotent, no DB table). Uniform auth_invalid on every failure (no email/factor leak). admin_audit on login_success/login_failed/mfa_challenged/mfa_failed/logout. Email+IP throttles (admin_login_email/ip 10|30 per 10min) on login, IP-only on verify-mfa. Added pyotp>=2.9.0. 15 new tests in test_admin_auth.py (happy no-mfa, wrong-password, unknown-email, disabled, mfa-challenge, mfa-success, mfa-wrong-code, bad-challenge, me-auth-required, me, logout-revokes, disabled-after-login, separate-signing-key, mfa-flag-off, rate-limit) green; my files ruff clean; makemigrations --check clean (no schema change). Shared edits additive: config/urls.py (admin/api/auth/ include, before django admin/), config/settings/base.py (ADMIN_JWT_*/ADMIN_MFA_* + 3 admin throttle rates). Full-suite reds limited to other agents' in-flight featureflags duplicate-seed-migration + pre-existing S3/botocore dmpforms+rent failures on shared branch.
2026-06-04 | EPIC-13/T-002 | backend | claude | done | flag CRUD + toggle + /config/public flags dict. New featureflags/{serializers,services,views,urls}.py: FeatureFlagViewSet (list/create/retrieve/update + @action toggle) at /admin/api/flags, lookup_field=key, super/ops gate via IsPlatformAdmin (SECTION_ROLES[PLATFORM]) + AdminJWTAuthentication/IsAdminAuthenticated. services.public_flags() returns global-scope {key:enabled} cached 60s under featureflags:public; toggle_flag flips enabled+sets updated_by+admin_audit(feature_flag.toggle, before/after enabled)+busts cache; perform_create/update audit feature_flag.create/update + bust cache. enabled is read-only on serializer (toggle-only); key immutable on update. health/views.config_public flags dict now = public_flags(). 17 new tests (CRUD, toggle flip/actor/audit/cache-bust, config_public exposes global + omits role/user, test_toggle_reflects_in_config_public propagation, anon/role gate) green; my files ruff clean; makemigrations --check clean (no schema change). Shared edits additive: config/urls.py (admin/api/ featureflags.urls include after admin_urls), health/views.py (flags=public_flags()). Full-suite reds limited to other agents' in-flight admin_portal user_views.py ruff + T-004 test_seed_flags + pre-existing S3/botocore dmpforms/rent failures.
2026-06-04 | EPIC-13/T-004 | backend | claude | done | seed 5 kill-switches (warnings_feature, reviews_feature, history_flags_feature, free_text_feature, master_kill_switch) + 2 default flags (voice_tenant_entry, dmp_enabled) — all enabled=true (kill-switch convention: enabled=true = feature ON; flip to false to kill). New featureflags/migrations/0002_seed_flags.py: idempotent update_or_create keyed on unique key, reversible unseed removes exactly the 7 keys, scope literal "global" (no app-code import). Test isolation: seed auto-run is a no-op under config.settings.test so it does not pollute the already-committed T-002/T-003 flag-endpoint tests (which assert an empty flags table and build rows with these same keys); test_seed_flags invokes seed_flags() directly to verify it. 7 new tests in test_seed_flags.py (flags-seeded, kill-switches-enabled, default-flags-enabled, voice-present, idempotent, reversible, auto-run-skipped-in-test-db) green; ruff clean; makemigrations --check clean. No shared-file edits (no routes/apps — migration+test only). Full-suite reds limited to other agents' in-flight admin_portal/dmpforms/rent + pre-existing S3/botocore failures, none in featureflags or tenants.
2026-06-04 | EPIC-12/T-003 | backend | claude | done | user search+detail+actions under /admin/api/users (admin_urls.py shared, appended additively below dashboard). New user_views.py: UserSearchView GET /admin/api/users (q= search by phone icontains / name icontains / numeric id exact / linked tenant_profiles__nid_number_masked icontains, distinct, StandardPageNumberPagination), UserDetailView GET /{id} (user + current Subscription + usage{buildings,tenant_profiles,subscriptions} + recent admin audit trail filtered entity_type=accounts.user), UserSuspendView/Reactivate/UpgradeSubscription POST. user_services.py: suspend_user sets is_active=False (simplejwt rejects inactive access tokens) AND blacklists every OutstandingToken refresh for user via BlacklistedToken.get_or_create (cannot re-mint), reason MANDATORY, ConflictError if already suspended; reactivate_user re-enables; upgrade_subscription manual tier override (updates latest Subscription or creates active one), reason mandatory, NotFound on bad tier; all wrapped @transaction.atomic + admin_audit (user.suspend/reactivate/upgrade_subscription, before/after diffs). Authz: AdminJWTAuthentication+IsAdminAuthenticated, users-section gate — IsUsersReadAdmin super/ops/support (support READ-ONLY), IsUsersWriteAdmin super/ops only (support 403 on writes), finance/compliance 403. 28 new tests in test_user_admin.py (search by phone/name/id/masked-nid, paginated, detail payload+404, suspend reason-required/deactivates+blacklists-jwt/audited/already-suspended-409, reactivate/audited/already-active-409, upgrade existing/creates-when-none/reason-required/unknown-tier-404, anon denied, read roles, support-cannot-write, ops+super-can-write, disabled-admin denied) green; ruff clean; makemigrations --check clean (no schema change — writes existing User.is_active + Subscription). Full-suite reds limited to pre-existing S3/botocore NoCredentialsError in dmpforms+rent storage tests (no AWS creds in sandbox), unrelated to this task.
2026-06-04 | EPIC-14/T-010 | backend | claude | done | seed 3 AI-gateway SystemConfig keys via core migration 0010_seed_ai_config (ai_gateway_url=text empty, ai_provider_cache_ttl_seconds=int 300, ai_gateway_secret=text). Non-secret rows update_or_create (idempotent); secret seeded with get_or_create empty placeholder so an operator-set value is never overwritten and the real secret is never embedded in the migration/logs. Reverse unseeds exactly the 3 owned keys. 2 new tests in test_seed_ai_config.py (keys seeded with correct type/desc + secret value empty; typed via get_config) green; my files ruff clean; makemigrations --check clean. No shared-file edits (migration+test only). Full-suite reds limited to other agents' in-flight admin_portal/dmpforms/rent + pre-existing S3/botocore failures, none in core or ai_providers (137 core+ai_providers tests green).
2026-06-04 | EPIC-15/T-005 | backend | claude | done | seed 3 system NotificationTemplate rows via notifications migration 0002_seed_system_templates: rent_reminder_due (trigger=rent_due, channels=[whatsapp,sms]), rent_receipt_generated (trigger=payment_verified, channels=[whatsapp,sms]), welcome_new_user (trigger=user_created, channels=[inapp]). All bilingual (title/body _en+_bn) with declared {placeholder} variables matching body tokens. Idempotent update_or_create keyed on unique key; reverse unseeds exactly the 3 owned keys. 7 new tests in test_seed_system_templates.py (3-seeded, per-template trigger/channels/active/variables, bilingual-and-distinct, placeholders-match-variables, idempotent) green; my files ruff clean; full ruff clean; makemigrations --check clean. No shared-file edits (migration+test only). 35 notifications tests green.
2026-06-04 | EPIC-15/T-009 | backend | claude | done | seed 3 per-message notification cost SystemConfig keys via core migration 0011_seed_notifications_config (whatsapp_cost_per_message=money 0.50, sms_cost_per_message=money 0.30, email_cost_per_message=money 0.00). All type=money so get_config returns Decimal for composer cost preview. update_or_create idempotent; reverse unseeds exactly the 3 owned keys. 2 new tests in test_seed_notifications_config.py (rows seeded with money type/value/desc; typed-via-get_config returns Decimal) green; my files ruff clean; full ruff clean; makemigrations --check clean (single linear chain — 0011 chains off 0010, another agent's in-flight 0012 already chains off 0011). No shared-file edits (migration+test only). Full-suite reds limited to pre-existing S3/botocore NoCredentialsError in dmpforms+rent storage tests, unrelated to this task.
2026-06-04 | EPIC-15/T-008 | backend | claude | done | admin notification-templates CRUD at /admin/api/notification-templates (List/Create/Retrieve/Update via DRF GenericViewSet, lookup by key). key + trigger_event immutable on update (serializer pops them); editable: title/body _en+_bn, channels, variables, active. super+ops gate via IsPlatformAdmin (SECTION_ROLES[PLATFORM]) + AdminJWTAuthentication. Every create/update audited via admin_audit (action notification_template.create/.update with before/after diffs). No deletes (deactivate via active). 13 new tests in test_template_endpoints.py (CRUD, key+trigger immutability, create+update audit diffs, anon reject, platform-roles-allowed, non-platform-denied) green; my files ruff clean; makemigrations --check clean (no model changes). Shared config/urls.py edited additively (one include). Full-suite reds limited to pre-existing S3/botocore NoCredentialsError in dmpforms+rent storage tests, unrelated to this task.
2026-06-04 | EPIC-16/T-005 | backend | claude | done | seed 2 compliance SLA SystemConfig keys via core migration 0012_seed_compliance_config (data_request_sla_days=int 30, data_delete_grace_days=int 7). type=int so get_config returns Python int. update_or_create idempotent; reverse unseeds exactly the 2 owned keys. 2 new tests in test_seed_compliance_config.py (rows seeded with int type/value/desc; typed-via-get_config returns int) green; my files ruff clean; full ruff clean; makemigrations --check clean (0012 chains off 0011). No shared-file edits (migration+test only). Full-suite reds limited to other agents' in-flight notifications/dmpforms/rent + pre-existing S3 failures, unrelated to this task (confirmed identical with my files removed).
2026-06-04 | EPIC-15/T-002 | backend | claude | done | compose_notification service in khatir/notifications/services.py: validates audience (all/role/segment/specific user_ids) + channels (dedup, known wire values) + bilingual content; resolves reach (count active Users matching audience); estimates cost (reach x sum per-channel SystemConfig cost, inapp free, defaults 0 so works without T-009); creates Notification in sending/scheduled status; NOW enqueues T-003 deliver_notification via named current_app.send_task on_commit (no hard import of not-yet-built task), SCHEDULED registers django_celery_beat ClockedSchedule one-off PeriodicTask, RECURRING registers CrontabSchedule PeriodicTask; future-time + recurrence-required validation; every compose audited via admin_audit (action notification.compose, after-diff with reach/cost/status). 21 new tests in test_compose.py (audience/reach all+role+specific, validation rejects, channel dedup, cost from config + zero default, now-enqueue via django_capture_on_commit_callbacks + mocked send_task, clocked/crontab beat rows, audit entry, sender set) green; 35-row notifications suite green; my files ruff clean; full ruff clean; makemigrations --check clean (no model changes). No shared-file edits (services+test only). Full-suite reds limited to pre-existing S3/botocore NoCredentialsError in dmpforms+rent storage tests, unrelated to this task.

2026-06-04 | EPIC-16/T-003 | backend | claude | done | read-only GET /admin/api/consent-records (compliance/views.py ConsentRecordListView + serializers.py + urls.py). Paginated (StandardPageNumberPagination envelope), filterable by user (user_id), consent_type, granted_from/granted_to date range. ConsentRecord is append-only so no write endpoints. compliance+super gate via IsComplianceAdmin (SECTION_ROLES[AUDIT]) + AdminJWTAuthentication. Read-only endpoint = no audit/no migration. 13 new tests in test_consent_endpoints.py (list+envelope, read-only projection shape, filter by user/consent_type/date-range, anon reject, audit-roles-allowed super+compliance, non-audit-roles-denied ops/finance/support) green; 29-row compliance suite green; my files ruff clean; full ruff clean; makemigrations --check clean (no model changes). Shared config/urls.py edited additively (one include). Full-suite reds limited to pre-existing S3/botocore NoCredentialsError in dmpforms+rent storage tests, unrelated to this task.
2026-06-04 | EPIC-08/T-012 | backend | claude | done | expense summary hook: maintenance/selectors.py with expense_total_by_category + expense_total_by_month read-only ORM aggregations (single grouped query each, no N+1), always scoped via Expense.objects.for_user, optional building/unit/date_from/date_to filters, money normalised to 2dp Decimal (0.00 when empty), by_month bucketed via TruncMonth ordered chronologically. Exposed GET /api/v1/expenses/summary as detail=False @action on ExpenseViewSet (IsLandlordOrManager gate, totals serialized as strings, months as ISO first-of-month). _filter_kwargs helper shared by list/export and summary. 11 new tests in test_expense_summary.py (by_category math, by_month math + chrono order, owner scoping x2, building+date filters, empty case, endpoint shape, endpoint scoped, auth-required, tenant-forbidden) green; full maintenance suite green except pre-existing S3/botocore NoCredentialsError in another agent's in-flight test_web_maint.py; my files ruff clean; makemigrations --check clean (no model changes). No shared config/urls.py edit needed (route is a viewset action on the already-registered expenses router). Full-suite reds limited to other agents' in-flight billing/tenants/dmpforms/rent work, unrelated to this task.
2026-06-04 | EPIC-12/T-004 | backend | claude | done | refund queue endpoints in khatir/admin_portal/refund_views.py: GET /admin/api/billing/refunds lists pending payment intents (no dedicated table — these are EPIC-10 stub customer-realm AuditEntry rows action=subscription.payment_intent with after.state=pending; queue excludes any intent already resolved via a follow-up entry whose after.resolves=<id>); POST /admin/api/billing/refunds/{id}/process approves (writes refunded resolution AuditEntry + cancels Subscription) or denies+reason (writes refund_denied resolution, subscription untouched). reason mandatory on deny (serializer + service re-check). Both decisions write immutable AdminAuditEntry action=refund.process. finance+super gate via IsBillingAdmin (SECTION_ROLES[BILLING]) + AdminJWTAuthentication. Re-process of a resolved intent -> 409; unknown -> 404. Append-only/idempotent (no audit row mutated). 15 new tests in test_refund.py (list, approve cancels sub, deny keeps sub active, deny-requires-reason, audit, process-twice 409, unknown 404, anon denied, finance/super allowed, ops/support/compliance denied, non-finance cannot process) green; full 160-row admin_portal suite green; my files ruff clean; makemigrations --check clean (no model changes). Routes added additively to admin_portal/admin_urls.py (the /admin/api/ app include), no config/urls.py edit needed. Full-suite ruff reds limited to other agents' in-flight billing/public_config.py + dashboard/tests, unrelated to this task.
2026-06-04 | EPIC-09/T-001 | backend | claude | done | dashboard aggregation selectors: new khatir.dashboard app (no models/migration, registered in INSTALLED_APPS) with selectors.py get_dashboard(user, months) -> typed frozen DashboardMetrics dataclass. Computes total_collected/pending/overdue + collection_rate (collected/(collected+pending+overdue)x100, 0.0 on empty denom) from RentSchedule status grouped (paid/pending+requested/overdue); occupancy (occupied vs total Unit) via single Count agg; monthly_series last N calendar months anchored to localdate, rent keyed off RentSchedule.period + expense off TruncMonth(Expense.date), zero-fill missing months; income_vs_expense + net; top 5 expense categories by amount; late_payer_count = distinct leases with >=1 overdue schedule. All reads scoped via Lease/Unit/Expense for_user (RentSchedule via lease__in for_user subquery) so anon/non-owner -> empty payload. Fixed bounded query count (no N+1). 14 new tests in test_selectors.py (each metric + empty/zero cases + requested-as-pending + scoping + anon empty + query-count guard) green; ruff clean on my files + full repo; makemigrations --check clean. Shared base.py edited additively (one INSTALLED_APPS entry). Full-suite reds limited to pre-existing S3/botocore NoCredentialsError in other agents in-flight dmpforms/rent/maintenance storage tests + pricing-admin, unrelated to this read-only task.
2026-06-04 | EPIC-12/T-001 | backend | claude | done | pricing tier list/preview/edit endpoints in khatir/admin_portal/pricing_views.py + pricing_services.py + pricing_serializers.py. GET /admin/api/pricing/tiers lists all tiers (active+inactive, sort_order order, full admin projection incl prices/active/sort_order). POST /admin/api/pricing/tiers/{key}/preview is a read-only impact calc (subscribers_affected = active subs on tier; monthly_revenue_delta = sum over affected subs of new-minus-old monthly-equivalent price, annual amortised /12, null price = 0; no write). PATCH /admin/api/pricing/tiers/{key} applies changes under select_for_update, reason mandatory, writes before/after field-diff AdminAuditEntry action=pricing.tier.edit, busts /config/public via new billing/public_config.invalidate_public_config_cache (safe no-op delete today; T-002 layers real cache on shared PUBLIC_CONFIG_CACHE_KEY). key immutable (not editable). finance+super gate via IsPricingAdmin + AdminJWTAuthentication; ops/support/compliance + anon denied. 17 new tests in test_pricing_admin.py (list incl inactive, preview count+delta monthly & annual-amortised, preview no-write, preview 404, edit applies, edit requires reason, before/after audit, cache bust mocked, edit 404, role gate super/finance allowed, ops/compliance/support denied read+write, disabled admin denied) green; full 228-row admin_portal+billing suite green; my files ruff clean; full ruff clean; makemigrations --check clean (no model changes - writes existing PricingTier). Routes added additively to admin_portal/admin_urls.py. Full-suite reds limited to other agents in-flight EPIC-09 test_tier_gate.py import + pre-existing S3/botocore NoCredentialsError in dmpforms+rent, unrelated to this task.
2026-06-04 | EPIC-10/T-009 | cross-cutting | claude | done | NID verification tier gate: check_can_verify(user) in billing/services.py raises TierFeatureGated (new ErrorCode.FEATURE_REQUIRES_UPGRADE, 402) unless the user has an active subscription whose tier includes_verification==True; wired into TenantOcrView + TenantVoiceView (gate runs before any image is stored / paid provider call), manual tenant create path untouched. New billing/tests/test_tier_gate.py: 14 tests (free blocked, paid-without-verification blocked, bundle-with-verification ok, cancelled-plan blocked, ocr_blocked_free, ocr_ok_bundle10, ocr gate-before-storage, voice_blocked_free, voice_ok_bundle10 + service cases) green; updated existing OCR/voice endpoint tests to grant a verification tier (28 ocr+voice+gate tests green). My files ruff clean; makemigrations --check clean (no model changes). No shared config/urls.py or base.py edit needed. Mobile upgrade-prompt intercept DEFERRED: T-008 (upgrade_prompt.dart) still todo and EPIC-04 mobile add-tenant OCR/voice screens absent at HEAD; backend gate complete, reuses the same feature_requires_upgrade intercept pattern. Full-suite reds limited to pre-existing S3/botocore NoCredentialsError + other agents in-flight tests, unrelated to this task.
2026-06-04 | EPIC-08/T-005 | backend | claude | done | token-scoped maintenance web form: khatir/maintenance/web_views.py + web_urls.py (/m/<token> GET form, /m/<token>/submit POST) + templates/maintenance/web_maint.html (extends shared rent/_web_base.html for the Notun Din token palette) + web_maint_error.html. GET resolves the T-004 signed unit token -> renders webMaint screen (category radios from MaintenanceCategory enum, bn+en, description textarea, optional photo); POST creates MaintenanceRequest (status=open, stamps the unit active Lease, photo via core.storage.store_encrypted kind=proof), per-token cache rate-limit (5/5min, SystemConfig-tunable), empty description bounces back, PRG redirect -> ?submitted=1 success state. Invalid token -> 404, expired -> 410, rate-limited -> 429, all via friendly bilingual error page. Route added additively to config/urls.py at root (browser HTML, not /api/v1/). 11 new tests in test_web_maint.py (render, submit-creates-request, lease-stamp+default-category, photo-encrypted via mocked storage, empty-bounce, expired 410, invalid 404, submit-invalid 404, GET-on-submit 405, palette-not-hardcoded, rate-limit) all green; my files ruff clean; full ruff clean; makemigrations --check clean (no model changes). Full-suite reds limited to other agents in-flight billing test_tier_gate.py import + pre-existing S3/botocore NoCredentialsError (repo .env S3_BUCKET set, no creds) in dmpforms/rent storage tests, unrelated to this task.
2026-06-04 | EPIC-13/T-008 | cross-cutting | claude | done | flag propagation regression test: new khatir/featureflags/tests/test_flag_propagation.py with test_flag_toggle_propagates_to_config_public — primes /config/public cache (flag OFF), toggles via real PATCH /admin/api/flags/{key}/toggle (super admin JWT), asserts next anon /config/public read flips to ON (cache busted, <60s budget), then toggles back and asserts immediate OFF (regression both directions). Pairs with EPIC-12 T-010. End-to-end via HTTP surfaces only, no internal cache plumbing touched. 1 new test green; full featureflags suite (61 tests) green; my file ruff clean; full ruff clean; makemigrations --check clean (no model changes). No shared config/urls.py or base.py edit needed. Full-suite reds limited to pre-existing S3/botocore NoCredentialsError in dmpforms/rent storage tests, unrelated to this test-only task.
2026-06-04 | EPIC-14/T-003 | infra | claude | done | provider-agnostic router in ai-gateway: services/ai-gateway/router.py ProviderRouter.route(category, payload) fetches active providers via injected ConfigSource (60s TTL cache per task §15, env-overridable), orders primary->fallback->rest, tries each, fails over on ProviderError; every attempt logged through pluggable UsageLogger (UsageRecord mirrors AIUsageLog schema: provider/category/success/latency_ms/tokens_used/cost_usd/failover_from), success records failover_from when it failed over. Raises NoProviderConfigured (empty) / AllProvidersFailed (all error). providers/base.py: Provider protocol + ProviderConfig/ProviderResult dataclasses + generic HTTPProvider (bearer-auth JSON POST, vendor clients subclass later). Logging failures swallowed so they never mask results. 11 tests green in tests/test_router.py (primary_success, primary_fail_fallback, both_fail_raises + no-provider, inactive-skip, ttl-cache) plus 5 pre-existing health tests; ruff clean; mypy clean (5 source files). pytest-asyncio added to dev deps (asyncio_mode=auto). Infra service - own uv project, no Django/shared base.py or urls.py edit needed.
2026-06-04 | EPIC-13/T-003 | backend | claude | done | kill-switch endpoints: featureflags/killswitch_views.py (GET /admin/api/killswitches lists the 5 named switches; POST /admin/api/killswitches/{key}/toggle) + kill-switch services in featureflags/services.py (KILL_SWITCH_KEYS, _verify_fresh_mfa fresh-TOTP re-confirm even mid-session, toggle_kill_switch atomic: flip FeatureFlag.enabled, record immutable KillSwitchEvent disable/enable, admin_audit kill_switch.toggle, bust public-flags cache) + urls (additive). Super-only (IsSuperAdmin); reason mandatory + optional lawyer_reference; wrong/absent MFA -> 403, missing reason -> 400, unknown key -> 404. 16 new tests in test_killswitch.py (valid-MFA kills, wrong-MFA blocked, no-reason blocked, immutable event+actor, enable-on-restore, audit entry, cache bust, /config/public propagation, 404 unknown, anon rejected, super-only, non-super denied) green; featureflags+admin_portal suites 237 green; my files ruff clean; makemigrations --check clean (no model changes). No shared base.py/urls.py edit (only featureflags/urls.py, app-local). Full-suite reds limited to pre-existing S3/botocore NoCredentialsError in dmpforms/rent storage tests, unrelated to this task.
2026-06-04 | EPIC-14/T-007 | backend | claude | done | Django AI gateway client: khatir/ai_providers/client.py call_gateway(category, payload, *, timeout) POSTs {AI_GATEWAY_URL}/v1/{category} with the shared AI_GATEWAY_INTERNAL_TOKEN on the Authorization header and returns a normalized AIGatewayResult (data/provider_key/model_name/raw). Thin + stateless: no routing/vendor logic (that lives in the gateway, T-003). AIGatewayError on unconfigured URL (clear config error), transport failure, non-2xx (carries status_code), non-JSON, or unexpected shape; token/payloads never logged. New AI_GATEWAY_URL/_INTERNAL_TOKEN/_TIMEOUT settings in config/settings/base.py (additive); requests added as explicit dep (uv.lock synced). 10 tests in ai_providers/tests/test_client.py (happy path forwards url+payload+token+timeout, string category, timeout override, missing-url config error, no-auth-header when token absent, transport/non-2xx/non-json/bad-shape errors, token-not-in-exception) green; my files ruff clean; full ruff clean; makemigrations --check clean (no model changes). Full-suite reds limited to pre-existing S3/botocore NoCredentialsError in dmpforms/rent storage tests, unrelated to this task.
2026-06-04 | EPIC-15/T-006 | backend | claude | done | retrofit EPIC-07 rent reminders onto the admin-editable rent_reminder_due template: added NotificationTemplate.render(variables) (str.format_map over a lenient mapping that echoes unfilled {placeholders} verbatim, returns rendered title_en/title_bn/body_en/body_bn) + module-level get_template(key) helper (active row by key, raises DoesNotExist if missing/inactive) in notifications/models.py. rent/messaging._render_message now renders the seeded template (Bangla body + English body, newline-joined) via _template_variables (tenant_name/amount/property_name/due_date/payment_link) with a fallback to the original built-in copy if the template row is absent/inactive — send path never breaks. Behavior identical (still bilingual, still carries /r/{token} link); copy now admin-editable. 7 new tests (render interpolates / leaves unknown placeholder / no-vars; get_template active+skip-inactive+missing; rent test_message_uses_admin_editable_template) + updated test_message_carries_public_link_and_is_bilingual to behavioural assertions; notifications+rent send/reminder suites green; my files ruff clean; full ruff clean; makemigrations --check clean (no model fields added). Full-suite reds limited to pre-existing S3/botocore NoCredentialsError in dmpforms/rent storage tests, unrelated to this task.
2026-06-04 | EPIC-14/T-009 | backend | claude | done | admin AI-provider endpoints already implemented at HEAD (ai_providers/admin_views.py: AIProviderListCreateView GET/POST, AIProviderDetailView PATCH, AIProviderTestConnectionView POST, AIUsageView GET) + AIProviderAdminSerializer (write-only plaintext api_key encrypted to api_key_enc, has_api_key flag, never returns ciphertext) with non-BD-OCR DPA validation (endpoint_is_bangladesh: empty/foreign endpoint => dpa_reference required, enforced on POST+PATCH); call_gateway-backed test-connection, admin_audit on create/update, super+ops (IsAIProviderAdmin) gate; routes in admin_portal/admin_urls.py. Completed finish protocol (frontmatter status=done + BOARD). 21 tests in test_providers_admin.py green; full suite 142 green; ruff clean; makemigrations --check clean (no model changes).
2026-06-04 | EPIC-15/T-003 | backend | claude | done | notification delivery Celery tasks (deliver_notification fan-out + deliver_to_recipient) implemented at HEAD in notifications/tasks.py: deliver_notification re-resolves the audience_filter at send time (all/role/segment/specific; fails closed to empty queryset on a malformed filter so a stale Beat row never crashes the worker), fans out one deliver_to_recipient sub-task per recipient x channel, marks the broadcast sent. deliver_to_recipient idempotently upserts the NotificationDelivery row (no duplicate on retry), renders the recipient language variant, calls get_sender per channel (inapp/email synchronous->delivered + delivered_count bump; whatsapp/sms->sent, confirmed later by webhook), records sent/delivered/failed with error capture, bumps Notification sent_count/delivered_count atomically via F(). Completed finish protocol (frontmatter status=done + this BOARD line). 14 tests in test_delivery.py green (fan-out per recipient x channel, marks sent, skips inactive, role/specific audiences, bad-audience fails closed, missing-notification no-op, inapp delivered+counts, sms sent-not-delivered, address=phone, sender-failure->failed, idempotent no-dupe, EN/BN language render); notifications suite 91 green; full suite 1150 passed; ruff clean; makemigrations --check clean (no model changes).
2026-06-04 | EPIC-15/T-007 | backend | claude | done | admin notification broadcast endpoints wired: registered NotificationViewSet on the notifications app router (notifications/urls.py) so /admin/api/notifications resolves -- GET list (newest first), POST compose (delegates to compose_notification: validates audience/channels/schedule, resolves reach, estimates cost, dispatches/schedules delivery, audits notification.compose; DomainValidationError -> 400; sender = acting admin; response carries reach + estimated_cost), GET /{id} (NotificationDetailSerializer embeds per-recipient deliveries), POST /{id}/send-test (preview send to acting admin only via get_sender(INAPP); no audience fan-out, no counter mutation; audited notification.send_test). View+serializer+service logic already present at HEAD; this task added the route registration + endpoint test coverage. Super+ops gate (IsPlatformAdmin). 15 tests in notifications/tests/test_broadcast_endpoints.py green (list, compose creates/sets-sender/audited/400-bad-audience/400-missing-channels, retrieve embeds deliveries, send-test admin-only+counters-untouched+audited, anon rejected, super/ops allowed, finance/compliance/support denied); notifications suite 106 green; my files ruff clean; makemigrations --check clean (no model changes, no migration). Full-suite reds limited to a concurrent agent's in-flight EPIC compliance audit-log endpoint (unrelated to this task; fails identically with its work stashed).
2026-06-04 | EPIC-15/T-006 | backend | claude | done | retrofit EPIC-07 rent-link/reminder copy onto the admin-editable rent_reminder_due NotificationTemplate. rent/messaging._render_message now sources its body from get_template('rent_reminder_due').render(_template_variables(req, link)) (tenant_name=lease.tenant.name, amount=৳{whole-taka}, property_name=lease.unit.building.name, due_date=period, payment_link=link) and joins body_bn + body_en newline-first — delivery behavior unchanged (bilingual body carrying the /r/{token} link via send_with_fallback WhatsApp→SMS). Reuses existing NotificationTemplate.render + module-level get_template from T-005 (no new model/migration). Updated test_message_carries_public_link_and_is_bilingual to assert template-driven copy (Please pay using this link / ভাড়া), interpolated tenant_name, and no leftover {payment_link} token. rent send+reminder + notifications suites green; my files (messaging.py, test_send.py) ruff clean; makemigrations --check clean (no model changes). Lone full-suite red is a concurrent agent's in-flight compliance audit-log-endpoint test (unrelated; its files are uncommitted on the shared branch).
2026-06-04 | EPIC-16/T-002 | backend | claude | done | enhanced admin audit-log endpoint at GET /admin/api/audit-log (compliance/views.AdminAuditEntryListView + route in compliance/urls.py; serializer AdminAuditEntrySerializer already present at HEAD). Paginated read-only JSON envelope over append-only AdminAuditEntry (newest first), full filters admin_user/action/entity_type/entity_id/date_from/date_to, plus ?format=csv streaming export via StreamingHttpResponse + csv.writer over an echo file-like (no full-set buffering, queryset.iterator()). format=csv negotiated through a passthrough _CsvRenderer registered on the view (renderer_classes=[JSONRenderer,_CsvRenderer]) so DRF content negotiation accepts the format value instead of 404ing; branch on request.accepted_renderer.format. Compliance+super (audit section) gate reusing IsComplianceAdmin. 13 tests in compliance/tests/test_audit_log_endpoint.py green (list, read-only shape, filter by admin_user/action/entity_type+id/date-range, csv streams filtered rows + headers, anon rejected, super+compliance allowed, ops/finance/support denied). Full suite 1178 passed; ruff clean; makemigrations --check clean (no model changes).

2026-06-04 | EPIC-16/T-004 | backend | claude | done | PDPA data-request queue endpoints in new compliance/data_request_views.py (kept out of the concurrently-edited views.py to avoid the shared-file race). GET /admin/api/data-requests: paginated read-only DataRequestSerializer (adds derived sla_state on_track/due_soon/overdue), filters status/type/sla (sla buckets from settings.DATA_REQUEST_SLA_DUE_SOON_DAYS, default 7; SLA window DATA_REQUEST_SLA_DAYS default 30 added to settings/base.py). POST /admin/api/data-requests/{id}/process: DataRequestProcessSerializer {action approve|reject, reason} (reason mandatory on reject); approve export -> generate export package stub + status=completed+completed_at; approve delete -> queue erasure stub + status=processing; reject -> status=rejected+completed_at; all stamp handled_by and write admin_audit (data_request.approve_export/approve_delete/reject) with before/after+reason+ip; select_for_update + not-pending=>409 ConflictError (idempotent, no double-audit), missing=>404. Compliance+super (audit section) gate via own IsComplianceAdmin. Routes appended to compliance/urls.py. 19 tests in compliance/tests/test_data_request_endpoints.py green (list, sla_state shape, filter status/type/sla buckets, auth+role gate compliance/super allow & ops/finance/support deny, approve-export completes+audits, approve-delete->processing, reject requires-reason(400)/rejected+audits, 409 already-resolved, 404 missing, process role-gated, no-double-process single audit). Full suite green; ruff clean; mypy clean on my files (pre-existing authentication.py noise only); makemigrations --check clean (no model changes).

2026-06-04 | EPIC-14/T-005 | infra | claude | done | ASR(voice) + chat provider stubs in ai-gateway (services/ai-gateway/providers/asr.py, chat.py) subclassing HTTPProvider: validate payload (audio_b64 / non-empty messages), default language=bn, normalise vendor JSON into voice {transcript,language,fields} and chat {reply,role} envelopes; exported via providers/__init__.py + factory helpers. API key sent as bearer header, never logged/embedded. 7 tests in tests/test_stub_providers.py (httpx.MockTransport mocked vendor, no real API): envelope normalise, sparse defaults, missing-audio/empty-messages reject, router voice+chat dispatch smoke. ruff clean on my files; full gateway suite 24 passed.

2026-06-04 | EPIC-14/T-006 | infra | claude | done | per-call usage logging in ai-gateway: new services/ai-gateway/usage.py HTTPUsageLogger (POSTs each router UsageRecord JSON to Django /admin/api/ai-usage with internal-token bearer auth) + build_usage_logger factory degrading to NoOpUsageLogger when no django_base_url configured; config.py adds django_base_url/ai_usage_path/ai_usage_timeout_seconds + usage_logging_enabled/ai_usage_url props. Internal token only in Authorization header, never in record/log; UsageRecord carries only accounting metadata (no api_key). 6 new tests in tests/test_usage.py via httpx.MockTransport (mocked Django, no real call): posts record+auth, omits header w/o token, raises on 5xx, factory noop-without-url + http-when-configured, router logging-failure never breaks live call. ruff All checks passed; mypy Success on usage.py/config.py; full gateway suite 24 passed.
2026-06-04 | EPIC-09/T-010 | backend | claude | done | dashboard N+1 / query-count guard in khatir/dashboard/tests/test_perf.py. setUpTestData seeds realistic portfolio (5 buildings, 20 units across them, 1 active lease/unit, 50 rent-schedule payments spread over leases/periods/statuses PAID/PENDING/REQUESTED/OVERDUE, 20 expenses across units+categories). test_dashboard_query_count wraps get_dashboard(landlord, months=6) in CaptureQueriesContext and asserts <= QUERY_BUDGET=12 (upper-bound per §15: selectors do ~8 fixed aggregate/grouped passes; any per-row loop over the 50 schedules/20 units adds tens and trips the ceiling). 1 new test green; full suite 1202 passed; ruff clean; makemigrations --check clean (no DB changes).
2026-06-04 | EPIC-12/T-002 | backend | claude | done | post_save/post_delete signal on PricingTier busts the shared /config/public cache key (billing/signals.py, registered via BillingConfig.ready()); reuses EPIC-10 T-005 PUBLIC_CONFIG_CACHE_KEY + invalidate_public_config_cache() so the explicit T-001 edit-service bust and the model-level signal are idempotent (catches admin/migration/other write paths too). 4 new tests in billing/tests/test_cache_invalidation.py: save/create/delete each clear primed key; /config/public reflects new price after bust. ruff clean; makemigrations --check clean (no DB changes); full suite green.
2026-06-04 | EPIC-09/T-002 | backend | claude | done | dashboard read endpoint GET /api/v1/dashboard?months=N (khatir/dashboard/views.py DashboardView APIView, serializers.py DashboardSerializer + MonthPoint/CategoryTotal output serializers, urls.py mounted in config/urls.py under api/v1/). Calls T-001 get_dashboard selectors; months param clamped 1..24, defaults to dashboard_months_default SystemConfig (T-003) via get_config w/ fallback 6; payload cached per-user (key dashboard:<pk>:<months>) 60s TTL (never global, §15); guarded by IsLandlordOrManager; owner-scoped through selectors' for_user. 8 new API tests in tests/test_dashboard_api.py: full response shape (all 13 metric keys), scoped (2nd landlord excluded), months param length, config-default length, invalid-months fallback, per-user caching, requires-auth (401/403), tenant-role forbidden (403). ruff clean; makemigrations --check clean (no DB changes); full suite 1210 passed.
2026-06-04 | EPIC-14/T-004 | infra | claude | done | concrete OCR provider HTTP client in ai-gateway (services/ai-gateway/providers/ocr.py GoogleVisionOcrProvider subclassing HTTPProvider): accepts image bytes (or base64 str), base64-encodes into Google Cloud Vision images:annotate DOCUMENT_TEXT_DETECTION request (endpoint_url defaultable/overridable), api_key from ProviderConfig sent as Vision ?key= query param (never in body/logs), parses fullTextAnnotation/legacy textAnnotations into normalized ExtractedTenant JSON {name,nid_number,dob,address each {value,confidence}} mirroring Django dto; dob normalized to ISO YYYY-MM-DD (DD/MM/YYYY + ISO), nid digits-only; unreadable image -> empty DTO, Vision per-image error/transport failure -> ProviderError (router fails over). build_ocr_provider factory + exports added additively to providers/__init__.py. 11 tests in tests/test_ocr.py via httpx.MockTransport (mocked Vision, no real API): request shape+key-as-query+base64, full extraction+normalization, ISO passthrough, empty image, base64-str input, missing-image/HTTP-5xx/Vision-error -> ProviderError, legacy shape, factory, router dispatch smoke. ruff clean; mypy Success on ocr.py; full gateway suite 35 passed.
2026-06-04 | EPIC-16/T-009 | docs | claude | blocked | MVP quality gate FAILS — MVP not shippable. make status shows EPIC-00→16 NOT all done/verified: 136/197 MVP tasks done (EPIC-04 8/16, EPIC-06 6/10, EPIC-07 9/14, EPIC-08 7/12, EPIC-09 4/10, EPIC-11 7/12, EPIC-12 4/10, EPIC-13 5/8, EPIC-14 9/12, EPIC-15 8/14, EPIC-16 5/9). Own deps EPIC-16 T-006/T-007/T-008 still todo. make screen-coverage target does not exist. EPIC-05 T-010 DMP hard gate IS done (satisfied) but insufficient alone. No _completion_report.md / RELEASE.md written (would be false). Backend ruff + makemigrations --check clean. Re-run only after all EPIC-00→16 reach done/verified and make test && make lint green across all 3 apps. status=in-progress. No tests (docs task).
2026-06-05 | EPIC-11/T-007 | admin | claude | done | admin login + MFA screen (Next.js). app/login/page.tsx now a real client email+password form; app/login/mfa/page.tsx 6-digit TOTP step. Both proxy through server-side route handlers app/api/auth/{login,verify-mfa,logout}/route.ts which call EPIC-11.T-003 backend (/admin/api/auth/*) via lib/auth/api.ts (zod-validated LoginResult|MfaChallenge|TokenResult) and write the admin access token into an HTTP-only SameSite=strict cookie (lib/auth/session.ts) — token never reaches client JS. MFA challenge token also held in a short-lived HTTP-only cookie between steps (read server-side), so it is never exposed. Error states: invalid creds/disabled (backend uniform 401), wrong code (clears field + retry), rate-limit 429, expired challenge 440 -> restart /login. Success -> /dashboard. guard.ts re-exports getSession/ADMIN_SESSION_COOKIE from session.ts (no dashboard behaviour change). 8 new RTL tests in test/login.test.tsx (form render, MFA-required routing, no-MFA direct dashboard, invalid-creds error, verify-disabled-until-6-digits, verify success, wrong-code error+clear, digit-strip). Gate: npm install + build OK (routes /login, /login/mfa, /api/auth/* present), tsc + eslint clean, vitest 10 passed.
2026-06-05 | EPIC-11/T-008 | admin | claude | done | authenticated shell + real session guard (Next.js). Replaced EPIC-00 stub guard: lib/auth/admin-session.ts getAuthenticatedAdmin() reads the T-007 HTTP-only session cookie then calls backend GET /admin/api/auth/me with the bearer token (token stays server-side), zod-validates the AdminUserSerializer response; missing cookie / 401-403 / unreachable backend (fail-closed) / disabled account -> null -> layout redirects /login. Role-aware sidebar: _nav.ts gains roles[] + navForRole(role) mirroring backend admin_portal/permissions.py SECTION_ROLES + spec §2.1 (super=all; ops+support=Users/Support; finance=Pricing; compliance=Features/Kill-switch/Compliance; Dashboard+Analytics=all; Notifications/AI providers/System/Admin users/Security=super-only); Sidebar takes a role prop and filters. Topbar (client) shows name + human-readable role badge + Log out button POSTing /api/auth/logout then router.replace('/login'). SessionTimeoutWarning client banner reads JWT exp (decoded unverified for the timestamp only), counts down within 5 min, "Stay signed in" -> server refresh re-validates /me, routes /login on expiry. 14 new tests (test/admin-session.test.ts guard: no-cookie/valid-me/401/disabled/unreachable/malformed; test/sidebar.test.tsx navForRole matrix + role-aware render; test/topbar.test.tsx name+badge+logout). Gate: npm install + build OK, tsc + eslint clean, vitest 24 passed.
2026-06-05 | EPIC-11/T-009 | admin | claude | done | platform dashboard page (Next.js) replacing the EPIC-00 placeholder. New data layer lib/api/dashboard.ts (zod-validated fetchDashboard for GET /admin/api/dashboard — T-005 payload: KPIs + live health; recent_activity modeled optional default [] since the audit list API is T-011/not-done). page.tsx is now a client component using TanStack Query with refetchInterval 60_000 (task §2) and loading (skeleton) / error (retry button) / data states. Three token-only components: KpiCard (uppercase label + title-font value + context chip), ActivityFeed (header + audit-entry rows with status dot/action/actor/summary/relative time, empty state when no entries), HealthTile (app/database/cache status rows -> chips ok→sage/degraded→butter/down→danger). KPI tiles surface real T-005 metrics (total_users, active_landlords, properties + occupied/total units, rent this-month/all-time, DMP forms, active subscriptions) — prototype MRR/churn mock dropped (no selectors). All colours/spacing/radii from Notun Din tokens. 5 new RTL tests in test/dashboard.test.tsx (KPI render from API, activity feed with entries, activity empty state, health panel incl DOWN chip, error state + Retry). Gate: npm install + build OK (route /dashboard present), tsc + eslint clean, vitest 29 passed (5 new).
2026-06-05 | EPIC-11/T-010 | admin | claude | done | sidebar nav + coming-soon stubs. The role-aware sidebar (all §3.1 nav items wired to routes, active-link highlight, "Soon" badges) and the reusable ComingSoon stub were delivered by T-008 (_nav.ts NAV_ITEMS+navForRole, components/features/sidebar.tsx, components/features/coming-soon.tsx); every unbuilt module page app/(dashboard)/<module>/page.tsx renders <ComingSoon title=…/>. Coming-soon is a shared component reused per module route (each stub keeps its own route/title/nav context) rather than one shared /coming-soon route. T-010 adds the test coverage T-008 left open: test/sidebar.test.tsx extended (all 13 §3.1 entries present, distinct absolute routes, comingSoon flags + "Soon" badge count, link hrefs match routes); new test/sidebar-active.test.tsx (usePathname=/pricing -> Pricing link gets bg-sageBg/text-sageDk while Dashboard does not; child route keeps parent active); new test/coming-soon.test.tsx (title heading + "Coming soon" placeholder). No production code changes needed. Gate: npm install + npm run build OK (all 13 module routes generated, tsc clean), vitest 38 passed across 7 files (9 new).
2026-06-05 | EPIC-11/T-011 | admin | claude | done | audit-log viewer (Next.js) + backend GET /admin/api/audit-log. Backend: admin_portal/audit_views.py AuditLogView (read-only, cursor-paginated newest-first via StandardCursorPagination over the immutable AdminAuditEntry ledger written by T-002 admin_audit; AdminJWTAuthentication + IsAdminAuthenticated, gated on the audit section super+compliance via new IsAuditReadAdmin reusing _UsersSectionPermission) + audit_serializers.py AuditLogEntrySerializer (read-only fields incl before_json/after_json + denormalized actor label, System for null admin); optional AND-combined filters admin_user(id|system) / action / entity_type / from / to (ISO created_at bounds via parse_datetime); route path("audit-log", ...) appended to admin_urls.py. No create/update/delete route -> non-GET 405 (matches append-only model). 18 tests in tests/test_audit_log_view.py (compliance+super read, ops/support/finance 403, anon 401/403, diff+actor payload, System actor, newest-first, filter action/entity_type/admin_user/system/date-range[backdated via raw SQL since model immutable], no-write-methods 405) green; full admin_portal suite 195 passed; ruff clean; mypy clean on my 2 new files (pre-existing authentication.py/maintenance noise only). Frontend: lib/api/audit.ts (zod-validated fetchAuditLog + auditLogPath/auditQueryKey + AuditFilters incl opaque cursor), components/admin/audit_table.tsx (filter bar admin_user/action/entity_type/from/to, token-styled table when/actor/action/entity/ip, per-row before/after JSON diff expander + reason, Prev/Next cursor buttons, empty state; read-only no edit/delete), app/(dashboard)/audit/page.tsx (client TanStack Query keepPreviousData, cursor extracted from DRF next/previous links, loading skeleton / error+Retry / data states), _nav.ts adds compliance-gated /audit "Audit log" (ScrollText) live item. All colours/spacing/radii from Notun Din tokens (rounded-sm/-button/-card, sageBg, mutedDk, font-mono). 6 new RTL tests in test/audit.test.tsx (render rows, expand diff incl reason, filter-change re-queries, cursor next-page, empty state, error+Retry); widened sidebar "unbuilt=comingSoon" test to allow built pages (Dashboard+Audit log). Gate: npm install + npm run build OK (route /audit generated, tsc + eslint clean via next build), vitest 44 passed (8 files).
2026-06-05 | EPIC-12/T-006 | admin | claude | done | reusable tier impact preview modal. New components/admin/impact_preview_modal.tsx (ImpactPreviewModal): presentational confirmation surface mirroring the "Impact preview" panel in KhatirAdmin.jsx (Pricing › Edit tier). Takes a TierImpact prop (tierLabel/tierKey, old+new monthly BDT, subscribersAffected, old+new includesVerification) rather than fetching, so T-005 pricing editor and T-008 manual-upgrade can both reuse it after calling the T-001 preview endpoint. Renders the change sentence (FREE/৳ codes), a 2-col stat grid (subscribers affected + est. monthly revenue delta, sage when +, rose when −, ink when 0), and an NID-verification-loss alert (butterBg/butter border, roseDk heading) shown only when old includesVerification && !new && subscribers>0. Exported pure helpers revenueDelta = (new−old)×subscribers and losesVerification for direct reuse/test. Loading skeleton state (aria-busy) when loading||!impact; data state otherwise. Modal: backdrop (bg-ink/40) + ✕ + Cancel all call onClose; optional onConfirm renders an "Apply change" CTA (disabled while loading/no-impact/confirmDisabled). lucide-react AlertTriangle/X icons. All colours/spacing/radii from Notun Din tokens (rounded-card/-chip/-tile, sageBg/sageDk, butterBg/butter, roseDk, mutedDk, font-title/-mono, s1..s8) — no hardcoded hex/px. 10 new tests in test/impact-preview.test.tsx (revenueDelta sign+magnitude, losesVerification truth table incl gain/zero-subs, closed→null, counts+negative delta+no-warning, positive delta +sign, NID warning, loading skeleton, onClose from ✕+backdrop, confirm CTA presence/fire, confirmDisabled). Gate: npm install + npm run build OK, tsc --noEmit clean, vitest 54 passed across 9 files (10 new).
2026-06-05 | EPIC-12/T-005 | admin | claude | done | pricing editor page (Next.js). New lib/api/pricing.ts (zod-validated fetchPricingTiers GET /admin/api/pricing/tiers, previewTier POST .../{key}/preview, editTier PATCH .../{key} with mandatory reason) consuming the T-001 endpoints; prices kept as wire strings. app/(dashboard)/pricing/page.tsx is now a SERVER component enforcing the finance+super route guard server-side (getAuthenticatedAdmin → role in {super,finance}; access-denied card otherwise, mirroring backend IsPricingAdmin) and renders the PricingEditor client surface. components/admin/pricing_editor.tsx: TanStack Query list with loading skeleton / error+Retry / data states. components/admin/tier_table.tsx: editable token-styled tier table (label/tenants/monthly/annual/verification credits/active + pencil), pencil opens an inline TierEditor modal (EN/BN labels, tenant min/max, monthly/annual price, credits, sort order, verification+active toggles), "Preview impact" (disabled until a non-blank reason) calls the read-only preview endpoint then opens the reusable T-006 ImpactPreviewModal showing subscribers affected + revenue delta; "Apply change" PATCHes with the reason and invalidates the tiers query after 2s so the cache-busted public catalogue (≤60s) is reflected. _nav.ts: Pricing item de-flagged comingSoon → live /pricing (finance-gated). All colours/spacing/radii from Notun Din tokens (rounded-card/-button/-chip/-input, sageBg/sageDk, roseDk, mutedDk, font-title/-mono) — no hardcoded hex/px. 4 new RTL tests in test/pricing.test.tsx (rows render incl FREE, error+Retry, open editor → reason-gated preview → confirm calls edit, blank reason does not call preview); widened sidebar live-pages set to include Pricing. Gate: npm install + npm run build OK (route /pricing dynamic/server-rendered, tsc clean), eslint clean on new files, vitest 58 passed across 10 files (4 new).
2026-06-05 | EPIC-12/T-007 | admin | claude | done | users list + search page (Next.js). New lib/api/users.ts (zod-validated fetchUsers GET /admin/api/users via single ?q= search + ?page= page-number pagination, consuming T-003 AdminUserListSerializer in the standard {results,pagination:{next,previous,count}} envelope; raw phone present on the wire but never rendered). app/(dashboard)/users/page.tsx replaces the ComingSoon stub and is a SERVER component enforcing the role guard server-side (getAuthenticatedAdmin → role in {super,ops,support}; access-denied card otherwise, mirroring backend IsUsersReadAdmin — support is read-only per spec §2.1, so the guard is ops+support+super, not ops+super); renders the UsersBrowser client island. components/admin/users_browser.tsx: TanStack Query with keepPreviousData, loading skeleton / error+Retry / empty / data states, search resets to page 1, Prev/Next drive ?page. components/admin/users_table.tsx: token-styled search form (role=search) + table columns Name (Link → /users/{id} detail T-008) / Phone (server masked_phone only) / Role / Tier / Status (sage Active vs danger Suspended chip); Tier renders — because the compact search projection carries no subscription tier (loaded on detail). All colours/spacing/radii from Notun Din tokens (rounded-sm/-button/-card/-chip, sageBg, mutedDk, font-title/-mono, s1..s8) — no hardcoded hex/px. _nav.ts: Users item de-flagged comingSoon → live /users (ops+support gated). 8 new RTL tests in test/users.test.tsx (rows render, masked phone shown + raw phone never rendered, row links /users/{id}, search submit re-queries {q,page:1}, Suspended chip, next-page → page:2, empty state, error+Retry); widened sidebar live-pages set to include Users. Gate: npm install + npm run build OK (route /users dynamic/server-rendered), tsc --noEmit clean, eslint clean on new files, vitest 66 passed across 11 files (8 new).
2026-06-05 | EPIC-12/T-008 | admin | claude | done | user detail + actions page (Next.js). New app/(dashboard)/users/[id]/page.tsx — a SERVER component that reads the async [id] param, enforces the role guard server-side (getAuthenticatedAdmin → read roles {super,ops,support} mirroring backend IsUsersReadAdmin; access-denied card otherwise) and passes canWrite={role∈{super,ops}} (write roles, mirroring IsUsersWriteAdmin — support is read-only per spec §2.1) plus a "Back to users" link to the UserDetail island. components/admin/user_detail.tsx: TanStack Query GET /admin/api/users/{id} (loading skeleton / error+Retry / data), lays out Profile (name/masked phone — raw phone never rendered/role/language/created/last-login), Subscription (tier_label/status/cycle/next-billing/started, or "no subscription"), Usage (buildings/tenants/subscriptions counters), and a recent admin Audit trail table (when/action/reason). Header shows active(sage)/suspended(danger) status chip. Action buttons (canWrite only): Suspend (active) / Reactivate (suspended) / Upgrade subscription, each opening a confirm dialog (role=dialog, aria-modal, backdrop+Cancel close). Suspend + upgrade require a non-blank reason (confirm disabled until entered); upgrade dialog loads tiers via the existing T-005 fetchPricingTiers select + billing-cycle select. On success each mutation invalidates the detail query so the status badge flips to Suspended immediately (spec §15). lib/api/users.ts extended with zod-validated adminUserDetail/adminSubscription/adminAuditTrailRow/adminUsage schemas + fetchUserDetail, suspendUser(reason), reactivateUser(reason?), upgradeSubscription({tierId,billingCycle?,reason}) consuming the T-003 endpoints (resource bodies returned directly). All colours/spacing/radii from Notun Din tokens (rounded-card/-button/-chip/-input, sageBg/sageDk, roseDk, danger, mutedDk, font-title/-mono, s1..s8) — no hardcoded hex/px. 8 new RTL tests in test/user-detail.test.tsx (renders profile+subscription+usage+audit, masked-not-raw phone, active badge, suspend dialog reason-gated, suspend confirm → suspendUser called + refetch flips badge to Suspended, reactivate dialog for suspended user, upgrade dialog tier+reason-gated → upgradeSubscription called with {tierId,billingCycle,reason}, action buttons hidden for read-only viewers, error+Retry). Gate: npm install + npm run build OK (route /users/[id] dynamic/server-rendered, TypeScript+eslint clean), vitest 74 passed across 12 files (8 new).
2026-06-05 | EPIC-12/T-009 | admin | claude | done | refund queue page (Next.js). New lib/api/refunds.ts (zod-validated fetchRefunds GET /admin/api/billing/refunds → results array of pending payment intents, processRefund POST .../{id}/process with {approve,reason}) consuming the T-004 endpoints (queue rows are unresolved customer-realm subscription.payment_intent audit entries per the T-004 MVP deviation). app/(dashboard)/billing/refunds/page.tsx is a SERVER component enforcing the finance+super route guard server-side (getAuthenticatedAdmin → role in {super,finance}; access-denied card otherwise, mirroring backend IsBillingAdmin) and renders the RefundQueue client island. components/admin/refund_queue.tsx: TanStack Query with loading skeleton / error+Retry / empty / data states; token-styled table (User #id / Tier / Cycle / Provider / Requested-date / per-row Approve+Deny). Approve and Deny each open a confirm dialog (role=dialog, aria-modal, backdrop+Cancel close); approve records an optional reason (audited), deny requires a non-blank reason (confirm disabled until entered; re-checked server-side). On success the queue query is invalidated so the processed row drops out immediately. _nav.ts: new live finance-gated /billing/refunds "Refunds" item (Receipt icon). All colours/spacing/radii from Notun Din tokens (rounded-card/-button/-input, sageBg/sageDk, danger, roseDk, mutedDk, font-title/-mono, s1..s8) — no hardcoded hex/px. 5 new RTL tests in test/refunds.test.tsx (rows render, empty state, approve reason-optional → processRefund {approve:true,reason:""} + refetch drops row, deny reason-gated → processRefund {approve:false,reason}, error+Retry); widened sidebar.test.tsx route regex to allow multi-segment paths and added Refunds to the live-page set. Gate: npm install + npm run build OK (route /billing/refunds dynamic/server-rendered, TypeScript+eslint clean), vitest 79 passed across 13 files (5 new).
2026-06-05 | EPIC-12/T-010 | admin | claude | done | pricing-change propagation integration test (the <60s guarantee test). New apps/api/khatir/admin_portal/tests/test_propagation.py::test_tier_change_propagates_to_config_public drives the full real chain end to end: issues an admin JWT (finance role, T-001 IsPricingAdmin gate), primes the shared PUBLIC_CONFIG_CACHE_KEY with a sentinel as a bust probe, asserts /api/v1/config/public reports bundle_20's seeded price, PATCHes a new monthly_price (+৳123, reason="Annual repricing") through the real /admin/api/pricing/tiers/{key} endpoint, then asserts (a) the cache key was busted (explicit edit_tier service call + the T-002 post_save signal, both idempotent) and (b) /config/public now returns the new price. Unlike the T-001/T-002 unit tests it mocks nothing and saves no model directly, so it regression-guards the whole path — admin auth, role gate, edit service write+audit+bust, invalidation signal, and the live serialized_tiers() selector (config_public is computed live today; the sentinel only proves the bust fired). No production code changed; integration test only. Gate: pytest test_propagation.py green; full related suite (test_pricing_admin + test_cache_invalidation + test_config_public + test_propagation) 28 passed; ruff clean on the new file (mypy "factory has no attribute pk/refresh_from_db" is the pre-existing factory_boy typing noise shared by test_pricing_admin.py, not introduced here).
2026-06-05 | EPIC-13/T-005 | admin | claude | done | flags console page (Next.js). New lib/api/flags.ts (zod-validated fetchFeatureFlags GET /admin/api/flags + toggleFeatureFlag PATCH /admin/api/flags/{key}/toggle, consuming the T-002 endpoints; list fetch tolerates both bare-array and {results} paginated envelopes per test_flag_endpoints.py). app/(dashboard)/features/page.tsx replaces the ComingSoon stub and is a SERVER component enforcing the super+ops route guard server-side (getAuthenticatedAdmin → role in {super,ops}; access-denied card otherwise, mirroring backend IsPlatformAdmin in featureflags/views.py) and renders the FlagsConsole island. components/admin/flags_console.tsx: TanStack Query with loading skeleton / error+Retry / data states; intro card (≤60s propagation note); token-styled flag table (key/description, scope chip, On/Off status chip, Toggle switch). Toggling opens a ToggleConfirm dialog (role=dialog, aria-modal, backdrop+Cancel close) BEFORE flipping — primary "Enable flag" / danger "Disable flag" CTA; on confirm calls toggleFeatureFlag (audited + cache-busted) then invalidates featureFlagsQueryKey so the row reflects the new state. _nav.ts: Features de-flagged comingSoon and re-gated ops (super sees all) to match the backend platform-section gate; doc comment updated. All colours/spacing/radii from Notun Din tokens (rounded-card/-button/-chip/-pill, sageBg/sageDk, roseDk, mutedDk, lineDk, font-title/-mono, s1..s8) — no hardcoded hex/px. 4 new RTL tests in test/flags.test.tsx (rows render key/scope/On+Off, error+Retry, toggle opens confirm and does NOT call API until confirmed → confirm fires toggleFeatureFlag, Cancel never calls toggle); widened sidebar.test.tsx live-pages set to include Features. Gate: npm install + npm run build OK (route /features dynamic/server-rendered, tsc --noEmit clean, eslint clean), vitest 83 passed across 14 files (4 new).
2026-06-05 | EPIC-13/T-006 | admin | claude | done | kill-switch panel page (Next.js). New lib/api/killswitch.ts (zod-validated fetchKillSwitches GET /admin/api/killswitches → the 5 named switches as FeatureFlagSerializer rows, list tolerant of bare-array + {results} envelopes; toggleKillSwitch POST .../{key}/toggle with {mfa_code,reason,lawyer_reference}) consuming the T-003 super-only endpoints; a switch is "live" while enabled=true. app/(dashboard)/kill-switch/page.tsx replaces the ComingSoon stub and is a SERVER component enforcing a super-only route guard server-side (getAuthenticatedAdmin → role==='super'; access-denied card otherwise, mirroring backend IsSuperAdmin in featureflags/killswitch_views.py — NOTE the implemented endpoint is super-only, not the spec §2.1 compliance assignment, so the nav+page mirror the real gate) and renders the KillSwitchPanel island. components/admin/killswitch_panel.tsx: prominent red "Emergency Kill-Switch Panel" header card, a red role=alert warning banner whenever ANY switch is OFF (safety-critical, task §15), and a per-switch row (check/x state icon, key, ★ MASTER badge for master_kill_switch, ENABLED/DISABLED chip, description, last-changed-by/at line sourced from updated_by/updated_at since T-003 GET returns no event list, Disable/Enable button); TanStack Query loading skeleton / error+Retry / data states. components/admin/killswitch_dialog.tsx: friction-heavy MFA confirm dialog (role=dialog, aria-modal, backdrop+Cancel) — mandatory reason textarea (≥20 chars, spec §4.4.2), optional lawyer reference, 6-digit numeric TOTP re-confirm field; Confirm-with-MFA disabled until reason≥20 AND a 6-digit code are entered; on confirm POSTs {mfaCode,reason,lawyerReference} then invalidates killSwitchesQueryKey (backend re-verifies TOTP, records immutable KillSwitchEvent, busts public-config cache ≤60s; bad code → 403 surfaced inline). _nav.ts: Kill-switch de-flagged comingSoon → live /kill-switch, re-gated super-only (empty roles list; super sees all) to match the IsSuperAdmin backend gate; doc comment updated. All colours/spacing/radii from Notun Din tokens (rounded-card/-button/-chip/-pill/-md, danger/dangerBg, roseBg, sageBg/sageDk, cream, ink2, mutedDk, font-title/-mono, s1..s8) — no hardcoded hex/px. 8 new RTL tests in test/killswitch.test.tsx (5 switches + ★ MASTER render, warning banner shows when any OFF, banner absent when all ON, Disable opens MFA dialog without calling endpoint + reason/MFA fields present, Confirm gated until reason≥20 AND 6-digit code, confirm fires toggleKillSwitch with {key,mfaCode,reason,lawyerReference}, Cancel never calls toggle, error+Retry); widened sidebar.test.tsx live-pages set to include Kill-switch. Gate: npm install + npm run build OK (route /kill-switch dynamic/server-rendered, tsc --noEmit clean), vitest 91 passed across 15 files (8 new).
2026-06-05 | EPIC-14/T-011 | admin | claude | done | AI providers page (Next.js). New lib/api/ai-providers.ts (zod-validated fetchAIProviders GET /admin/api/ai-providers, createAIProvider POST, updateAIProvider PATCH .../{id}, testAIProviderConnection POST .../{id}/test-connection — consuming the T-009 endpoints; list tolerant of bare-array + {results} envelopes; api_key write-only/never returned, only has_api_key surfaced; exported endpointIsBangladesh mirroring the backend non-BD .bd-host DPA rule for UI gating). app/(dashboard)/ai-providers/page.tsx replaces the ComingSoon stub and is a SERVER component enforcing the super+ops route guard server-side (getAuthenticatedAdmin → role in {super,ops}; access-denied card otherwise, mirroring IsAIProviderAdmin in ai_providers/admin_views.py) and renders the AIProvidersPanel island. components/admin/ai_providers_panel.tsx: 4 category tabs (Chat/Voice/OCR/Lease, role=tablist/tab/tabpanel) filtering one shared TanStack Query list client-side, primary+fallback active-summary cards, per-provider rows (vendor, Primary/Fallback/Active chips, model + masked "key ••••••••••" / "no key" line, Edit) + Add provider; loading skeleton / error+Retry states. ProviderForm modal (role=dialog, aria-modal, backdrop+Cancel): vendor (datalist seeded with spec §4.6.1 suggested vendors per category, free-text allowed), model, API key (type=password, placeholder notes "leave blank to keep current" when a key exists, sent only when typed — task §15), endpoint URL, primary/fallback/active toggles; on OCR with a non-BD endpoint shows a butter Data-residency warning (role=alert) + required DPA reference field and disables Save until supplied (mirrors the T-009 serializer rule); Save create/update-PATCHes then invalidates the query. Test-connection row (edit mode) fires testAIProviderConnection and shows Connected·model (sageDk check) / detail (roseDk x) / Request failed. _nav.ts: AI providers de-flagged comingSoon → live /ai-providers, gated ops (super sees all) to match the platform-section backend gate. All colours/spacing/radii from Notun Din tokens (rounded-card/-button/-input/-pill, sageBg/sageDk, butterBg/butter/butterDk, roseDk, mutedDk, ink/card, font-title/-body/-mono, s1..s8) — no hardcoded hex/px. 8 new RTL tests in test/ai-providers.test.tsx (endpointIsBangladesh truth table, 4 tabs render, tab filters providers + masked key shown/raw never, error+Retry, test-connection fires + shows Connected result, DPA warning blocks save until reference entered then updateAIProvider called, add-provider form → createAIProvider); widened sidebar.test.tsx live-pages set to include AI providers. Gate: npm install + npm run build OK (route /ai-providers dynamic/server-rendered), eslint clean on new files, vitest 99 passed across 16 files (8 new).
2026-06-05 | EPIC-14/T-012 | admin | claude | done | AI usage panel page (Next.js). New lib/api/ai-usage.ts (zod-validated fetchAIUsage GET /admin/api/ai-usage → {by_category[{category,request_count,tokens_used,cost_usd(string USD),call_count,success_count}], totals} consuming the T-009 endpoint returned directly via core.responses.success; range-scoped aiUsageQueryKey; date window forwarded as from/to query params; pure errorRate/successRate helpers = (calls−successes)/calls returning null on zero calls). app/(dashboard)/ai-providers/usage/page.tsx is a SERVER component enforcing the super+ops route guard server-side (getAuthenticatedAdmin → role in {super,ops}; access-denied card otherwise, mirroring IsAIProviderAdmin in ai_providers/admin_views.py) and renders the AIUsagePanel island. components/admin/ai_usage_panel.tsx: TanStack Query loading skeleton / error+Retry / data states; date filter (From/To date inputs + Clear, role=form "Usage date filter", min/max cross-bound) that re-keys the query; 4 SummaryCards (Requests / Tokens / Total cost USD emphasised / Success rate — totals as the running cost for the period, task §15); token-styled "Usage by category" table (one row per AICategory incl. zero-usage categories: Category / Requests / Tokens / Cost(USD) / derived Error rate, elevated rates in roseDk); "Failover & errors" log listing each category with ≥1 failed call (call_count−success_count) with an all-clear CheckCircle state otherwise. All colours/spacing/radii from Notun Din tokens (rounded-card/-input/-chip, sageBg/sageDk, roseDk, mutedDk, ink/card/muted, font-title/-mono, s1..s8) — no hardcoded hex/px. DEVIATION: T-009 endpoint exposes no latency column or standalone failover stream, so error-rate + failover log are derived from the call/success counts it returns. 9 new RTL tests in test/ai-usage.test.tsx (errorRate/successRate truth + null-on-zero, range-scoped query key, table row-per-category + OCR cost/5.0% error, running cost total, failover log lists 5-of-100, all-clear state, date-range re-fetch from/to, error+Retry). Gate: npm install + npm run build OK (route /ai-providers/usage dynamic/server-rendered, TypeScript clean), vitest 108 passed across 17 files (9 new).
2026-06-05 | EPIC-15/T-010 | admin | claude | done | notification composer page (Next.js). New lib/api/notifications.ts (zod-validated fetchNotifications GET /admin/api/notifications, composeNotification POST /admin/api/notifications → broadcast + injected reach/estimated_cost preview fields, sendTestNotification POST .../{id}/send-test — consuming the T-007 super+ops endpoints; list tolerant of bare-array + {results} envelopes; AUDIENCE_TYPES/CHANNELS/SCHEDULE_TYPES/CUSTOMER_ROLES/TEMPLATE_VARIABLES constants mirroring notifications/enums.py + core Channel/Role; CHANNEL_COST_BDT + pure estimateCost(reach,channels)=reach×Σ paid-channel cost matching services.py with inapp/email free). app/(dashboard)/notifications/page.tsx replaces the ComingSoon stub and is a SERVER component enforcing the super+ops route guard server-side (getAuthenticatedAdmin → role in {super,ops}; access-denied card otherwise, mirroring IsPlatformAdmin in notifications/views.py) and renders the NotificationComposer island. components/admin/notification_composer.tsx: audience radiogroup (all/role/segment/specific — role+segment expose a customer-role checkbox set, specific exposes a comma/space-separated User IDs textarea), channels multi-select cards with per-channel Free/৳/msg cost chips, bilingual Title+Body (EN/BN, dir=auto) with insertable {name}/{unit}/{tier}/{rent_amount}/{building_name} variable chips that splice at the caret, schedule radiogroup (now/scheduled datetime-local/recurring minute+hour+day-of-week crontab), and a sticky reach+cost preview sidebar (explicit count for a specific audience, em-dash + "resolved on the server" note for broad audiences). Submit (disabled until ≥1 channel + valid audience + all four content fields + valid schedule) calls composeNotification via TanStack Query mutation then swaps to a success panel showing the server-resolved reach + estimated cost (with "Compose another" reset); error state shows an inline role=alert. _nav.ts: Notifications de-flagged comingSoon → live /notifications, re-gated ops (super sees all) to match the platform-section backend gate. All colours/spacing/radii from Notun Din tokens (rounded-card/-button/-input/-chip, sageBg/sage/sageDk, butterBg/butter/butterDk, roseDk, mutedDk, ink/card/muted/line, font-title/-body/-mono, s1..s8) — no hardcoded hex/px. DEVIATION: provisional client reach is computed only for the specific-user audience; broad-audience reach+cost come back from the compose response (services.py injects them); WhatsApp/SMS preview bubbles + MFA-over-Tk-5000 re-prompt and the dedicated reach+cost (T-014) / audience+channel (T-011) widgets are out of scope here. 8 new RTL tests in test/notifications.test.tsx (estimateCost paid-vs-free truth, 4 sections render, submit gated until content filled, variable-chip inserts into EN body, specific-audience explicit reach, compose fires with all=now+whatsapp + success panel shows 412/৳206, by-role requires a role selection, compose-failure error). Widened sidebar.test.tsx live-pages set to include Notifications. Gate: npm install + npm run build OK (route /notifications dynamic/server-rendered, TypeScript+eslint clean), vitest 116 passed across 18 files (8 new).
2026-06-05 | EPIC-15/T-011 | admin | claude | done | Audience + channel selector widgets (Next.js). New components/admin/audience_selector.tsx (AudienceSelector — reusable controlled audience picker: all/role/segment/specific radiogroup; role+segment expose the customer-role cohort checkbox set; specific exposes the comma/space-separated User-ID search textarea; all shows the platform-wide reach note; aria-checked radios + named fieldset/legend) and components/admin/channel_selector.tsx (ChannelSelector — reusable controlled role=group checkbox-per-channel picker for inapp/whatsapp/sms/email with per-channel Free/৳/msg cost chips from the shared CHANNEL_COST_BDT table; exported CHANNEL_LABELS). Both widgets reuse the AUDIENCE_TYPES/CHANNELS/CUSTOMER_ROLES/CHANNEL_COST_BDT constants + AudienceType/ChannelValue/CustomerRole types from lib/api/notifications.ts (T-010). Refactored components/admin/notification_composer.tsx (T-010) to consume AudienceSelector + ChannelSelector inside its Audience/Channels Cards, deleting the previously-inline duplicated AudienceSection/ChannelsSection + their local AUDIENCE_LABELS/CHANNEL_LABELS/ROLE_LABELS maps — rendered markup + behaviour unchanged so the T-010 composer tests still pass. DEVIATION: the selectors are presentational/controlled (caller owns state); the super+ops route guard + TanStack Query data/loading/error states live in the host /notifications page + composer, not the widgets; "specific users search" is the user-ID entry field carried from T-010 (T-007 exposes no live user-search endpoint). All colours/spacing/radii from Notun Din tokens (rounded-card/-button/-input/-chip, sageBg/sage/sageDk, butterBg/butter, ink/card/muted/line, mutedDk, font-title/-body, s1..s8) — no hardcoded hex/px. 8 new RTL tests in test/audience-channel-selectors.test.tsx (AudienceSelector: 4 radios render, all-users note, By role reveals + selects cohort chips, Specific reveals user-ID search field, aria-checked tracks selection; ChannelSelector: checkbox-per-channel with 2 Free + ৳0.5/৳0.3 cost chips, in-app default + WhatsApp toggle on/off, onToggle fires with channel value). Gate: npm install + npm run build OK (route /notifications dynamic/server-rendered, eslint clean on new+touched files), vitest 124 passed across 19 files (8 new; T-010 composer suite green after refactor).
2026-06-05 | EPIC-15/T-012 | admin | claude | done | notification history page (Next.js). Extended lib/api/notifications.ts with the broadcast detail layer consuming the T-007 super+ops endpoints: notificationDeliverySchema/notificationDetailSchema (+ NotificationDelivery/NotificationDetail types) and fetchNotificationDetail GET /admin/api/notifications/{id} (embeds the per-recipient NotificationDelivery rows), a date NotificationFilters {from,to} window forwarded as query params via notificationsPath, a now-filtered notificationsQueryKey(filters) + a stable notificationsQueryPrefix for prefix-match invalidation, and notificationDetailQueryKey; refactored the T-010 composer's onSuccess invalidation to the new prefix (rendered behaviour unchanged, T-010 suite still green). app/(dashboard)/notifications/history/page.tsx is a SERVER component enforcing the super+ops route guard server-side (getAuthenticatedAdmin → role in {super,ops}; access-denied card otherwise, mirroring IsPlatformAdmin in notifications/views.py) and renders the history_client.tsx island. history_client.tsx: TanStack Query (keepPreviousData) loading skeleton / error+Retry / data states over fetchNotifications(filters), owns the date-filter state, threads it into the presentational table. components/admin/notification_history_table.tsx: token-styled table per Admin Portal spec §4.5.2 (Sent at / Title / Audience / Channels / Sent / Delivered / Opened / Status chip / Sender) with a From/To date filter bar; exported describeAudience() renders a human audience description from audience_type+filter (All users / Role: x / Segment / N specific users); status + delivery chips tone-mapped (sent/delivered/opened→sage, sending/sent→butter, scheduled/draft/queued→neutral, failed→danger); each row expands to a DeliveriesDetail that LAZILY fetches GET .../{id} via its own query (loading skeleton / error+Retry / empty / per-recipient table: Recipient / Channel / Status chip / Delivered / Opened / Error). _nav.ts: new live super/ops-gated /notifications/history "Notification history" item (History icon). All colours/spacing/radii from Notun Din tokens (rounded-card/-button/-sm/-chip, sageBg/sageDk, butterBg/butterDk, roseBg/roseDk, dangerBg/danger, line, ink/card/muted/mutedDk, font-title/-mono, s1..s8) — no hardcoded hex/px. DEVIATION: history is a sibling route to the T-010 composer (which owns /notifications); the T-007 list endpoint is not server-paginated so the date window narrows client-fetched rows without cursor controls; spec §4.5.2 search/export columns are out of scope (no endpoint). 6 new RTL tests in test/notification-history.test.tsx (describeAudience truth, rows render with counts+status, From date re-queries with {from}, row expand LAZILY fires fetchNotificationDetail(id) + shows per-recipient delivery, empty state, error+Retry); added "Notification history" to sidebar.test.tsx live-pages set. Gate: npm install + npm run build OK (route /notifications/history dynamic/server-rendered), eslint clean on new+touched files, vitest 130 passed across 20 files (6 new). Pre-existing unrelated tsc --noEmit strictness errors in test/ai-providers.test.tsx (vi.fn arg-count, confirmed present with this task's changes stashed) are not introduced here.
2026-06-05 | EPIC-15/T-013 | admin | claude | done | notification templates page (Next.js). Extended lib/api/notifications.ts with the system-template layer consuming the T-008 super+ops endpoints: notificationTemplateSchema (+ NotificationTemplate/TemplateUpdateInput types; api fields id/key/trigger_event/channels/title_en/title_bn/body_en/body_bn/variables/active/timestamps), fetchTemplates GET /admin/api/notification-templates (tolerant of bare-array + {results} envelopes, ordered by key) and updateTemplate PATCH /admin/api/notification-templates/{key} (encodeURIComponent key lookup; patch contract DELIBERATELY excludes key+trigger_event since the T-008 serializer drops them on update — they are immutable), plus templatesQueryKey + a stable templatesQueryPrefix for prefix-match invalidation. app/(dashboard)/notifications/templates/page.tsx is a SERVER component enforcing the super+ops route guard server-side (getAuthenticatedAdmin → role in {super,ops}; access-denied card otherwise, mirroring IsPlatformAdmin on NotificationTemplateViewSet in notifications/views.py) and renders the NotificationTemplates island. components/admin/notification_templates.tsx: TanStack Query loading skeleton / error+Retry / empty / data states; token-styled table per Admin Portal spec §4.5.3 (Template title+key / Trigger event / Channel chips / Active chip / Edit). Edit opens a remounted (key=template.key, so useState seeds fresh — no setState-in-effect) modal editor (role=dialog, aria-modal, backdrop, X+Cancel close) showing the IMMUTABLE trigger_event + the variable reference as {placeholder} chips read-only, and editable bilingual Title+Body (EN/BN, dir=auto), a reused ChannelSelector (T-011) with per-channel Free/৳ cost chips, and an Active Toggle; Save (gated until all four content fields non-blank AND ≥1 channel) PATCHes title_en/title_bn/body_en/body_bn/channels/active via a mutation then invalidates templatesQueryPrefix + closes; inline role=alert on failure. _nav.ts: new live super/ops-gated /notifications/templates "Notification templates" item (FileText icon). All colours/spacing/radii from Notun Din tokens (rounded-card/-button/-chip/-input, sageBg/sage/sageDk, butterBg/butterDk, roseDk, mutedDk, ink/card/muted/line, font-title/-body/-mono, s1..s8) — no hardcoded hex/px. DEVIATION: key/trigger_event are read-only (immutable server-side); spec §4.5.3 enabled/disabled maps to the model active flag; no create UI (system templates are migration-seeded in T-008, so the page edits the seeded set). 6 new RTL tests in test/notification-templates.test.tsx (list renders key/trigger/channels/Active+Inactive, error+Retry, editor shows immutable trigger event + {variable} chips, Save patches edited title WITHOUT key/trigger_event + editor closes on success, Save gated when a required field cleared, save-failure error); added "Notification templates" to sidebar.test.tsx live-pages set. Gate: npm install + npm run build OK (route /notifications/templates dynamic/server-rendered), eslint clean on new+touched files, vitest 136 passed across 21 files (6 new). Pre-existing unrelated tsc --noEmit strictness errors in test/ai-providers.test.tsx (vi.fn arg-count, confirmed present with this task's changes stashed) are not introduced here.
2026-06-05 | EPIC-16/T-006 | admin | claude | done | enhanced audit-log page (Next.js, replaces EPIC-11.T-011). Moved the audit log under the Compliance module at app/(dashboard)/compliance/audit/: page.tsx server component enforces the compliance+super role guard (mirrors compliance/views.py SECTION_ROLES, EPIC-16.T-002) with an access-denied panel for other roles; audit_client.tsx client viewer wires TanStack Query (keepPreviousData cursor pagination) into the existing token-styled AuditTable (full actor/action/entity-type/from/to filters + per-row before/after JSON diff) and adds an Export CSV button. lib/api/audit.ts gains auditCsvUrl(filters) building the absolute ?format=csv download URL from the active filters (cursor dropped — export covers the whole filtered set) against the T-002 CSV stream. compliance/page.tsx and legacy audit/page.tsx now redirect to /compliance/audit; _nav.ts repoints Audit log there and promotes Compliance from coming-soon to a live compliance-gated route. Tests: audit.test.tsx render test retargeted to AuditLogClient + 2 new CSV-export-link/filter-threading cases; sidebar.test.tsx adds Compliance to live-pages. All colours/spacing/radii from Notun Din tokens. Gate: next build green, vitest 144 passed (22 files). Note: unrelated EPIC-15 notification WIP (notification_composer.tsx, reach_cost_preview.tsx) left uncommitted in the worktree — not part of this task, not staged.
2026-06-05 | EPIC-16/T-007 | admin | claude | done | consent records page (Next.js). New lib/api/consent.ts (zod-validated fetchConsentRecords GET /admin/api/consent-records consuming the T-003 compliance+super endpoint; standard {results,pagination{next,previous,count}} envelope; CONSENT_TYPES/CONSENT_TYPE_LABELS mirroring khatir.compliance.enums.ConsentType; ConsentFilters {user,consent_type,granted_from,granted_to,page}; consentRecordsPath/consentQueryKey + pageFromLink extracting the ?page=N value from the page-number pagination links). app/(dashboard)/compliance/consent/page.tsx is a SERVER component enforcing the compliance+super route guard server-side (getAuthenticatedAdmin → role in {compliance,super}; access-denied card otherwise, mirroring IsComplianceAdmin in compliance/views.py, EPIC-16.T-003) and renders the ConsentRecordsClient island. consent_client.tsx: TanStack Query (keepPreviousData) loading skeleton / error+Retry / data states over fetchConsentRecords(filters), owns the filter+page state, threads it into the presentational table. components/admin/consent_table.tsx: read-only token-styled table (User / Consent type / Granted / Revoked / Expires) with a filter bar (user id text + consent-type select + granted from/to date inputs) and page-number Previous/Next pager; null dates render as em-dash; append-only log so no edit/delete affordance. _nav.ts: new live compliance/super-gated /compliance/consent "Consent records" item (ShieldCheck icon) sitting in the Compliance module beside the audit log. All colours/spacing/radii from Notun Din tokens (rounded-card/-button/-sm, sageBg, line, ink/card/muted/mutedDk, font-title/-mono, s1..s8) — no hardcoded hex/px. DEVIATION: T-003 uses page-number pagination (not cursor), so the pager threads ?page=N; consent records are append-only/read-only so the page is a filterable read-only table only. 7 new RTL tests in test/consent.test.tsx (rows render with consent-type label + user, consent-type filter re-queries with {consent_type}, user filter re-queries with {user}, page-number Next pager re-queries with {page:'2'}, empty state, error+Retry, null revoked_at renders em-dash); added "Consent records" to sidebar.test.tsx live-pages set. Gate: npm install + npm run build OK (route /compliance/consent dynamic/server-rendered), vitest 151 passed across 23 files (7 new).
2026-06-05 | EPIC-15/T-014 | admin | claude | done | Reach + cost preview widget (Next.js). New components/admin/reach_cost_preview.tsx (ReachCostPreview — reusable controlled+presentational widget surfacing estimated reach + estimated cost before composing, per Admin Portal spec §4.5.1 "Reach summary" + "Cost estimate"). Props: channels (ChannelValue[]), reach (number|null), optional action node (e.g. send button at card foot) + optional errorMessage (role=alert). Cost derives from the shared estimateCost(reach, channels) helper in lib/api/notifications.ts (reach × Σ CHANNEL_COST_BDT; inapp/email free, whatsapp ৳0.5, sms ৳0.3) — mirrors the backend formula (services.py). Renders a token-styled Card (CardTitle + Users icon) with a reach/cost definition list, a Chip per selected channel via CHANNEL_LABELS (T-011) or a "No channels selected" hint, and a server-resolution note + em-dashes when reach is null (broad all/role/segment audiences are resolved authoritatively server-side at send via POST /admin/api/notifications, T-007 — there is no standalone estimate endpoint). Integrated into components/admin/notification_composer.tsx (T-010): replaces the inline preview sidebar, fed the composer's provisional reach (explicit recipient count for the specific-user audience, null otherwise) + selected channels + compose error + the Send button as its action. DEVIATION: widget is presentational/controlled (caller owns reach/channels/state); the super+ops route guard + TanStack Query loading/error/data states live in the host /notifications page + composer (T-010), not the widget; reach for broad audiences is intentionally a dash until the server resolves it (no estimate endpoint in T-007). All colours/spacing/radii from Notun Din tokens (rounded-card/-chip, sage, ink, muted, roseDk, line, font-title/-body, s1..s6) — no hardcoded hex/px. 6 new RTL tests in test/reach-cost-preview.test.tsx (explicit reach + derived cost 412×whatsapp=৳206, chip-per-channel + sms cost ৳3, no-channels hint + ৳0, null reach dashes both + server-resolution note, errorMessage as role=alert, provided action node click). Gate: npm install + npm run build OK (route /notifications dynamic/server-rendered), vitest 158 passed across 24 files (6 new). Unrelated EPIC-16.T-008 data-requests WIP left uncommitted in the worktree — not part of this task, not staged.
2026-06-05 | EPIC-16/T-008 | admin | claude | done | data requests page (Next.js). New lib/api/data-requests.ts (zod-validated layer over the T-004 PDPA endpoints: fetchDataRequests GET /admin/api/data-requests {results,pagination} envelope filterable by status/type/sla; processDataRequest POST /admin/api/data-requests/{id}/process {action:approve|reject,reason}; DATA_REQUEST_TYPE/STATUS + SLA_STATE labels mirroring khatir.compliance.enums; field names matched to DataRequestSerializer — request_type/status/sla_due/sla_state/completed_at/handled_by; dataRequestsQueryKey + pageFromLink). app/(dashboard)/compliance/requests/page.tsx SERVER component enforces the compliance+super route guard (getAuthenticatedAdmin → role in {compliance,super}; access-denied card otherwise, mirroring IsComplianceAdmin in compliance/data_request_views.py) and mounts requests_client.tsx → DataRequestQueue island. components/admin/data_request_queue.tsx: two tabs (Pending / Completed) via TanStack Query (keepPreviousData) with loading skeleton / error+Retry / empty / data states. Pending tab table (User/Type/SLA due/SLA badge/Requested/Actions) with SLA badge red=overdue/amber=due_soon/green=on_track and Approve/Reject buttons; both open a confirm ProcessDialog (role=dialog, aria-modal) where a rejection requires a non-blank reason (re-checked server-side) and approve reason is optional+recorded; success invalidates the queue query so the row drops out. Completed tab: read-only history (non-pending rows) with page-number Previous/Next pager. _nav.ts: new live compliance/super-gated /compliance/requests "Data requests" item (FileText icon) in the Compliance module beside audit + consent. All colours/spacing/radii from Notun Din tokens (rounded-card/-button/-input, dangerBg/danger, butterBg/butterDk, sageBg/sageDk, line, ink/card/muted, font-title/-body/-mono, s1..s8) — no hardcoded hex/px. 8 new RTL tests in test/data-requests.test.tsx (pending rows + SLA badge, pending-only filter, empty, error+Retry, approve via dialog, reject reason-gated, completed tab resolved rows); added "Data requests" to sidebar.test.tsx live-pages set. Gate: npm install + npm run build OK (route /compliance/requests dynamic/server-rendered), vitest 158 passed across 24 files.
