# Placeholder — completed by T-004 (Django API scaffold).
#
# The real image (uv-managed Django + DRF, gunicorn/uvicorn entrypoint) is
# added by T-004, which also uncomments and enables the `api` service in
# docker-compose.yml. Do not build this image yet.
