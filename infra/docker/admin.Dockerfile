# Placeholder — completed by T-009 (Next.js admin scaffold).
#
# The real image (Next.js App Router, standalone build) is added by T-009,
# which also uncomments and enables the `admin` service in
# docker-compose.yml. Do not build this image yet.
