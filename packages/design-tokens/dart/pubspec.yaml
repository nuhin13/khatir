name: khatir_tokens
description: "Notun Din design tokens generated for Flutter (Dart). Consumed via path by apps/mobile."
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.12.0
  flutter: ">=3.44.0"

dependencies:
  flutter:
    sdk: flutter
